# 24 · 深潜：14 层检索管线的真实代码

> 这一篇只做一件事：把 `packages/api/src/domains/memory/` 下那 2175 行的 `SqliteEvidenceStore.ts` 以及它周边 30 多个小文件，拆成「函数名 + 字段名 + 常量值 + 分支条件」的可复述形式。
> 解决的追问是：「RRF 具体怎么算」「progressive relaxation 几级、怎么降」「consumption prior 的贝叶斯公式写全」「constitutional immunity 在哪一行生效」「embedding 挂了到底怎么 fail-open」。
> 设计层笔记（clowder-book/07 记忆检索）讲的是「为什么这么设计」；这一篇讲的是「代码到底怎么写的」，面试官追问实现细节时你能直接背出来。

**证据等级约定**（本篇每个技术点都标）：
- 【源码】= 代码或代码注释里直接有的，可以说「我看过那段代码」
- 【ADR】= `docs/decisions/` 里写明的，可以说「有 ADR 背书」
- 【推断】= 我根据代码形态反推的，面试时要说「我的理解是…」

---

<!-- BEGIN ZERO-BASE EXPANSION 24 -->
## 0A. 零基础导读：检索不是“搜一下数据库”，而是一条排序流水线

> 本章代码涉及 SQL、全文检索、向量和多阶段排序。第一次只要能解释“文档怎样进入索引、查询怎样召回候选、候选怎样融合和过滤”即可。

### 0A.1 先区分写入链与查询链

```text
写入链：原始资料 → 清洗/切块 → passage/document → FTS/向量索引
查询链：用户 query → 多路召回 → 打分融合 → 过滤/去重 → topK → prompt
```

SQLite 文件里的索引是派生数据，不应自动等同于业务真相源。真相源变化后，索引可能需要重建。

### 0A.2 SQL 最小补课

```sql
SELECT id, content
FROM evidence_docs
WHERE scope = ?
ORDER BY updated_at DESC
LIMIT ?;
```

- `SELECT` 选字段；`FROM` 选表；`WHERE` 过滤；`ORDER BY` 排序；`LIMIT` 限量。
- `?` 是参数占位符，值由驱动绑定，不要拼接用户输入。
- index 加速查询；transaction 保证一组写入要么都成功，要么都回滚。

阅读 `.prepare(...).get/all/run(...)` 时：`get` 取一行，`all` 取多行，`run` 执行写入/更新（具体以驱动 API 为准）。

### 0A.3 FTS 与向量检索分别擅长什么

- **FTS/关键词检索**：擅长精确术语、函数名、错误码；“callbackToken”这种词命中很强。
- **向量检索**：擅长语义相近但字面不同；例如“如何防止两个 Agent 来回调用”与“乒乓熔断”。

两者不是替代关系。混合检索通常同时召回，再融合排序，以兼顾精确性与语义覆盖。

### 0A.4 chunk 为什么影响效果

整篇文档直接向量化会主题过宽；切得过碎又失去上下文。chunk 设计要平衡：长度、重叠、标题继承、代码块完整性和来源定位。

检索返回的不只是 content，还应保留 documentId、路径、位置、时间、scope 等 provenance。否则模型给出答案后无法追溯证据。

### 0A.5 多路召回与融合

假设 FTS 和向量各返回一份排名：

```text
FTS：A, B, C
Vector：B, D, A
```

不能直接比较两边原始分数，因为量纲不同。常见做法是按名次融合（如 RRF 思想）：排名越前贡献越大，同一文档在多路出现会累积优势。具体公式和常量以后文源码为准，先理解“融合名次比硬比不同分数更稳健”。

### 0A.6 过滤顺序为什么重要

检索链常见阶段：权限/scope 过滤、时间与状态过滤、去重、质量阈值、topK。若先 topK 后权限过滤，可能最后剩不够；若先做昂贵 rerank 再去重，会浪费计算。

阅读时把每个阶段写成：输入数量 → 规则 → 输出数量。可观测性也应记录候选在哪一关被丢弃。

### 0A.7 `Promise` 与同步 SQLite

部分 Node SQLite 驱动查询本身是同步的；外层服务方法仍可能是 async，因为 embedding、文件 IO 或统一接口是异步的。看到 `async` 不代表每一步都非阻塞。大查询和大事务仍可能占用事件循环，应关注查询上限和索引。

### 0A.8 防 SQL 注入与路径/内容安全

- 用户值必须参数化，不拼进 SQL。
- 动态表名/排序字段不能靠参数占位符，需要白名单。
- 返回给模型的 evidence 是外部内容，应防 prompt injection，标明它是证据而非系统指令。
- scope/visibility 过滤属于授权边界，不能只靠 prompt 告诉模型“不要看”。

### 0A.9 检索质量怎么评估

不要只问“搜到了吗”，至少区分：

- Recall：该找到的是否进入候选。
- Precision：返回结果里相关内容比例。
- MRR/排名：第一个正确结果是否靠前。
- Freshness：新资料是否能及时进入索引。
- Provenance：答案能否指出来源。
- Latency：多路检索和 rerank 是否过慢。

安全测试经验可转成对抗样例：同名文档、过期资料、恶意指令文本、超长代码块、空 query、特殊字符和跨 scope 查询。

### 0A.10 第一次源码陪读

```text
schema.ts（先看表与迁移）
→ IEvidenceStore/相关类型
→ SqliteEvidenceStore 写入方法
→ 文本/FTS 查询方法
→ 向量查询与融合
→ 去重、阈值、topK
→ IndexBuilder/编译入口
→ recall event 与遥测
```

先画“写入链”和“查询链”两张图，不要按 2175 行顺序读。

```bash
rg -n "CREATE TABLE|CREATE INDEX|FTS|vec|search|query|rank|topK" \
  packages/api/src/domains/memory/schema.ts \
  packages/api/src/domains/memory/SqliteEvidenceStore.ts
```

### 0A.11 最小实验

自己建 5 条小文档：两条包含精确函数名、两条语义相近、一条恶意 prompt。分别用关键词和语义问题查询，记录两路排名，再手工融合。随后测试 scope 过滤是否在结果进入 prompt 前生效。

### 0A.12 面试回答模板

> “检索管线分写入和查询两条链。资料先清洗切块并写入 SQLite 的正文、FTS 与向量索引；查询时关键词和向量多路召回，通过排名融合、权限/时间过滤、去重和 topK 得到证据。SQLite 索引是可重建的编译产物，业务真相源仍在上游存储。安全上参数化 SQL、scope 过滤和把 evidence 视作不可信内容都很关键。”

### 0A.13 自测

1. FTS 与向量各擅长什么？
2. 为什么不能直接比较两种原始分数？
3. chunk 太大和太小分别有什么问题？
4. scope 过滤为何不能只写在 prompt 中？
5. 索引为何应当可重建？

---
<!-- END ZERO-BASE EXPANSION 24 -->


## 1. 文件地图与职责边界

### 1.1 核心文件清单（按行数排序，全部在 `packages/api/src/domains/memory/`）

| 文件 | 行数 | 一句话职责 |
|---|---:|---|
| `SqliteEvidenceStore.ts` | 2175 | **检索主体**。`searchWithMeta()` 是唯一入口，lexical/semantic/hybrid/raw 四条路径 + authority boost + consumption rerank 都在这里 |
| `IndexBuilder.ts` | 1163 | 扫描 → hash → 增量判定 → upsert → 抽边 → 索引 passage → embed |
| `schema.ts` | 786 | 全部建表 SQL + `applyMigrations()`（V1→V26）+ 两个 vec0 表的 `ensure*` |
| `EntityRegistry.ts` | 621 | 实体别名字典 + mention 倒排索引，`resolveQuery()` / `findMentionAnchors()` / `findMentionPassages()` |
| `interfaces.ts` | 436 | `SearchOptions` / `EvidenceItem` / `SearchExecutionMeta` 等全部类型 + `resolveEmbedConfig()` |
| `GraphQueryResolver.ts` | 402 | `graph_resolve` 的「精确 anchor vs 模糊候选」分流 |
| `GraphResolver.ts` | 348 | BFS 子图构建 + 脱敏 + 度数封顶 |
| `factory.ts` | 306 | `createMemoryServices()`：装配 store / embedding / vectorStore / catalog / 各 collection store |
| `KnowledgeResolver.ts` | 303 | 跨 collection 联邦检索 + collection 级 RRF |
| `CoverageSearchService.ts` | 310 | `intent=coverage` 的穷举式搜索（不走 top-K） |
| `TopkExpansionService.ts` | 175 | F256 Phase B：把 coverage 的 expansion 投影成 topk 的 "Related directions" |
| `EmbeddingService.ts` | 172 | HTTP 客户端，打本地 `embed-api.py` 的 `/health` 和 `/v1/embeddings` |
| `CollectionIndexBuilder.ts` | 152 | 外部 collection 的索引器（带 SecretScanner 拦截） |
| `lexical-backfill.ts` | 128 | 子串回捞的排序信号（keywordHits / titleHits / textHits） |
| `IndexStateManager.ts` | 121 | `index_state` 表的状态机（missing/stale/building/ready/failed） |
| `SecretScanner.ts` | 102 | 7 条密钥正则 + 香农熵兜底 |
| `f200-types.ts` | 98 | `freezeF200Flags()`（只有一个 flag：`consumptionRerank`） |
| `fts-query-builder.ts` | 87 | `buildProgressiveFtsQueries()` — 三级放松 |
| `magic-word-confidence.ts` | 83 | 魔法词置信度分级（与检索管线无关，属 F227 事件记忆） |
| `consumption-prior.ts` | 75 | 贝叶斯收缩 CTR + 四分支 |
| `PassageVectorStore.ts` | 49 | `passage_vectors` 的 vec0 CRUD + key 编解码 |
| `VectorStore.ts` | 68 | `evidence_vectors` 的 vec0 CRUD + `embedding_meta` 版本锚 |
| `embed-utils.ts` | 70 | 批量 embed 管线（doc 批 64 / passage 批 8） |
| `mmr.ts` | 42 | MMR + keyword Jaccard |
| `graph-edge-weight.ts` | 32 | 边权重（typeBase + traversalBoost） |
| `recency-decay.ts` | 14 | `T/(T+age)` 分数衰减 |

【源码】行数由 `wc -l` 统计得到。

### 1.2 调用链（谁调谁）

```
MCP: cat_cafe_search_evidence           packages/mcp-server/src/tools/evidence-tools.ts
        │  fetch GET /api/evidence/search?q=...&scope=...&mode=...
        ▼
REST route                              packages/api/src/routes/evidence.ts
        │  ① intent=coverage → CoverageSearchService.search()   ← 完全绕开下面
        │  ② freezeFlags() + computeVariantId() + getOrAssignCohort()
        ▼
KnowledgeResolver.resolve()             KnowledgeResolver.ts
        │  按 dimension 分流：project / global / all / library / collection
        ▼
SqliteEvidenceStore.searchWithMeta()    ← 真正的 14 层在这里
        │
        ├─ EntityRegistryStore.resolveQuery() / findMentionAnchors()
        ├─ buildProgressiveFtsQueries()
        ├─ rankLexicalBackfillRows()
        ├─ VectorStore.search()  ←  EmbeddingService.embed()  ←  HTTP :9880
        ├─ applyAuthorityBoost()
        └─ enrichWithDrillDown() → applyConsumptionRerank()
                                      ├─ computeConsumptionPrior()
                                      ├─ computeRecencyDecay()
                                      └─ applyMMR()
        ▲
route 回来后再做：TopkExpansionService.expand() + applySalienceRerank()
```

【源码】链路由 `evidence-tools.ts` 的 `handleSearchEvidence()`、`evidence.ts` 的 `app.get('/api/evidence/search')`、`KnowledgeResolver.resolve()`、`SqliteEvidenceStore.searchWithMeta()` 逐层读出。

### 1.3 三条边界，必须说清楚

**边界一：store 不认识 `dimension` / `collections` / `intent`。**
这三个字段虽然声明在 `SearchOptions` 里，但 `SqliteEvidenceStore.ts` 全文没有一处读它们（grep `dimension` 在该文件命中 0 次，grep `intent` 在整个 `domains/memory/*.ts` 命中 0 次）。`dimension`/`collections` 由 `KnowledgeResolver` 消费，`intent` 由 route 消费。【源码】

**边界二：F163 salience rerank 不在 store 里。**
`applySalienceRerank()` 定义在 `f163-types.ts`，调用点在 `routes/evidence.ts`，在 store 返回之后才跑。所以「14 层」是 store 级的，用户实际看到的顺序还多一层。【源码】

**边界三：写路径和读路径共用一把锁。**
`SqliteEvidenceStore` 内有 `private readonly writeQueue = new EvidenceWriteQueue()`，`upsert` / `deleteByAnchor` / `addEdge` / `createSummary` / `refreshEntityMentions` 全部走 `this.writeQueue.enqueue(...)`；`search` 不走。`EvidenceWriteQueue` 的实现就是一条 promise 链：

```ts
export class EvidenceWriteQueue {
  private tail: Promise<void> = Promise.resolve();
  enqueue<T>(fn: () => T | Promise<T>): Promise<T> {
    return new Promise<T>((resolve, reject) => {
      this.tail = this.tail.then(async () => {
        try { resolve(await fn()); } catch (e) { reject(e); }
      });
    });
  }
}
```
【源码】`evidence-write-queue.ts`，注释写明 "F163: Single-writer scheduler for ALL evidence.sqlite mutations. Design Gate contract 3"。

> **面试官追问：为什么读不加锁？**
> 「SQLite 开了 WAL（`journal_mode = WAL`），读写不互斥，读永远看到一个一致快照。加锁只为了防多个写并发触发 `SQLITE_BUSY`——另外 `busy_timeout` 也设了 5000ms 做二次兜底。这个单写者队列的语义是 FIFO、不交错，注释里明确叫 Design Gate contract 3。」

---

## 2. 核心数据结构

### 2.1 `SearchOptions`（`interfaces.ts`）——逐字段解释

```ts
export interface SearchOptions {
  kind?: EvidenceKind;
  status?: EvidenceStatus;
  keywords?: string[];
  limit?: number;
  scope?: 'docs' | 'memory' | 'threads' | 'sessions' | 'all';
  mode?: 'lexical' | 'semantic' | 'hybrid';
  depth?: 'summary' | 'raw';
  dateFrom?: string;
  dateTo?: string;
  contextWindow?: number;
  threadId?: string;
  dimension?: SearchDimension;
  collections?: string[];
  provenanceTier?: ProvenanceTier;
  includeBackstop?: boolean;
  worldId?: string;
  sceneId?: string;
  explain?: boolean;
  intent?: 'topk' | 'coverage';
}
```

| 字段 | 默认 | 这个字段为什么存在 / 它到底改变了什么 |
|---|---|---|
| `limit` | `10`（store 内 `options?.limit ?? 10`）；**route 层是 `5`**（`limit ?? 5`）；MCP schema 限制 `1..20` | 三处默认值不同，是很好的追问点 |
| `scope` | 无（=不过滤） | `threads`→`kind='thread'`；`sessions`→`kind='session'`；`docs`/`memory`→`AND kind != 'session' AND kind != 'thread'`；`all`→不加条件 |
| `mode` | `'lexical'`（`options?.mode ?? 'lexical'`） | 决定走 `lexicalResults` 直接返回 / `semanticNNSearch()` / `hybridRRFSearch()`。**只有 `hybrid` 会把 BM25 候选池撑大** |
| `depth` | `'summary'` | `'raw'` 会在 authority boost **之前**跳进 `rawPassageSearch()`，走完全不同的 passage 分支 |
| `kind` | 无 | 优先级高于 `scope`：`options?.kind ?? (scope 推导)` |
| `keywords` | 无 | 拼成 `keywords LIKE ?` + 参数 `%"kw"%`（注意带双引号，因为 keywords 列存的是 JSON 数组字符串） |
| `dateFrom/dateTo` | 无 | 打在 `updated_at`（doc）或 `created_at`（passage）上；`dateTo` 长度 ==10 时自动补 `T23:59:59` |
| `contextWindow` | 无 | 只在 `depth=raw` 生效，取命中 passage 前后 N 条（`position BETWEEN ?-N AND ?+N`） |
| `threadId` | 无 | 转成 `threadAnchor = 'thread-' + threadId`，然后 `AND anchor = ?` |
| `provenanceTier` | 无 | `AND provenance_tier = ?` |
| `includeBackstop` | `false` | 为 false 且 `F163_COMPRESSION !== 'off'` 时 → `suppressBackstop=true` → 所有分支加 `AND activation != 'backstop'` |
| `worldId`/`sceneId` | 无 | F093 世界观隔离，`AND world_id = ?` / `AND scene_id = ?` |
| `explain` | `false` | 让 `annotateMatchReasons()` 额外写 `item.rankingFactors = { bm25Score: index+1 }` |
| `intent` | `'topk'` | **store 不读**；route 里 `intent === 'coverage'` 直接 `return coverageResult`，连 KnowledgeResolver 都不进 |
| `dimension` | route 默认 `'project'`（MCP 侧 `const { dimension = 'project' }`），resolver 内 `options?.dimension ?? 'all'` | `'all'` 会回一条 deprecationWarning |

【源码】全部来自 `interfaces.ts` 的 `SearchOptions`、`SqliteEvidenceStore.searchWithMeta()` 的参数解析段、`routes/evidence.ts` 的 `searchSchema`、`evidence-tools.ts` 的 `searchEvidenceInputSchema`。

> **面试官追问：`limit` 默认值为什么三处不一样？**
> 「store 是库，默认 10 是库的合理默认；REST route 面向 agent，默认 5 是为了省 token；MCP schema 上限 20 是为了防猫一次要 100 条把上下文打爆。这三个值分别在 `searchWithMeta()` 的 `options?.limit ?? 10`、`evidence.ts` 的 `limit ?? 5`、`searchEvidenceInputSchema` 的 `.max(20)`。」

### 2.2 `EvidenceItem` 关键字段（只列检索会用到的）

| 字段 | DB 列 | 为什么存在 |
|---|---|---|
| `anchor` | `anchor` (PK) | 全系统的稳定 ID，也是 exact-anchor 旁路的匹配键 |
| `kind` | `kind` | 9 个取值：`feature/decision/plan/session/lesson/thread/discussion/research/pack-knowledge` |
| `status` | `status` | `active/done/archived/review/invalidated` |
| `summary` | `summary` | 参与 FTS5（evidence_fts 只索引 title + summary！）、参与 embedding 文本 |
| `keywords` | `keywords`（JSON 字符串） | 参与 lexical backfill 排序、参与 MMR 的 Jaccard 相似度 |
| `supersededBy` | `superseded_by` | ORDER BY 第一列：非 null 的排最后 |
| `sourcePath` | `source_path` | ORDER BY 第二列（`LIKE 'archive/%'` 降权）；也是 drillDown 的 `read_file_slice` 路径来源 |
| `authority` | `authority` | `constitutional/validated/candidate/observed`，authority boost 的权重键 |
| `activation` | `activation` | `always_on/scoped/query/backstop`，backstop 会被压缩期抑制 |
| `provenance.tier` | `provenance_tier` | ORDER BY 第三列（authoritative=0, 其它非 null=1, null=2） |
| `firstIndexedAt` | `first_indexed_at`（INTEGER, epoch ms） | consumption prior 的 14 天冷启动保护期判定 |
| `summaryOfAnchor` | `summary_of_anchor` | 非 null 表示「这篇本身是压缩摘要」，级联压缩守卫靠它 |
| `entityMatches` | 无（运行时挂载） | 第 14 层输出增强 |
| `passages` | 无（运行时挂载） | `depth=raw` 的段落明细 |
| `drillDown` | 无（运行时挂载） | `{ tool, params, hint }` 三元组 |
| `matchReason` | 无（运行时挂载） | `entity:xxx` / `anchor` / `title` / `summary` / `keyword` / `content` |

【源码】`interfaces.ts` 的 `EvidenceItem` + `SqliteEvidenceStore.ts` 的 `interface RowShape` + `rowToItem()`。

`rowToItem()` 有个值得记的写法：**所有可选字段都是 `if (row.x != null) item.y = ...`，不是展开赋值**——这样 `undefined` 和 `null` 不会污染输出 JSON。【源码】

### 2.3 `SearchExecutionMeta`——可观测性的唯一出口

```ts
export type SearchDegradeReason =
  | 'passage_embedding_unavailable'
  | 'passage_vector_search_error'
  | 'evidence_store_error'
  | 'raw_lexical_only';

export interface SearchExecutionMeta {
  degraded: boolean;
  degradeReason?: SearchDegradeReason;
  effectiveMode?: 'lexical' | 'semantic' | 'hybrid';
}
export interface EvidenceSearchExecution { items: EvidenceItem[]; meta: SearchExecutionMeta; }
```

**关键事实**：doc 级的降级（embedding 不可用 → 退 lexical）**不会**写 meta——那三个 `return` 全都写死 `meta: { degraded: false }`。只有 `depth=raw` 的 passage 分支才会填 `degradeReason`。这是一个真实的可观测性缺口，面试时可以主动指出。【源码】`searchWithMeta()` 的 `if (searchMode === 'semantic') { const embeddingAvailable = ...; if (!embeddingAvailable) { return { ..., meta: { degraded: false } }; } }`

`degradeReason` 的优先级在 `KnowledgeResolver.degradeReasonPriority()` 里定死：`evidence_store_error`=4 > `passage_vector_search_error`=3 > `passage_embedding_unavailable`=2 > `raw_lexical_only`=1。多个 collection 同时降级时取优先级最高的那个。【源码】

### 2.4 完整 schema：所有表、所有列、所有索引

`applyMigrations(db)` 在 `initialize()` 里被调用，前面先跑四条 pragma：

```ts
this.db.pragma('journal_mode = WAL');
this.db.pragma('foreign_keys = ON');
this.db.pragma('busy_timeout = 5000');
```
（`schema.ts` 里另有一个 `PRAGMA_SETUP` 常量，多一条 `journal_size_limit = 67108864` = 64MB，但 `SqliteEvidenceStore.initialize()` 并没有用它——它是给别处用的。【源码】）

#### V1（F102 基座）

```sql
CREATE TABLE evidence_docs (
  anchor TEXT PRIMARY KEY, kind TEXT NOT NULL, status TEXT NOT NULL,
  title TEXT NOT NULL, summary TEXT, keywords TEXT,
  source_path TEXT, source_hash TEXT, superseded_by TEXT,
  materialized_from TEXT, updated_at TEXT NOT NULL);

CREATE VIRTUAL TABLE evidence_fts USING fts5(
  title, summary, content=evidence_docs, content_rowid=rowid);

CREATE TABLE edges (
  from_anchor TEXT NOT NULL, to_anchor TEXT NOT NULL, relation TEXT NOT NULL,
  PRIMARY KEY (from_anchor, to_anchor, relation));

CREATE TABLE markers (id TEXT PRIMARY KEY, content, source, status DEFAULT 'captured',
                      target_kind, created_at);
CREATE TABLE schema_version (version INTEGER PRIMARY KEY, applied_at TEXT NOT NULL);
```
外加 3 个 FTS 同步触发器（`evidence_docs_ai` / `_ad` / `_au`），存在 `FTS_TRIGGER_STATEMENTS` 数组里，**必须一条一条 `db.exec()`**，因为 better-sqlite3 的 `exec` 不能一次跑多个含 `BEGIN...END` 的 trigger。【源码】

**极重要的细节**：`evidence_fts` 只索引 `title` 和 `summary` 两列，**没有正文**。而且它用的是 FTS5 默认 tokenizer（unicode61 默认配置），**没有** `tokenchars`。

#### V2 → V26 逐版做了什么

| V | Feature | 做了什么 |
|---|---|---|
| V2 | F102 Phase C | `embedding_meta(key PK, value)` — 模型版本锚 |
| V3 | F102 Phase E | `evidence_passages(id AUTOINCREMENT, doc_anchor, passage_id, content, speaker, position, created_at, UNIQUE(doc_anchor, passage_id))` + `passage_fts`（**带 `tokenize="unicode61 tokenchars '_-'"`**）+ 3 个 trigger |
| V4 | F102 Phase G | `summary_segments`（append-only 账本）+ `summary_state`（水位线）+ 3 个索引 |
| V5 | F139 1a | `task_run_ledger` + 2 个索引 |
| V6 | F129 A | `evidence_docs.pack_id` + `idx_evidence_docs_pack` |
| V7 | F139 1b | `task_run_ledger.assigned_cat_id` |
| V8 | F139 3A | `dynamic_task_defs` + `task_run_ledger.error_summary` |
| V9 | F139 3B | `scheduler_global_control`（`CHECK(id=1)` 单行表）/ `scheduler_task_overrides` / `scheduler_emissions` / `pack_template_defs` |
| V10 | F152 A | `provenance_tier` + `provenance_source` + `idx_evidence_docs_provenance` |
| V11 | F152 B | `index_state`（`CHECK(status IN ('missing','stale','building','ready','failed'))`）|
| V12 | F152 C | `generalizable INTEGER DEFAULT NULL` |
| V13 | F163 A | `authority DEFAULT 'observed'` / `activation DEFAULT 'query'` / `verified_at` + `f163_cohorts` / `f163_suggestions` / `f163_logs`（+2 索引） |
| V14 | F163 B | `source_ids` / `summary_of_anchor` / `compression_rationale` |
| V15 | F163 C | `contradicts` / `invalid_at` / `review_cycle_days` |
| V16 | F093 A | `world_id` / `scene_id` + `idx_evidence_docs_world` + `idx_evidence_docs_world_scene` |
| V17 | F186 A | `collection_id` / `review_status` + `idx_evidence_docs_collection`；markers 加 5 列 |
| V18 | F186 F | edges 加 `from_collection_id` / `to_collection_id` / `edge_sensitivity` / `provenance` / `created_at` |
| V19 | F200 A | `recall_events`（+3 索引）+ edges 加 `traversal_count DEFAULT 0` / `last_traversed_at` |
| V20 | F200 B | `anchor_recall_metrics(anchor PK, consumed_count_30d, exposure_count_30d, last_consumed_at, dormancy_days, updated_at)` + `idx_anchor_metrics_dormancy` |
| V21 | F200 C | `global_ctr_baseline(doc_kind PK, mean_ctr REAL DEFAULT 0.2, sample_count, updated_at)` + `evidence_docs.first_indexed_at INTEGER NOT NULL DEFAULT 0` + `recall_events.shadow_ranking_json` |
| V22 | F200 D | `task_trajectories`（+4 索引） |
| V23 | F200 HW-4 | `recall_events.result_set_id` / `.attribution_clarity` |
| V24 | F209 B | `entity_registry` / `entity_aliases`（+`idx_entity_aliases_norm`）/ `entity_mentions`（+2 索引，双 FK 且 ON DELETE CASCADE） |
| V25 | F102 bugfix | `recall_events.thread_id DEFAULT ''` + 索引 + **一次 UPDATE 回填**（从 `task_trajectories` 按 `invocation_id` 反查 thread_id） |
| V26 | RecallFeed | `recall_events.result_count` |

【源码】全部逐条来自 `schema.ts` 的 `applyMigrations()`。

两个 vec0 表**不在迁移里**，是单独的函数，且返回 boolean 而不抛异常：

```ts
export function ensureVectorTable(db, dim): boolean {
  try {
    db.exec(`CREATE VIRTUAL TABLE IF NOT EXISTS evidence_vectors USING vec0(
      anchor TEXT PRIMARY KEY, embedding float[${dim}])`);
    return true;
  } catch { return false; }   // sqlite-vec not loaded — fail-open
}
// ensurePassageVectorTable 同理，表名 passage_vectors，主键列 passage_key
```
【源码】注释原文就是 `// sqlite-vec not loaded — fail-open`。

**迁移的三个可复述特征**【源码】：
1. `CURRENT_SCHEMA_VERSION = 26` 这个常量**导出了但 `applyMigrations()` 里没用它**——每一步都是硬编码的 `if (currentVersion < N)`。
2. 每个 `ALTER TABLE ... ADD COLUMN` 都单独包 `try { } catch { }`，注释是 "Column may already exist from a partial migration"。因为 SQLite 没有 `ADD COLUMN IF NOT EXISTS`。
3. `applyMigrations()` 第一件事是 `CREATE TABLE IF NOT EXISTS schema_version`，注释写着 "P1 fix (codex review R2): schema_version may not exist on empty DB"。

> **面试官追问：为什么 vec0 表不走迁移？**
> 「因为它依赖运行时 `import('sqlite-vec')` 成功加载扩展。迁移是 `initialize()` 必跑的路径，如果把 vec0 放进去，扩展没装就整个 DB 起不来。把它挪到 `factory.ts` 里 embed 分支下、用 try/catch + 返回 boolean，就实现了『没装扩展 = 没有 vectorStore = 自动只走 lexical』的 fail-open。」

---

<!-- BEGIN INLINE SOURCE EXPANSION 24-TYPES -->
### 2.5 把 `SearchOptions` 变成一条可以在脑中执行的查询

假设调用方搜索“Agent 之间如何防止乒乓”，并传入 `limit=5`、某个 pack/thread scope 和默认 depth。`SearchOptions` 不是简单分页参数，它会决定后面走哪些 SQL、候选池开多大、是否启用向量/图/entity/消费先验，以及最终是否做 drill-down。

先分清三种对象：`SearchOptions` 是查询意图，`EvidenceItem` 是知识文档本身，`SearchExecutionMeta` 是这一次搜索如何执行的诊断报告。结果分数不应该回写进长期 `EvidenceItem`，因为同一文档在不同 query 下得分不同；运行时 rank、matchReason、来源通道更适合放在搜索结果包装或 meta 中。

`EvidenceItem.anchor` 是稳定业务标识，不等于 SQLite 自增 rowid；title/summary/keywords/content 是不同召回字段；`sourcePath/sourceHash/provenance` 回答“它从哪里来、是否已变化”；`authority/activation/verifiedAt` 回答“可信度和激活方式”；`supersededBy/invalidAt/contradicts` 让旧知识不会因为关键词匹配强就继续当作当前答案。

把一次搜索写成数量变化更容易理解：

```text
limit=5
→ candidatePool 可能放大到几十条
→ exact anchor、FTS、lexical、entity、vector 各自产生候选
→ 同 anchor 合并并记录命中来源
→ scope/status/失效过滤
→ RRF/authority/recency/consumption rerank
→ MMR 去掉过度相似结果
→ 取前 5 条并补 drillDown
```

因此 `limit=5` 绝不代表 SQL 每路只查 5 条；若每路过早截断，正确结果可能没有机会进入融合。反过来候选池无限放大又会拖慢 SQLite、embedding 和 rerank。

`SearchExecutionMeta` 是排障关键：没有它，只能看到“结果不好”；有它才能区分 FTS 没召回、embedding 降级、scope 过滤掉、rerank 降权或 MMR 去重。读 §3 时为每一阶段记录 `输入候选数/输出候选数/降级原因`，检索代码就会从算法名词变成一条可调试流水线。

---
<!-- END INLINE SOURCE EXPANSION 24-TYPES -->

## 3. 主流程逐段拆解：`searchWithMeta()` 从头到尾

签名（`SqliteEvidenceStore.ts`）：

```ts
async search(query: string, options?: SearchOptions): Promise<EvidenceItem[]> {
  return (await this.searchWithMeta(query, options)).items;
}
async searchWithMeta(query: string, options?: SearchOptions): Promise<EvidenceSearchExecution>
```
`search()` 只是丢掉 meta 的薄壳。真正的实现是 `searchWithMeta()`。【源码】

### 3.0 全景时序（先记这张图）

```
searchWithMeta(query, options)
 │
 ├─[A] ensureOpen(); limit = options.limit ?? 10
 │     bm25Pool = mode==='hybrid' ? clamp(limit*4, 20, 100) : limit
 │     trimmed = query.trim();  if (!trimmed) return { items: [], meta:{degraded:false} }
 │
 ├─[B] lexicalBackfillWords = splitLexicalBackfillWords(trimmed)
 │     queryEntityMatches   = entityRegistry.resolveQuery(trimmed)
 │
 ├─[C] 解析过滤上下文：effectiveKind / excludeSessionAndThread /
 │     excludePackKnowledge / threadAnchor / suppressBackstop
 │
 ├─[D] exact-anchor 旁路   SELECT * FROM evidence_docs WHERE anchor = ? COLLATE NOCASE
 │
 ├─[E] FTS5 逐级放松循环   for (ftsQuery of buildProgressiveFtsQueries(trimmed))
 │        命中 >0 就 break
 │
 ├─[F] lexical backfill    LOWER(title/summary/keywords) LIKE %word%
 │        rankLexicalBackfillRows() → 再整体 results.sort(compareEvidenceItems...)
 │
 ├─[G] entity mention 挂载 hydrateEntityMentionDocs()
 │
 ├─[H] ★ if (depth === 'raw') → rawPassageSearch() → return    （不走下面任何一步）
 │
 ├─[I] applyAuthorityBoost(results, trimmed)      try/catch 吞掉
 │     lexicalCandidates = results.slice(0, bm25Pool)
 │     lexicalResults    = results.slice(0, limit)
 │
 └─[J] switch (mode)
        ├ lexical  → 直接 enrich(lexicalResults, targetLimit=undefined)
        ├ semantic → isEmbeddingAvailable()? semanticNNSearch(limit) : lexicalResults
        │            → mergeEntityResults(..., limit) → enrich(targetLimit=undefined)
        └ hybrid   → isEmbeddingAvailable()? hybridRRFSearch(lexicalCandidates, f200Pool)
                     → mergeEntityResults(..., f200Pool) → enrich(targetLimit=limit)
```

### 3.1 [A] 候选池怎么算的（这个数字面试常被追）

```ts
const limit = options?.limit ?? 10;
// P2 fix (砚砚): hybrid needs a wider BM25 candidate pool for meaningful RRF
const bm25Pool = options?.mode === 'hybrid' ? Math.min(Math.max(limit * 4, 20), 100) : limit;
```
**只有 hybrid 撑大候选池，semantic 和 lexical 都是 `limit`。**【源码】

`clamp(limit*4, 20, 100)` 这个公式在代码里出现了 **4 次**，全是一样的写法：
1. `searchWithMeta()` 的 `bm25Pool`
2. `semanticNNSearch()` 内部的 `pool`（注释 `// 砚砚: generous pool, cap 100`）
3. `hybridRRFSearch()` 内部的 `pool`（NN 侧）
4. `rawPassageSearch()` 的 `pool`

【ADR】ADR-020 也写明了 "hybrid 的 BM25 候选池 = max(limit×4, 20) cap 100"。

空查询直接短路：`if (!trimmed) return { items: [], meta: { degraded: false } };`【源码】

### 3.2 [C] scope → SQL 条件的完整映射

```ts
const effectiveKind =
  options?.kind ??
  (options?.scope === 'threads' ? ('thread' as EvidenceKind)
   : options?.scope === 'sessions' ? ('session' as EvidenceKind)
   : undefined);
const excludeSessionAndThread = options?.scope === 'docs' || options?.scope === 'memory';
// F129 AC-A10
const excludePackKnowledge = effectiveKind !== 'pack-knowledge';
const threadAnchor = options?.threadId ? `thread-${options.threadId}` : undefined;
```

| scope | effectiveKind | 生成的 SQL 片段 |
|---|---|---|
| `threads` | `'thread'` | `AND kind = 'thread'` |
| `sessions` | `'session'` | `AND kind = 'session'` |
| `docs` / `memory` | undefined | `AND kind != 'session' AND kind != 'thread'` |
| `all` / 未给 | undefined | （无） |

注意注释里明写 `scope='threads' → kind='thread' (P1 fix: was incorrectly mapped to 'session')` ——曾经写反过。【源码】

`excludePackKnowledge` 是「反向默认」：**除非你显式 `kind='pack-knowledge'`，否则永远排除 pack 知识**，条件是 `AND kind != 'pack-knowledge'`。【源码】

`suppressBackstop` 的判定是**动态 import + try/catch**：

```ts
let suppressBackstop = false;
if (!options?.includeBackstop) {
  try {
    const { freezeFlags } = await import('./f163-types.js');
    suppressBackstop = freezeFlags().compression !== 'off';
  } catch { /* f163-types not available — no suppression */ }
}
```
【源码】—— 有意思的是文件顶部已经 `import { freezeFlags } from './f163-types.js'` 了，这里又动态 import 一次，属于历史遗留的防御写法。

### 3.3 [D] exact-anchor 旁路：为什么需要它

注释原文：

> FTS5 unicode61 tokenizer splits "F042" → "F"+"042" and "ADR-005" → "ADR"+"005".
> For anchor-shaped queries, do a direct lookup so precision isn't lost.

【源码】

SQL 起手式是 `SELECT * FROM evidence_docs WHERE anchor = ? COLLATE NOCASE`，然后按需追加 `kind` / `kind != session/thread` / `kind != pack-knowledge` / `status` / `keywords LIKE` / `anchor = threadAnchor` / `provenance_tier` / `activation != 'backstop'` / `world_id` / `scene_id`。

**它不加 `dateFrom` / `dateTo`。** 这是 FTS 分支和 backfill 分支都有、唯独 exact-anchor 分支缺的过滤。命中的话直接 `results.push(rowToItem(exactRow)); seenAnchors.add(...)`，永远排在结果第一位。【源码】

> **面试官追问：exact-anchor 命中后还继续跑 FTS 吗？**
> 「跑。它只是先把这一条塞进 `results` 并登记到 `seenAnchors`，后面所有分支都用 `if (!seenAnchors.has(row.anchor))` 去重，所以精确命中天然排第一、且不会重复。它也是后面 `compareEvidenceItemsByLexicalBackfill(a, b, signals, exactRow?.anchor)` 的『免疫锚』——backfill 重排时精确命中依然钉在第一。」

### 3.4 [E] FTS5 层：SQL、BM25 权重、progressive relaxation

#### SQL 本体

```sql
SELECT d.*, bm25(evidence_fts, 5.0, 1.0) AS rank
FROM evidence_fts f
JOIN evidence_docs d ON d.rowid = f.rowid
WHERE evidence_fts MATCH ?
  -- ... 各种可选过滤 ...
ORDER BY (d.superseded_by IS NOT NULL),
         (d.source_path LIKE 'archive/%'),
         (CASE WHEN d.provenance_tier = 'authoritative' THEN 0
               WHEN d.provenance_tier IS NOT NULL THEN 1
               ELSE 2 END),
         rank
LIMIT ?
```
【源码】

**BM25 权重 `5.0, 1.0` 对应的是 `evidence_fts` 的两列：`title` 和 `summary`。** 不是「标题 vs 正文」——`evidence_fts` 里根本没有正文列。这是架构文档写得不准确的地方（见本篇 §7 末尾的「文档与代码不一致清单」）。

`bm25()` 在 SQLite FTS5 里返回的是**负数，越小越相关**，所以 `ORDER BY ... rank` 升序是对的。代码里 `PassageResult.rank` 的注释也确认了这点：`/** BM25 relevance score from passage_fts (lower = more relevant) */`。【源码】

ORDER BY 的四列语义（SQLite 里 boolean 表达式产出 0/1，0 排前面）：
1. `superseded_by IS NOT NULL` → 被取代的排最后
2. `source_path LIKE 'archive/%'` → **注意是 `archive/`，不是 `internal-archive/`**（文档写错了）
3. provenance_tier 三档：authoritative(0) < 其它非 null(1) < null(2)
4. BM25 rank

#### `buildProgressiveFtsQueries()` 完整算法（`fts-query-builder.ts`）

```ts
export function buildProgressiveFtsQueries(query: string): string[] {
  const tokens = query.trim().split(/\s+/).filter(Boolean);
  if (tokens.length === 0) return [];
  const escaped = tokens.map(escapeToken);          // `"tok"`，内部 " → ""
  if (tokens.length <= 3) return [escaped.join(' ')];   // ← 关键分支：≤3 词不放松

  const queries: string[] = [];
  queries.push(escaped.join(' '));                  // Level 1: AND-all（空格=隐式AND）

  const strong: string[] = []; const weak: string[] = [];
  for (let i = 0; i < tokens.length; i++) {
    if (isStrongToken(tokens[i])) strong.push(escaped[i]); else weak.push(escaped[i]);
  }
  if (strong.length > 0 && weak.length > 0) {
    const strongPart = strong.join(' AND ');
    const weakPart = weak.length === 1 ? weak[0] : `(${weak.join(' OR ')})`;
    queries.push(`${strongPart} AND ${weakPart}`);  // Level 2
  }
  queries.push(escaped.join(' OR '));               // Level 3: OR-all
  return queries;
}
```

`isStrongToken()` 的四条规则（**按顺序短路**）：
1. `/^(?:F|ADR|LL|KD|TD|AC|HW)-?\d+[a-zA-Z]?$/i` → 实体标识（F042 / ADR-005 / AC-B1 / HW-6）
2. `/^Phase[A-Z]$/i` → PhaseA / PhaseB
3. `/[一-鿿㐀-䶿]/` → 含 CJK 字符（注意这里用的是字符区间字面量，不是 `\p{Script=Han}`，和 `hasCJKCharacters()` 是**两套不同的正则**）
4. `token.length >= 4`
否则弱。【源码】

**三个容易被追问的细节**【源码】：
- **≤3 个 token 只返回 1 条查询，完全不放松。** 所以「短查询 0 结果」不会被 relaxation 救回来。
- Level 2 在 `strong.length === 0` 或 `weak.length === 0` 时**整级被跳过**，数组只有 2 条。
- `strongPart AND weakPart` 里的 `AND` 是显式的，注释写明原因：*"FTS5 requires explicit AND before parenthesized OR groups — implicit AND (space) before `(...OR...)` throws syntax error."*

#### 调用侧的降级循环

```ts
for (const ftsQuery of ftsQueries) {
  try {
    // ... 拼 SQL, 执行 ...
    const rows = this.db?.prepare(sql).all(...params) as RowShape[];
    for (const row of rows) { if (!seenAnchors.has(row.anchor)) { results.push(rowToItem(row)); seenAnchors.add(row.anchor); } }
    if (rows.length > 0) break;   // HW-6: 命中就不再放松
  } catch {}
}
```
【源码】`catch {}` 是空的——FTS5 语法错误（比如 query 里有裸的 `*` 或 `^`）会被静默吞掉，然后自动尝试下一级。这既是防御也是坑（见 §5）。

> **面试官追问：progressive relaxation 具体几级、怎么降？**
> 「三级，但要分情况说：token ≤ 3 时只有一级（AND-all），根本不放松；token ≥ 4 时才是三级——Level 1 AND-all、Level 2『强 token 全 AND + 弱 token 括号 OR』、Level 3 全 OR。降级靠调用侧的 for 循环，每级执行完看 `rows.length > 0` 就 break。强 token 的判定是四条规则：实体正则 `^(F|ADR|LL|KD|TD|AC|HW)-?\d+[a-zA-Z]?$`、`^Phase[A-Z]$`、含 CJK、长度 ≥4。设计动机在注释里：F200 HW-6 发现长查询在 AND-all 语义下 75% 空结果。」

### 3.5 [F] Lexical backfill：匹配规则与排序规则

**触发条件**：`if (lexicalBackfillWords.length > 0)` —— **不是「FTS 没结果时才跑」，是无条件跑**（只要 query 分得出词）。这是架构文档描述错误的第二处。【源码】

分词：
```ts
export function splitLexicalBackfillWords(query: string): string[] {
  return [...new Set(query.trim().toLowerCase().split(/\s+/).filter(Boolean))];
}
```
—— 小写 + 空白切分 + 去重。中文不分词，整段中文会当成一个「词」。【源码】

匹配 SQL（每个 word 生成一个三段 OR，词与词之间也是 OR）：
```sql
SELECT * FROM evidence_docs
WHERE ((LOWER(title) LIKE ? OR LOWER(COALESCE(summary,'')) LIKE ? OR LOWER(COALESCE(keywords,'')) LIKE ?)
    OR (…第二个词…))
  AND …同样一套过滤（kind/status/keywords/anchor/world/scene/date/provenance/backstop）…
```
参数是 `%word%`，每个词灌 3 次。【源码】

排序：`rankLexicalBackfillRows(rows, words)` 返回 `{ rows, signals }`。信号计算（`computeLexicalBackfillSignal`）：

```ts
for (const word of words) {
  const keywordHit = keywords.some(k => k.includes(word));   // keywords 是 JSON.parse 后小写化的数组
  const titleHit   = title.includes(word);
  const summaryHit = summary.includes(word);
  if (keywordHit) keywordHits += 1;
  if (titleHit)   titleHits   += 1;
  if (keywordHit || titleHit || summaryHit) textHits += 1;
}
```

排序比较器是**四级 lexicographic**：
1. `keywordHits` 降序
2. `titleHits` 降序
3. `textHits` 降序
4. `compareLexicalBackfillRowQuality()`：`superseded_by` 有的排后 → `source_path.startsWith('archive/')` 的排后 → `provenanceRank`（authoritative=0 / 非null=1 / null=2）升序 → `b.updated_at.localeCompare(a.updated_at)`（新的在前）

【源码】`lexical-backfill.ts`

然后回到 store，**backfill 会对整个 `results` 数组重排一次**（不只是新加的那些）：

```ts
if (signals.size > 0) {
  results.sort((a, b) => compareEvidenceItemsByLexicalBackfill(a, b, signals, exactRow?.anchor));
}
```
`compareEvidenceItemsByLexicalBackfill` 里第一件事是 exactAnchor 免疫：精确命中恒返回 -1 排最前；其余按三个 hits 比。**没有信号的 item（比如纯 FTS 命中但不含子串的）signals 取不到，退化成 `{0,0,0}`，会被挤到后面。**【源码】

> **面试官追问：backfill 会不会把 BM25 的排序毁掉？**
> 「会，而且是有意的。只要 backfill 匹配到任何行（`signals.size > 0`），整个 results 数组就按 keywordHits/titleHits/textHits 重排一遍，BM25 的 rank 顺序被覆盖。唯一的保护是 exact-anchor 恒排第一。我的理解是：这是把『关键词标签命中』当成比 BM25 更强的信号——因为 keywords 是 scanner 从 frontmatter topics 和 section heading 抽出来的人工语义标签。【推断】」

### 3.6 [G] Entity 层：别名解析 + mention 挂载

**第一步 `resolveQuery(query)`**（`EntityRegistry.ts`）：

```ts
resolveQuery(query: string): QueryEntityMatch[] {
  const rows = this.loadAliases({ includeCanonical: true });
  // rows 已按 length(alias_norm) DESC 排序 —— 长别名优先匹配
  for (const row of rows) {
    if (seen.has(row.entity_id)) continue;      // 每个实体只出一条
    if (!aliasMatchesText(query, row.alias)) continue;
    matches.push({ entityId, type, canonicalName, matchedAlias: row.alias, provenance });
  }
}
```

匹配器 `compileAliasMatcher(aliasNorm)` 分两种：
```ts
if (hasCJK(aliasNorm)) return (textNorm) => textNorm.includes(aliasNorm);   // CJK：裸子串
const escaped = aliasNorm.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
const pattern = new RegExp(`(^|[^\\p{L}\\p{N}_@-])${escaped}(?=$|[^\\p{L}\\p{N}_@-])`, 'u');
return (textNorm) => pattern.test(textNorm);    // 拉丁：词边界（把 _ @ - 也算词内字符）
```
归一化 `normalizeEntityAlias(v) = v.normalize('NFKC').trim().replace(/\s+/g,' ').toLowerCase()`。【源码】

**第二步 `findMentionAnchors(queryMatches, limit, filters)`**：

```sql
SELECT m.doc_anchor,
       MAX(CASE WHEN m.source = 'passage' THEN 1 ELSE 0 END) AS has_passage,
       MAX(m.created_at) AS latest_mention_at
FROM entity_mentions m JOIN evidence_docs d ON d.anchor = m.doc_anchor
WHERE m.entity_id IN (...)  -- 加全套 filters
GROUP BY m.doc_anchor
ORDER BY has_passage DESC, latest_mention_at DESC, m.doc_anchor
LIMIT ?
```
**排序规则可复述：有 passage 级提及的文档优先 > 最近提及时间倒序 > anchor 字典序（保证确定性）。**【源码】limit 传的是 `bm25Pool`。

**第三步 hydrate**：`hydrateEntityMentionDocs()` 拿到 anchors 后再 `SELECT * FROM evidence_docs WHERE anchor IN (...)` 加同一套过滤，然后**按 mentionHits.anchors 的原顺序**重排（`mentionHits.anchors.map(a => rowMap.get(a))`），保住上面那个 ORDER BY。【源码】

**mention 索引是怎么建的**：`refreshMentions(docAnchors?)`——先 `DELETE FROM entity_mentions`（给了 anchors 就只删这些），然后**全表扫 docs 和 passages，对每个 alias 做一次 matcher**：

```ts
for (const doc of docRows) {
  const textNorm = normalizeEntityAlias([doc.title, doc.summary ?? '', doc.keywords ?? ''].join('\n'));
  for (const alias of aliases) { if (!alias.matchesNormalizedText(textNorm)) continue; insertStmt.run(...); }
}
for (const passage of passageRows) { const textNorm = normalizeEntityAlias(passage.content); /* 同上 */ }
```
复杂度 O(docs × aliases + passages × aliases)，全在一个 transaction 里。触发点：`upsert()` 尾部 `this.entityRegistry?.refreshMentions(items.map(i => i.anchor))`、`upsertEntities()` 里 `if (changed) refreshMentions()`（全量！）、`IndexBuilder.indexPassages()` 每个 thread 一次。【源码】

**实体种子从哪来**（`entity-seeds.ts`）：
- `config/entity-seeds.json`（相对 `entity-seeds.ts` 往上 5 层），格式必须是 `{ version: 1, entities: [...] }`，否则 **throw**（不是 fail-open）
- 加 `buildRosterEntitySeeds(loadCatConfig())`：每只猫生成 `entityId = 'cat:' + catId`，aliases = `[catId, '@'+catId, name, displayName, nickname, ...mentionPatterns, ...去掉@的mentionPatterns]`
- `catSpecificAlias()` 会**剔除** `breedId` / `breedDisplayName` / `variantLabel` 以及**被多只猫共用的 nickname**（`findSharedNicknameNorms()` 统计 count>1 的）——避免「小猫」这种昵称把所有猫都召回
- provenance 固定写 `'One-way roster to F209 entity registry retrieval anchor; never written back to roster.'`

【源码】

> **面试官追问：entity 召回的结果怎么和 BM25 结果合并？**
> 「分两步。第一步在 `searchWithMeta` 里，entity hydrate 出来的 items 用 `if (!seenAnchors.has(...))` 追加到 results 尾部，不插队。第二步在 `mergeEntityResults(entityItems, items, limit)` 里给 entity 结果**配额保护**：`entityCap = items.length > 0 ? max(1, ceil(limit/2)) : limit`——也就是说 semantic/hybrid 结果非空时，entity 最多占一半，先填 entity 再填主路结果，去重后 slice 到 limit。这个只在 semantic 和 hybrid 分支调用，lexical 分支不调。」

### 3.7 [H] `depth=raw` 分支：一条完全不同的路

**这是最容易被忽略的分支边界**：`depth==='raw'` 在 authority boost **之前**就 return 了：

```ts
if (options?.depth === 'raw') {
  const rawResult = await this.rawPassageSearch(query, results, limit, options,
                                                queryEntityMatches, entityMentionDocs.matchesByAnchor);
  return { items: this.enrichWithDrillDown(rawResult.items, undefined, options, trimmed),
           meta: rawResult.meta };
}
```
所以 **raw 路径没有 authority boost、没有 mergeEntityResults、targetLimit 是 undefined（因此没有 MMR）**。【源码】

`rawPassageSearch()` 的第一件事是**一个 scope 短路**：

```ts
if (options.scope && options.scope !== 'all' && options.scope !== 'threads') {
  return { items: this.attachEntityMatches(this.rankRawResults(baseResults, limit), entityMatchesByAnchor),
           meta: { degraded: false } };
}
```
**`depth=raw` + `scope=docs`（或 `sessions`/`memory`）根本不做 passage 检索**，直接返回 doc 级结果。因为 passages 只有 thread 消息。【源码】

之后的分支：

```
pool = clamp(limit*4, 20, 100);  lexical = () => this.searchPassages(query, pool, {dateFrom, dateTo})

mode==='semantic':
   isPassageEmbeddingAvailable()? 
      ├ 否 → passages = lexical(); meta = { degraded:true, degradeReason:'passage_embedding_unavailable', effectiveMode:'lexical' }
      └ 是 → try semanticPassageNNSearch(query, pool, options)
             catch → lexical(); meta = { degraded:true, degradeReason:'passage_vector_search_error', effectiveMode:'lexical' }
mode==='hybrid':
   同上两个降级分支，成功路径是 hybridPassageRRFSearch(query, lexical(), pool, options)
else: passages = lexical()   （meta 保持 degraded:false）
```
【源码】

然后 **entity passage 合并**：
```ts
const entityPassageHits = this.entityRegistry?.findMentionPassages(queryEntityMatches, pool,
                            { threadId, dateFrom, dateTo });
const entityPassages = entityPassageHits?.passages.map(p => this.entityHitToPassageResult(p)) ?? [];
const mergedPassages = mergePassageResults(entityPassages, passages);   // entity 在前，按 key 去重
```
`entityHitToPassageResult()` 会**硬标 `_semanticHit: true`**，注释：`// entity registry is a strong signal, not cross-post contamination`。【源码】

#### `searchPassages()` 的 SQL

```sql
SELECT p.doc_anchor, p.passage_id, p.content, p.speaker, p.position, p.created_at,
       bm25(passage_fts) AS rank
FROM passage_fts f JOIN evidence_passages p ON p.rowid = f.rowid
WHERE passage_fts MATCH ?
  [AND p.created_at >= ?] [AND p.created_at <= ?]
ORDER BY rank LIMIT ?
```
`bm25(passage_fts)` **不带权重参数**（只有一列 content）。同样跑 progressive relaxation 循环，命中就 return。【源码】

#### `hydratePassageResults()` 里的 BUG-UX-14 跨帖污染护栏

三个状态变量：
```ts
const scopeActive = options?.scope && options.scope !== 'all';
const hasExplicitThreadTarget = !!threadAnchor;
const hasDocLevelResults = results.length > 0;
```
对每个「passage 命中但 doc 级没命中」的 anchor：
```ts
if (scopeActive && !hasExplicitThreadTarget && hasDocLevelResults) {
  const hasPurelySemanticDiscovery = pList.some(p => p._semanticHit && !p._alsoLexical);
  if (!hasPurelySemanticDiscovery) continue;      // 挡掉
}
// R3 kind-scope 二次校验：即使上面放行，parentDoc.kind 必须匹配 scope
if (scopeActive && !hasExplicitThreadTarget) {
  const effectiveKind = scope==='threads' ? 'thread' : scope==='sessions' ? 'session' : undefined;
  if (effectiveKind && parentDoc.kind !== effectiveKind) continue;
  if (excludeSessionAndThread && (parentDoc.kind==='session' || parentDoc.kind==='thread')) continue;
}
item = rowToItem(parentDoc);
item.summary = `[passage match] ${speaker ? speaker+': ' : ''}${content.slice(0, 200)}`;
```
【源码】

`_alsoLexical` 是在 `hybridPassageRRFSearch()` 里打的：
```ts
const existing = passageMap.get(key);
if (existing && !existing._semanticHit) { passage._alsoLexical = true; }
```
—— 也就是「这条 NN 命中的 passage，之前已经被 lexical FTS 找到过」。注释解释了判据：*"Cross-posts share literal query terms and appear in both lexical and NN results. Purely semantic discoveries appear only in NN results."*【源码】

`rankRawResults()` 的三级排序：**有 passages 的排前 → 按 `passageOrder`（该 anchor 第一条 passage 在合并列表里的下标）→ 按原始顺序**，最后 `slice(0, limit)`。【源码】

`toEvidencePassage()` 顺手做了 ID 反解：`threadId = docAnchor.startsWith('thread-') ? slice(7) : undefined`、`messageId = passageId.startsWith('msg-') ? slice(4) : undefined`。这两个字段直接喂给第 14 层的 drillDown。【源码】

### 3.8 [I] Authority boost

```ts
const AUTHORITY_WEIGHTS: Record<F163Authority, number> = {
  constitutional: 1.3, validated: 1.2, candidate: 1.1, observed: 1.0,
};

function applyAuthorityBoost(results: EvidenceItem[], query = ''): void {
  const flags = freezeFlags();
  if (flags.authorityBoost === 'off' || results.length < 2) return;
  if (findExactLexicalProtectedAnchors(results, query).size > 0) return;   // ← 整体跳过

  const K = 60;
  const scored = results.map((item, i) => ({
    item, score: (1 / (i + K)) * AUTHORITY_WEIGHTS[(item.authority as F163Authority) ?? 'observed'],
  }));
  scored.sort((a, b) => b.score - a.score);
  if (flags.authorityBoost === 'on') { for (let i=0;i<results.length;i++) results[i] = scored[i].item; }
  // shadow: order unchanged, but boost was computed
}
```
【源码】

四个可复述要点：
1. **权重区间 1.0–1.3**，代码注释写着 `(1.0–1.3 range, spec constraint)`。
2. 位置分是 `1/(i + 60)`，**注意是 `i + K` 不是 `K + i` 的另一种写法——数值相同**，注释解释选它的理由：*"RRF-style positional score: 1/(rank+k) keeps adjacent positions close so the 1.0–1.3 authority weight can meaningfully reorder near-tied items."*
3. **shadow 模式什么都不改**，只是白算一遍（注释：`(logging in Task 7)`——日志其实没实现）。
4. 调用侧包了 try/catch，注释 `// Kill-switch: boost failure → continue with original ranking`。

`findExactLexicalProtectedAnchors()` 是 authority boost 和 consumption rerank **共用**的护栏：

```ts
function findExactLexicalProtectedAnchors(results, query): Set<string> {
  const words = splitLexicalBackfillWords(query.trim());
  if (words.length === 0) return new Set();
  if (words.length < 2 && !hasCJKCharacters(trimmed)) return new Set();   // 单个英文词不保护
  for (const item of results) if (isExactLexicalMatch(item, words)) protectedAnchors.add(item.anchor);
}
function isExactLexicalMatch(item, words): boolean {
  const haystack = `${title} ${summary} ${keywords.join(' ')}`.toLowerCase();
  if (!words.every(w => haystack.includes(w))) return false;         // 全部词都要在
  return words.some(w => title.includes(w) || keywords.includes(w)); // 且至少一个在 title/keywords
}
```
【源码】—— 「单个英文词不触发保护，但单个中文词会（因为 `hasCJKCharacters`）」是个很细但真实的分支。

**两处用法不同**：authority boost 是「有任何保护锚 → 整个 boost 不做」；consumption rerank 是「保护锚拎出来置顶，剩下的才参与重排」。【源码】

### 3.9 [J] 三种 mode 的分派

#### `mode='lexical'`（默认）
```ts
return { items: this.enrichWithDrillDown(
           this.attachEntityMatches(lexicalResults, entityMentionDocs.matchesByAnchor),
           undefined, options, trimmed),
         meta: { degraded: false } };
```
`lexicalResults = results.slice(0, limit)`。**targetLimit=undefined → 不触发 MMR、不截断。**【源码】

#### `mode='semantic'`
```ts
const embeddingAvailable = await this.isEmbeddingAvailable();
if (!embeddingAvailable) { return 上面那个 lexical 结果（meta 依然 degraded:false）; }
try {
  const semanticItems = await this.semanticNNSearch(query, limit, options, suppressBackstop);
  return enrich(attachEntity(mergeEntityResults(entityItems, semanticItems, limit)), undefined, ...);
} catch { return 同样的 lexical 结果; }
```
`semanticNNSearch()` 内部：
```ts
const pool = Math.min(Math.max(limit * 4, 20), 100);
const queryVec = await this.embedDeps!.embedding.embed([query]);
const nnResults = this.embedDeps!.vectorStore.search(queryVec[0], pool);
if (nnResults.length === 0) return [];
// 一条 IN(...) hydrate，注释 "(砚砚: no N+1)"
let sql = `SELECT * FROM evidence_docs WHERE anchor IN (${placeholders})`;
// 重新算一遍 effectiveKind / excludeSessionAndThread / excludePackKnowledge（没有复用外层变量）
// + status / keywords / threadAnchor / worldId / sceneId / provenanceTier / suppressBackstop
return nnResults.filter(r => docMap.has(r.anchor)).map(r => docMap.get(r.anchor)!).slice(0, limit);
```
**返回顺序 = NN 距离顺序**（vec0 的 `k=?` 查询本身按 distance 升序返回），过滤只做减法不重排。【源码】

注意：`semanticNNSearch` 里 **`dateFrom`/`dateTo` 没有被应用**——doc 级 semantic 路径不支持时间过滤。【源码】

#### `mode='hybrid'`
```ts
const f200Pool = freezeF200Flags().consumptionRerank !== 'off' ? limit * 3 : limit;
const hybridItems = await this.hybridRRFSearch(query, lexicalCandidates, f200Pool, options, suppressBackstop);
return enrich(attachEntity(mergeEntityResults(entityItems, hybridItems, f200Pool)), limit, options, trimmed);
```
**这是唯一 `targetLimit` 非 undefined 的路径**，也是唯一可能触发 MMR 的路径。`f200Pool = limit*3` 的目的就是给 consumption rerank 和 MMR 留够候选。【源码】

一个具体数值链（limit=5，`F200_CONSUMPTION_RERANK=on`）：
```
bm25Pool = clamp(20, 20, 100) = 20        → lexicalCandidates 最多 20 条
f200Pool = 5 * 3 = 15
hybridRRFSearch 内 pool = clamp(15*4, 20, 100) = 60   → NN 取 60 条
RRF 融合后 slice(0, 15)
mergeEntityResults(..., 15)：entityCap = max(1, ceil(15/2)) = 8
enrichWithDrillDown(items, targetLimit=5)
  → applyConsumptionRerank(results, db, 5, query)
      → MMR 触发条件 movable.length >= 3*5 = 15
  → results.length = 5
```
【源码】按代码逐步推算。

> **面试官追问：embedding 挂了会怎样？**
> 「三层判定。第一层 `isEmbeddingAvailable()`：`if (!deps || deps.mode !== 'on') return false;` → `await deps.embedding.reprobeIfNeeded()`（catch 即 false）→ `return deps.embedding.isReady()`。第二层：不可用就直接返回 lexical 结果，**但 meta 依然是 `degraded:false`**——doc 级降级是静默的。第三层：可用但 `embed()` 抛异常（HTTP 超时/500），外层 try/catch 兜住，也退 lexical。所以 hybrid 在 embedding 全挂时行为上等于 lexical，只是 CPU 白跑了一遍 reprobe。」

### 3.10 出口层：`enrichWithDrillDown()`

执行顺序是固定的四步：

```ts
private enrichWithDrillDown(results, targetLimit?, options?, query = ''): EvidenceItem[] {
  try { if (this.db) applyConsumptionRerank(results, this.db, targetLimit, query); }
  catch { /* F200 kill-switch */ }
  if (targetLimit && results.length > targetLimit) results.length = targetLimit;   // ← 截断在 rerank 之后
  annotateMatchReasons(results, query, options?.explain);
  for (const item of results) { /* drillDown 四分支 */ }
  return results;
}
```
【源码】

**drillDown 四分支（按 if-else 顺序，先匹配先赢）**：

| 条件 | tool | params |
|---|---|---|
| `item.passages` 里存在同时有 `threadId` 和 `messageId` 的 passage | `cat_cafe_get_thread_context` | `{ threadId, messageId, before:'3', after:'3' }` |
| `kind === 'thread'` 且 `anchor.startsWith('thread-')` | `cat_cafe_get_thread_context` | `{ threadId: anchor.replace('thread-','') }` |
| `kind === 'session'` 且 `anchor.startsWith('session-')` | `cat_cafe_read_session_digest` | `{ sessionId }` |
| `item.sourcePath` 存在且能解析 | `cat_cafe_read_file_slice` | `{ path, startLine:'1', endLine:'120' }` |

【源码】注意 params 的值全是**字符串**（`'3'` 不是 `3`），因为 `EvidenceDrillDown.params` 类型是 `Record<string, string>`。

`resolveSourcePathForDrillDown()` 的路径逃逸防护：
```ts
if (!this.sourceRoot) return null;
const resolved = isAbsolute(sourcePath) ? resolve(sourcePath) : resolve(this.sourceRoot, sourcePath);
const rel = relative(this.sourceRoot, resolved);
if (!rel || rel.startsWith('..') || isAbsolute(rel)) return null;   // ← 越界直接不给 drillDown
const publicRel = rel.split(/[\\/]+/).filter(Boolean).join('/');    // Windows 反斜杠归一
if (!this.sourceRef) {
  return basename(this.sourceRoot) === 'docs' && !publicRel.startsWith('docs/') ? `docs/${publicRel}` : publicRel;
}
return `cat-cafe://collection/${encodeURIComponent(this.sourceRef)}/${publicRel.split('/').map(encodeURIComponent).join('/')}`;
```
【源码】外部 collection 用自定义 URI scheme `cat-cafe://collection/<id>/<path>`。

`annotateMatchReasons()` 的优先级链（第一个命中即停）：
`entityMatches[0]` → `entity:<entityId>` ；`anchor.includes(q)` → `'anchor'`；`title` → `'title'`；`summary` → `'summary'`；`keywords.some` → `'keyword'`；否则 `'content'`。
`q` 是**整条 query 小写后的原串**（不分词），所以多词 query 基本都会落到 `'content'`。【源码】

`explain=true` 时额外写 `item.rankingFactors = { bm25Score: results.indexOf(item) + 1 }` —— 这个字段名叫 bm25Score，值其实是**1-based 名次**，不是 BM25 分。【源码】

### 3.11 `intent=coverage`：绕开整条管线

在 `routes/evidence.ts` 里，coverage 判定在**所有其它逻辑之前**：

```ts
if (intent === 'coverage') {
  const { CoverageSearchService } = await import('../domains/memory/CoverageSearchService.js');
  const coverageService = new CoverageSearchService(opts.evidenceStore);
  const coverageResult = await coverageService.search(q);
  return coverageResult;    // ← 直接返回，不进 KnowledgeResolver、无 variantId、无 salience
}
```
【源码】注释里明写了两个 v1 缺口：`conventionGraph` 没有生产实现（F242 软依赖）、`onCoverageEvent` 遥测没接线（`catId`/`invocationId` 是空串占位）。

`CoverageSearchService.search()` 五步：

```
Step 1  Promise.all([ searchScope(q,'docs',25), searchScope(q,'threads',20) ])
        每个 scope 的 opts 都是 { scope, mode:'hybrid', limit }
Step 2  addDirectHits(docs 先, threads 后)  → matchType:'direct', confidence: item.confidence ?? 1
Step 3a expandViaFrontmatter(docsItems)：把命中文档的每个 keyword 当 query 再搜 docs limit=5
        → matchType:'alias', confidence ?? 0.7, expansionProvenance {source:'frontmatter-alias', via:`keyword:${term}`, confidence:'heuristic'}
Step 3b expandViaSourceThreads([...docs, ...threads])：正则 /thread-[a-z0-9_-]+/gi 从 sourceIds 和 summary 里抠 thread 引用，
        每个再搜 threads limit=3 → matchType:'source-thread', confidence ?? 0.6
Step 4  expandViaConventionGraph：无 adapter 或 !isAvailable() → degraded.push({source:'convention-graph', reason});
        否则 queryConsumers(hit.anchor)，consumer.stale → staleSkips++ 跳过；
        confidence = consumer.confidence==='static' ? 0.9 : 0.7；配额 10
Step 5  capped = matrix.slice(0, COVERAGE_MAX_TOTAL)   // 50
```
配额常量在 `coverage-search-types.ts`：`COVERAGE_QUOTA = { docs: 25, threads: 20, conventionGraph: 10 }`，`COVERAGE_MAX_TOTAL = 50`。【源码】

**两个坑**【源码】：
- Step 3a/3b **没有 `MAX_TERMS_PER_TYPE` 限制**（只有 topk 版的 `TopkExpansionService` 才有 `MAX_TERMS_PER_TYPE = 5`）。keywords 有多少就发多少次子查询，每次都是 hybrid。
- `expandViaSourceThreads` 里 `threadPattern` 带 `g` 标志却用 `.test()`，所以每次都要手动 `threadPattern.lastIndex = 0` 复位——代码里确实写了这行，注释 `// reset regex state`。

`gaps` 字段始终返回 `[]`（硬编码），说明「覆盖缺口」这个能力还没实现。【源码】

### 3.12 索引构建：扫描 → hash → 增量判定 → 写入 → embed

`IndexBuilder.rebuild()` 的完整阶段（`report(phase, percent)` 回调值就是进度条）：

```
report('scanning', 0)   → scanner.discover(scanRoot, buildScanOptions())
report('scanning', 15)
  逐条：sourceHash = sha256(rawContent).slice(0,16)     ← 只取前 16 位十六进制
        增量判定：!effectiveForce && existing?.sourceHash === item.sourceHash
                  && !(缺 provenance 但新扫描有) → skipped++, continue
        kind-priority 守卫：newPriority < existingPriority && 旧文件还在磁盘 → skipped++
        store.upsert([item])
report('indexing', 30)  → 删 frontmatter 边（relation IN ('related','related_to') AND provenance='frontmatter'）
                          重抽 related_features → addEdge(relation:'related_to', provenance:'frontmatter')
                          删 provenance='content' 的边，重抽 wikilink / feature_ref / doc_link
report('indexing', 40)  → discoverSessionDigests()：扫 dataDir/threads/*/*/sessions/*/digest.extractive.json
report('indexing', 55)  → indexPassages(threads) + backfillPassagesFromTranscript(threadId)
report('cleanup', 70)   → 删除 currentAnchors 里没有的 anchor（+ vectorStore.delete）
report('embedding', 80) → embedIndexedItems({...})
report('done', 100)
```
【源码】

**增量判定的两个 hash**【源码】：
- 文件类：`createHash('sha256').update(scanned.rawContent).digest('hex').slice(0, 16)`
- thread 类：`computeThreadSourceHash(title, summary, keywords) = sha256(JSON.stringify({title, summary, keywords})).slice(0,16)`
- `CollectionIndexBuilder` 用的是**完整 64 位** hash（`.digest('hex')` 不 slice），且额外比 `existing.authority === manifest.reviewPolicy.authorityCeiling`

**KIND_PRIORITY**（决定同一 anchor 被多个文件声明时谁赢）：
```ts
feature: 4, decision: 3, plan: 2, discussion: 2, research: 2,
session: 1, lesson: 1, thread: 1, 'pack-knowledge': 0
```
【源码】

**`INDEXING_VERSION = 5`** 是「scanner 逻辑变更版本号」，存在 `embedding_meta` 的 `indexing_version` 键。`hasIndexingVersionChanged()` 读不到或不等 → `effectiveForce = options?.force || versionChanged`，自动全量重建。版本历史在注释里：1 初始 / 2 section heading→keywords / 3 pathToAuthority 回填 / 4 entity mention 抽取 / 5 SVG text + message block alt/caption。【源码】

**passage 索引**（`indexPassages`）用的是**条件 UPSERT**：
```sql
INSERT INTO evidence_passages (...) VALUES (...)
ON CONFLICT(doc_anchor, passage_id) DO UPDATE SET content=..., speaker=..., position=..., created_at=...
WHERE evidence_passages.content IS NOT excluded.content
   OR evidence_passages.speaker IS NOT excluded.speaker
   OR evidence_passages.position IS NOT excluded.position
   OR evidence_passages.created_at IS NOT excluded.created_at
```
`WHERE` 子句保证内容没变时 `result.changes === 0`，而 `if (result.changes > 0) passageVectorStore?.delete(key)` —— **内容变了才删向量，让 warmup 重新 embed**。取消息用 `messageListFn(thread.id, 2000)`。注释 `// Phase I (AC-I2): never delete missing passages on rebuild.`【源码】

**JSONL 回填**（`backfillPassagesFromTranscript`）：按 `invocationId` 聚合 `evt.event.type === 'text'` 的 content，`passage_id = 'transcript-' + invId`，`position` 从 **10000** 起（注释：`offset to avoid collision with Redis-sourced positions (0-based)`），用 `INSERT OR IGNORE` 保证幂等。【源码】

**debounce 参数**（`packages/api/src/index.ts`）：
```ts
const DIRTY_THREAD_FLUSH_INTERVAL_MS = 30_000;
// appendListener: ib.markThreadDirty(msg.threadId); ib.accumulateSummaryDelta(msg.threadId, msg.content);
const dirtyFlushTimer = setInterval(async () => { const flushed = await ib.flushDirtyThreads(); ... },
                                    DIRTY_THREAD_FLUSH_INTERVAL_MS);
dirtyFlushTimer.unref();
```
**30 秒一轮**，`unref()` 保证不阻塞进程退出。`flushDirtyThreads()` 里 thread summary 由最近 **100** 条消息拼成、截断到 **3000** 字符（`joined.length > 3000 ? joined.slice(0,2997)+'...' : joined`）。另有一条同步路径：`routes/callbacks.ts` 里 `markThreadDirty` 后立刻 `await flushDirtyThreads()`。【源码】

`accumulateSummaryDelta()` 的信号位（`summary-config.ts` 的 `SIGNAL_FLAGS`）：
```ts
tokenEstimate = Math.ceil(messageContent.length / 4);
/(?:决定|agreed|kd-|decided|confirmed)/i  → SIGNAL_FLAGS.DECISION
/(?:\.ts|\.js|\.tsx|pr\s*#|commit|merge|diff)/i → SIGNAL_FLAGS.CODE
/(?:fix|bug|error|修复|报错)/i → SIGNAL_FLAGS.ERROR_FIX
```
用 `pending_signal_flags | ?` 累积进 `summary_state`。【源码】

### 3.13 EmbeddingService：HTTP、超时、fail-open 的完整判定

```ts
const EMBED_BATCH_SIZE = 64;                       // 镜像 embed-api.py 的 MAX_BATCH_SIZE
private static readonly REPROBE_COOLDOWN_MS = 30_000;
```

**端点解析**（每次请求都重新解析，注释说明是为了让 `/install` `/reconfigure` 改端口后不用重启 API）：
```ts
const service = getServiceManifest('embedding-model');
if (!service) { const port = process.env.EMBED_PORT ?? '9880';
                return normalizeLoopbackUrl(process.env.EMBED_URL ?? `http://127.0.0.1:${port}`); }
return normalizeLoopbackUrl(resolveServiceEndpoint(service, process.env, getServiceConfig('embedding-model'))
                            ?? 'http://127.0.0.1:9880');
```
【源码】默认端口 **9880**。【ADR】ADR-020 同样写了 `:9880 embed-api.py · Qwen3-Embedding-0.6B · dim=768`。

**`load()`**：
```ts
const res = await fetch(`${this.resolveBaseUrl()}/health`, { signal: AbortSignal.timeout(5000) });
if (!res.ok) throw ...;
const health = await res.json() as HealthResponse;   // { status, model, backend, device, dim }
if (health.status === 'ok') { this.ready = true; this.modelId = health.model || this.config.embedModel; }
catch { this.ready = false; }   // 注释: fail-open: server not running → isReady()=false → lexical-only degradation
```
health 超时 **5000ms** 硬编码，和 `embedTimeoutMs` 无关。【源码】

**`reprobeIfNeeded()`**：
```ts
if (this.ready) return;                                   // 已 ready 就不再探
const now = Date.now();
if (now - this.lastProbeAt < REPROBE_COOLDOWN_MS) return; // 30s 冷却，防止每次搜索都打 /health
this.lastProbeAt = now;
await this.load();
```
【源码】注意 `lastProbeAt` 在 `load()` **之前**就更新了，所以冷却从「发起探测」算起。

**`embed(texts)`**：
```ts
if (!this.ready) throw new Error('EmbeddingService not ready — embed-api server not available');
for (let offset = 0; offset < texts.length; offset += EMBED_BATCH_SIZE) { ... embedBatch(batch) ... }
```
`embedBatch()`：`POST /v1/embeddings`，body `{ input: texts }`，`AbortSignal.timeout(this.config.embedTimeoutMs)`（默认 **3000ms**）。响应按 `d.index` 排序后转 Float32Array：
```ts
const arr = new Float32Array(targetDim);
for (let i = 0; i < Math.min(emb.length, targetDim); i++) arr[i] = emb[i]!;
```
**维度不匹配时静默截断/零填充**——服务端返回 1024 维、配置 768 维，前 768 维照用，不报错。【源码】

**默认配置**（`resolveEmbedConfig()`）：
```ts
embedMode: 'off', embedModel: 'unknown', embedDim: 768,    // 注释: LL-034: 768 is sweet spot for CJK bilingual; 256 too low
maxModelMemMb: 800, embedTimeoutMs: 3000
```
`embedMode` 不在 `{off, shadow, on}` 里会 **throw**。【源码】

**embed 的批大小分两档**（`embed-utils.ts`）：
```ts
const EMBED_BATCH_SIZE = 64;
// Passage contents are much longer than doc summaries; keep warmup calls below the 3s client timeout.
const PASSAGE_EMBED_BATCH_SIZE = 8;
```
doc 的 embed 文本是 `` `${i.title} ${i.summary ?? ''}` ``；passage 的是 `p.content` 原文。【源码】

**模型漂移检测**（`embedIndexedItems` 里）：
```ts
if (ctx.allDocsProvider) {
  const consistency = ctx.vectorStore.checkMetaConsistency(ctx.embedding.getModelInfo());
  if (!consistency.consistent) { ctx.vectorStore.clearAll(); ctx.onVectorReset?.(); toEmbed = ctx.allDocsProvider(); }
}
```
`checkMetaConsistency()` 比 `embedding_model_id` 和 `embedding_dim`，任一变了就返回 `{consistent:false, reason:'model changed: X → Y'}` → **清空全部向量 + 清空 passage 向量（`onVectorReset` 回调）+ 全量重 embed**。没有旧 meta 时返回 `{consistent:true, reason:'no prior meta'}`。【源码】

**warmup 的三层保护**（`embedMissingPassages()`）：
```ts
const PASSAGE_EMBED_SCAN_BATCH_SIZE = 256;   // 每轮扫 256 条 passage
// keyset 分页：WHERE id > ? ORDER BY id LIMIT ?
const missing = rows.filter(r => !existingStmt.get(passageVectorKey(r.docAnchor, r.passageId)));
try { await embedPassages(...); consecutiveErrors = 0; }
catch { consecutiveErrors++; if (consecutiveErrors >= 3) return; }   // 连续 3 批失败就停
```
外层 `startPassageEmbeddingWarmup()` 用 `setImmediate(() => void this.runPassageEmbeddingWarmup())`，注释写明：*"passage embedding is a recall accelerator (passage_fts stays canonical), so it MUST NOT block rebuild()/listen()"*。`isPassageWarmupActive()` 靠 `passageEmbeddingWarmupInFlight !== null` 判断。【源码】

---

<!-- BEGIN INLINE SOURCE EXPANSION 24-FLOW -->
### 3.12 源码执行复盘：同一条查询如何经过 lexical、semantic 和 hybrid

假设查询是：`F200 callback 鉴权失败如何降级`，`limit = 5`，`mode = hybrid`。不要把 `searchWithMeta()` 想成“一条 SQL 返回 5 行”，它更像一条多阶段漏斗。

#### 阶段 0：规范化输入和确定预算

先 `trim` 查询，解析 scope/kind/status/provenance 等过滤条件，并根据 `limit` 计算候选池。候选池通常大于最终 5 条，因为后面还有去重、失效过滤、重排和多样性裁剪。若每路只取 5 条，过滤掉 3 条后就没有候选可补。

#### 阶段 1：exact anchor 是快速通道，但不是唯一通道

如果 query 本身像一个 anchor，源码会尝试精确命中。anchor 是业务稳定标识，而 SQLite `rowid` 只是数据库内部位置。即使 FTS tokenizer 把特殊字符拆开，exact anchor 仍应找到目标；`sqlite-evidence-store.test.js` 有“exact anchor 即使被 FTS5 tokenizer 拆分也能找到”的测试。

#### 阶段 2：FTS/BM25 生成 lexical 候选

查询会被转换成安全的 FTS5 表达式。长查询如果所有 token 都强制 AND，召回可能为 0，所以实现有 progressive relaxation：先严格 AND；失败后保留强 token，并逐级放宽。`fts-progressive-relaxation.test.js` 覆盖中英混合、引号转义、强 token（如 F 编号）优先，以及启用 relaxation 时 meta 必须报告 degradation。

这一层拿到的不是最终答案，而是按词法相关性排序的候选。随后还会做 exact/substring backfill、实体提及补充、去重和业务排序。

#### 阶段 3：状态和来源过滤是“资格审查”

候选可能被排除或降权：

- scope 不匹配；
- kind/status 不匹配；
- provenanceTier 不满足；
- invalidated 不应继续作为有效证据；
- superseded 通常降权而非和新证据平级；
- threadId/sourcePath 等范围不允许泄露到当前查询。

这一步像安全测试中的授权边界：相关不代表有资格返回。测试既要看“应该找到什么”，也要看“绝不能返回什么”。

#### 阶段 4：根据 mode 分流

- `lexical`：直接使用词法候选，不探测 embedding；
- `semantic`：embedding 可用时做向量近邻；不可用时 fail-open 到 lexical；
- `hybrid`：保留完整 BM25 候选池，与向量候选通过 RRF 等方式融合；embedding 不可用仍降级 lexical。

`search-mode-split.test.js` 对这三种模式的边界有真实断言：lexical 不调用向量且找不到纯语义相似项；semantic 能靠向量命中；hybrid 能补出 BM25 漏掉的文档；embedding 不可用时 semantic/hybrid 均降级。

#### 阶段 5：rerank 和 MMR 解决“前五条都一样”

rerank 重新评估相关性，MMR 则在相关与多样之间取平衡。例如 20 条候选中有 8 条都是同一 session digest，只按分数可能霸榜；MMR 会保留最代表的一条，把名额让给代码证据、文档证据和决策记录。

rerank 必须 fail-open。`store-rerank-integration.test.js` 验证 embedding 抛错时仍返回 lexical 结果，`mode=shadow` 只观测不改变顺序，未 ready 时跳过。生产系统中“增强模块失败不摧毁基础搜索”通常比追求完美排序更重要。

#### 用候选数量理解 meta

一次可能出现如下数量变化（数字用于理解，不是固定常量）：

```text
exact 1 + FTS 30 + entity 4
去重后 28
scope/status/provenance 过滤后 19
semantic 补入 12，融合去重后 25
rerank 后仍 25
MMR/limit 输出 5
```

如果最终结果差，`SearchExecutionMeta` 应帮助定位：是 lexical 一开始就没召回、embedding 不可用、过滤过严、relaxation 被触发，还是 rerank 没生效。面试不要只说“我们做混合搜索”，要说清楚**每一阶段的输入输出数量、降级行为和可观测字段**。
<!-- END INLINE SOURCE EXPANSION 24-FLOW -->

## 4. 关键算法与判定逻辑

### 4.1 RRF：三处实现，公式一样、权重不一样

#### （a）doc 级 hybrid — `hybridRRFSearch()`

```ts
const pool = Math.min(Math.max(limit * 4, 20), 100);
const queryVec = await this.embedDeps!.embedding.embed([query]);
const nnResults = this.embedDeps!.vectorStore.search(queryVec[0], pool);

const RRF_K = 60;
const scores = new Map<string, number>();
const nnWeight = hasCJKCharacters(query) ? CJK_NN_WEIGHT : 1.0;   // CJK_NN_WEIGHT = 1.5

for (let i = 0; i < lexicalResults.length; i++)
  scores.set(anchor, (scores.get(anchor) ?? 0) + 1 / (RRF_K + i));          // BM25 腿，权重恒 1.0
for (let i = 0; i < nnResults.length; i++)
  scores.set(anchor, (scores.get(anchor) ?? 0) + nnWeight / (RRF_K + i));   // NN 腿，CJK 时 ×1.5

// 缺失的 NN anchor 用一条 IN(...) hydrate（带全套过滤）
return allAnchors.filter(a => lexicalMap.has(a))
                 .sort((a,b) => (scores.get(b) ?? 0) - (scores.get(a) ?? 0))
                 .map(a => lexicalMap.get(a)!).slice(0, limit);
```
【源码】

**可复述的公式**：

```
RRF(d) = Σ_legs  w_leg / (60 + rank_leg(d))

rank 从 0 开始（数组下标）
w_lexical = 1.0（永远）
w_nn      = 1.5 若 query 含 CJK，否则 1.0
```

**手算一遍**（英文 query，某文档 BM25 第 2、NN 第 5，rank 都是 0-based）：
```
1/(60+2) + 1/(60+5) = 0.016129 + 0.015385 = 0.031514
```
另一篇只在 BM25 排第 0：`1/(60+0) = 0.016667`。前者胜。

**同一篇文档换成中文 query**：
```
1/(60+2) + 1.5/(60+5) = 0.016129 + 0.023077 = 0.039206
```

`hasCJKCharacters()` 的正则（和 `isStrongToken` 里的**不是同一个**）：
```ts
export const CJK_NN_WEIGHT = 1.5;
export function hasCJKCharacters(text: string): boolean {
  return /[\p{Script=Han}\p{Script=Hiragana}\p{Script=Katakana}\p{Script=Hangul}]/u.test(text);
}
```
注释：*"DF-8: asymmetric RRF weight for CJK queries — BM25/FTS5 has poor recall for Chinese text, so boost NN contributions to prevent suppression."*【源码】

**一个重要的过滤顺序坑**：最后 `.filter(a => lexicalMap.has(a))`——NN 命中但 hydrate 时被过滤条件挡掉的 anchor，虽然已经贡献了分数，但会被剔除。所以 RRF 分数是「先算后过滤」，不影响剩余项的相对顺序。【源码】

#### （b）passage 级 hybrid — `hybridPassageRRFSearch()`

同样 `rrfK = 60`、同样 CJK 1.5 权重，唯一区别是 key 变成 `passageVectorKey(docAnchor, passageId)`：
```ts
export function passageVectorKey(docAnchor, passageId) { return JSON.stringify([docAnchor, passageId]); }
export function parsePassageVectorKey(key) {
  const parsed = JSON.parse(key);
  if (!Array.isArray(parsed) || parsed.length !== 2 || typeof parsed[0] !== 'string' || typeof parsed[1] !== 'string')
    throw new Error(`Invalid passage vector key: ${key}`);
  return { docAnchor: parsed[0], passageId: parsed[1] };
}
```
用 JSON 数组做复合主键，避免分隔符冲突。【源码】

#### （c）collection 级联邦 — `KnowledgeResolver`

```ts
const RRF_K = 60;
function rrfFusionN(groups: CollectionGroup[], limit: number): EvidenceItem[] {
  for (const group of groups)
    for (let i = 0; i < group.items.length; i++) {
      const score = 1 / (RRF_K + i);
      const existing = scoreMap.get(item.anchor);
      if (existing) existing.score += score; else scoreMap.set(item.anchor, { item, score });
    }
  return [...scoreMap.values()].sort((a,b) => b.score - a.score).slice(0, limit).map(({item}) => item);
}
```
**没有权重项**，所有 collection 平权。`rrfFusion(project, global, limit)` 是双路特化版，注释里写「dedup: project wins」——同 anchor 时保留 project 那份 item 数据。【源码】

【ADR】ADR-020 明确写了 "hybrid: BM25 召回 + 向量 NN 召回 → RRF 融合（k=60）" 和 "IKnowledgeResolver: 联邦检索 → RRF rank fusion（project + global）"。

> **面试官追问：k=60 是怎么来的、为什么不用加权求和？**
> 「k=60 是 Cormack 2009 那篇 RRF 论文的经验常数，代码注释写的是 `k=60 (standard constant)`。不用加权求和的原因是量纲：BM25 分是负数、量级跟文档长度和语料相关，vec0 的 distance 是 L2 平方距离，两者没有可比的归一化方式。RRF 只用名次，天然免疫量纲；代价是丢掉了『第一名比第二名好多少』的信息。k 越大越平滑——k=60 时第 0 名 1/60≈0.0167、第 1 名 1/61≈0.0164，差 2%，所以单路的名次差很难压过双路都命中。」

### 4.2 Consumption Prior：完整贝叶斯算法（逐行可复述）

`consumption-prior.ts` 全文核心：

```ts
const ALPHA_0 = 2;
const BETA_0 = 8;
const GRACE_PERIOD_MS = 14 * 86_400_000;          // 14 天

const KIND_HALF_LIVES: Record<string, number | null> = {
  adr: null, lesson: null, canon: null,
  feature: 90, decision: 90,
  plan: 45, research: 45, phase: 45,
  discussion: 21, reflection: 21,
  thread: 14, session: 14,
};

export function computeConsumptionPrior(input, globalMeanCtr): ConsumptionPriorResult {
  // ── 分支 0：冷启动宽限期（最先判，早于 constitutional！）
  if (input.firstIndexedAt > 0 && Date.now() - input.firstIndexedAt < GRACE_PERIOD_MS)
    return { shrunkCtr:0, meanCtrKind:0, recencyFactor:0, rawLift:0, prior:0, branch:'cold-start' };

  // ── 贝叶斯收缩 CTR
  const shrunkCtr = (input.consumedCount30d + ALPHA_0) / (input.exposureCount30d + ALPHA_0 + BETA_0);
  const meanCtrKind = globalMeanCtr[input.docKind] ?? 0.2;
  const halfLife = input.docKind in KIND_HALF_LIVES ? KIND_HALF_LIVES[input.docKind] : 45;
  const recencyFactor =
    input.daysSinceLastConsumed == null ? 0.5
    : halfLife == null ? 1.0
    : halfLife / (halfLife + input.daysSinceLastConsumed);
  const rawLift = (shrunkCtr - meanCtrKind) * recencyFactor;

  const isConstitutional = input.authority === 'constitutional' || ['decision','lesson'].includes(input.docKind);

  if (isConstitutional)            return { ..., prior: Math.max(0, rawLift), branch:'constitutional' };
  if (input.exposureCount30d < 5)  return { ..., prior: 0,                    branch:'cold-start' };
  if (input.exposureCount30d < 20) return { ..., prior: Math.max(0, rawLift), branch:'low-sample' };
  return                                  { ..., prior: rawLift,              branch:'full' };
}
```
【源码】

#### 决策表（面试时可以直接背这张）

| # | 判定顺序 | 条件 | `prior` | `branch` |
|---|---|---|---|---|
| 0 | 最先 | `firstIndexedAt > 0 && now - firstIndexedAt < 14d` | `0`（其余字段也全 0） | `cold-start` |
| 1 | 次之 | `authority === 'constitutional'` **或** `docKind ∈ {decision, lesson}` | `max(0, rawLift)` | `constitutional` |
| 2 | | `exposureCount30d < 5` | `0` | `cold-start` |
| 3 | | `5 ≤ exposureCount30d < 20` | `max(0, rawLift)` | `low-sample` |
| 4 | 兜底 | `exposureCount30d ≥ 20` | `rawLift`（可负） | `full` |

**三个非常容易答错、但代码写死的细节**【源码】：
1. **宽限期判定在 constitutional 之前**。一篇 3 天前入库的 ADR，`branch` 是 `cold-start` 不是 `constitutional`——因此它在 `applyConsumptionRerank` 里**不会被 pin**。
2. `firstIndexedAt > 0` 是必要条件。V21 迁移给这列的默认值是 `0`（`NOT NULL DEFAULT 0`），所以**所有 V21 之前入库、之后没被 upsert 过的老文档 `firstIndexedAt = 0`，永远不走宽限期分支**。
3. `docKind ∈ {decision, lesson}` 就算 constitutional，**跟 authority 无关**。所以一篇 `authority='observed'` 的 lesson 也享受「只升不降」。

#### `recencyFactor` 三分支
```
daysSinceLastConsumed == null → 0.5     （从没被消费过：中性）
halfLife == null              → 1.0     （lesson/adr/canon：不衰减）
其它                          → T/(T+d)
```
注意这里的 `d` 是 **`anchor_recall_metrics.dormancy_days`（距上次被消费的天数）**，不是文档年龄。【源码】

#### 数据从哪来（`RecallMetricsComputer`）
```ts
refreshAnchorMetrics():
  cutoff = Date.now() - 30 * 86_400_000
  遍历 recall_events(timestamp >= cutoff)：candidates_json 每出现一次 exposed++，
       且 anchor 在 consumed_json 里则 consumed++
  last_consumed_at 用**全时段**（不带 cutoff）的 recall_events 里 consumed_json != '[]' 的最大 timestamp
  dormancy_days = floor((now - lastConsumed) / 86_400_000)，没消费过则 null
  写入前先 DELETE FROM anchor_recall_metrics（全量重算）

refreshGlobalCtrBaseline():
  按 evidence_docs.kind 分组，对每个 anchor 算 consumed/max(exposure,1)，
  然后 mean_ctr = 该 kind 下所有 anchor 的 CTR **算术平均**（不是加权），sample_count = anchor 数
```
【源码】—— 注意 `mean_ctr` 是「每文档 CTR 的平均」而不是「总消费/总曝光」，小曝光文档权重被放大。

触发点在 `recall-correlation-hook.ts` 的 `triggerRecallCorrelation()`：只要该 invocation 里出现了 `search_evidence` / `graph_resolve` / `list_recent` 之一，就 correlate → persist → `refreshAnchorMetrics()` + `refreshGlobalCtrBaseline()`。有 `_f200HasPrivateHits === true` 时**整个 correlation 直接 return**（隐私护栏）。【源码】

### 4.3 `applyConsumptionRerank()`：合成公式 + pin + MMR

```ts
export function applyConsumptionRerank(results, db, targetLimit?, query = ''): void {
  const f200Flags = freezeF200Flags();
  if (f200Flags.consumptionRerank === 'off' || results.length < 2) return;

  // 1) 精确词法保护分区
  const exactProtectedAnchors = findExactLexicalProtectedAnchors(results, query);
  const protectedResults = ...filter(在集合里);
  const rerankPool       = ...filter(不在集合里);
  if (rerankPool.length < 2) { /* 只把 protected 提前，然后 storeShadowRanking 后 return */ }

  // 2) 快照原始顺序做 shadow baseline（HW-7 + Cloud-P2：必须用原 results，不能用分区后的）
  const preRerankOrder = results.map((item, i) => ({ anchor: item.anchor, shadowRank: i }));

  // 3) 拉指标
  const anchorMetrics  = loadAnchorMetrics(db, rerankPool.map(r => r.anchor));   // 一条 IN(...)
  const globalMeanCtr  = loadGlobalCtrBaseline(db);                              // 全表

  const K = 60, BETA = 0.15, GAMMA = 0.1;

  const scored = rerankPool.map((item, i) => {
    const metrics = anchorMetrics.get(item.anchor);
    const prior = computeConsumptionPrior({
      consumedCount30d:     metrics?.consumed_count_30d ?? 0,
      exposureCount30d:     metrics?.exposure_count_30d ?? 0,
      daysSinceLastConsumed: metrics?.dormancy_days ?? null,
      docKind:   item.kind ?? 'unknown',
      authority: (item.authority as F163Authority) ?? 'observed',
      firstIndexedAt: item.firstIndexedAt ?? 0,
    }, globalMeanCtr);

    const ageDays = item.updatedAt ? (Date.now() - new Date(item.updatedAt).getTime()) / 86_400_000 : 365;
    const decay = computeRecencyDecay(ageDays, item.kind ?? 'unknown');

    const positionalScore = 1 / (i + K);
    const newScore = positionalScore + BETA * prior.prior + GAMMA * (decay.factor - 0.5);
    return { item, newScore, originalIndex: i, isConstitutional: prior.branch === 'constitutional' };
  });

  // 4) pin：constitutional 保持原下标不动
  const pinned = new Map<number, EvidenceItem>();
  let movable = [];
  for (const s of scored) { if (s.isConstitutional) pinned.set(s.originalIndex, s.item); else movable.push(s); }
  movable.sort((a, b) => b.newScore - a.newScore);

  // 5) MMR：只在候选足够多时
  if (targetLimit && movable.length >= 3 * targetLimit) {
    const mmrItems = movable.map(m => ({ item: m.item, score: m.newScore }));
    const mmrResults = applyMMR(mmrItems, targetLimit, 0.7);
    movable = mmrResults.map((item, i) => ({ item, newScore: targetLimit - i }));
  }

  // 6) 回填：pinned 占回原下标，其余按 movable 顺序填空
  const reranked = [];
  let mi = 0;
  for (let i = 0; i < rerankPool.length; i++) {
    if (pinned.has(i)) reranked.push(pinned.get(i)!);
    else if (mi < movable.length) reranked.push(movable[mi++].item);
  }
  const final = protectedResults.length > 0 ? [...protectedResults, ...reranked] : reranked;
  if (f200Flags.consumptionRerank === 'on') { for (...) results[i] = final[i]; results.length = final.length; }

  // 7) shadow 语义按模式反转
  const shadowOrder = f200Flags.consumptionRerank === 'shadow'
    ? final.map((item, i) => ({ anchor: item.anchor, shadowRank: i }))   // shadow：live=BM25, shadow=重排后
    : preRerankOrder;                                                    // on：    live=重排后, shadow=BM25
  storeShadowRanking(keyAnchors, shadowOrder);
}
```
【源码】

**最终合成公式（背这个）**：
```
newScore(d) = 1/(rank_d + 60)  +  0.15 × prior(d)  +  0.10 × (decayFactor(d) − 0.5)

rank_d       : 该文档在 rerankPool 中的 0-based 下标
prior(d)     : computeConsumptionPrior 的四分支输出
decayFactor  : T / (T + ageDays)，ageDays 由 updatedAt 算，无 updatedAt 时取 365
```

**量级直觉**（面试可以直接说）：
- `positionalScore` 第 0 名 = 1/60 ≈ **0.01667**，第 1 名 ≈ 0.01639，相邻差约 **0.00028**
- `GAMMA × (decay−0.5)` 的范围是 `0.1 × (−0.5 … +0.5)` = **±0.05**
- `BETA × prior` 中 prior ≈ `(shrunkCtr − meanCtr) × recencyFactor`，绝对值一般 < 0.5，所以贡献 < **0.075**

→ **行为信号（±0.05~0.075）比位置信号的相邻差（0.00028）大两个数量级**。这意味着 BETA/GAMMA 一旦开启，位置分基本只起「稳定 tiebreak」作用，实际排序由 prior 和 decay 主导。这跟架构文档说的「温和调整」有出入，值得在面试里作为「我读代码发现的量级问题」讲出来。【推断】（公式和常量是【源码】，量级结论是我算的）

**shadow ranking 的 LRU**：
```ts
const _shadowRankingMap = new Map<string, Array<Array<{anchor, shadowRank}>>>();
const MAX_SHADOW_ENTRIES = 32;
function shadowKey(anchors: string[]) { return anchors.slice(0, 8).join('|'); }   // 前 8 个 anchor 拼 key
```
`storeShadowRanking` 满 32 条时删最老的（`_shadowRankingMap.keys().next().value`）；`lookupShadowRanking` 是**破坏性读取**（`stack.shift()`，空了就 delete）——同 key 多次搜索按 FIFO 消费。取用点在 `recall-correlation-hook.ts`，把结果写进 `recall_events.shadow_ranking_json`。【源码】

### 4.4 Recency Decay

```ts
export function computeRecencyDecay(ageDays: number, docKind: string): DecayResult {
  const halfLife = docKind in KIND_HALF_LIVES ? KIND_HALF_LIVES[docKind] : 45;
  if (halfLife == null) return { factor: 1.0, halfLife: null };
  return { factor: halfLife / (halfLife + ageDays), halfLife };
}
```
【源码】—— 全文就这 4 行。

**「半衰期」名不副实**：`T/(T+age)` 在 `age = T` 时确实是 0.5，但它是**分数衰减**不是指数衰减，尾巴很长（age=10T 时还有 0.09）。

**一张容易被追问的对照表**（把 `KIND_HALF_LIVES` 和 `EVIDENCE_KINDS` 对齐看）：

| KIND_HALF_LIVES 的键 | T | 是否是真实的 `EvidenceKind`？ |
|---|---|---|
| `lesson` | null | ✅ 是 |
| `feature` | 90 | ✅ 是 |
| `decision` | 90 | ✅ 是 |
| `plan` | 45 | ✅ 是 |
| `research` | 45 | ✅ 是 |
| `discussion` | 21 | ✅ 是 |
| `thread` | 14 | ✅ 是 |
| `session` | 14 | ✅ 是 |
| `adr` | null | ❌ **不在 EVIDENCE_KINDS 里，永远匹配不到** |
| `canon` | null | ❌ 同上 |
| `phase` | 45 | ❌ 同上（`phases/` 目录被 KIND_DIRS 映射成 `plan`） |
| `reflection` | 21 | ❌ 同上（`reflections/` 被映射成 `lesson`） |
| （缺）`pack-knowledge` | — | 落 default 45 |

`EVIDENCE_KINDS` 的九个值：`feature, decision, plan, session, lesson, thread, discussion, research, pack-knowledge`。所以 **`adr`/`canon`/`phase`/`reflection` 四个键是死代码**——ADR 文档实际的 kind 是 `decision`（走 90 天），不是 null。这是一个很好的「我读到代码和文档不一致」的例子。【源码】+【推断】（"死代码"是我对照两个常量得出的结论）

### 4.5 MMR

```ts
export function applyMMR(items: ScoredItem[], limit: number, lambda: number = 0.7): EvidenceItem[] {
  if (items.length < 3 * limit) return items.slice(0, limit).map(i => i.item);   // 二次守卫
  const selected: ScoredItem[] = [];
  const remaining = [...items];
  while (selected.length < limit && remaining.length > 0) {
    let bestIdx = 0, bestScore = -Infinity;
    for (let i = 0; i < remaining.length; i++) {
      const relevance = remaining[i].score;
      const maxSim = selected.length === 0 ? 0
        : Math.max(...selected.map(s => keywordSimilarity(remaining[i].item, s.item)));
      const mmrScore = lambda * relevance - (1 - lambda) * maxSim;
      if (mmrScore > bestScore) { bestScore = mmrScore; bestIdx = i; }
    }
    selected.push(remaining.splice(bestIdx, 1)[0]);
  }
  return selected.map(s => s.item);
}

export function keywordSimilarity(a, b): number {   // Jaccard
  const setA = new Set(a.keywords ?? []), setB = new Set(b.keywords ?? []);
  if (setA.size === 0 && setB.size === 0) return 0;
  const intersection = [...setA].filter(k => setB.has(k)).length;
  const union = setA.size + setB.size - intersection;
  return union === 0 ? 0 : intersection / union;
}
```
【源码】

**可复述的四点**：
1. **λ = 0.7 硬编码在调用侧**：`applyMMR(mmrItems, targetLimit, 0.7)`。函数默认值也是 0.7。
2. 公式：`MMR(d) = 0.7 × relevance(d) − 0.3 × max_{s∈selected} sim(d, s)`
3. `relevance` 传的是 `newScore`（consumption rerank 的合成分，量级 ~0.017），`maxSim` 是 Jaccard（量级 0~1）。**两者量纲差 60 倍**——`0.3 × sim` 会彻底压过 `0.7 × relevance`。实际效果接近「纯多样性优先」。【推断】
4. 相似度只看 `keywords` 数组的精确字符串交集。**没有 keywords 的文档之间 sim = 0（永不互相抑制）**；一个有 keywords、一个没有时，`union = size(A) + 0 - 0`，sim 也 = 0。
5. **触发条件写了两遍**：调用侧 `if (targetLimit && movable.length >= 3 * targetLimit)`，函数内 `if (items.length < 3 * limit) return slice`。所以只有 `mode=hybrid` + `movable ≥ 3×limit` 才真跑。
6. 贪心是 **O(limit × |remaining| × |selected|)**，每步全扫剩余候选。

> **面试官追问：MMR 的 λ 和相似度度量是什么？**
> 「λ=0.7，相似度是 keywords 的 Jaccard 指数（`|A∩B| / |A∪B|`），不是向量余弦——虽然 store 里有 embedding，MMR 这一层没用它。触发门槛是候选数 ≥ 3×limit，而且只有 hybrid 模式会传 targetLimit，所以 lexical/semantic/raw 三条路径永远不做 MMR。我读代码时注意到一个量纲问题：relevance 用的是 consumption 合成分，量级只有 0.017，而 Jaccard 是 0~1，`0.3×sim` 会压过 `0.7×relevance`，实际更接近纯多样性选择——这是我根据常量算出来的推断，不是注释里写的。」

### 4.6 图检索：边权重、遍历、孤儿边

#### 边权重（`graph-edge-weight.ts`，全文 32 行）

```ts
const TYPE_BASE: Record<string, number> = {
  wikilink: 1.0, doc_link: 0.9, feature_ref: 1.1, related_to: 0.8,
  evolved_from: 1.0, blocked_by: 0.7,
};
const LAMBDA_EDGE = 0.05;
const EDGE_DECAY_T = 30;

export function computeEdgeWeight(relation, traversalCount30d, daysSinceLastTraversal): EdgeWeight {
  const typeBase = TYPE_BASE[relation] ?? 1.0;
  const recencyDecay = daysSinceLastTraversal == null ? 0 : EDGE_DECAY_T / (EDGE_DECAY_T + daysSinceLastTraversal);
  const traversalBoost = LAMBDA_EDGE * traversalCount30d * recencyDecay;
  return { typeBase, traversalBoost, recencyDecay, total: typeBase + traversalBoost };
}
```
公式：`weight = typeBase + 0.05 × traversalCount × 30/(30 + daysSinceLastTraversal)`。
**从没被遍历过（`lastTraversedAt` 为 null）时 `recencyDecay = 0` → boost = 0 → weight = typeBase。**【源码】

`traversal_count` / `last_traversed_at` 由 `recordEdgeTraversals()` 递增，是 fire-and-forget：
```ts
UPDATE edges SET traversal_count = COALESCE(traversal_count,0) + 1, last_traversed_at = ?
WHERE from_anchor = ? AND to_anchor = ? AND relation = ?
try { tx(edges); } catch { /* fire-and-forget: traversal recording must not break graph queries */ }
```
【源码】只在 `recall-correlation-hook` 里、且 `edge.to` 在 consumed anchors 集合里时才记。

#### 遍历（`GraphResolver.buildSubgraph()`）

```ts
const MAX_EDGES_PER_NODE = 15;
const MAX_TOTAL_NODES = 50;
```
BFS 层数由 `depth`（默认 1）控制：`for (let d = 0; d <= depth && frontier.length > 0; d++)`，且 `if (d >= depth) continue;` 保证最后一层只建节点不展开边。REST 层校验 `depth` 必须在 **0..3**，MCP schema 是 `.min(1).max(3)`。【源码】

封顶判定在展开每条边之前：
```ts
if (currentNodeEdges >= MAX_EDGES_PER_NODE || nodesMap.size >= MAX_TOTAL_NODES) {
  truncated = true; nodeCapped = true; break;
}
```
结果里带 `truncated: true`，MCP 层会打印 `⚠️ Graph was truncated (hub-node degree cap hit)`。【源码】

**relation 过滤在遍历时生效**（不是渲染时）：`if (relationFilter && !relationFilter.has(rel.relation)) continue;`。注释解释得很清楚：*"filter edges by relation type AT TRAVERSAL TIME, not just at render time. Without this, disallowed-relation edges still expanded the frontier."* 合法值只有四个：`wikilink / doc_link / feature_ref / related_to`，route 层不合法直接 400。【源码】

**边去重**：`edgeKey = from→to:relation`，同时检查 `reverseKey = to→from:relation`，任一存在就跳过——**图是无向去重的**。【源码】

**脱敏**（三层）：
- 节点：`sensitivity ∈ {private, restricted}` 且 caller 不在该 collection → anchor 换成 `[redacted:N]`、title 换成 `[redacted — {sensitivity} collection]`、kind 换成 `'redacted'`
- 边：`edgeSensitivity` 取 `rel.edgeSensitivity ?? stricterSensitivity(两端)`；`stricterSensitivity` 用 `COLLECTION_SENSITIVITY_ORDER = { restricted:0, private:1, internal:2, public:3 }` 取小
- 未解析节点：`unresolvedRedactionMap` 记录，最终 title 是 `[redacted — {sensitivity} unresolved]`

【源码】`GraphResolver.ts` + `collection-types.ts`

**孤儿边**（指向不存在 anchor 的边）的处理有两条路径：
1. **运行时**：`inferCollectionId()` 返回 undefined → 若 `d > 0` 就建一个 `kind: 'unresolved'` 的占位节点（title = anchor 本身），`d === 0` 时直接 continue。**不会崩，也不会静默丢边**。【源码】
2. **离线修复**（`f188-orphan-edge-repair.ts`）：`SELECT ... FROM edges WHERE from_anchor NOT IN (SELECT anchor FROM evidence_docs) OR to_anchor NOT IN (...)`，然后分五类：
   - `feature_ref_zero_pad` → `canonicalizeFeatureRef('F42') = 'F042'`，action `update`
   - `feature_ref_true_ghost` → action `delete`
   - `wikilink_code_artifact` → 正则 `/^[a-z]+[A-Z][a-zA-Z]*$|^[a-zA-Z]+(\.[a-zA-Z]+)+$/`（camelCase 或 a.b.c），action `delete`
   - `wikilink_potential_doc` → action `review`
   - `related_field_ghost` → action `review`
   有 dry-run 报告（`sqlPreview`）和 apply（写备份表）。【源码】

#### `GraphQueryResolver`：精确 vs 模糊的分流

```ts
const CANDIDATE_LIMIT = 8;
const exactMatches = await this.findExactMatches(query);     // 每个 store 都 getByAnchor(query)
if (visibleExactMatches.length === 1) → status:'graph'（直接建子图）
if (visibleExactMatches.length > 1)   → status:'candidates'（多 collection 同名，让猫选）
if (exactMatches.length > 0)          → noMatch（有精确匹配但全被脱敏挡住 → 假装没有）
否则 searchCandidates() → status:'candidates' 或 noMatch
```
`searchCandidates()` 对每个 store 跑 `store.search(query, { mode:'hybrid', scope:'all', limit: CANDIDATE_LIMIT * 2 })`，再用 `hasExplainableFieldMatch()` 二次过滤（必须能指出是哪个字段命中），最后三级排序：

```ts
pool.sort((a, b) => {
  const textDiff = (b.textMatchScore ?? 0) - (a.textMatchScore ?? 0);   // 一级：字段匹配分
  if (textDiff !== 0) return textDiff;
  const aDomainPenalty = a.collectionId === primaryCollectionId ? 0 : 2; // 非主 collection 罚 2
  const bDomainPenalty = ...;
  const scoreDiff = (b.weightedEdgeScore ?? 0) - bDomainPenalty - ((a.weightedEdgeScore ?? 0) - aDomainPenalty);
  if (scoreDiff !== 0) return scoreDiff;                                 // 二级：边权重 − 域罚分
  return aDomainPenalty - bDomainPenalty;                                // 三级：主域优先
});
return pool.slice(0, CANDIDATE_LIMIT);
```
`TEXT_MATCH_SCORES = { anchor: 10, title: 6, source: 3, summary: 3, keyword: 3, content: 1 }`。
`weightedEdgeScore = Σ computeEdgeWeight(rel.relation, rel.traversalCount ?? 0, daysSinceTraversal).total`（对该 anchor 所有可见边求和）。【源码】

`queryTokens()` 的中文处理值得单说——它做了 **bigram + trigram 切分**：
```ts
for (const run of query.match(/\p{Script=Han}+/gu) ?? []) {
  if (run.length < 2) continue;
  tokens.add(run);
  for (let i = 0; i < run.length - 1; i++) tokens.add(run.slice(i, i + 2));   // bigram
  for (let i = 0; i < run.length - 2; i++) tokens.add(run.slice(i, i + 3));   // trigram
}
```
英文侧：`/[a-z0-9]+/g`、长度 ≥3、排除纯数字、排除 20 个停用词（`SEARCH_STOPWORDS`：anchor/anchors/and/doc/docs/document/documents/feature/features/graph/graphs/node/nodes/query/search/such/the）。【源码】

### 4.7 边抽取（`edge-extractors.ts`）

三个 extractor，都在 `IndexBuilder.rebuild()` 的 `provenance='content'` 阶段跑：

| 函数 | 正则 | relation | 特殊处理 |
|---|---|---|---|
| `extractWikiLinkEdges` | `/\[\[([^\]|]+)(?:\|[^\]]+)?\]\]/g` | `wikilink` | 小写去重、跳过自引用 |
| `extractFeatureRefEdges` | `/\bF(\d{2,4})(?![-a-zA-Z])\b/g` | `feature_ref` | **先 mask 掉 frontmatter、`[[...]]`、`[..](..)`**（替换成等长空格，保持偏移）；`num > 999` 丢弃；补零成 `F042` |
| `extractDocLinkEdges` | `/\[[^\]]+\]\(([^)]+)\)/g` | `doc_link` | 跳过 `http` / `#` 开头；剥掉 `#?` 后缀；`/docs/` 前缀特判；相对路径用 `normalize(join(sourceDir, linkPath))` 解析；查 `pathToAnchor` 映射 |

`pathToAnchor` 在 IndexBuilder 里建，带 `docs/` 前缀双向别名：
```ts
pathToAnchor.set(key, anchor);
if (key.startsWith('docs/')) setAlias(key.slice('docs/'.length), anchor);
else setAlias(`docs/${key}`, anchor);
```
【源码】

抽边前会先清场（保证幂等）：
```sql
DELETE FROM edges WHERE relation IN ('related','related_to') AND provenance = 'frontmatter'
DELETE FROM edges WHERE provenance = 'content'
```
—— **手工加的边（`provenance='manual'`/`'promote'`）不会被清掉**。【源码】

### 4.8 隐私：SecretScanner 与 redactor

#### `SecretScanner`（`SecretScanner.ts`，102 行）

七条正则：
```ts
{ type:'aws-access-key',  regex: /(?<![A-Z0-9])AKIA[0-9A-Z]{16}(?![A-Z0-9])/ }
{ type:'github-token',    regex: /gh[pousr]_[A-Za-z0-9]{36}/ }
{ type:'github-token',    regex: /github_pat_[A-Za-z0-9_]{82}/ }
{ type:'openai-key',      regex: /sk-(?:proj-)?[A-Za-z0-9]{32,}/ }
{ type:'anthropic-key',   regex: /sk-ant-[A-Za-z0-9-]{90,}/ }
{ type:'slack-token',     regex: /xox[baprs]-[A-Za-z0-9-]{10,}/ }
{ type:'private-key',     regex: /-----BEGIN (?:RSA |EC |DSA )?PRIVATE KEY-----/ }
```
加一条**熵兜底**：
```ts
const KEY_CONTEXT_RE = /(?:key|token|secret|password|credential|auth)\s*[:=]/i;
const PLACEHOLDER_RE = /EXAMPLE|PLACEHOLDER|YOUR[_-]|REPLACE|CHANGEME|xxx/i;
// 命中 KEY_CONTEXT 且值 ≥32 字符 且 不是 placeholder 且 shannonEntropy(value) > 3.5 → 'high-entropy-secret'
```
**代码块内不扫**：`CODE_FENCE_RE = /^\s{0,3}[`~]{3,}/` 做开关，`if (inFence) continue;`。
命中后 `maskSecret()` 把所有 ≥12 字符的串换成 `前4 + '*'×min(len-8,16) + 后4`。【源码】

拦截点在 `CollectionIndexBuilder.rebuild()`：
```ts
const { findings } = SecretScanner.scanBatch(results.map(r => ({ path: r.item.sourcePath ?? r.item.anchor, content: r.rawContent })));
if (findings.length > 0) { await this.purgeCollection(); return { indexed:0, skipped:0, blocked:true, secretFindings: findings }; }
```
**一条命中就 purge 整个 collection**（删掉所有 `anchor LIKE '{id}:%'` 的行）——不是跳过那个文件，是全灭。【源码】

#### `privacy-redactor.ts`（全文 18 行）

```ts
export function redactForTranscript(items, collectionSensitivity): EvidenceItem[] {
  if (collectionSensitivity === 'public' || collectionSensitivity === 'internal') return items;
  return items.map(item => ({
    anchor: item.anchor, kind: item.kind, status: item.status,
    title: `[redacted — ${collectionSensitivity} collection]`, updatedAt: item.updatedAt,
  }));
}
```
**private/restricted 只留 4 个字段**：anchor、kind、status、updatedAt；title 被替换，summary/keywords/sourcePath 全丢。调用点在 `KnowledgeResolver.resolveNCollection()` 组装 `CollectionGroup.items` 时。【源码】

#### `DeidentificationService`（全局知识回流用，不在检索路径上）

`sanitize(item)` 按顺序替换：`projectPath` → `[PROJECT]`；`projectName`（`\b...\b` 大小写敏感）→ `[PROJECT]`；`https?://[^\s),]+` → `[URL]`；人名黑名单（`\b...\b` 不区分大小写）→ `[PERSON]`；email → `[EMAIL]`；`@[A-Za-z0-9_-]{2,}` → `[HANDLE]`。keywords 里被整体替换成 `[PROJECT]`/`[URL]` 的会被丢弃。返回 `removedPatterns`（去重）。【源码】

### 4.9 F163 / F200 flag 全清单与 shadow 的实现方式

```ts
// f163-types.ts
export function freezeFlags(): F163FlagSnapshot {
  return Object.freeze({
    authorityBoost:         process.env.F163_AUTHORITY_BOOST         ?? 'off',   // off|shadow|on
    alwaysOnInjection:      process.env.F163_ALWAYS_ON_INJECTION     ?? 'off',   // off|shadow|on
    retrievalRerank:        process.env.F163_RETRIEVAL_RERANK        ?? 'off',   // off|shadow|on
    compression:            process.env.F163_COMPRESSION             ?? 'off',   // off|suggest|apply
    promotionGate:          process.env.F163_PROMOTION_GATE          ?? 'off',   // off|suggest|apply
    contradictionDetection: process.env.F163_CONTRADICTION_DETECTION ?? 'off',   // off|suggest|apply
    reviewQueue:            process.env.F163_REVIEW_QUEUE            ?? 'off',   // off|suggest|apply
  });
}
// f200-types.ts
export function freezeF200Flags(): F200FlagSnapshot {
  return Object.freeze({ consumptionRerank: process.env.F200_CONSUMPTION_RERANK ?? 'off' });  // off|shadow|on
}
```
【源码】**一共 8 个 flag，全部默认 `off`，全部从环境变量读，全部 `Object.freeze`。**

| flag | 谁读它 | 三态各做什么 |
|---|---|---|
| `F163_AUTHORITY_BOOST` | `applyAuthorityBoost()` | off=直接 return；shadow=算分排序但**不写回** `results`；on=写回 |
| `F163_RETRIEVAL_RERANK` | `routes/evidence.ts` | off=不算；shadow=算 `applySalienceRerank` 但用户看原序、只写 `f163_logs`；on=用重排序 |
| `F163_ALWAYS_ON_INJECTION` | `routes/evidence.ts` | 非 off 时调 `queryAlwaysOn()` 填 `injectionSources` |
| `F163_COMPRESSION` | `searchWithMeta()` | 非 off 时 `suppressBackstop = true` |
| `F163_CONTRADICTION_DETECTION` | `upsert()` | 非 off 时跑 `ContradictionDetector.check()` |
| `F163_PROMOTION_GATE` / `F163_REVIEW_QUEUE` | 不在检索路径 | — |
| `F200_CONSUMPTION_RERANK` | `applyConsumptionRerank()` + `searchWithMeta()` 算 `f200Pool` | off=return；shadow=算但不写回、**shadow 存重排后顺序**；on=写回、**shadow 存原始顺序** |

**shadow 的三种实现套路（可复述）**【源码】：
1. **算而不写回**（authority boost）：`scored.sort(...)` 照做，但 `if (flags.authorityBoost === 'on')` 才 `results[i] = scored[i].item`。
2. **算 + 落日志 + 不用**（salience rerank）：`salienceResult` 总是算，`reranked` 只在 `=== 'on'` 时取它，日志 `logSalienceRerank` 无论 shadow 还是 on 都写 before/after/scores。
3. **算 + 反转基线**（consumption rerank）：最巧的一个。注释写得很直白：
   > `'on' mode: live = reranked, shadow = original BM25 → compare rerank vs BM25.`
   > `'shadow' mode: live = BM25, shadow = would-be reranked → compare BM25 vs rerank.`
   > `Using the same order for both would make shadow ≡ live → zero signal.`
   
   HW-7 的注释还记录了 bug 本身：*"Previously shadow was stored from `final` (post-rerank), making shadow ≡ live by construction in 'on' mode — shadowConsumedMRR / liveOnShadowSubsetMRR ≈ 1 always."*

**variantId** 是 flag 快照的确定性哈希：
```ts
export function computeVariantId(flags: F163FlagSnapshot): string {
  const sorted = Object.entries(flags).sort(([a],[b]) => a.localeCompare(b));
  return createHash('sha256').update(JSON.stringify(sorted)).digest('hex').slice(0, 12);
}
```
**排序后再 hash**，保证键序无关；取 **12 个十六进制字符**。【源码】

**cohort sticky routing**：同一 thread 首次遇到时把 variantId 写进 `f163_cohorts`，之后无论 flag 怎么改都返回旧 variant：
```ts
const existing = db.prepare('SELECT variant_id FROM f163_cohorts WHERE thread_id = ?').get(threadId);
if (existing) return existing.variant_id;
db.prepare('INSERT INTO f163_cohorts (thread_id, variant_id, assigned_at) VALUES (?, ?, ?)').run(...);
```
【源码】

### 4.10 authority 是怎么来的：`pathToAuthority()`

```ts
export function pathToAuthority(sourcePath: string): F163Authority {
  const p = sourcePath.replace(/^doc:/, '').replace(/^docs\//, '');
  if (/^(lessons-learned|SOP)\.md$/i.test(p) || /shared-rules\.md$/i.test(p)) return 'constitutional';
  if (/^(decisions|features)\//i.test(p))                                      return 'validated';
  if (/^(discussions|plans|research|reflections)\//i.test(p))                   return 'candidate';
  return 'observed';
}
```
调用点在 `upsert()`：`item.authority ?? pathToAuthority(item.sourcePath ?? item.anchor)` —— **没有显式 authority 时用路径推导，且 sourcePath 缺失时拿 anchor 当路径**。【源码】

另有一条离线补刀 `tagConstitutional(db)`（`f163-tag-constitutional.ts`）：
- 精确匹配 `CONSTITUTIONAL_PATTERNS = ['docs/SOP.md', 'CLAUDE.md', 'docs/lessons-learned.md']`
- LIKE 匹配 `['%shared-rules%', '%iron-rules%', '%decisions/ADR-%']`
- 命中就 `SET authority='constitutional', activation='always_on', verified_at=now`，且只更 `status='active'` 的行

【源码】

---

<!-- BEGIN INLINE SOURCE EXPANSION 24-ALGO -->
### 4A. 检索系统的测试方法：分开测召回、资格、排序和降级

检索测试最常见错误是只断言 `results[0].anchor === ...`。这样一旦排序算法调整，测试很脆；同时也不知道失败发生在哪一层。应拆成四类。

#### 1. 召回测试：候选有没有进池

- exact anchor：特殊字符被 tokenizer 拆分时仍命中；
- lexical：关键词可由 FTS 找到；
- progressive relaxation：长 AND 查询严格层为 0 时，放宽层能召回；
- semantic：无关键词但语义相近的文档可命中；
- hybrid：BM25 漏掉而 NN 命中的文档能进入融合池；
- entity mention：实体别名/提及可补候选。

对应 `sqlite-evidence-store.test.js`、`search-mode-split.test.js`、`fts-progressive-relaxation.test.js`。

#### 2. 资格测试：不该返回的必须消失

给同一关键词造多条证据，只改变 scope、kind、status、provenanceTier、threadId、invalid/superseded，再分别查询。断言不是“总数小了”，而是逐 anchor 说明为什么排除或降权。

特别要测路径与 drillDown：相对路径逃逸 source root 时不能生成可读文件提示；虚拟 collection path 不能泄露存储根目录。`search-mode-split.test.js` 已覆盖 sourcePath 越界时省略 drillDown。

#### 3. 排序测试：用相对关系，少绑死绝对分数

更稳的断言是：

```text
exact > 普通词法
active > superseded
强 token 命中 > 仅弱词命中
高 authority 在开关启用时上升
MMR 后相似重复项不应占满 top-k
```

除非分数本身是公共契约，否则不要断言浮点数等于固定值。BM25、embedding 或权重稍调都会让这种测试无意义地失败。

#### 4. 降级测试：增强失败后基础功能仍可用

`store-rerank-integration.test.js` 验证 embedding throw 时返回 lexical，shadow 不改变顺序，not-ready 跳过。还应断言 meta 明确记录降级原因，否则“结果还有”但运维无法知道语义检索已经失效。

#### 一个可手工执行的小数据集

插入 8 条证据：2 条同 anchor 更新、1 条 exact、2 条只有语义相关、1 条 superseded、1 条 invalidated、1 条不同 scope。分别跑 lexical/semantic/hybrid，并记录每阶段数量和最终 anchor。这个小实验比直接读 2000 行 store 更容易建立直觉。

#### 安全测试迁移

- FTS 查询中放引号、括号、操作符和超长 token，不能形成 SQL/FTS 注入或崩溃；
- 查询一个 thread 时不能召回另一用户/范围的私有证据；
- sourcePath 不能通过 `../` 生成越界 drillDown；
- invalidated 证据不能因向量高相似重新混回有效结果；
- meta/日志不应泄露原始 embedding 或敏感绝对路径。

#### 面试复述

> 检索管线先生成大于 limit 的候选，再做资格过滤、词法/向量融合、rerank 和 MMR。测试要分召回、过滤、相对排序和降级四层；语义与重排组件 fail-open 到 lexical，同时通过 execution meta 暴露是否降级。anchor 是业务稳定 ID，rowid 只是 SQLite 实现细节。
<!-- END INLINE SOURCE EXPANSION 24-ALGO -->

## 5. 边界情况与防御性代码清单

这一节是护城河。每一条都是「防的是什么失败 → 代码怎么防 → 出处」。

### 5.1 检索路径的防御

| # | 防的是什么失败 | 代码怎么防 | 出处 |
|---|---|---|---|
| 1 | 空/纯空白 query 打穿 FTS5 | `const trimmed = query.trim(); if (!trimmed) return { items: [], meta: { degraded: false } };` | `searchWithMeta()` 开头 |
| 2 | store 没 initialize 就查 | `ensureOpen()` → `throw new Error('SqliteEvidenceStore not initialized — call initialize() first')` | `ensureOpen()` |
| 3 | FTS5 分词切碎 anchor（`F042`→`F`+`042`） | exact-anchor 旁路 `WHERE anchor = ? COLLATE NOCASE`，命中即置顶 | `searchWithMeta()` 的 anchorSql |
| 4 | 长 query 在 AND-all 下 0 结果 | 三级 progressive relaxation + `if (rows.length > 0) break` | `fts-query-builder.ts` + FTS 循环 |
| 5 | FTS5 MATCH 语法错误（query 含 `*` `^` `NEAR` 等保留字） | 每一级都包 `try { ... } catch {}`（空 catch），继续下一级；三级都失败则该层无贡献 | FTS 循环 / `searchPassages()` |
| 6 | FTS5 双引号注入 | `escapeToken(t) = '"' + t.replace(/"/g, '""') + '"'` | `fts-query-builder.ts` |
| 7 | unicode61 漏掉子串（heading / keyword） | lexical backfill 无条件跑 `LIKE %word%` 三字段 | `searchWithMeta()` backfill 段 |
| 8 | backfill SQL 失败拖垮整次搜索 | `try { ... } catch { /* substring backfill failed — continue with existing results */ }` | backfill 段 |
| 9 | `keywords` 列不是合法 JSON | `parseKeywordList()` 内 `try { JSON.parse } catch { return [] }`，且非数组也返回 `[]` | `lexical-backfill.ts` |
| 10 | embedding 服务没起 | `isEmbeddingAvailable()`：`!deps \|\| deps.mode !== 'on'` → false；`reprobeIfNeeded()` catch → false；最后 `isReady()` | `isEmbeddingAvailable()` |
| 11 | embedding 起了但请求超时/500 | semantic/hybrid 分支整体 `try { ... } catch { return lexicalResults }` | mode 分派段 |
| 12 | sqlite-vec 扩展没装 | `ensureVectorTable()` / `ensurePassageVectorTable()` 返回 false → `vectorStore` 为 undefined → `embedDeps` 为 undefined → `isEmbeddingAvailable()` false | `schema.ts` + `factory.ts` |
| 13 | 向量表存在但查询抛错 | `removeBySourcePrefix()` 里 `try { DELETE FROM evidence_vectors ... } catch { /* vec0 may not exist */ }` | `removeBySourcePrefix()` |
| 14 | authority boost 出错毁掉排序 | `try { applyAuthorityBoost(results, trimmed); } catch { /* Kill-switch */ }` | `searchWithMeta()` |
| 15 | consumption rerank 出错毁掉排序 | `try { applyConsumptionRerank(...) } catch { /* F200 kill-switch */ }` | `enrichWithDrillDown()` |
| 16 | 精确词法命中被 authority 挤掉 | `if (findExactLexicalProtectedAnchors(results, query).size > 0) return;`（整个 boost 跳过） | `applyAuthorityBoost()` |
| 17 | 精确词法命中被行为信号挤掉 | 保护锚拎出 `protectedResults`，`final = [...protectedResults, ...reranked]` | `applyConsumptionRerank()` |
| 18 | 新文档被「零使用」误杀 | 14 天 `GRACE_PERIOD_MS`，`prior = 0`，`branch = 'cold-start'` | `consumption-prior.ts` |
| 19 | 小样本噪声导致降权 | `exposureCount30d < 5` → prior 0；`< 20` → `max(0, rawLift)` 只升不降 | `consumption-prior.ts` |
| 20 | 宪法级文档被行为信号沉底 | `isConstitutional` → `prior = max(0, rawLift)`；且 `applyConsumptionRerank` 里 `pinned.set(originalIndex, item)` 锁死原位 | 两处 |
| 21 | `global_ctr_baseline` 没有该 kind | `globalMeanCtr[input.docKind] ?? 0.2`（表默认值也是 `DEFAULT 0.2`） | `consumption-prior.ts` + V21 |
| 22 | `KIND_HALF_LIVES` 没有该 kind | `docKind in KIND_HALF_LIVES ? ... : 45` | `consumption-prior.ts` / `recency-decay.ts` |
| 23 | `updatedAt` 缺失导致 NaN 年龄 | `item.updatedAt ? (Date.now() - new Date(...).getTime())/86_400_000 : 365` | `applyConsumptionRerank()` |
| 24 | shadow map 无限增长 | `MAX_SHADOW_ENTRIES = 32`，满了删最老的 key；`lookupShadowRanking` 破坏性读取 | `SqliteEvidenceStore.ts` |
| 25 | 小结果集被 MMR 过度去重 | 双重守卫：调用侧 `movable.length >= 3*targetLimit`，函数内 `items.length < 3*limit → slice` | `applyConsumptionRerank()` + `mmr.ts` |
| 26 | 跨帖污染（cross-post）把别的 thread 拽进来 | BUG-UX-14 三重守卫：`scopeActive && !hasExplicitThreadTarget && hasDocLevelResults` → 要求 `_semanticHit && !_alsoLexical`；R3 再校验 `parentDoc.kind` 匹配 scope | `hydratePassageResults()` |
| 27 | passage vector key 损坏 | `hydratePassageVectorHits()` 里 `try { parsePassageVectorKey } catch { return null }` 然后 filter 掉 | `hydratePassageVectorHits()` |
| 28 | vec0 返回的 anchor 在 docs 里已删 | `nnResults.filter(r => docMap.has(r.anchor))` / RRF 侧 `allAnchors.filter(a => lexicalMap.has(a))` | `semanticNNSearch()` / `hybridRRFSearch()` |
| 29 | NN 结果绕过 scope/status 过滤 | hydrate SQL 重新套一整份过滤条件（注释 `P1 fix: hydrate missing NN anchors WITH filters`） | `hybridRRFSearch()` |
| 30 | `dateTo='2026-06-19'` 漏掉当天数据 | `options.dateTo.length === 10 ? `${dateTo}T23:59:59` : dateTo`（这行在 6 个地方重复） | 多处 |
| 31 | drillDown 路径穿越 | `if (!rel \|\| rel.startsWith('..') \|\| isAbsolute(rel)) return null;` | `resolveSourcePathForDrillDown()` |
| 32 | Windows 反斜杠泄漏到 URI | `rel.split(/[\\/]+/).filter(Boolean).join('/')` | 同上 |
| 33 | entity 结果淹没主路结果 | `entityCap = items.length > 0 ? max(1, ceil(limit/2)) : limit` | `mergeEntityResults()` |
| 34 | 同一 entity 重复挂载 | `dedupeEntityMatches()` 用 `entityId\0docAnchor\0passageId\0surface` 做 key | `SqliteEvidenceStore.ts` |
| 35 | 别名匹配误伤（`opus` 命中 `opusculum`） | 拉丁别名用词边界正则 `(^|[^\p{L}\p{N}_@-])alias(?=$|[^\p{L}\p{N}_@-])`；CJK 才用裸子串 | `compileAliasMatcher()` |
| 36 | 短别名压过长别名 | `loadAliases()` 的 `ORDER BY length(a.alias_norm) DESC, a.alias_norm` + `seen.has(entity_id)` 只出一条 | `EntityRegistry.ts` |
| 37 | 多猫共用昵称把所有猫召回 | `findSharedNicknameNorms()` 统计 count>1 的 nickname 并从别名里剔除 | `entity-seeds.ts` |
| 38 | 实体种子文件格式错误 | **fail-loud**：`throw new Error('Invalid entity seed file ...: expected { version: 1, entities: [...] }')`；每个字段单独校验并抛 | `entity-seeds.ts` |
| 39 | LIKE 通配符注入（`removeBySourcePrefix`） | `prefix.replace(/\\/g,'\\\\').replace(/%/g,'\\%').replace(/_/g,'\\_')` + `ESCAPE '\\'` | `removeBySourcePrefix()` |
| 40 | 批量 DELETE 超过 SQLite 变量上限 | `const CHUNK = 400;` 分块 | `removeBySourcePrefix()` |

### 5.2 写入 / 索引路径的防御

| # | 防的是什么失败 | 代码怎么防 | 出处 |
|---|---|---|---|
| 41 | 并发写触发 `SQLITE_BUSY` | 单写者队列 `EvidenceWriteQueue`（promise 链）+ `busy_timeout = 5000` | `evidence-write-queue.ts` |
| 42 | 摘要的摘要（级联压缩） | `upsert()` 里查 `source_ids` 指向的 doc 是否 `summary_of_anchor IS NOT NULL`，是则 **throw** `cascade compression prohibited` | `upsert()` + `createSummary()` |
| 43 | 自我矛盾标记 | `filtered = hits.filter(h => h.anchor !== item.anchor)` | `upsert()` |
| 44 | 低优先级文档覆盖高优先级 | `KIND_PRIORITY` 比较 + `existingFileExists` 二次确认（旧文件还在磁盘才拦） | `IndexBuilder.rebuild()` |
| 45 | scanner 逻辑升级后索引不更新 | `INDEXING_VERSION = 5` 存 `embedding_meta.indexing_version`，不匹配就 `effectiveForce = true` | `hasIndexingVersionChanged()` |
| 46 | 版本号写入失败卡住 | `try { ... } catch { /* fail-open: version not persisted → next restart will re-index (safe) */ }` | `storeIndexingVersion()` |
| 47 | thread 列表接口瞬时故障导致误删所有 thread | `threadListFailed` 标志；清理阶段 `if (threadListFailed && row.anchor.startsWith('thread-')) continue;` | `rebuild()` cleanup 段 |
| 48 | Redis 消息过期导致 passage 丢失 | 条件 UPSERT + 注释 `never delete missing passages on rebuild`；再加 JSONL 回填 | `indexPassages()` / `backfillPassagesFromTranscript()` |
| 49 | JSONL 里坏行 | `try { JSON.parse(line) } catch { /* skip malformed lines */ }` | `backfillPassagesFromTranscript()` |
| 50 | JSONL 时间戳非法 | `const ts = new Date(data.t); if (Number.isNaN(ts.getTime())) continue;` | 同上 |
| 51 | 回填 position 与 Redis position 冲突 | `let position = 10000;` 起始偏移 | 同上 |
| 52 | embedding 失败阻塞索引 | `try { await embedIndexedItems(...) } catch { /* fail-open: embedding errors don't block indexing */ }` | `rebuild()` |
| 53 | passage warmup 阻塞 listen() | `setImmediate(() => void this.runPassageEmbeddingWarmup())` + 注释 `MUST NOT block rebuild()/listen()` | `startPassageEmbeddingWarmup()` |
| 54 | warmup 在服务挂掉时空转 | `consecutiveErrors >= 3` 直接 return | `embedMissingPassages()` |
| 55 | warmup 全表扫内存爆 | keyset 分页 `WHERE id > ? ORDER BY id LIMIT 256` | 同上 |
| 56 | 换模型后向量语义错乱 | `checkMetaConsistency()` 比 model_id + dim，不一致就 `clearAll()` + 全量重 embed + `onVectorReset()` 清 passage 向量 | `embed-utils.ts` |
| 57 | 服务端 batch 上限 | 客户端按 `EMBED_BATCH_SIZE = 64` 切；注释 `Server rejects batches > this with HTTP 400` | `EmbeddingService.embed()` |
| 58 | passage 太长撑爆 3s 超时 | `PASSAGE_EMBED_BATCH_SIZE = 8`，注释写明理由 | `embed-utils.ts` |
| 59 | 大 repo 扫描过慢 | `readdirSync(scanRoot).length > 200 → { skipSoftClues: true }` | `buildScanOptions()` |
| 60 | 子 collection 被父 scanner 重复索引 | `computeChildExcludes(parentRoot, children)` 生成 `${rel}/**` 传给 CatCafeScanner | `factory.ts` |
| 61 | 密钥进索引 | `SecretScanner.scanBatch()` 命中 → `purgeCollection()` + `blocked: true` | `CollectionIndexBuilder.rebuild()` |
| 62 | 全局知识库缺失/损坏 | `try { mkdirSync; new SqliteEvidenceStore; initialize; new GlobalIndexBuilder } catch { /* fail-open: project-only search */ }` | `factory.ts` |
| 63 | 单个外部 collection 损坏 | 每个 manifest 单独 `try/catch`，注释 `fail-open: skip broken external collections` | `factory.ts` |
| 64 | 某个 collection 搜索超时/抛错 | `Promise.allSettled` + rejected 分支写 `status: 'error'` + `degradeReason: 'evidence_store_error'` | `KnowledgeResolver.resolveNCollection()` |
| 65 | global store 抛错拖垮 project 结果 | `.catch(() => ({ items: [], meta: { degraded: true, degradeReason: 'evidence_store_error' } }))` | `resolveLegacy()` |
| 66 | 整个搜索路由抛错 | route 层 catch → `{ results: [], degraded: true, degradeReason: 'evidence_store_error', variantId }`，同时 `request.log.error` 打 errMsg/errStack/errCause | `routes/evidence.ts` |
| 67 | expansion / 日志失败影响主结果 | 两处 `catch { /* fail-open: ... does not block search */ }` | `routes/evidence.ts` |
| 68 | reindex/rebuild/warmup 被远程调用 | 三个 POST 端点都有 `ip !== '127.0.0.1' && ip !== '::1' && ip !== '::ffff:127.0.0.1' → 403` | `routes/evidence.ts` |
| 69 | 客户端自授权私有 collection | route 里 `const callerCollections: string[] \| undefined = undefined;` 硬写死，注释说明这是 ACL 提权修复 | `routes/library.ts` |
| 70 | 私有命中泄漏到遥测 | `if (hasPrivateHits) return;` 整个 correlation 不做 | `recall-correlation-hook.ts` |

### 5.3 已知缺口（面试时主动说，加分）

| 缺口 | 具体表现 | 我的判断 |
|---|---|---|
| doc 级降级不写 meta | embedding 不可用时 semantic/hybrid 退 lexical，`meta` 依然是 `{ degraded: false }`；只有 `depth=raw` 会填 `degradeReason` | 【源码】确认；用户界面上看不出「你以为在跑 hybrid，其实在跑 lexical」 |
| exact-anchor 旁路不做时间过滤 | `dateFrom`/`dateTo` 在 anchorSql 里缺失 | 【源码】确认；影响面小（anchor 精确命中通常就是想要的那条） |
| semantic 分支不做时间过滤 | `semanticNNSearch()` 的 hydrate SQL 没有 `updated_at` 条件 | 【源码】确认 |
| 空 catch 吞语法错误 | FTS 循环的 `catch {}` 没有任何日志 | 【源码】确认；线上排查只能靠复现 |
| `CURRENT_SCHEMA_VERSION` 是死常量 | 导出了但 `applyMigrations()` 不用 | 【源码】确认 |
| `adr`/`canon`/`phase`/`reflection` 半衰期是死键 | 不在 `EVIDENCE_KINDS` 里 | 【推断】（对照两个常量得出） |
| MMR 量纲失衡 | relevance ~0.017 vs Jaccard 0~1 | 【推断】 |
| `authorityBoost === 'shadow'` 没有日志 | 注释写 `(logging in Task 7)`，但代码里没有 | 【源码】确认注释与实现不符 |
| coverage 的 expansion 无 query 预算 | `expandViaFrontmatter` 对每个 keyword 发一次 hybrid 子查询，无上限 | 【源码】确认（topk 版有 `MAX_TERMS_PER_TYPE = 5`，coverage 版没有） |
| `gaps` 永远是 `[]` | `CoverageSearchResult.gaps` 硬编码空数组 | 【源码】确认 |

---

## 6. 可观测性：出问题怎么查

### 6.1 数据落在哪五张表

| 表 | 写入者 | 关键列 | 查什么 |
|---|---|---|---|
| `f163_logs` | `F163ExperimentLogger`（route 层） | `log_type`（`search`/`write`/`salience_rerank`/`compression_scan`/`compression_apply`）、`variant_id`、`effective_flags`(JSON)、`payload`(JSON) | 「这次搜索用的什么 flag 组合、返回几条、每个 collection 各几条」 |
| `f163_cohorts` | `getOrAssignCohort()` | `thread_id` → `variant_id` | 「这个 thread 被粘在哪个实验组」 |
| `recall_events` | `RecallEventCorrelator.persistBatch()` | `candidates_json`、`consumed_json`、`shadow_ranking_json`、`reformulated`、`fell_back_to_grep`、`abandoned`、`result_set_id`、`attribution_clarity` | 「搜到了什么 / 猫真的读了哪条 / 有没有 fallback 到 grep」 |
| `anchor_recall_metrics` | `RecallMetricsComputer.refreshAnchorMetrics()` | `consumed_count_30d`、`exposure_count_30d`、`dormancy_days` | 「这个 anchor 的 CTR 和休眠天数」——直接决定 consumption prior |
| `global_ctr_baseline` | `refreshGlobalCtrBaseline()` | `doc_kind` → `mean_ctr`、`sample_count` | 「这类文档的平均 CTR 基线是多少」 |
| `task_trajectories` | `TrajectoryAggregator.persist()` | `search_event_ids_json`、`files_read_json`、`files_modified_json`、`output_verified` | 「一次任务里搜了什么 → 读了什么 → 改了什么 → 结果验证没」 |

【源码】

### 6.2 诊断入口（可以直接说出 URL）

| 入口 | 返回什么 | 备注 |
|---|---|---|
| `GET /api/evidence/status` | `docs_count` / `threads_count` / `passages_count` / `passage_vectors_count` / `passage_vectors_supported` / `passage_warmup_active` / `edges_count` / `vectors_count` / `last_rebuild_at` / `embedding_model` / `functionalStatus` / `configWarnings[]` | **不需要 localhost 限制**；`passage_vectors < passages` 说明 warmup 还没跑完 |
| `POST /api/evidence/reindex` | `{ ok, paths, invalidated[] }` | localhost only；`invalidated` 是通过 `edges` 反查 `relation IN ('related','evolved_from')` 的依赖方 |
| `POST /api/evidence/warmup` | `{ ok: true }` | localhost only；重启 passage 向量回填 |
| `POST /api/evidence/rebuild` | `{ taskId }` | localhost only；异步，配 `GET /api/evidence/rebuild/:taskId` 轮询进度 |
| `GET /api/library/graph/resolve?query=&depth=&relations=` | `{status:'graph'\|'candidates'\|'no_match', ...}` | localhost only；depth 0..3 |
| `GET /api/library/tool-usage-metrics` | 含 `recallEventStats`（`getRecallStats24h`） | 需要 Redis |

【源码】

`getRecallStats24h(db)` 的 SQL（24 小时窗口）：
```sql
SELECT COUNT(*) AS total,
       SUM(CASE WHEN consumed_json != '[]' THEN 1 ELSE 0 END) AS consumed,
       SUM(reformulated) AS reformulated,
       SUM(abandoned) AS abandoned,
       SUM(fell_back_to_grep) AS fellBackToGrep
FROM recall_events WHERE timestamp >= ?     -- Date.now() - 86_400_000
```
【源码】—— 「搜了 N 次，其中 M 次真的读了东西」是判断检索质量最直接的指标。

### 6.3 客户端侧的遥测（MCP 层，打到 stderr）

```ts
searchCount++;
const depthLine = `📊 本轮第 ${searchCount} 次搜索 | 搜到 doc anchor → Read 源文件，不要用摘要推理`;
console.error(`[cat-cafe-search-depth] ${JSON.stringify({
  metric: 'search_depth', searchCount, resultCount: data.results.length,
  docHitCount: docHitsForTelemetry.length, scope: input.scope ?? 'all', mode: input.mode ?? 'lexical',
})}`);
```
`searchCount` 是 **MCP 进程级模块变量**（`let searchCount = 0` 在文件顶层），所以它统计的是「这个 MCP server 进程生命周期内的搜索次数」，不是单轮对话。【源码】

出错时的日志非常详细（三个 `console.error`，分别覆盖 HTTP 非 2xx、fetch throw）：
```ts
console.error(`[cat-cafe-search-evidence] fetch threw for ${url}\n  query=${queryLabel}\n  err=${message}\n  cause=${JSON.stringify(cause)}\n  stack=${stack?.split('\n').slice(0,5).join('\n')}`);
```
【源码】

### 6.4 降级横幅：用户能看到的唯一信号

`formatDegradedBanner(degraded, degradeReason, effectiveMode)`：

| degradeReason | 横幅文案 |
|---|---|
| `raw_lexical_only` | `[DEGRADED] depth=raw currently uses lexical retrieval only (effectiveMode=X)` |
| `passage_embedding_unavailable` | `[DEGRADED] raw passage embeddings unavailable; fell back to lexical retrieval (effectiveMode=X)` |
| `passage_vector_search_error` | `[DEGRADED] raw passage vector search failed; fell back to lexical retrieval (effectiveMode=X)` |
| 其它（含 `evidence_store_error`） | `[DEGRADED] Evidence store error — results may be incomplete` |

【源码】注意 `raw_lexical_only` 的注释写着 *"Kept for legacy/web contract compatibility; F209 Phase A no longer emits this reason in production."*——它现在只可能由 `missingResolverMeta()` / `legacyStoreMeta()`（store 不实现 `searchWithMeta` 的旧路径）产生。

### 6.5 低命中 nudge：确定性引导

```ts
function composeMemoryNavigationNudge(data): string | null {
  if (data.results.length === 0) return '🧭 Memory navigation — no match, try a different entry: ...';
  const hasHighOrMidDocHit = data.results.some(
    r => (r.confidence === 'high' || r.confidence === 'mid') && DOC_SOURCE_TYPES.has(r.sourceType));
  if (!hasHighOrMidDocHit) return '🧭 Memory navigation — low confidence hits, ...';
  return null;
}
const DOC_SOURCE_TYPES = new Set(['feature','decision','phase','lesson','plan','research']);
```
`confidence` 来自 `rankToConfidence(index)`：**`index <= 1 → 'high'`，`<= 4 → 'mid'`，否则 `'low'`**——注意它是**名次函数，不是相关度分数**，第 0、1 名必定 high。【源码】

> **面试官追问：怎么排查「搜不到」？**
> 「按管线顺序四刀。第一刀 `GET /api/evidence/status` 看 `docs_count` 和 `last_rebuild_at`——索引空的就没戏。第二刀看 `vectors_count` 和 `passage_vectors_count`：前者 0 说明 sqlite-vec 或 embed 没起，hybrid 静默退成 lexical；后者远小于 `passages_count` 说明 warmup 没跑完，`depth=raw` 的 semantic 会降级。第三刀直接查 `evidence_fts` 验证分词：`SELECT * FROM evidence_fts WHERE evidence_fts MATCH '"F042"'`，如果空说明 tokenizer 把 anchor 切碎了、只能靠 exact-anchor 旁路。第四刀查 `f163_logs WHERE log_type='search'` 看 payload 里的 resultCount 和 flag 快照，确认是不是某个 rerank flag 把结果重排掉了。」

---

## 7. 面试追问应对

### Q1 「RRF 具体怎么算？把公式和 k 值说出来。」

> 「`RRF(d) = Σ_legs w_leg / (60 + rank_leg(d))`，k 硬编码 60，rank 是 0-based 数组下标。代码里有三处实现：doc 级在 `SqliteEvidenceStore.hybridRRFSearch()`，passage 级在 `hybridPassageRRFSearch()`，collection 级在 `KnowledgeResolver.rrfFusionN()`。前两处的 BM25 腿权重恒为 1.0，NN 腿在 `hasCJKCharacters(query)` 为 true 时乘 `CJK_NN_WEIGHT = 1.5`；collection 级完全平权，没有权重项。
> 手算：英文 query 下 BM25 第 2 名 + NN 第 5 名 = `1/62 + 1/65 = 0.0315`；只在 BM25 第 0 名 = `1/60 = 0.0167`，双路命中赢。同一篇换成中文 query = `1/62 + 1.5/65 = 0.0392`。
> 一个实现细节：融合完之后才做 `filter(a => lexicalMap.has(a))`——NN 命中但 hydrate 时被 scope/status 过滤掉的 anchor 已经贡献过分数，但不会出现在结果里。分数是先算后过滤。」

加分点：「不用加权求和是因为量纲——BM25 返回负数、量级跟文档长度相关，vec0 是 L2 距离，没有可比的归一化。RRF 只用名次，代价是丢掉了『第一名比第二名好多少』。k=60 时相邻名次差只有 2%，所以单路名次优势很难压过双路都命中。」

### Q2 「Progressive relaxation 几级？降级的判据是什么？」

> 「名义三级，但 **token ≤ 3 时只有一级**——`buildProgressiveFtsQueries()` 里 `if (tokens.length <= 3) return [escaped.join(' ')];` 直接返回，不做任何放松。
> token ≥ 4 时：Level 1 = 全部 token 用空格连（FTS5 里空格是隐式 AND）；Level 2 = 强 token 全 `AND` + 弱 token 包一层括号 `OR`，两段之间用**显式 `AND`**（注释写明 FTS5 在括号 OR 组前不接受隐式 AND，会抛语法错）；Level 3 = 全部 `OR`。如果强或弱有一边为空，Level 2 整级跳过，数组只有 2 条。
> 降级判据在调用侧：`for (const ftsQuery of ftsQueries) { try { ...执行... ; if (rows.length > 0) break; } catch {} }`——每级执行完看命中行数，>0 就 break；`catch {}` 是空的，语法错误会静默进下一级。
> 强 token 四条规则按顺序短路：实体正则 `^(?:F|ADR|LL|KD|TD|AC|HW)-?\d+[a-zA-Z]?$`、`^Phase[A-Z]$`、含 CJK 字符、长度 ≥ 4。」

加分点：「动机是 F200 HW-6：注释原文写 *'Root cause of 75% empty search results: FTS5 AND-all query semantics'*。另外强 token 的 CJK 判定用的是字符区间 `/[一-鿿㐀-䶿]/`，和 RRF 里 `hasCJKCharacters()` 用的 `\p{Script=Han}` 系列**不是同一套正则**——这是两个 feature 分别加的，没统一。」

### Q3 「consumption prior 的贝叶斯公式完整说一遍，包括分支。」

> 「贝叶斯收缩 CTR：`shrunkCtr = (consumed_30d + α₀) / (exposed_30d + α₀ + β₀)`，`α₀ = 2`、`β₀ = 8`——等价于 Beta(2,8) 先验，即『默认 20% CTR、等效 10 次观测』。
> 提升量：`rawLift = (shrunkCtr − meanCtrKind) × recencyFactor`。`meanCtrKind` 从 `global_ctr_baseline` 表按 doc kind 查，缺失取 `0.2`。`recencyFactor` 三分支：`daysSinceLastConsumed == null → 0.5`（从没被消费，中性）；`halfLife == null → 1.0`（lesson 这类不衰减）；否则 `T/(T + d)`，d 是 `anchor_recall_metrics.dormancy_days`。
> 然后四分支决定 prior，**判定顺序很重要**：
> ① 最先判宽限期 `firstIndexedAt > 0 && now − firstIndexedAt < 14 天` → prior=0、branch='cold-start'，其余字段也全 0；
> ② `authority === 'constitutional'` **或** `docKind ∈ {decision, lesson}` → `max(0, rawLift)`、branch='constitutional'；
> ③ `exposed_30d < 5` → 0、'cold-start'；
> ④ `5 ≤ exposed_30d < 20` → `max(0, rawLift)`、'low-sample'；
> ⑤ 否则 → `rawLift`（可负）、'full'。」

加分点（这是真正拉开差距的地方）：「有两个坑。第一，**宽限期在 constitutional 之前**——一篇 3 天前入库的 ADR，branch 是 cold-start 而不是 constitutional，所以它在 `applyConsumptionRerank` 里**不会被 pin**。第二，`firstIndexedAt > 0` 是必要条件，而 V21 迁移给这列的默认值是 `NOT NULL DEFAULT 0`，所以所有 V21 之前入库、之后没被 upsert 过的老文档永远不走宽限期分支。」

### Q4 「Constitutional immunity 到底在哪一行生效？是几个机制？」

> 「两层，都在 `applyConsumptionRerank()` 这条链上，不是独立的 pass。
> 第一层在 `computeConsumptionPrior()` 里：constitutional 分支返回 `prior: Math.max(0, rawLift)`——**只升不降**，负 lift 被夹到 0。
> 第二层在 `applyConsumptionRerank()` 里：`isConstitutional = prior.branch === 'constitutional'`，然后
> ```ts
> for (const s of scored) { if (s.isConstitutional) pinned.set(s.originalIndex, s.item); else movable.push(s); }
> ```
> pinned 用 `Map<originalIndex, item>` 记原下标，回填时 `for (let i = 0; i < rerankPool.length; i++) { if (pinned.has(i)) reranked.push(pinned.get(i)!); else reranked.push(movable[mi++].item); }`——**pinned 项保持原下标不动，movable 只填空位**。
> 判定条件是 `authority === 'constitutional' || docKind ∈ {decision, lesson}`，所以一篇 `authority='observed'` 的 lesson 也享受 pin。」

加分点：「注意 authority boost（`applyAuthorityBoost`）不是 immunity 的一部分——它是独立的一层，权重表 `constitutional:1.3 / validated:1.2 / candidate:1.1 / observed:1.0`，公式 `1/(i+60) × weight`，而且**只作用于 lexical candidate order**，NN-only 命中的文档不会再单独跑一次 authority sort。」

### Q5 「embedding 挂了会怎样？完整的 fail-open 判定说一遍。」

> 「四层，从外到内。
> **第一层 装配期**（`factory.ts`）：`embedMode === 'off'` 就完全不创建 `EmbeddingService`；创建了也 `try { await embeddingService.load() } catch {}`；`import('sqlite-vec')` 失败或 `ensureVectorTable()` 返回 false → `vectorStore` 是 undefined → `embedDeps` 是 undefined → store 拿不到 embedDeps。
> **第二层 查询期判定**（`isEmbeddingAvailable()`）：
> ```ts
> if (!deps || deps.mode !== 'on') return false;          // shadow 也算不可用！
> try { await deps.embedding.reprobeIfNeeded(); } catch { return false; }
> return deps.embedding.isReady();
> ```
> `reprobeIfNeeded()` 有 30 秒冷却（`REPROBE_COOLDOWN_MS = 30_000`），且 `if (this.ready) return` 提前退出，所以健康时零开销。
> **第三层 调用期**：semantic/hybrid 整段包 `try { ... } catch { return lexicalResults }`。
> **第四层 HTTP**：`/health` 超时 5000ms 硬编码，`/v1/embeddings` 超时用 `embedTimeoutMs`（默认 3000）。
> 结论：embedding 全挂时 hybrid 的行为等于 lexical。**但 `meta` 依然是 `degraded: false`**——doc 级降级完全静默，只有 `depth=raw` 的 passage 分支才会写 `degradeReason: 'passage_embedding_unavailable'` 和 `effectiveMode: 'lexical'`。这是我读代码发现的可观测性缺口。」

### Q6 「`depth=raw` 走的是什么路？和默认路径差在哪？」

> 「`depth=raw` 在 authority boost **之前**就 return 了：
> ```ts
> if (options?.depth === 'raw') {
>   const rawResult = await this.rawPassageSearch(query, results, limit, options, queryEntityMatches, entityMentionDocs.matchesByAnchor);
>   return { items: this.enrichWithDrillDown(rawResult.items, undefined, options, trimmed), meta: rawResult.meta };
> }
> ```
> 差四点：**没有 authority boost**、**没有 mergeEntityResults 配额**、**targetLimit 是 undefined 所以没有 MMR 和截断**、**meta 会真的填 degradeReason**。
> 而且 `rawPassageSearch()` 第一行就有 scope 短路：`if (options.scope && scope !== 'all' && scope !== 'threads') return doc 级结果` —— **`depth=raw` + `scope=docs` 根本不做 passage 检索**，因为 passage 只有 thread 消息。
> passage 的三条路径和 doc 级对称：lexical 走 `searchPassages()`（`bm25(passage_fts)` 无权重参数 + 同样的 progressive relaxation）、semantic 走 `semanticPassageNNSearch()`、hybrid 走 `hybridPassageRRFSearch()`（k=60，CJK 1.5）。」

加分点：「`passage_fts` 的 tokenizer 和 `evidence_fts` **不一样**：`passage_fts` 建表时写了 `tokenize="unicode61 tokenchars '_-'"`，把下划线和连字符当词内字符；`evidence_fts` 用默认配置。所以 `ADR-005` 在 passage 里是一个 token，在 doc 里被切成两个。」

### Q7 「跨帖污染（cross-post contamination）是什么问题，怎么修的？」

> 「问题：passage FTS 和 passage NN 都是**全表搜索、不带 scope 过滤**。如果 thread B 里有人转贴了 thread A 的内容，搜 `scope=threads` 时 thread B 会被 passage 命中拽进结果，导致 teleport 按钮跳到错误的 thread。
> 修法是 `hydratePassageResults()` 里的三重守卫（注释标 BUG-UX-14 R3/R4）：
> ```ts
> const scopeActive = options?.scope && options.scope !== 'all';
> const hasExplicitThreadTarget = !!threadAnchor;
> const hasDocLevelResults = results.length > 0;
> // 对 passage-only 的新 anchor：
> if (scopeActive && !hasExplicitThreadTarget && hasDocLevelResults) {
>   const hasPurelySemanticDiscovery = pList.some(p => p._semanticHit && !p._alsoLexical);
>   if (!hasPurelySemanticDiscovery) continue;
> }
> ```
> 判据很巧：**跨帖转贴会共享字面查询词，所以既被 lexical FTS 命中也被 NN 命中（`_alsoLexical = true`）；纯语义发现只出现在 NN 结果里**。所以『有 `_semanticHit` 但没有 `_alsoLexical`』= 真正的语义发现，放行；否则挡掉。
> `_alsoLexical` 是在 RRF 融合时打的：`if (existing && !existing._semanticHit) passage._alsoLexical = true;`。
> 后面还有 R3 二次校验：即使放行了，也要 `parentDoc.kind` 匹配 scope，防止 session doc 的 passage 混进 `scope=threads`。」

### Q8 「MMR 什么时候触发？λ 和相似度是什么？」

> 「触发条件写了两遍。调用侧：`if (targetLimit && movable.length >= 3 * targetLimit)`；函数内：`if (items.length < 3 * limit) return items.slice(0, limit)`。而 `targetLimit` **只有 `mode=hybrid` 会传**（传的是 `limit`），lexical / semantic / raw 三条路径都传 undefined。所以 MMR 只在 hybrid + 候选数 ≥ 3×limit 时真跑。
> λ = 0.7（调用侧 `applyMMR(mmrItems, targetLimit, 0.7)`，函数默认值也是 0.7），公式 `MMR(d) = 0.7 × relevance(d) − 0.3 × max_{s∈selected} sim(d, s)`。
> 相似度是 **keywords 数组的 Jaccard 指数**：`|A∩B| / (|A| + |B| − |A∩B|)`，不用 embedding。两边都没 keywords 时返回 0，一边有一边没有时 `union = |A|`、`intersection = 0`，也返回 0——**所以没打 keywords 的文档永远不会被互相抑制**。
> 贪心是 O(limit × remaining × selected)，每步全扫。选中后用 `remaining.splice(bestIdx, 1)` 移出。」

加分点：「我算过量纲：relevance 传的是 consumption 合成分，第 0 名约 `1/60 + 0.15×prior + 0.1×(decay−0.5)` ≈ 0.017 量级；Jaccard 是 0~1。`0.3 × sim` 最大 0.3，比 `0.7 × 0.017 ≈ 0.012` 大一个多数量级。所以只要两篇文档有任何 keyword 重叠，多样性项就完全主导——实际行为接近『纯多样性优先』而不是文档里说的『70% 相关性 / 30% 多样性』。这是我根据常量推断的，注释里没写。」

### Q9 「schema 迁移到 V26 了，讲讲迁移是怎么做的、怎么保证幂等。」

> 「`applyMigrations(db)` 就是 26 个 `if (currentVersion < N) { ...; INSERT INTO schema_version }`，`currentVersion = SELECT MAX(version) FROM schema_version`。
> 三个可复述特征：
> ① **第一件事是 `CREATE TABLE IF NOT EXISTS schema_version`**，注释写着 `P1 fix (codex review R2): schema_version may not exist on empty DB`——不然空库查 MAX 就崩了。
> ② **每个 `ALTER TABLE ADD COLUMN` 单独包 try/catch**，注释 `Column may already exist from a partial migration`，因为 SQLite 没有 `ADD COLUMN IF NOT EXISTS`，而且一次 `db.exec` 多条 ALTER 里前面成功后面失败会留下半截状态。
> ③ **`CURRENT_SCHEMA_VERSION = 26` 这个常量导出了但没被 `applyMigrations` 使用**——每一步都硬编码。
> 两个 vec0 表故意不在迁移里：`ensureVectorTable(db, dim)` / `ensurePassageVectorTable(db, dim)` 返回 boolean 不抛异常，注释 `sqlite-vec not loaded — fail-open`。这样扩展没装时 DB 照常能起，只是没有 `vectorStore`。
> V25 还是唯一带数据回填的迁移：加完 `recall_events.thread_id` 后跑一条 `UPDATE ... SET thread_id = (SELECT t.thread_id FROM task_trajectories t WHERE t.invocation_id = recall_events.invocation_id LIMIT 1) WHERE thread_id = '' AND EXISTS (...)`。」

### Q10 「shadow 模式是怎么实现的？为什么 consumption rerank 的 shadow 要反转？」

> 「三种套路。
> ① **算而不写回**（authority boost）：`scored.sort()` 照跑，`if (flags.authorityBoost === 'on')` 才 `results[i] = scored[i].item`。
> ② **算 + 落日志 + 不用**（salience rerank，在 route 层）：`salienceResult` 总是算，`reranked` 只在 `=== 'on'` 时取它；`logSalienceRerank` 无论 shadow 还是 on 都写 before/after/scores 到 `f163_logs`。
> ③ **算 + 反转基线**（consumption rerank）：这个最有意思。注释原文：
> > `'on' mode: live = reranked, shadow = original BM25 → compare rerank vs BM25.`
> > `'shadow' mode: live = BM25, shadow = would-be reranked → compare BM25 vs rerank.`
> > `Using the same order for both would make shadow ≡ live → zero signal.`
>
> 代码：
> ```ts
> const shadowOrder = f200Flags.consumptionRerank === 'shadow'
>   ? final.map((item, i) => ({ anchor: item.anchor, shadowRank: i }))
>   : preRerankOrder;
> ```
> 反转的原因：shadow 的目的是『拿反事实顺序和实际顺序比 MRR』。on 模式下用户看到的是重排后，所以反事实是原 BM25；shadow 模式下用户看到的是 BM25，反事实是重排后。如果两个模式都存同一个顺序，on 模式下 shadow ≡ live，MRR 恒等于 1，指标就废了——HW-7 的注释就记录了这个 bug：*'making shadow ≡ live by construction in on mode — shadowConsumedMRR ≈ 1 always'*。
> 还有一个 Cloud-P2 修复：`preRerankOrder` 必须从**原始 `results` 数组**快照，不能用 `[protectedResults, rerankPool]`，否则把词法保护的偏置烘进 baseline 里了。」

加分点：「shadow ranking 不写 DB，是进程内一个 `Map<string, Array<...>>`，key 是**前 8 个 anchor 拼的字符串**，容量 `MAX_SHADOW_ENTRIES = 32`，满了删最老的。`lookupShadowRanking()` 是破坏性读取（`stack.shift()`），在 `recall-correlation-hook.ts` 里取出来写进 `recall_events.shadow_ranking_json`。所以进程重启会丢未关联的 shadow。」

### Q11 「entity 召回和 BM25 结果怎么合并？会不会互相压制？」

> 「分两处。
> **第一处**在 `searchWithMeta()` 里：entity mention hydrate 出来的 items 用 `if (!seenAnchors.has(item.anchor))` **追加到 results 尾部**，不插队。
> **第二处**是 `mergeEntityResults(entityItems, items, limit)`，有配额保护：
> ```ts
> const entityCap = items.length > 0 ? Math.max(1, Math.ceil(limit / 2)) : limit;
> // 先填 entityItems.slice(0, entityCap)，再填 items，用 seen 去重，最后 slice(0, limit)
> ```
> 主路结果非空时 entity 最多占一半，为空时 entity 可以占满。**注意这个函数只在 semantic 和 hybrid 分支调用，lexical 分支不调**——lexical 模式下 entity 结果只能靠上面那个『追加到尾部』，且会被 `slice(0, limit)` 截掉。
> entity anchor 本身的排序在 `EntityRegistry.selectMentionAnchors()` 的 SQL 里：`ORDER BY has_passage DESC, latest_mention_at DESC, m.doc_anchor`——有段落级提及的优先，然后最近提及，最后 anchor 字典序保证确定性。hydrate 完还要按这个顺序重排（`mentionHits.anchors.map(a => rowMap.get(a))`），不能让 `IN (...)` 的返回顺序覆盖它。」

### Q12 「`intent=coverage` 和默认搜索差在哪？」

> 「差在**它根本不走那条管线**。`routes/evidence.ts` 里 coverage 判定在所有逻辑之前：`if (intent === 'coverage') { ... return coverageResult; }`——不进 KnowledgeResolver、没有 variantId、没有 salience rerank、没有 expansion hints，返回的 JSON 形状也完全不同（`{query, totalHits, bySource, matrix, gaps, degraded?}` 而不是 `{results, degraded, variantId}`）。
> `CoverageSearchService.search()` 五步：并行搜 docs(配额 25) + threads(配额 20)，都是 `mode: 'hybrid'`；union 去重（docs 优先，`matchType: 'direct'`）；frontmatter 扩展（每个 keyword 再搜 docs limit=5，`matchType: 'alias'`，confidence 兜底 0.7）；source-thread 扩展（正则 `/thread-[a-z0-9_-]+/gi` 从 sourceIds 和 summary 抠 thread 引用，再搜 threads limit=3，confidence 兜底 0.6）；convention graph 扩展（配额 10，soft dep，无 adapter 就 push 一条 degraded）。最后 `slice(0, COVERAGE_MAX_TOTAL = 50)`。
> 两个 v1 缺口注释里写明了：convention graph 没有生产 adapter，`onCoverageEvent` 遥测没接线（`catId`/`invocationId` 是空串）。另外 `gaps` 永远返回 `[]`。」

加分点：「coverage 的扩展**没有查询预算上限**——`expandViaFrontmatter` 对命中文档的每一个 keyword 都发一次 hybrid 子查询。F256 Phase B 的 `TopkExpansionService` 是同样逻辑的 topk 版本，那边加了 `MAX_TERMS_PER_TYPE = 5`，注释写着 *'Scanner-generated keywords have no upper bound. Cap internal searchWithMeta calls to avoid amplifying every default topk search into many sub-queries. (砚砚 review P1-2)'*——说明这个问题被发现过，但只在新代码里修了。」

---

### 文档与代码不一致清单（以代码为准）

对照 `docs/architecture/retrieval-pipeline-deep-dive.md`：

| # | 文档写的 | 代码实际 | 出处 |
|---|---|---|---|
| 1 | ORDER BY 里是 `d.source_path LIKE 'internal-archive/%'` | `d.source_path LIKE 'archive/%'`（`lexical-backfill.ts` 的 `isArchivePath()` 也是 `startsWith('archive/')`） | `searchWithMeta()` FTS SQL |
| 2 | BM25 权重是「title 5.0 / **body** 1.0」 | `evidence_fts` 只有 `title, summary` 两列，1.0 是 **summary** 的权重，索引里**没有正文** | `schema.ts` SCHEMA_V1 |
| 3 | Lexical Backfill「FTS 无结果或结果不足时启动 fallback」 | **无条件执行**，只要 `lexicalBackfillWords.length > 0`；而且会对整个 results 重排 | `searchWithMeta()` backfill 段 |
| 4 | 半衰期表列出 ADR / Lesson / Canon = ∞ | `EVIDENCE_KINDS` 里没有 `adr` / `canon` / `phase` / `reflection`，这四个键**永远匹配不到**；只有 `lesson` 是真实 kind 且为 null。ADR 文档实际 kind 是 `decision` → **90 天**，不是 ∞ | `interfaces.ts` + `consumption-prior.ts` |
| 5 | 「Cold-start：入库 < 14 天 **或** exposed < 5」放在同一分支 | 是**两个独立分支**，且 14 天宽限期判定在 constitutional **之前**，所以新入库的 constitutional 文档 branch 是 cold-start、不会被 pin | `consumption-prior.ts` |
| 6 | 第 14 层写「DrillDown 推荐 `graph_resolve(anchor)` / `read_session_events(sessionId)`」 | 实际四个分支是 `cat_cafe_get_thread_context` ×2 / `cat_cafe_read_session_digest` / `cat_cafe_read_file_slice`，**没有 graph_resolve 也没有 read_session_events** | `enrichWithDrillDown()` |
| 7 | 「优雅降级：Embedding 服务挂了 → 自动降级到纯 BM25」暗示可观测 | doc 级降级**不写 meta**（`degraded: false`），用户和日志都看不出来；只有 `depth=raw` 会填 degradeReason | mode 分派段 |
| 8 | 「BETA=0.15、GAMMA=0.10 使行为学习是温和的调整」 | 位置分相邻差 ≈ 0.00028，而 `GAMMA×(decay−0.5)` 范围 ±0.05、`BETA×prior` 可达 0.075，**相差两个数量级**，行为信号实际主导排序 | 【推断】按常量计算 |
| 9 | 「λ=0.7 → 70% 相关性 / 30% 多样性」 | relevance ≈ 0.017 量级、Jaccard 0~1，`0.3×sim` 压过 `0.7×relevance` | 【推断】按常量计算 |
| 10 | `SemanticReranker` 列为第 13 层 | 它是 legacy helper，**当前 store 的三条路径都不调用它**（grep `SemanticReranker` 在 `SqliteEvidenceStore.ts` 命中 0 次）。文档自己也承认了这点，但仍占一层编号 | `SemanticReranker.ts` |

> **面试官追问：你怎么发现文档和代码不一致的？**
> 「我把文档的『完整参数速查表』当 checklist，逐条回代码里 grep 常量。`internal-archive` 那条是 grep `archive/` 发现只有一个前缀；BM25 权重那条是回去看 `CREATE VIRTUAL TABLE evidence_fts USING fts5(title, summary, ...)` 只有两列；半衰期那条是把 `KIND_HALF_LIVES` 的键和 `EVIDENCE_KINDS` 的九个值做交集。这种对照本身就是最快的代码理解方式——文档给你假设，代码给你事实。」

---

## 8. 本篇速查表

### 8.1 常量（考前背这张）

| 常量 | 值 | 定义在 | 作用 |
|---|---|---|---|
| BM25 权重 | `bm25(evidence_fts, 5.0, 1.0)` | `SqliteEvidenceStore.searchWithMeta()` | title=5.0, **summary**=1.0 |
| passage BM25 | `bm25(passage_fts)` | `searchPassages()` | 无权重参数（单列） |
| 候选池公式 | `Math.min(Math.max(limit*4, 20), 100)` | 4 处 | 只 hybrid / semantic / NN / raw 用 |
| `RRF_K` / `rrfK` | `60` | `hybridRRFSearch()` / `hybridPassageRRFSearch()` / `KnowledgeResolver` | RRF 常数 |
| `CJK_NN_WEIGHT` | `1.5` | `SqliteEvidenceStore.ts`（导出） | CJK query 时 NN 腿权重 |
| `AUTHORITY_WEIGHTS` | `constitutional:1.3, validated:1.2, candidate:1.1, observed:1.0` | `SqliteEvidenceStore.ts` | authority boost |
| authority boost `K` | `60` | `applyAuthorityBoost()` | `1/(i+60) × weight` |
| `ALPHA_0` / `BETA_0` | `2` / `8` | `consumption-prior.ts` | Beta 先验 |
| `GRACE_PERIOD_MS` | `14 * 86_400_000` | `consumption-prior.ts` | 冷启动宽限期 |
| exposure 阈值 | `< 5` → prior 0；`< 20` → 只升不降；`≥ 20` → 可升可降 | `consumption-prior.ts` | 四分支 |
| meanCtr 兜底 | `0.2` | `consumption-prior.ts` + `global_ctr_baseline DEFAULT 0.2` | |
| rerank `K/BETA/GAMMA` | `60` / `0.15` / `0.1` | `applyConsumptionRerank()` | 合成公式 |
| MMR `lambda` | `0.7` | `applyMMR()` 默认值 + 调用侧显式 | |
| MMR 触发 | `movable.length >= 3 * targetLimit` | `applyConsumptionRerank()` | 且 targetLimit 只有 hybrid 传 |
| `MAX_SHADOW_ENTRIES` | `32` | `SqliteEvidenceStore.ts` | shadow map LRU |
| shadow key | 前 `8` 个 anchor 拼 `\|` | `shadowKey()` | |
| 半衰期表 | `lesson:null, feature:90, decision:90, plan:45, research:45, discussion:21, thread:14, session:14`，默认 `45` | `KIND_HALF_LIVES` | |
| `EDGE_DECAY_T` / `LAMBDA_EDGE` | `30` / `0.05` | `graph-edge-weight.ts` | `typeBase + 0.05×count×30/(30+d)` |
| `TYPE_BASE` | `feature_ref:1.1, wikilink:1.0, evolved_from:1.0, doc_link:0.9, related_to:0.8, blocked_by:0.7`，默认 `1.0` | `graph-edge-weight.ts` | |
| `MAX_EDGES_PER_NODE` / `MAX_TOTAL_NODES` | `15` / `50` | `GraphResolver.ts` | 图截断 |
| graph depth | 默认 `1`，校验 `0..3`（MCP `1..3`） | `routes/library.ts` / `graph-tools.ts` | |
| `CANDIDATE_LIMIT` | `8`（search 时用 `×2`） | `GraphQueryResolver.ts` | |
| `TEXT_MATCH_SCORES` | `anchor:10, title:6, source:3, summary:3, keyword:3, content:1` | `GraphQueryResolver.ts` | |
| `COVERAGE_QUOTA` | `docs:25, threads:20, conventionGraph:10` | `coverage-search-types.ts` | |
| `COVERAGE_MAX_TOTAL` | `50` | 同上 | |
| coverage confidence 兜底 | direct `1` / alias `0.7` / source-thread `0.6` / convention `static→0.9, heuristic→0.7` | `CoverageSearchService.ts` | |
| `MAX_TERMS_PER_TYPE` | `5` | `TopkExpansionService.ts` | topk expansion 才有 |
| topk expansion 默认 | `maxHitsToExpand=3`, `maxHintsPerType=3` | 同上 | |
| `EMBED_BATCH_SIZE` | `64` | `EmbeddingService.ts` + `embed-utils.ts` | 镜像 embed-api.py 的 MAX_BATCH_SIZE |
| `PASSAGE_EMBED_BATCH_SIZE` | `8` | `embed-utils.ts` | passage 更长，防 3s 超时 |
| `PASSAGE_EMBED_SCAN_BATCH_SIZE` | `256` | `IndexBuilder.ts` | warmup 分页 |
| warmup 容错 | 连续 `3` 批失败停止 | `embedMissingPassages()` | |
| `REPROBE_COOLDOWN_MS` | `30_000` | `EmbeddingService.ts` | |
| health 超时 | `5000` ms（硬编码） | `EmbeddingService.load()` | |
| `embedTimeoutMs` | `3000` ms（默认） | `resolveEmbedConfig()` | `/v1/embeddings` |
| `embedDim` | `768` | `resolveEmbedConfig()` — 注释 `LL-034: 768 is sweet spot for CJK bilingual` | 【ADR】ADR-020 同 |
| `maxModelMemMb` | `800` | `resolveEmbedConfig()` | HTTP 客户端其实不用 |
| embed 端口 | `9880` | `EmbeddingService.resolveBaseUrl()` | env `EMBED_PORT` / `EMBED_URL` 可覆盖 |
| `INDEXING_VERSION` | `5` | `IndexBuilder.ts` | 存 `embedding_meta.indexing_version` |
| `KIND_PRIORITY` | `feature:4, decision:3, plan/discussion/research:2, session/lesson/thread:1, pack-knowledge:0` | `IndexBuilder.ts` | anchor 归属仲裁 |
| sourceHash 长度 | `sha256(...).slice(0, 16)`（CollectionIndexBuilder 用全长 64） | `IndexBuilder.ts` / `CollectionIndexBuilder.ts` | |
| thread summary 上限 | 最近 `100` 条消息，截断到 `3000` 字符（`slice(0,2997)+'...'`） | `rebuild()` / `flushDirtyThreads()` | |
| passage 索引消息数 | `messageListFn(thread.id, 2000)` | `indexPassages()` | |
| JSONL 回填 position 起点 | `10000` | `backfillPassagesFromTranscript()` | 避开 Redis 的 0-based |
| `DIRTY_THREAD_FLUSH_INTERVAL_MS` | `30_000` | `packages/api/src/index.ts` | debounce 周期，`unref()` |
| token 估算 | `Math.ceil(content.length / 4)` | `accumulateSummaryDelta()` | |
| 大 repo 阈值 | `readdirSync(scanRoot).length > 200` → `skipSoftClues` | `buildScanOptions()` | |
| DELETE 分块 | `CHUNK = 400` | `removeBySourcePrefix()` | |
| `CURRENT_SCHEMA_VERSION` | `26`（**未被使用**） | `schema.ts` | |
| pragma | `journal_mode=WAL`, `foreign_keys=ON`, `busy_timeout=5000` | `initialize()` | `PRAGMA_SETUP` 另有 `journal_size_limit=67108864` 但未被 store 使用 |
| `variantId` 长度 | sha256 前 `12` 位十六进制 | `computeVariantId()` | 键排序后再 hash |
| `rankToConfidence` | `<=1 → high`, `<=4 → mid`, else `low` | `f163-types.ts` | **是名次函数不是分数** |
| 熵阈值 | `shannonEntropy(value) > 3.5` 且 `≥32` 字符 | `SecretScanner.ts` | |
| 掩码规则 | `≥12` 字符 → 前 4 + `*`×min(len−8,16) + 后 4 | `maskSecret()` | |
| 重复检测阈值 | `threshold ?? 0.6`，kinds `['lesson','decision','discussion']` | `f163-duplicate-scanner.ts` | TF-IDF 余弦 + 单链聚类 |
| 矛盾检测阈值 | `SIMILARITY_THRESHOLD = 0.3`，候选 `limit: 10` | `f163-contradiction-detector.ts` | TF 余弦（不加 IDF） |
| `snooze` 默认 | `7` 天 | `IndexStateManager.snooze()` | |

### 8.2 函数名 → 做什么（定位速查）

| 函数 | 文件 | 做什么 |
|---|---|---|
| `searchWithMeta()` | `SqliteEvidenceStore.ts` | 唯一检索入口，14 层主干 |
| `search()` | 同上 | `searchWithMeta()` 的薄壳，丢掉 meta |
| `rawPassageSearch()` | 同上 | `depth=raw` 的分支入口（含 scope 短路） |
| `searchPassages()` | 同上 | passage 级 FTS5（public，也被别处调） |
| `semanticNNSearch()` | 同上 | doc 级纯 NN + 单条 IN hydrate |
| `hybridRRFSearch()` | 同上 | doc 级 BM25+NN RRF |
| `semanticPassageNNSearch()` / `hybridPassageRRFSearch()` | 同上 | passage 级对应版本 |
| `hydratePassageVectorHits()` | 同上 | passage key 解析 + 从 `evidence_passages` 补全 |
| `hydratePassageResults()` | 同上 | passage → doc 挂载 + BUG-UX-14 守卫 |
| `attachPassageContext()` | 同上 | `contextWindow` 前后取段 |
| `rankRawResults()` | 同上 | raw 分支三级排序 |
| `hydrateEntityMentionDocs()` | 同上 | entity anchors → EvidenceItem |
| `attachEntityMatches()` / `mergeEntityResults()` | 同上 | 挂 entityMatches / 配额合并 |
| `enrichWithDrillDown()` | 同上 | rerank → 截断 → matchReason → drillDown |
| `resolveSourcePathForDrillDown()` | 同上 | 路径逃逸防护 + collection URI |
| `applyAuthorityBoost()` | 同上（模块级函数） | authority 重排 |
| `applyConsumptionRerank()` | 同上（导出） | 合成分 + pin + MMR + shadow |
| `findExactLexicalProtectedAnchors()` / `isExactLexicalMatch()` | 同上 | 精确词法保护 |
| `annotateMatchReasons()` | 同上 | matchReason + explain |
| `lookupShadowRanking()` | 同上（导出） | 破坏性读取 shadow |
| `hasCJKCharacters()` | 同上（导出） | `\p{Script=Han\|Hiragana\|Katakana\|Hangul}` |
| `rowToItem()` | 同上 | DB row → EvidenceItem |
| `buildProgressiveFtsQueries()` / `isStrongToken()` / `escapeToken()` | `fts-query-builder.ts` | 三级放松 |
| `splitLexicalBackfillWords()` / `rankLexicalBackfillRows()` / `compareEvidenceItemsByLexicalBackfill()` | `lexical-backfill.ts` | 子串回捞 |
| `computeConsumptionPrior()` | `consumption-prior.ts` | 贝叶斯 + 四分支 |
| `computeRecencyDecay()` | `recency-decay.ts` | `T/(T+age)` |
| `applyMMR()` / `keywordSimilarity()` | `mmr.ts` | MMR + Jaccard |
| `computeEdgeWeight()` | `graph-edge-weight.ts` | 边权重 |
| `recordEdgeTraversals()` | `edge-traversal.ts` | fire-and-forget 计数 |
| `extractWikiLinkEdges()` / `extractFeatureRefEdges()` / `extractDocLinkEdges()` | `edge-extractors.ts` | 内容抽边 |
| `buildSubgraph()` | `GraphResolver.ts` | BFS + 脱敏 + 截断 |
| `resolve()` / `searchCandidates()` / `toCandidate()` | `GraphQueryResolver.ts` | 精确/模糊分流 |
| `resolveQuery()` / `findMentionAnchors()` / `findMentionPassages()` / `refreshMentions()` | `EntityRegistry.ts` | 别名 + mention |
| `normalizeEntityAlias()` / `compileAliasMatcher()` | 同上 | NFKC 归一 + CJK/拉丁双匹配器 |
| `loadEntitySeeds()` / `buildRosterEntitySeeds()` | `entity-seeds.ts` | 种子加载（fail-loud） |
| `freezeFlags()` / `computeVariantId()` / `pathToAuthority()` / `salience()` / `applySalienceRerank()` / `getOrAssignCohort()` / `rankToConfidence()` | `f163-types.ts` | F163 全套 |
| `freezeF200Flags()` | `f200-types.ts` | 唯一 F200 flag |
| `applyMigrations()` / `ensureVectorTable()` / `ensurePassageVectorTable()` | `schema.ts` | 建库 |
| `rebuild()` / `incrementalUpdate()` / `indexPassages()` / `embedMissingPassages()` / `flushDirtyThreads()` / `accumulateSummaryDelta()` / `backfillPassagesFromTranscript()` | `IndexBuilder.ts` | 索引全流程 |
| `embedIndexedItems()` / `embedPassages()` | `embed-utils.ts` | 批量 embed |
| `load()` / `reprobeIfNeeded()` / `embed()` / `resolveBaseUrl()` | `EmbeddingService.ts` | HTTP 客户端 |
| `checkMetaConsistency()` / `initMeta()` | `VectorStore.ts` | 模型版本锚 |
| `passageVectorKey()` / `parsePassageVectorKey()` | `PassageVectorStore.ts` | JSON 数组复合键 |
| `rrfFusion()` / `rrfFusionN()` / `combineSearchMeta()` / `degradeReasonPriority()` | `KnowledgeResolver.ts` | 联邦融合 |
| `redactForTranscript()` | `privacy-redactor.ts` | private/restricted 只留 4 字段 |
| `SecretScanner.scan()` / `.scanBatch()` | `SecretScanner.ts` | 7 正则 + 熵 |
| `getRecallStats24h()` | `recall-stats.ts` | 24h 检索健康度 |
| `refreshAnchorMetrics()` / `refreshGlobalCtrBaseline()` | `RecallMetricsComputer.ts` | 喂 consumption prior 的数据 |
| `triggerRecallCorrelation()` | `recall-correlation-hook.ts` | 关联 + 持久化 + 刷指标 + 记边遍历 |

### 8.3 判定规则速查

| 问题 | 规则 |
|---|---|
| 候选池多大 | `mode==='hybrid'` → `clamp(limit*4, 20, 100)`；否则 `limit` |
| FTS 放松几级 | token ≤3 → 1 级；≥4 → 2 或 3 级（强/弱有一边空则跳过 Level 2） |
| 强 token | 实体正则 / `^Phase[A-Z]$` / 含 CJK / 长度 ≥4 |
| backfill 何时跑 | 只要 `splitLexicalBackfillWords()` 非空（**无条件**） |
| backfill 排序 | keywordHits ↓ → titleHits ↓ → textHits ↓ → superseded ↑ → archive ↑ → provenanceRank ↑ → updated_at ↓ |
| MMR 何时跑 | `mode==='hybrid'` 且 `movable.length >= 3*limit` |
| 谁被 pin | `prior.branch === 'constitutional'`，即 `authority==='constitutional'` 或 `kind∈{decision,lesson}`，**且不在 14 天宽限期内** |
| 谁被词法保护 | query 词数 ≥2（或含 CJK）且所有词都在 `title+summary+keywords` 且至少一词在 `title` 或 `keywords` |
| authority boost 何时整体跳过 | flag `off` / `results.length < 2` / 存在词法保护锚 |
| embedding 可用 | `embedDeps` 存在 且 `mode==='on'` 且 `reprobeIfNeeded()` 不抛 且 `isReady()` |
| `depth=raw` 何时不搜 passage | `scope` 存在且既不是 `all` 也不是 `threads` |
| passage-only 新 anchor 何时放行 | `!scopeActive` 或 有显式 threadId 或 doc 级零结果 或 存在 `_semanticHit && !_alsoLexical` 的 passage；且 `parentDoc.kind` 必须匹配 scope |
| 索引跳过 | `sourceHash` 相同 且 不缺 provenance 且 无 force 且 `INDEXING_VERSION` 未变 |
| anchor 归属 | `KIND_PRIORITY` 高者赢；低优先级要覆盖高优先级还需旧文件已从磁盘消失 |
| 向量全量重建 | `checkMetaConsistency()` 发现 `embedding_model_id` 或 `embedding_dim` 变了 |
| confidence 怎么算 | `rankToConfidence(index)`：0/1 → high，2..4 → mid，≥5 → low |
| 图截断 | 单节点边数 ≥15 或总节点 ≥50 |
| coverage 上限 | docs 25 + threads 20 + convention 10，总 slice 50 |

### 8.4 数据结构一页速览

```
evidence_docs (anchor PK)
  ├── kind status title summary keywords(JSON) source_path source_hash
  ├── superseded_by materialized_from updated_at pack_id
  ├── provenance_tier provenance_source generalizable
  ├── authority activation verified_at                    ← F163 A
  ├── source_ids summary_of_anchor compression_rationale  ← F163 B
  ├── contradicts invalid_at review_cycle_days            ← F163 C
  ├── world_id scene_id                                   ← F093
  ├── collection_id review_status                         ← F186
  └── first_indexed_at (INTEGER NOT NULL DEFAULT 0)       ← F200 C

evidence_fts   = fts5(title, summary) external content    ← 无 tokenchars
evidence_passages (id AUTOINC, UNIQUE(doc_anchor, passage_id))
passage_fts    = fts5(content) tokenize="unicode61 tokenchars '_-'"
evidence_vectors = vec0(anchor TEXT PK, embedding float[768])
passage_vectors  = vec0(passage_key TEXT PK, embedding float[768])   key = JSON.stringify([docAnchor, passageId])
embedding_meta (key PK) → embedding_model_id / embedding_model_rev / embedding_dim / indexing_version / last_rebuild_at

edges (from_anchor, to_anchor, relation) PK
  + from_collection_id to_collection_id edge_sensitivity provenance created_at   ← F186 F
  + traversal_count last_traversed_at                                            ← F200 A

entity_registry (entity_id PK) / entity_aliases (entity_id, alias_norm) PK
entity_mentions (entity_id, doc_anchor, passage_id, surface_norm) PK  ← 双 FK CASCADE

recall_events (recall_id PK) → candidates_json consumed_json shadow_ranking_json
                               result_set_id attribution_clarity thread_id result_count
anchor_recall_metrics (anchor PK) → consumed_count_30d exposure_count_30d dormancy_days
global_ctr_baseline (doc_kind PK) → mean_ctr sample_count
task_trajectories (trajectory_id PK)
f163_cohorts / f163_suggestions / f163_logs
index_state (id PK, project_path UNIQUE) → status fingerprint snoozed_until
```

---

*24 · 深潜：14 层检索管线的真实代码 · 依据仓库 `d:/AI/clower-1/clowder-ai` 当前 HEAD 源码逐行核对 · schema V26 · INDEXING_VERSION 5*

---

# 补充卷：把“看懂源码”提升到“能在白板上复现执行”

> 本补充不是把前面的章节换一种说法，而是把阅读源码时最容易跳过的实现细节补齐。重点放在真实 TypeScript、SQL 和运行时状态：一个值从 HTTP 查询参数进入 `SearchOptions` 后，如何一路变成 SQL 参数、FTS 查询、向量请求、候选集合和最终的 `EvidenceItem`。文中的代码片段均为缩写或等价伪代码，目的是解释语法和控制流，不是复制整个文件。

## A. 先建立一张“源码坐标系”：六类对象、三种边界、两个时间点

### A.1 六类对象不是同一个层次

读 `domains/memory` 时，最容易把表、接口、服务和算法混在一起。可以先把对象分成六类：

1. **契约对象**：`SearchOptions`、`EvidenceItem`、`SearchExecutionMeta`。它们描述输入输出，不主动访问数据库。
2. **持久化对象**：`evidence_docs`、`evidence_passages`、`edges`、`recall_events` 等 SQLite 表。它们保存事实、索引和遥测。
3. **索引对象**：`evidence_fts`、`passage_fts`、`evidence_vectors`、`passage_vectors`。这些是从持久化事实派生出来的加速结构，能删除后重建。
4. **编排对象**：`SqliteEvidenceStore.searchWithMeta()`、`KnowledgeResolver.resolve()`、REST route。它们决定调用顺序、预算和降级。
5. **算法对象**：`buildProgressiveFtsQueries()`、`computeConsumptionPrior()`、`applyMMR()`、`computeRecencyDecay()`。它们尽量是小的纯函数，输入明确，便于单测。
6. **适配对象**：`EmbeddingService`、`VectorStore`、`PassageVectorStore`。它们把外部 HTTP 或 SQLite 扩展能力包装成内部接口。

面试时可以用一句话区分：**契约决定“允许传什么”，编排决定“先做什么”，算法决定“怎么算”，适配器决定“怎么接外部系统”，表和索引决定“数据放在哪里”。** 这句话能避免把“向量搜索”误说成 `SqliteEvidenceStore` 自己训练模型，也避免把 `evidence_fts` 误说成事实源。

### A.2 三个边界要画在白板上

第一条边界是“路由层 vs store 层”。路由解析 `intent=coverage`、`dimension` 和 collection，并可能做 salience rerank；store 只认它收到的 `SearchOptions` 中真正参与 SQL 或检索的字段。源码中 `SqliteEvidenceStore.ts` 没有读取 `dimension` 和 `intent` 的分支，因此不能说“store 根据 dimension 选择 project/global”。

第二条边界是“文档级 vs passage 级”。`evidence_docs` 的主键是 `anchor`，适合返回摘要；`evidence_passages` 的唯一键是 `(doc_anchor, passage_id)`，适合回答“哪条消息命中了”。`depth='raw'` 并不是给摘要多加一个字段，而是切到 passage 召回、hydrate、上下文窗口和跨帖污染守卫的一套流程。

第三条边界是“在线请求 vs 后台索引”。查询不能假设 embedding 一定存在；索引构建也不能因为单条文件坏了就让全部文档消失。前者通过 fail-open，后者通过 hash、增量判定和逐项错误隔离实现。

### A.3 两个时间点：索引时间与消费时间

`first_indexed_at` 代表“系统第一次把这个文档放入索引的时间”，它用于 14 天 grace period；`updated_at` 代表文档内容的更新时间，用于 recency decay。两者不能混为一谈：新文档可能内容很旧（导入历史资料），也可能内容刚更新但早已索引多年。`computeConsumptionPrior()` 看 `firstIndexedAt`，`computeRecencyDecay()` 看 `updatedAt`，这是两个完全不同的业务问题。

## B. TypeScript 实现细节：为什么这些类型写法决定了运行时行为

### B.1 `interface` 会消失，所以仓库额外导出 Symbol

`interfaces.ts` 中的 `IEvidenceStore` 是 TypeScript `interface`。编译成 JavaScript 后，interface 不存在，不能写 `instanceof IEvidenceStore`。仓库因此定义了：

```ts
export const IEvidenceStoreSymbol = Symbol.for('IEvidenceStore');
```

`Symbol.for` 使用全局 symbol registry：同一进程里不同模块调用同一个字符串会得到同一个 symbol。它适合做运行时能力标记或依赖注入检查，但不会自动让一个对象“实现接口”。真正的实现仍是 `SqliteEvidenceStore` 的方法集合；symbol 只是运行时 guard 的共同名字。

追问“为什么不用抽象类”时，可以回答：接口只约束编译期，适配器可以轻量实现；抽象类会引入继承关系和运行时构造函数，难以替换测试 double。仓库用 interface + symbol，兼顾静态契约和运行时识别。

### B.2 `as const`、联合类型与数据库字符串

```ts
export const EVIDENCE_KINDS = ['feature', 'decision', ...] as const;
export type EvidenceKind = (typeof EVIDENCE_KINDS)[number];
```

`as const` 把数组元素从普通 `string` 收窄成字符串字面量；`(typeof ...)[number]` 表示取数组所有索引的值类型，于是 `EvidenceKind` 变成有限联合。这样 `options.kind` 不能随便传 `'foo'`。但是从 SQLite 读出来的 `row.kind` 仍然是运行时字符串，所以 `rowToItem()` 里出现 `row.kind as EvidenceItem['kind']`。这里的 `as` 只是告诉编译器“我保证它符合类型”，不会在运行时校验。真正的安全性来自数据库写入路径和测试；如果数据库被手工改坏，`as` 不会救你。

同理，`row.generalizable === 1` 把 SQLite INTEGER 映射成布尔值；`row.keywords`、`row.source_ids`、`row.contradicts` 用 `JSON.parse` 从 TEXT 恢复数组。面试官若问“类型安全在哪里”，要说清楚：**编译期类型安全在接口，运行时数据安全在约束、解析和异常处理，不在 `as`。**

### B.3 可选字段的意义：`undefined`、`null` 和空数组不同

`EvidenceItem.summary?: string` 表示调用方可以完全不返回该字段；SQLite 行的 `summary` 则可能是 `NULL`。`rowToItem()` 采用：

```ts
if (row.summary != null) item.summary = row.summary;
```

`!= null` 是这里有意使用的宽松比较：同时排除 `null` 和 `undefined`，而不会把空字符串误判为缺失。`keywords` 也是一样。如果把条件写成 `if (row.keywords)`，空 JSON 字符串或其他边界值会被静默跳过；如果直接 `JSON.parse(row.keywords)`，NULL 会抛异常。

返回 `passages` 时使用 `item.passages = ...`，而不是总是给空数组，表示“summary 搜索没有 passage 细节”和“raw 搜索明确得到零个 passage”是两种状态。上游可以用 `item.passages?.length` 判断是否存在 raw 证据。

### B.4 `Promise` 链和单写者队列的语义

`EvidenceWriteQueue` 的核心是一个尾 Promise：

```ts
private tail: Promise<void> = Promise.resolve();

enqueue<T>(fn: () => T | Promise<T>): Promise<T> {
  return new Promise<T>((resolve, reject) => {
    this.tail = this.tail.then(async () => {
      try { resolve(await fn()); }
      catch (error) { reject(error); }
    });
  });
}
```

调用 `enqueue(A)` 后，`tail` 变成“执行 A 的 Promise”；再调用 `enqueue(B)`，B 的 `then` 挂到新的尾部，因此 B 必须等 A 的回调完成。注意这里 `resolve`/`reject` 只解决当前调用者拿到的 Promise；`tail` 本身没有把错误向后传播为 rejected chain，因为回调捕获了错误并调用了 `reject`。这意味着后续写入不会因为前一个业务写失败而永久停摆。

这不是通用互斥锁：它只约束经过 `writeQueue.enqueue` 的 mutation。查询没有进队列，SQLite WAL 允许读写并行；如果有人绕过队列直接写，就破坏了单写者假设。面试时应说“队列提供应用层 FIFO，WAL 和 busy_timeout 负责数据库层并发容忍”，不要说“Promise 自带数据库锁”。

### B.5 `async` 方法中同步 SQLite 的真实代价

`better-sqlite3` 的 `.get()`、`.all()`、`.run()` 是同步调用。`searchWithMeta()` 标成 `async` 是因为它需要等待 `isEmbeddingAvailable()`、`semanticNNSearch()` 等异步路径，并统一实现 `IEvidenceStore` 的 Promise 接口；这不会把同步 SQL 变成后台线程。`all()` 读取几万行仍然会占住 Node 事件循环。

因此源码通过三种方式控制风险：

- FTS 和向量查询都有 `LIMIT`，hybrid pool 上限为 100；
- hydrate 使用一次 `IN (?, ?, ...)`，避免逐条查询造成 N+1，但也不把无界 anchor 列表直接拼进 SQL；
- embedding 通过独立 HTTP 服务执行模型推理，避免 API 进程执行 GPU/ONNX。

如果面试官继续追问“那大事务怎么办”，答案是：写入队列保证顺序，但不能替代批量大小设计；`IndexBuilder` 应分批 upsert，`embed-utils` 以 64 个 doc 或 8 个 passage 为批次，避免单个事务和请求过大。

## C. `SearchOptions` 到 SQL：逐字段跟踪参数如何落地

### C.1 默认值是在不同层分别决定的

`SearchOptions.limit` 没有接口级默认。`SqliteEvidenceStore.searchWithMeta()` 使用 `options?.limit ?? 10`；REST route 另有 `limit ?? 5`；MCP schema 再限制为 1 到 20。`??` 只在左侧为 `null` 或 `undefined` 时取右侧，传入 `0` 不会被替换，这和 `|| 10` 不同。不同层的默认值代表不同职责：API 对用户负责，store 是库级兜底，MCP 是工具协议约束。

`mode` 在 store 中通过 `options?.mode ?? 'lexical'` 决定；`depth` 只有显式 `'raw'` 才切换；`includeBackstop` 则先结合 F163 compression flag 推导 `suppressBackstop`。这说明“字段默认值”不一定是一个常量，而可能是由其他 feature flag 计算出来的派生状态。

### C.2 scope 的映射是条件组合，不是简单枚举

源码先计算：

```ts
const effectiveKind = options?.kind
  ?? (options?.scope === 'threads' ? 'thread'
    : options?.scope === 'sessions' ? 'session' : undefined);
const excludeSessionAndThread = options?.scope === 'docs' || options?.scope === 'memory';
const excludePackKnowledge = effectiveKind !== 'pack-knowledge';
```

然后每条 SQL 都重复应用这些条件。这里有三个容易答错的点：

1. `kind` 优先于 `scope`。如果同时传 `kind='lesson'` 和 `scope='threads'`，effective kind 是 lesson，不会再被 thread 覆盖。
2. `docs` 和 `memory` 不是 `kind='doc'`，而是排除 `session` 和 `thread`，因此 `discussion` 等文档型 kind 仍可能命中。
3. 只要没有显式请求 `pack-knowledge`，全局路径默认排除它；这是 F129 隔离，不是 FTS 的行为。

### C.3 参数化 SQL 的具体形态

以 `keywords` 为例，源码构造的是列名固定、值用占位符的 SQL：

```ts
sql += ` AND (${options.keywords.map(() => 'd.keywords LIKE ?').join(' OR ')})`;
params.push(...options.keywords.map((kw) => `%"${kw}"%`));
```

`map(() => '...')` 只生成和关键词数量相同的占位符；真正的词放到 `params`。这里不能把 `kw` 直接插入 SQL，否则关键词中的引号或 SQL 片段会改变语句结构。由于 keywords 在表中是 JSON 字符串，例如 `["RRF","FTS"]`，模式带双引号是为了降低 `rrf` 命中 `myrrf` 的误报，但这仍是 substring LIKE，不是 JSON 查询。

动态 SQL 只有列条件和固定片段来自代码；用户值永远走 bind。FTS 查询也不能直接把原始 query 拼到 SQL，而是先经过 `escapeToken()` 生成安全的 FTS5 双引号短语。

### C.4 日期边界的“补全天”规则

`dateTo` 如果长度为 10，例如 `2026-08-19`，源码追加 `T23:59:59`；这让“按天筛选”包含当天末尾。`dateFrom` 不补 `T00:00:00`，因为 ISO 日期字符串与 ISO datetime 的字典序在固定格式下仍可用于下界比较。raw passage 使用 `created_at`，doc FTS 使用 `updated_at`，这是数据粒度不同导致的两套时间字段。

面试追问“为什么不统一成时间戳”，可以回答：当前 schema 同时存在 ISO TEXT 和 INTEGER（例如 `first_indexed_at`），迁移成本和可读性不同；查询路径必须尊重各表已有约定。若要重构，应统一存储格式并补索引，而不是只改一个比较运算符。

### C.5 `threadId` 的约定和双重过滤

调用方传 `threadId='abc'` 时，store 生成 `threadAnchor='thread-abc'`，在 doc SQL 中追加 `AND anchor = ?`，在 passage vector hydrate 中也追加 `doc_anchor = ?`。这两层都做是为了防止向量索引里出现 stale key：即使向量命中别的文档，hydrate 也不会把它装进指定 thread。

`rawPassageSearch()` 入口的 `scope` 短路不能代替 `threadId` 过滤；显式 threadId 是更窄的目标，后续 `hydratePassageResults()` 还会再次检查 `passage.docAnchor === threadAnchor`。这种重复过滤看起来啰嗦，但属于防御性边界：召回来源可能没有同样的过滤能力，hydrate 是最后一道资格审查。

## D. 主入口逐行复盘：一条 query 的变量生命周期

### D.1 初始化阶段：先确定预算，再规范化 query

入口前几行的顺序很重要：`ensureOpen()` → 读 limit → 计算 BM25 pool → `trim()` → 空 query 返回。这样做有三个效果：

- 未初始化时尽早抛错，避免后面出现“db undefined”的迷惑错误；
- hybrid 在任何分支之前就确定宽池，后面 lexical、NN 和 RRF 使用同一预算；
- 空白 query 不会触发 entity 解析、FTS 或 embedding 请求。

对 `limit=5, mode='hybrid'`，`bm25Pool = min(max(20,20),100)=20`；对 `limit=50`，pool 是 `min(max(200,20),100)=100`。这是候选池预算，不是最终返回条数；最终仍由 `slice(0, limit)` 或 `enrichWithDrillDown` 截断。

### D.2 `seenAnchors` 是跨召回通道的去重边界

exact-anchor、FTS、substring backfill、entity mention 都会向同一个 `results` 数组追加。每次追加前都检查 `seenAnchors`。这比最后统一 `Map` 去重更重要：它保留了通道优先级，先命中的 exact anchor 不会被后面的 backfill 替换；entity 命中也只补充尚未出现的 anchor。

但是 entity match 详情另存于 `matchesByAnchor`，所以“去重 item”和“合并解释”是两件事。最终 `attachEntityMatches()` 会把多个实体解释挂回同一个 item，并用 `dedupeEntityMatches()` 去掉重复 `(entityId, docAnchor, passageId, surface)`。

### D.3 exact-anchor 为什么放在 FTS 前

FTS5 的 `unicode61` tokenizer 可能把 `F042` 分成 `F` 和 `042`，把 `ADR-005` 分成 `ADR` 和 `005`。如果完全依赖 FTS，精确 anchor 会因为分词而漏掉。源码直接：

```sql
SELECT * FROM evidence_docs
WHERE anchor = ? COLLATE NOCASE
```

`COLLATE NOCASE` 只影响大小写比较；并没有使用 `LIKE`，所以不会把 `F042-extra` 当成同一个 anchor。注意 exact lookup 仍然应用 kind、status、thread、provenance、world、scene 和 backstop 条件，因此它是“精确查询旁路”，不是“绕过权限和 scope”。

### D.4 FTS 循环的停止条件

`buildProgressiveFtsQueries()` 返回最严格到最宽松的字符串数组；调用侧逐个 `prepare(sql).all(...params)`。如果某一级 `rows.length > 0`，立即 `break`，不会继续收集更宽松级别的结果。这样做是“首个非空级别胜出”，而不是三层结果合并。

这会产生一个值得主动说明的 trade-off：严格级别只命中一条时，系统不会再用 OR-all 补充其他相关文档；但它避免了宽松查询大量噪声污染精确结果。hybrid 的语义覆盖由向量通道承担，而不是靠同时合并所有 FTS relaxation levels。

## E. FTS5 与 lexical backfill：两个“文本检索”其实解决不同问题

### E.1 `escapeToken()` 的双引号不是装饰

`fts-query-builder.ts` 中的实现只有一行核心逻辑：

```ts
return `"${token.replace(/"/g, '""')}"`;
```

FTS5 的双引号表示 phrase/token literal。用户 query 中如果包含引号，先把一个 `"` 替换成两个 `""`，避免提前结束 phrase。这里的转义语法和普通 SQL 不完全一样：它是在 FTS5 查询字符串层面转义，外层 SQL 仍然用 `MATCH ?` 参数绑定。也就是说有两层安全边界：SQL 占位符防 SQL 注入，FTS 转义防 query grammar 被破坏。

### E.2 强 token判定是启发式，不是分词器

`isStrongToken()` 按四类规则判强：实体编号正则、`PhaseA` 形式、CJK 字符、长度至少 4。它不会调用自然语言分词，也不会查询数据库统计词频。原因是这个函数要在进入 SQLite 前快速决定 query 结构，且对 `F042`、`ADR-005`、中文短词这类 domain token 有确定性行为。

例如 query：

```text
F042 fix bug now
```

`F042`、`fix`、`bug`、`now` 中，`F042`、`fix`、`bug`、`now` 都可能因长度规则成为 strong；Level 2 可能退化为所有词都 strong，因而不会产生 strong+weak 组合，只会保留 Level 1 和 Level 3。对于：

```text
ADR-005 the old behavior
```

`ADR-005` 和 `behavior` 是强词，`the`、`old` 可能是弱词，Level 2 形如：

```text
"ADR-005" AND "behavior" AND ("the" OR "old")
```

注意源码显式写 `AND`。FTS5 在括号 OR 组前使用隐式空格并不总是合法，代码注释明确指出会触发 syntax error。这类细节是面试官很喜欢追问的“为什么不能少写一个 AND”。

### E.3 三层 progressive relaxation 的输入输出

假设 query 有四个 token：`["F042", "cache", "timeout", "fix"]`，且强词是 `F042/cache/timeout/fix`，返回：

```text
Level 1: "F042" "cache" "timeout" "fix"
Level 2: （只有存在 weak 时才有）
Level 3: "F042" OR "cache" OR "timeout" OR "fix"
```

短 query（不超过 3 个 token）直接返回一条 AND-all，不生成 OR-all。原因不是 OR-all 不能用，而是短 query 的 AND-all 误杀概率可接受，减少一次 SQL。空 query 返回 `[]`，调用侧自然不会进入循环。

调用侧的 `try/catch {}` 会吞掉单个 FTS query 的语法或索引错误，然后尝试下一个级别。若所有级别都失败，后面的 contains backfill 仍可能返回结果。这里的 fail-open 是“文本检索局部失败不阻断整个搜索”，不是把错误完全隐藏：生产遥测应结合 `SearchExecutionMeta` 和日志补充诊断。

### E.4 BM25 权重的列顺序

FTS SQL 使用：

```sql
bm25(evidence_fts, 5.0, 1.0) AS rank
```

`evidence_fts` 定义列顺序是 `title, summary`，因此 5.0 作用于 title，1.0 作用于 summary。better-sqlite3 返回的 bm25 值在该实现中越小越相关；但源码并没有把它直接暴露给最终 `EvidenceItem`，而是用 SQL `ORDER BY ... rank`，再在 `explain=true` 时用结果位置写入 `rankingFactors.bm25Score`。这个字段实际是 `index + 1` 的名次，不是原始 BM25 数值，命名略带历史包袱，面试时不要把它说成真实分数。

排序前还有三层固定优先级：

```sql
(d.superseded_by IS NOT NULL),
(d.source_path LIKE 'archive/%'),
CASE WHEN provenance_tier = 'authoritative' THEN 0
     WHEN provenance_tier IS NOT NULL THEN 1 ELSE 2 END,
rank
```

SQLite 的布尔表达式返回 0/1，因此 `false`（0）排在 `true`（1）前面；已 superseded 和 archive 会自然靠后。`NULL LIKE 'archive/%'` 结果不是 1，配合 CASE 和 backfill 的显式排序，保证没有 source_path 的行不会因为异常值被提升。

### E.5 contains backfill 为什么不使用 FTS rank

FTS 的 unicode61 对词边界、连字符和 CJK 有固定规则，可能漏掉“子串命中”。backfill 使用三列 OR：

```sql
(LOWER(title) LIKE ?
 OR LOWER(COALESCE(summary, '')) LIKE ?
 OR LOWER(COALESCE(keywords, '')) LIKE ?)
```

每个 query word 产生三个参数 `%word%`，因此一个词能在 title、summary、keywords 任意位置命中。`COALESCE` 把 NULL 变成空串，避免 `LIKE` 的三值逻辑把整条表达式变成 NULL。

backfill 没有 `LIMIT`，先拿到所有候选，再在 TypeScript 中算 signal 并排序。原因是 SQL 的 substring signal 需要解析 JSON keywords、区分 titleHits 和 textHits，并与 superseded/archive/provenance 组合；在当前数据规模下把候选送入内存更简单。若数据量上升，面试时可以提出“先用粗粒度 LIMIT 或 FTS 辅助，再保留同一 comparator”的优化。

### E.6 `rankLexicalBackfillRows()` 的 comparator 不是普通 sort

每一行生成：

```ts
{ keywordHits, titleHits, textHits }
```

其中 `keywordHit` 使用 `keywords.some(keyword => keyword.includes(word))`；`titleHit` 和 `summaryHit` 使用字符串 includes。每个 word 最多给三类计数各加 1，不会因为同一字段出现 10 次而加 10 分。排序按降序比较 signal；signal 相同再比较质量：

1. `superseded_by` 存在的行排后；
2. `source_path` 以 `archive/` 开头的排后；
3. `provenance_tier='authoritative'` 的 rank 为 0，其他非空为 1，NULL 为 2；
4. `updated_at` 字典序倒排，较新的排前。

`compareEvidenceItemsByLexicalBackfill()` 还有一个 `exactAnchor` 特殊参数：如果 exact row 存在，它无条件排在非 exact anchor 前，再比较 signal。这是在 backfill 之后再次恢复精确查询的优先级，防止 contains 查出一堆高 signal 文档把精确 anchor 顶掉。

### E.7 JSON keyword 解析失败为何选择“空数组”

`parseKeywordList()` 对 NULL、非数组 JSON、非法 JSON 都返回 `[]`。这是搜索路径上的 fail-open：坏 keywords 不应让整次查询失败，最多失去 keyword signal。与之相对，`rowToItem()` 对 `row.keywords` 直接 `JSON.parse`，因为它需要把数据库数据返回给 API；如果 JSON 已损坏，调用 `enrichWithDrillDown()` 前会抛异常。这个不对称值得指出：**回捞排序允许降级，数据映射更倾向暴露数据损坏。** 实际系统可以再给 `rowToItem` 加 try/catch 和诊断字段，但那是改进而不是当前代码行为。

## F. Entity Registry：别名召回为什么不会和 BM25 互相压死

### F.1 query entity match 与 document mention 是两次匹配

`searchWithMeta()` 先执行：

```ts
const queryEntityMatches = this.entityRegistry?.resolveQuery(trimmed) ?? [];
```

这一步只判断“用户 query 是否包含某个实体的 canonical name 或 alias”。随后 `hydrateEntityMentionDocs()` 用匹配到的 entity id 去 `entity_mentions` 查文档和 passage。换句话说，Entity Registry 把“query 中的别名”转换成 anchor 候选，不依赖 FTS 能否识别别名。

`entity_mentions` 的主键由 `(entity_id, doc_anchor, passage_id, surface_norm)` 组成。同一个实体在同一文档同一 passage 里出现两次但 surface 归一化相同，只保留一条；不同 passage 会保留多条。`surface` 保留原始写法，`surface_norm` 用于去重和查询。

### F.2 归一化必须先于匹配

Entity Registry 的 alias normalization 使用 NFKC、trim 和大小写策略。NFKC 会把全角字符转换成兼容的半角形式，减少“ＡＢＣ”和“ABC”被视为两个 alias 的问题。CJK 和拉丁文本的边界规则不同，源码的 matcher 对两类文本采用不同的边界判断，避免中文 alias 需要空格才能命中，也避免英文 alias 命中更长单词的一部分。

面试可以举例：alias 为 `Cat Cafe`，query 为 `Cat Cafe search`，应命中；alias 为 `cat`，query 为 `catalog`，不能因为 substring 就误命中。CJK alias 如 `小猫` 在 `小猫系统` 中可以按领域规则命中，但是否视为独立词需要 matcher 的 Unicode 规则，而不是简单 `includes`。

### F.3 Entity 结果和普通结果的合并配额

`mergeEntityResults(entityItems, items, limit)` 的目标不是把 entity 结果永远放在最前，而是避免实体召回被 BM25 完全压制。实现上先保留 entity item，再按 anchor 去重并补普通 item，最终 slice 到 limit；`attachEntityMatches` 则为已经存在的普通 item 补上 `entityMatches`。因此同一个 anchor 可能：

- 由 entity 通道先发现，作为主结果出现；
- 由 FTS 先发现，entity 只补充解释；
- 同时由多个 alias 命中，最终解释被 dedupe。

`mergePassageResults()` 使用 `passageVectorKey(docAnchor, passageId)` 去重，而不是只按 doc anchor；这是 passage 粒度必须保留同一文档多个消息的原因。

### F.4 `matchReason` 如何暴露证据来源

`annotateMatchReasons()` 的优先级为：entity → anchor → title → summary → keyword → content。它使用整个 query 的 lowercase 字符串比较，而不是逐 token 重新计算 BM25。因此 `matchReason='content'` 只表示“前面几种简单解释没有命中”，不表示向量相似度一定高。

`explain=true` 时：

```ts
item.rankingFactors = { bm25Score: results.indexOf(item) + 1 };
```

这里的 `results.indexOf(item)` 是 O(n)；结果数量被 limit/pool 控制，当前可接受。若未来要输出真实分解，应在候选对象中携带 lexical rank、NN distance、prior 和 MMR penalty，而不是沿用这个位置占位字段。

## G. `depth='raw'`：从摘要检索切到消息级证据

### G.1 raw 不是“把 summary 换成原文”

`rawPassageSearch()` 的第一条分支是：当 `scope` 存在且既不是 `'all'` 也不是 `'threads'` 时，直接返回 `baseResults` 的 raw 排序，不搜索 passage。原因是当前 passage 表主要承载 thread 消息；在 `scope='docs'` 或 `scope='memory'` 下盲搜所有 passage 会把 session/thread 的原文泄漏进文档范围。

这意味着下面两个请求的行为不同：

```text
{ depth: 'raw', scope: 'threads' }  -> 搜索 passage_fts/向量，并挂 passages
{ depth: 'raw', scope: 'docs' }     -> 只返回 doc 级候选，可能没有 passages
```

这不是 bug，而是一个明确的安全/语义取舍。若未来需要 docs 的 raw 内容，应建立文档 chunk 的统一 passage 索引，并给每个 passage 标注 kind/scope，不能只删除这个短路。

### G.2 passage 级候选池和 doc 级候选池

raw 路径把 `pool` 设置为 `clamp(limit * 4, 20, 100)`，和 hybrid doc 级的宽池规则一致。先搜索最多 100 个 passage，再通过 `hydratePassageResults()` 聚合到 doc。一个 doc 命中多个 passage 仍只占一个最终 item，但 `item.passages` 里保留多个证据。

在 `searchPassages()` 中，FTS 查询连接 `passage_fts` 和 `evidence_passages`，返回 `doc_anchor`、`passage_id`、content、speaker、position、created_at。`rank` 保存 FTS 返回的相关性名次；向量 hydrate 则把 distance 暂存到同一个 rank 字段。两者不是同量纲，但 raw 排序主要按“是否有 passage、首次出现顺序”，不会直接把 distance 和 BM25 混算。

### G.3 semantic passage 与 hybrid passage 的 `_semanticHit` 标记

`hydratePassageVectorHits()` 解析复合 key 后把 `_semanticHit: true` 写入结果。hybrid 合并时，如果同一 key 先出现在 lexical map，再出现在 semantic list，则给 semantic passage 标 `_alsoLexical = true`。这两个内部字段不是 API 契约，而是 BUG-UX-14 scope guard 的证据：

- `_semanticHit=true, _alsoLexical=false`：只由向量发现，可能是真正语义相关的新项；
- `_semanticHit=true, _alsoLexical=true`：词面也命中，可能是 cross-post 共享 query 词；
- 两者都没有：纯 lexical passage。

为什么 cross-post 需要这个区分？假设 thread A 的摘要命中“RRF”，thread B 的一条消息引用了同一个词。passage FTS 全库搜索会把 B 也找出来；如果 scope 是某个业务范围，B 可能是错误的 teleport 目标。纯向量命中则没有共享字面词，保留它能避免 scope 过滤把所有语义发现都杀掉。

### G.4 `hydratePassageResults()` 的三步装载

第一步，按 passage 顺序建立 `passagesByAnchor` 和 `passageOrder`。显式 `threadId` 在这一步就过滤 `docAnchor`，所以后面的 scope guard 可以把“指定 thread”作为可信目标。

第二步，对每个没有出现在 `baseResults` 的 anchor，依据 scope 和 doc-level 是否已有结果决定是否放行。`hasDocLevelResults = results.length > 0` 是一个全局判断：只要摘要层已经有主结果，scope active 时 passage-only 新 anchor 就要更谨慎。

第三步，查询父文档 `SELECT * FROM evidence_docs WHERE anchor = ?`，做 kind-scope 检查，再用首个 passage 的前 200 个字符创建摘要占位，最后把全部 passage 挂到 item。查询父文档是一次按 anchor 的 hydrate；当前 passage anchor 数量最多 100，虽然存在 N 次查询，但它发生在防污染和类型检查边界。若要优化可批量 `IN` hydrate，同时保留每个 anchor 的 map。

### G.5 contextWindow 为什么必须带 `doc_anchor`

上下文 SQL 是：

```sql
SELECT doc_anchor, passage_id, content, speaker, position, created_at
FROM evidence_passages
WHERE doc_anchor = ?
  AND position BETWEEN ? AND ?
  AND passage_id != ?
ORDER BY position
```

只按 `position` 查会把另一个 thread 的消息混进来；`doc_anchor = ?` 是跨帖隔离的关键。`BETWEEN position-contextWindow AND position+contextWindow` 是包含两端的整数范围，例如 position=10、window=2 会取 8 到 12，再排除命中本身。转换到 API 时，`thread-` 前缀剥成 `threadId`，`msg-` 前缀剥成 `messageId`，方便 MCP drill-down。

### G.6 raw 结果的排序不是 BM25 排序

`rankRawResults()` 先把有 passages 的 item 排在没有 passages 的 item 前；两者都有时按照 passage 首次出现位置；都没有时保持 baseResults 原序。它不重新计算“一个 doc 命中多少 passage”的分数，也不按 passage content 长度排序。这个策略偏向可解释性：用户看到的第一项一定有原文证据，且顺序对应召回通道首次发现顺序。

## H. 向量与 RRF：从 HTTP 到最终 anchor 的完整细节

### H.1 `EmbeddingService.load()` 只探活，不加载模型

API 进程中的 `EmbeddingService` 是 HTTP client。`load()` 请求 `${baseUrl}/health`，超时 5000ms；health 返回 `status === 'ok'` 才将 `ready=true`，并把 `health.model` 记录成 modelId。模型实际在 `scripts/services/embed-api.py` 的独立进程中运行。

这里的 `AbortSignal.timeout(5000)` 是 Web Fetch API 的超时信号；超时会进入 catch，设置 `ready=false`。catch 不重新抛出，故调用方可以继续 lexical。`reprobeIfNeeded()` 只在未 ready 且距离上次探测超过 30 秒时调用 load，避免每次请求都打健康检查。

### H.2 embed 批处理的索引保持

`embed(texts)` 先创建固定长度的 `results` 数组，再按 64 切 batch。服务端响应的 `data` 可能不是输入顺序，`embedBatch()` 按 `index` 排序后写入 `Float32Array(targetDim)`；不足维度的尾部默认 0，超过维度的部分被截断。最后通过 `results[offset + i] = vectors[i]` 恢复全局顺序。

面试追问“为什么不直接返回 number[][]”，可以回答：`Float32Array` 更适合 sqlite-vec 的绑定和内存布局；固定维度保证向量表 `float[768]` 的契约。如果服务端返回错误维度，当前实现采取截断/零填充而不是抛错，属于兼容性优先；生产可进一步加维度校验并把异常纳入 meta。

### H.3 `semanticNNSearch()` 的 single-IN hydrate

NN 搜索先拿候选：

```ts
const pool = Math.min(Math.max(limit * 4, 20), 100);
const queryVec = await this.embedDeps!.embedding.embed([query]);
const nnResults = this.embedDeps!.vectorStore.search(queryVec[0], pool);
```

随后把所有 anchor 生成 `IN (?, ?, ...)`，再追加 status、kind、scope、keywords、thread、world、scene、provenance 和 backstop 条件。这样避免了对每个 NN hit 做一次 `SELECT`。但要注意：向量搜索本身先在全表取 pool，过滤是在 hydrate SQL 中发生；如果 pool=20 而前 20 个大多不符合 scope，最终结果可能少于 limit。扩大 pool 是 recall/延迟的折中。

### H.4 doc-level hybrid RRF 的两个权重

源码在 `hybridRRFSearch()` 中用 `RRF_K = 60`，lexical 每个排名 i 贡献：

```text
1 / (60 + i)
```

semantic NN 贡献：

```text
nnWeight / (60 + i)
```

其中 CJK query 通过 `hasCJKCharacters()` 选择 `CJK_NN_WEIGHT = 1.5`，非 CJK 为 1.0。i 从 0 开始，所以第一名 lexical 是 1/60，第一名 NN（中文）是 1.5/60。这个权重不是把中文分数“乘 1.5”，而是只提高 NN 通道在 RRF 总和中的名次贡献。

如果同一 anchor 同时出现在两路，分数累加；只出现在一路也能保留。源码用 `Map<string, number> scores` 累加，并用 `Set` 或 map 保存 item。最后按 score 降序，过滤掉没有 hydrate 到的 anchor，再 slice 到目标 limit。

### H.5 为什么 hybrid 的 lexical pool 不能先 slice 到 limit

`searchWithMeta()` 明确区分：

```ts
const lexicalCandidates = results.slice(0, bm25Pool);
const lexicalResults = results.slice(0, limit);
```

lexical 模式只需要最终 limit；hybrid 则需要完整 BM25 pool 参与 RRF。如果先 slice 到 limit，所有 lexical 排名 11 之后的文档永远没有机会和 NN 结果融合，向量通道的覆盖会被错误截断。F200 consumption rerank 开启时，hybrid 还把目标 pool 提高到 `limit * 3`，为后续重排和 MMR 留空间。

### H.6 passage-level RRF 的 key 不是 anchor

passage hybrid 使用：

```ts
JSON.stringify([docAnchor, passageId])
```

作为 map key。不能只用 `docAnchor`，否则同一 thread 的多条消息会互相覆盖。`parsePassageVectorKey()` 对 JSON 结构做严格检查：必须是长度 2 的数组，两个元素都是 string，否则抛错；hydrate 侧 catch 后丢弃坏 key，避免一条损坏向量阻断整次 raw 查询。

### H.7 VectorStore 的 delete+insert 不是遗漏 ON CONFLICT

sqlite-vec `vec0` 虚拟表不支持普通 SQLite 表的 `ON CONFLICT DO UPDATE`。`VectorStore.upsert()` 和 `PassageVectorStore.upsert()` 都显式：

```sql
DELETE FROM ... WHERE key = ?;
INSERT INTO ... (key, embedding) VALUES (?, ?);
```

这两步应在写队列或外层事务语义中执行；如果中间进程崩溃，可能暂时没有该向量，但不会留下同 key 的重复行。`clearAll()` 同时删除向量和 `embedding_meta`，表示下一次启动需要重新建立版本锚。

## I. 联邦检索：KnowledgeResolver 如何把多个 SQLite 变成一个结果集

### I.1 `dimension` 决定 fan-out 的拓扑

`KnowledgeResolver.resolve()` 先读 `dimension = options?.dimension ?? 'all'`。`'project'`、`'global'` 和旧的 `'all'` 走 legacy 分支；`'library'`、`'collection'` 走 `resolveNCollection()`。所以 `dimension` 不是传给 store 的 SQL 字段，而是决定“调用一个 store、两个 store，还是 catalog 中 N 个 collection store”。

`dimension='all'` 当前仍兼容 project+global，但返回 `deprecationWarnings`，提示迁移到 `library`。这体现了 API 兼容策略：先保持旧行为，再通过结果元数据告诉调用方新语义，而不是突然改变默认范围。

### I.2 `Promise.all` 和 `Promise.allSettled` 的选择

legacy project+global 使用 `Promise.all`，但 global promise 自己 catch 成 degraded empty，因此 project 成功、global 失败不会 reject 整体。N-collection 使用 `Promise.allSettled`，每个 manifest 都能变成 `ok`、`skipped` 或 `error` group；一个外部 collection 超时不会取消其他 collection。

`allSettled` 返回的 entry 只有 fulfilled/rejected 状态，rejected 时源码用 `settled.indexOf(entry)` 找回 manifest。这个写法保持输入顺序，但查找是 O(n)；当前 collection 数量有限。每个 fulfilled store 记录 `durationMs`，为诊断“哪个库慢”提供依据。

### I.3 collection fan-out 时每个 store仍收到同一 limit

`resolveNCollection()` 对每个 manifest 调 `searchStoreWithMeta(store, query, { ...options, limit })`。也就是说每个 collection 都返回最多 limit 条，再由 `rrfFusionN()` 汇总；它不会把全局 limit 除以 collection 数量。这样能保持小 collection 不被大 collection 的预算压制，但代价是总候选数约为 `N * limit`。

每个 group 先根据 sensitivity 调 `redactForTranscript()`。public/internal 原样返回；private/restricted 只保留 `anchor/kind/status/title/updatedAt`，title 替换成 `[redacted — ... collection]`。注意脱敏发生在跨 collection 融合之前，因此融合仍可用 anchor 去重和排名，但不会把敏感 summary 带进最终 transcript。

### I.4 federated RRF 的公式与项目数据胜出规则

`KnowledgeResolver` 的 RRF 常量也是 60：collection 内第 i 名贡献 `1/(60+i)`。同一 anchor 在多个 collection 出现时，分数累加；第一次写入 `scoreMap` 的 item 数据被保留。在 legacy project+global 路径，project 先遍历，因此相同 anchor 的 project item 胜出；N-collection 则由 groups 的顺序决定胜出数据。

这和 doc-level hybrid 的差别要说清楚：doc-level hybrid 是“一个 store 内 lexical vs vector 两路”；KnowledgeResolver 是“多个 store/collection 的结果融合”。公式相同，但输入排名的来源和数据优先级不同。

### I.5 degraded meta 的优先级合并

多个 collection 的 meta 可能同时降级。`combineSearchMeta()` 只返回一个最严重原因，优先级是：

```text
evidence_store_error (4)
passage_vector_search_error (3)
passage_embedding_unavailable (2)
raw_lexical_only (1)
```

它不会把所有错误数组塞进响应，而是提供一个确定性 banner 原因。面试时可以指出改进方向：保留 `perCollectionMeta` 能更好定位，但当前接口 `SearchExecutionMeta` 设计成单原因摘要，优先保证客户端简单。

## J. 索引构建：扫描、hash、所有权和 passage 写入

### J.1 scanner 是策略对象，IndexBuilder 是流水线

`IndexBuilder` 构造时自动检测：docsRoot 下有 `features/` 或 `decisions/` 就用 `CatCafeScanner`；有 `package.json`、`Cargo.toml` 等项目 manifest 就用 `GenericRepoScanner`；否则回退 CatCafeScanner。scanner 负责 discover/parse，IndexBuilder 负责 hash、upsert、边抽取、passage 和 embedding。

这是一种策略模式的轻量实现：`RepoScanner` 只需提供 `discover(projectRoot, options)`，不需要让 IndexBuilder 知道每种仓库的 frontmatter 细节。构造时调用 `store.setSourceRoot(this.scanRoot)`，让 drill-down 路径和实际扫描根目录一致。

### J.2 sourceHash 为什么只取 SHA-256 前 16 位

文件 rawContent 用：

```ts
createHash('sha256').update(rawContent).digest('hex').slice(0, 16)
```

这提供 64-bit 十六进制展示长度，足以用于本地增量判断，同时避免数据库字段过长。它不是密码学完整标识；如果跨系统需要极低碰撞风险，`CollectionIndexBuilder` 另有全长 hash 语义。线程摘要 hash 则对 `{title, summary, keywords}` 做 JSON.stringify 后 hash，内容不变就跳过 upsert。

### J.3 `INDEXING_VERSION` 是派生逻辑的 schema 版本

数据库 schema V26 只描述表结构；scanner 逻辑改变时，表结构没变，sourceHash 也可能没变，旧索引仍然错误。`IndexBuilder.hasIndexingVersionChanged()` 从 `embedding_meta` 读 `indexing_version`，与常量 5 比较；不一致就把 `effectiveForce` 置为 true。重建成功后通过 `store.runExclusive()` 写入版本。

写版本失败不会让 rebuild 失败，而是下次重启再次全量索引。这是典型的“宁可多做一次昂贵工作，也不要保留过期派生数据”的 fail-open。

### J.4 增量跳过的两个条件

源码不是看到 sourceHash 相同就无条件跳过：

```ts
if (existing?.sourceHash === item.sourceHash) {
  const needsProvenanceBackfill = !existing.provenance?.tier && item.provenance?.tier;
  if (!needsProvenanceBackfill) skipped++;
}
```

如果旧记录缺 provenance、新扫描能提供 provenance，即便内容 hash 相同也会重新写入。这样不会让“新增元数据”被 hash 优化吞掉。`force` 或 indexing version bump 则完全跳过 hash short-circuit。

### J.5 anchor ownership：高优先级文档保护自己

同一个 anchor 可能由不同目录生成。`KIND_PRIORITY` 中 feature=4、decision=3、plan/discussion/research=2、session/lesson/thread=1、pack-knowledge=0。新 item 优先级低于旧 item，且旧 sourcePath 文件仍存在时，跳过覆盖；只有旧文件已从磁盘消失，低优先级项才可能接管 anchor。

这不是数据库 UNIQUE 冲突解决，而是写入前的业务判定。若面试官问“为什么不直接让最新文件覆盖”，回答是 anchor 是稳定引用，覆盖会让一个 feature 锚点突然变成 session 摘要，破坏边、向量和外部链接的语义。

### J.6 passage upsert 的 `WHERE` 避免无意义更新

`indexPassages()` 使用：

```sql
ON CONFLICT(doc_anchor, passage_id) DO UPDATE SET ...
WHERE evidence_passages.content IS NOT excluded.content
   OR evidence_passages.speaker IS NOT excluded.speaker
   OR evidence_passages.position IS NOT excluded.position
   OR evidence_passages.created_at IS NOT excluded.created_at
```

只有实际字段变化才更新。`result.changes > 0` 时删除旧 passage vector，迫使 warmup 重新 embed；未变化的 passage 保留已有向量。这里利用 `IS NOT` 而不是 `!=`，正确处理 NULL 与 NULL 的比较。

### J.7 “永不删除缺失 passage”是索引一致性取舍

注释明确：rebuild 不删除 messageListFn 当前没返回的 passages。原因可能是消息分页、临时不可用或历史数据不完整；直接 DELETE 会造成数据丢失。代价是删除后的消息可能在索引中残留，需要专门清理任务或 source tombstone。面试时主动说出这个 trade-off，比简单说“upsert 保证一致”更准确。

### J.8 passage embedding warmup 的 256 扫描、8 请求批次和三次熔断

`embedMissingPassages()` 按自增 `id > lastId`、每批 256 行扫描 evidence_passages；对每行检查 vector key 是否已存在，只对 missing 调 `embedPassages()`。embed-utils 对 passage 使用更小的 HTTP batch（8），避免长文本请求超时。

批次失败时不抛出到 rebuild，而是 `consecutiveErrors++`；成功就归零，连续 3 批失败则停止 warmup。FTS 仍是 canonical recall，所以向量 backfill 停止不会让 raw lexical 搜索不可用。`lastId` 在批次读取后推进，即使本批 embedding 失败也继续扫下一批，避免卡在坏数据；连续失败阈值负责在服务整体不可用时及时退出。

### J.9 dirty thread 的 debounce 与 summary_state

`markThreadDirty()` 只把 id 放进 Set，重复标记自动去重。`accumulateSummaryDelta()` 在 append 时估算 token：`Math.ceil(messageContent.length / 4)`，并用正则 OR 计算 decision/code/error_fix signal bit flags。SQL 使用 `ON CONFLICT(thread_id) DO UPDATE`，计数累加、flags 按位 OR。

`flushDirtyThreads()` 先复制 dirtyIds 再清空 Set，读取线程快照，按相同 sourceHash 判断是否需要 upsert；最后同时 `indexPassages()` 和 `embedMissingPassages()`，保证摘要和新消息近实时可搜。若 threadListFn 失败，返回 0；dirty ids 已清空，后续由新的消息再次标脏，这是“避免重复堵塞”与“可能丢一次刷新”的平衡。

## K. Schema V26：迁移脚本的幂等性、事务边界和 vec0 解耦

### K.1 `applyMigrations()` 的版本游标

启动时先 `CREATE TABLE IF NOT EXISTS schema_version`，再执行：

```sql
SELECT MAX(version) AS v FROM schema_version
```

空表得到 NULL，通过 `?? 0` 变成版本 0。每个版本使用 `if (currentVersion < N)`，执行 DDL 后插入 `(N, applied_at)`。已应用版本不会重复执行，满足幂等启动。

### K.2 为什么很多 ALTER TABLE 都包在 try/catch

SQLite 没有统一的 `ADD COLUMN IF NOT EXISTS` 语法兼容策略，仓库对部分增量列采用：

```ts
try { db.exec('ALTER TABLE evidence_docs ADD COLUMN authority TEXT ...'); }
catch { /* partial migration or already exists */ }
```

这样可以从部分迁移状态继续启动，但也会吞掉非“已存在”的真实错误。版本号仍会插入，所以如果错误是磁盘损坏，可能需要一致性检查发现。更严格的实现应检查异常消息或在事务中迁移；当前设计偏向升级容错。

### K.3 V1 的 external-content FTS 触发器

`evidence_fts` 定义 `content=evidence_docs, content_rowid=rowid`，自身不保存完整文档。INSERT/UPDATE/DELETE 通过三个 trigger 同步：更新时先向 FTS 发送 `'delete'` 旧 row，再插入新 title/summary。这样内容真相只在 evidence_docs，FTS 只是索引。

passage_fts 使用同样机制，但 tokenizer 加 `tokenchars '_-'`，保留代码标识符和连字符。触发器必须逐条 `db.exec(stmt)` 执行，不能把多条 trigger 定义拼成一个驱动不支持的批量语句。

### K.4 V24 entity schema 的两个级联删除

`entity_aliases.entity_id` 外键引用 entity_registry，`entity_mentions.entity_id` 也引用 registry，mentions 还引用 evidence_docs.anchor；都使用 `ON DELETE CASCADE`。删除实体会清理 alias 和 mention，删除文档会清理 mention。`PRAGMA foreign_keys = ON` 是必要条件；没有开启 pragma，SQLite 可能只保存声明而不执行级联。

### K.5 V26 的 `result_count` 为什么不能从 candidates_json 推导

线程 scope 的 RecallFeed 可能只保存摘要候选，`candidates_json.length` 不等于用户界面报告的“Found N”。V26 给 `recall_events` 增加独立 `result_count`，把“实际报告命中数”和“持久化的候选 anchor 数”分开。这是数据契约修复，不是检索算法修改。

### K.6 vec0 表为什么从 migration 中解耦

`ensureVectorTable(db, dim)` 和 `ensurePassageVectorTable(db, dim)` 在 sqlite-vec 扩展加载后单独执行。扩展不可用时 catch 返回 false，不让普通 SQLite schema migration 失败。这样 `evidence_docs`、FTS 和 lexical 搜索仍可用；向量表是可选加速器。

## L. 消费重排：贝叶斯收缩、宪法保护、MMR 和 shadow

### L.1 Beta-Binomial 收缩的每一项从哪里来

`computeConsumptionPrior()` 使用 Beta(α0=2, β0=8) 先验：

```text
shrunkCtr = (consumedCount30d + 2)
            / (exposureCount30d + 2 + 8)
```

它不是直接用 `consumed/exposure`，所以曝光 1 次、消费 1 次的 CTR 不会变成 1.0，而会被先验拉回 0.3 左右。`meanCtrKind` 从 `global_ctr_baseline[docKind]` 读取，缺失时为 0.2。`rawLift = (shrunkCtr - meanCtrKind) * recencyFactor`，表示相对同 kind 基线的提升。

### L.2 recencyFactor 的三个分支

```ts
daysSinceLastConsumed == null -> 0.5
halfLife == null              -> 1.0
otherwise                     -> halfLife / (halfLife + days)
```

`null days` 表示从未消费，给中性偏低因子 0.5；ADR/lesson/canon 的 half-life 为 null，视为不衰减；其他 kind 按 half-life 衰减。例如 thread half-life 14 天，14 天未消费时 factor=14/(14+14)=0.5；feature 90 天，90 天未消费也是 0.5。

### L.3 constitutional immunity 的真实三分支

先判断：

```ts
const isConstitutional = authority === 'constitutional'
  || ['decision', 'lesson'].includes(docKind);
```

constitutional、decision、lesson 进入 constitutional 分支，`prior=Math.max(0, rawLift)`，不会因低曝光被清零，也不会因为负 lift 把 prior 变成负数。随后才是 exposure<5 的 cold-start、exposure<20 的 low-sample、否则 full。

注意 14 天 grace period 在最前面：只要 `firstIndexedAt` 在 14 天内，函数直接返回 branch='cold-start' 且 prior=0，甚至不会计算 kind baseline。面试官问“constitutional 是否绝对永远置顶”，正确回答是：**它免疫的是消费先验的低样本/负向惩罚，不等于跳过所有排序；在 14 天新鲜期内仍返回零 prior，最终是否置顶还要看 pin 和基础名次。**

### L.4 `applyConsumptionRerank()` 的合成分

对每个非 exact-protected item，源码计算：

```text
positionalScore = 1 / (i + 60)
newScore = positionalScore
         + 0.15 * prior
         + 0.10 * (decay.factor - 0.5)
```

`i` 是 rerankPool 中的原始位置，不是全局最终位置。`decay.factor` 以 `updatedAt` 计算，减 0.5 后让中性点为零；fresh 文档得到正加成，过旧文档得到负加成。消费 prior 和 recency decay 是两个独立项。

### L.5 pin 的位置保持算法

constitutional item 进入 `pinned: Map<number, EvidenceItem>`，key 是它在 rerankPool 中的原始 index；其他 item 进入 movable 并按 newScore 降序。重建结果时遍历原 rerankPool 的位置：该位置有 pin 就放回 pin，否则从 movable 取下一个。这样 constitutional 文档保持原来占据的槽位，不会全部挤到顶部，也不会被消费重排删除。

exact lexical protection 在更前面：精确保护项从 rerankPool 中移出，最终用 `[...protectedResults, ...reranked]` 放到结果头部。保护条件是 query 至少两个词或含 CJK；所有词出现在 title+summary+keywords，且至少一个词在 title 或 keywords。它是“检索精确性保护”，constitutional pin 是“知识权威保护”，是两个机制。

### L.6 MMR 的触发条件、λ 和 Jaccard

只有 `targetLimit` 存在且 `movable.length >= 3 * targetLimit` 才触发 MMR；否则直接按 newScore 截断。`applyMMR(..., lambda=0.7)` 每轮从剩余候选中选：

```text
mmrScore = 0.7 * relevance - 0.3 * maxSimilarityToSelected
```

`keywordSimilarity()` 对两个 item 的 keywords 转 Set，计算 Jaccard：`intersection / union`。没有关键词时相似度为 0。第一轮 `maxSim=0`，先选 relevance 最高；后续会惩罚关键词高度重叠的重复结果。

注意 applyConsumptionRerank 在 MMR 后把 movable 映射成 `{ item, newScore: targetLimit - i }`，后续只关心顺序，不再保留原始分数。这是合理的，因为 MMR 已经完成选择，pin 重建只需要序列。

### L.7 shadow ranking 为什么要“反转”

函数先保存原始 `preRerankOrder`。在 `consumptionRerank='on'` 时 live 是 final reranked，shadow 保存原始 BM25；在 `'shadow'` 时 live 保持原数组，shadow 保存 would-be final。两种模式必须保存相反顺序，否则线上和 shadow 完全相同，离线 MRR 比较没有信号。

shadow 只保存在进程内 Map，key 是前 8 个 candidate anchors 拼接，最多保留 32 个 key；`lookupShadowRanking()` 取出后 `shift()`，是一次性消费。它不是可靠持久化，真正长期分析还要依赖 recall_events 的 shadow_ranking_json。

## M. 图检索和边抽取：BFS 之外还有权限、脱敏和截断

### M.1 边权重是“类型基线 + 遍历热度”

`computeEdgeWeight()` 的公式：

```text
typeBase = TYPE_BASE[relation] ?? 1.0
recencyDecay = days == null ? 0 : 30 / (30 + days)
traversalBoost = 0.05 * traversalCount30d * recencyDecay
total = typeBase + traversalBoost
```

`feature_ref=1.1`、`wikilink=1.0`、`doc_link=0.9`、`blocked_by=0.7` 等只影响输出 weight；当前 `GraphResolver` BFS 仍按边枚举扩展，不按 weight 做优先队列。面试时不要把 weight 说成“已经用于决定遍历顺序”，它目前更多是解释和后续排序信号。

### M.2 BFS 的 visited 语义和 canonical anchor

`buildSubgraph()` 用 `frontier` 按层推进，`visited` 防环。collection store 可能接受 alias anchor 并返回 canonical doc，代码用 `rememberLookupAlias()` 记录 alias，后续同一 canonical 节点可尝试多个 lookup anchor。节点 map 以 canonical anchor 为 key，避免 alias 产生重复节点；输出 anchor 在敏感 collection 下可能替换成 `[redacted:n]`。

### M.3 截断是遍历时生效，不是最后 slice

每扩展一个 current node 前检查：

```ts
currentNodeEdges >= 15 || nodesMap.size >= 50
```

一旦超限设置 `truncated=true`，停止该节点/后续扩展。这样不仅限制返回 JSON 大小，也限制 backend `getRelated()` 带来的成本。relation filter 也在扩展时应用；源码注释特意强调不能只在 render 时过滤，否则被禁止关系仍会扩展 frontier，造成隐性成本和节点泄漏。

### M.4 sensitivity 取更严格者

跨 collection 边如果没有显式 `edgeSensitivity`，使用 `stricterSensitivity(from, to)`。如果边一端 private、一端 public，则按 sensitivity order 选择更严格者；调用方没有两个 collection 的权限时，节点或边输出 redacted。对 unresolved anchor，系统会先记住待脱敏状态，最后再统一替换 opaque anchor，避免中途因尚未解析 collection 而短暂泄漏真实 id。

### M.5 三类文本边的抽取细节

- wikilink 用 `/\[\[([^\]|]+)(?:\|[^\]]+)?\]\]/g`，支持 `[[target|label]]`，按小写 target 去重，并跳过 self edge。
- feature_ref 先去掉 frontmatter、wikilink 和 Markdown link，再匹配 `\bF(\d{2,4})(?![-a-zA-Z])\b`，把 `F42` 规范成 `F042`，过滤大于 999 的编号。
- doc_link 区分 `/docs/` 绝对路径、相对路径和 sourceDir resolve；跳过 http、fragment-only、self 和无法映射到 anchor 的链接。

这些 extractor 返回的是 `provenance:'content'` 的纯数据，真正写入 edges 的事务和删除旧 content edges 由 IndexBuilder 管理。这样重建不会无限叠加重复边。

## N. 安全和可观测性：检索不是“只要搜到就行”

### N.1 SecretScanner 的两层检测

第一层是 7 类固定 regex：AWS AKIA、GitHub token、OpenAI/Anthropic key、Slack token、私钥头。每行匹配后立即记录 type/file/line，并用 `maskSecret()` 只保留前 4 和后 4，中间最多 16 个 `*`。

第二层只在行包含 `key/token/secret/password/credential/auth:` 或 `=` 时执行，提取至少 32 字符的值，排除 `EXAMPLE/PLACEHOLDER/YOUR_/REPLACE/CHANGEME/xxx`，再计算 Shannon entropy；大于 3.5 才报 `high-entropy-secret`。它不是密码学证明，只是降低“随机长字符串”漏检率和文档示例误报率。

代码块 fence 内直接跳过，减少 Markdown 示例误报；但这也意味着真实 secret 若被放进 fenced block 会被忽略。面试时可以主动指出这是已知取舍，并建议对私有 collection 使用更严格策略。

### N.2 transcript redactor 是字段白名单，不是字符串替换

`redactForTranscript()` 对 public/internal 直接返回原 item；private/restricted 返回新对象，只复制 `anchor/kind/status/title/updatedAt`，并改写 title。它不会递归删 `drillDown` 或 `entityMatches`，因为这些字段根本没有被复制；也不会修改原数组中的 item，使用 `items.map` 创建新对象。

这比对 summary 做正则脱敏更可靠：不会因遗漏一个敏感字段而泄漏；代价是 restricted 结果几乎不可读，只能用于“某 collection 有命中”的提示。GraphResolver 的 opaque anchor 是另一种边界内脱敏，二者不要混为一谈。

### N.3 factory 的 fail-open 组合

`createMemoryServices()` 是能力组装点：先初始化主 store 和 entity seeds，再按 embedMode 创建 EmbeddingService；load 失败不抛，sqlite-vec import/ensure 失败也不抛；没有完整 `embedDeps` 时 IndexBuilder 仍可运行 lexical。global store 初始化失败则只返回 project-only；外部 collection 单个失败则跳过该 collection。

这种设计让“核心服务可启动”优先于“增强能力全部在线”。但也要看到边界：如果 `embedMode='on'` 而 vectorStore 创建失败，`store.setEmbedDeps` 不会设置，查询会自然 lexical；客户端 meta 目前可能仍是 `{ degraded:false }`（doc-level semantic catch 返回空降级标记），这是可观测性上的已知缺口，不能在面试中声称所有 embedding 错误都会出现在 meta。

### N.4 诊断一条搜索的建议顺序

1. 先看输入：route/MCP 是否把 `limit/mode/depth/scope/dimension` 传对。
2. 再看 store：空 query、exact anchor、FTS progressive level、backfill 是否命中。
3. raw 问题看 `passages`、`_semanticHit/_alsoLexical` 逻辑和父 doc kind。
4. semantic/hybrid 问 embedding health、vector count、embedding_meta model/dim、一批 hydrate 后剩多少行。
5. 多 collection 看 collectionGroups 的 status/duration/sensitivity 和 combineSearchMeta 的最高降级原因。
6. 排序问题最后看 authority、consumption prior、recency、MMR、shadow；不要一上来怀疑 BM25。

## O. 面试白板演练：给定一个 query，逐个变量写出来

假设请求：

```json
{
  "q": "F042 cache timeout",
  "limit": 5,
  "scope": "threads",
  "mode": "hybrid",
  "depth": "raw",
  "contextWindow": 1,
  "explain": true
}
```

可以按以下顺序复述：

1. `trimmed='F042 cache timeout'`，`limit=5`，`bm25Pool=20`；scope 推出 `effectiveKind='thread'`，不排除 thread，默认排除 pack-knowledge。
2. exact SQL 用 `anchor COLLATE NOCASE`，即使 query 不是完整 anchor，也只是一次无害的精确尝试。
3. FTS token 数 3，builder 只返回一条 AND-all；SQL title 权重 5、summary 权重 1，追加 `kind='thread'` 和 `LIMIT 20`。
4. lexical backfill 对三个词做 LOWER+LIKE；结果按 keyword/title/text signal 和质量排序，和 FTS anchor 用 `seenAnchors` 去重。
5. Entity Registry 解析 F042 是否是实体 alias；命中的 mention docs 加入候选并挂 `entityMatches`。
6. 因为 `depth='raw'` 且 scope 是 threads，进入 passage 路径；pool=20，FTS/NN 召回 passage，`contextWindow=1` 按同一 `doc_anchor` 取前后消息。
7. hybrid 若 embedding ready，doc/passage RRF 使用 k=60；中文权重只有 query 含 CJK 才 1.5，本例为 ASCII，NN weight=1.0。
8. `hydratePassageResults` 只允许 parent kind=thread；scope active 且 doc-level 有结果时，cross-post 只有纯 NN（`_semanticHit && !_alsoLexical`）才可新增。
9. raw 结果优先有 passage 的 doc，截到 limit；`enrichWithDrillDown` 追加 consumption rerank、matchReason、解释字段和 thread context drill-down。
10. 若 embedding health 失败，raw semantic/hybrid meta 应降到 lexical passage，`degradeReason` 为 `passage_embedding_unavailable` 或 `passage_vector_search_error`；doc-level mode 的当前实现仍可能只返回 lexical items 而不标 degraded，这是应主动说明的代码事实。

面试官继续追问“为什么 FTS 先 LIMIT 20，而最终只返回 5”，回答是：hybrid 需要宽 lexical pool 与 NN 做 RRF；raw 还需要多个 passage 聚合到更少的 doc，过早 limit 会丢失跨通道覆盖。

## P. 代码审查视角：当前实现的优点与可诚实指出的缺口

### P.1 优点

- 事实表、FTS、向量表分离，索引可重建；
- SQL 值全部参数化，scope/status/date 等资格条件在多路 hydrate 中重复应用；
- lexical、semantic、hybrid、raw 各自有明确分支，失败时保留 lexical 基线；
- entity、authority、consumption、MMR 都是可解释的独立阶段；
- collection fan-out 使用 allSettled，敏感库先脱敏再融合；
- schema/version/hash 让增量索引和迁移具备幂等特性。

### P.2 不能过度包装的缺口

- doc-level semantic/hybrid 在 embedding 不可用或异常时返回 lexical，但 `meta.degraded` 当前写成 false；这会让客户端无法准确显示降级横幅。
- `rowToItem()` 对损坏 JSON keywords/source_ids/contradicts 直接 JSON.parse，数据损坏可能打断请求；backfill 自身则是 fail-open。
- passage hydrate 对每个新 anchor 单独查父 doc，存在可批量优化的 N+1；当前 pool 上限 100，风险可控但不为零。
- backfill contains 查询没有 SQL LIMIT，大量文档时内存排序成本会上升。
- graph edge weight 已计算但 BFS 未使用优先队列，weight 目前更像解释字段和后续排序信号。
- shadow ranking 主要是内存 Map，进程重启会丢；长期分析依赖 recall_events 侧是否持久化。
- “永不删除缺失 passage”保护历史数据，但需要额外 tombstone/GC 机制处理真正删除。

主动说缺口不是自我否定，而是说明你区分了“当前代码行为”和“理想设计”。面试官继续问怎么改时，可以按风险排序：先修 meta 误报和 JSON 解析，再做 batch hydrate/回收 tombstone，最后再评估更复杂的排序模型。

## Q. 最终复述模板：两分钟讲清楚，十分钟扛住追问

“Clowder 的 memory search 不是一个 SQL `LIKE`，而是写入链和查询链分开的派生索引系统。写入时 scanner 把文档或 thread 摘要转成 `EvidenceItem`，用 sourceHash 和 indexing version 做增量判定，写入 `evidence_docs`；FTS5 通过 external-content trigger 同步 title/summary，thread 消息另写 `evidence_passages`，向量是可选的 vec0 加速器。

查询从 `searchWithMeta` 开始：先规范化 query、计算 limit 和 hybrid 宽池，应用 kind/scope/status/date/provenance/world 等资格条件；然后 exact-anchor、FTS progressive relaxation、substring backfill 和 entity mention 形成候选。summary 模式按 lexical/semantic/hybrid 分流，hybrid 用 k=60 的 RRF 把 BM25 和 NN 名次相加，中文 query 给 NN 1.5 权重；raw 模式改搜 passage，并通过 doc_anchor、thread kind 和 `_semanticHit/_alsoLexical` 防止 cross-post 污染。

候选出来后，authority boost、消费 prior、recency decay、constitutional pin、MMR 和 explain/drill-down 负责排序和可解释性；embedding 或 sqlite-vec 挂掉时保留 lexical 基线，多 collection 则由 KnowledgeResolver 并发 fan-out、脱敏后再做 collection-level RRF。最后用 `SearchExecutionMeta`、recall_events 和 collectionGroups 诊断召回是否降级。

如果要改这套系统，我会先保持 evidence_docs/FTS 作为稳定基线，把增强能力都做成可观测、可回退的阶段；再针对 meta 误报、passage batch hydrate 和 tombstone GC 做精确改进，而不会直接把所有排序逻辑揉成一个不可解释的总分。”

## R. 查询入口逐行复盘：`searchWithMeta()` 怎样把一个字符串变成检索计划

前面的两分钟版本适合先给面试官建立全局图；这一节进入真正的函数体。建议把 `searchWithMeta` 看成一个“查询编译器”：它并不直接把 query 拼成一条 SQL，而是先把用户意图编译成一组布尔过滤器、召回通道和后处理阶段。函数的返回值是 `{ items, meta }`，其中 `items` 是业务对象，`meta` 是执行状态。这个返回形状很重要，因为调用方不能只看有没有结果，还要知道结果是不是降级出来的。

### R.1 第一组局部变量：`limit`、`bm25Pool`、`trimmed`

源码第一段可以缩写成：

```ts
const limit = options?.limit ?? 10;
const bm25Pool = mode === 'hybrid'
  ? Math.min(Math.max(limit * 4, 20), 100)
  : limit;
const trimmed = query.trim();
if (!trimmed) return { items: [], meta: { degraded: false } };
```

这里同时用了三个 TypeScript/JavaScript 语法点。

第一，`options?.limit` 是 optional chaining。 `options` 为 `undefined` 时不会抛 `Cannot read properties of undefined`，整个表达式结果是 `undefined`。第二，`??` 是 nullish coalescing，只在左侧为 `null` 或 `undefined` 时使用右侧默认值；如果有人传 `limit: 0`，`??` 不会把它改成 10，这和 `||` 的行为不同。当前函数后面并没有把 0 重新纠正为正数，所以接口层通常还需要 zod 或其他 schema 做 `min(1)` 校验。第三，`Math.max` 先保证 hybrid 至少取 20 条，`Math.min` 再把上限压到 100。这个顺序不是形式主义：当 `limit=3` 时，`limit*4=12`，先 `max` 后得到 20；当 `limit=100` 时，`400` 最后被压成 100。

为什么 hybrid 不直接查最终的 5 条？因为 lexical 和 vector 是两条独立排名，RRF 需要知道一个候选在两条列表中的名次。只拿各自前 5 条，某条通道排第 6、但在另一条通道排第 1 的文档会完全消失。 `bm25Pool` 是召回宽度，`limit` 是最终展示宽度，两者是不同概念。面试官问“这是不是浪费数据库资源”，可以回答：这是以少量额外候选换取融合召回率；同时通过 20 到 100 的硬上限控制最坏成本。

`trim()` 的意义也不只是美观。它让精确锚点查询的 `anchor = ?` 不会因为用户在末尾多敲一个空格而失效；空字符串提前返回还能避免后续 FTS `MATCH ''`、LIKE `%%%` 这种无意义扫描。空 query 的 `degraded=false` 表示“没有执行检索”，不是“检索成功但没有命中”。如果 API 需要区分这两种情况，应该另加 `empty_query` reason，而不是滥用 degraded。

### R.2 `splitLexicalBackfillWords` 为什么要在最前面计算

```ts
const lexicalBackfillWords = splitLexicalBackfillWords(trimmed);
const queryEntityMatches = registry?.resolveQuery(trimmed) ?? [];
```

这两步是并行准备不同索引的输入。`splitLexicalBackfillWords` 产生短词数组，例如 `"cache timeout F042"` 可能得到 `['cache', 'timeout', 'F042']`；它服务于 SQLite 的 `LOWER(...) LIKE ?`，不依赖 FTS tokenizer。`resolveQuery` 则把原始 query 送进 Entity Registry，可能把 `Postgres`、`pg`、`PGSQL` 归一到同一个实体 ID。一个是词法分词，一个是领域别名解析，不能用同一个结果替代。

`?? []` 让实体注册表成为可选增强：测试中没有 registry、旧数据库尚未初始化 registry 时，后续 `for...of queryEntityMatches` 仍然安全。这里体现了整个模块的 fail-open 原则：基础 evidence_docs/FTS 不应因为实体索引不存在而不可用。需要强调的是，fail-open 不是吞掉所有错误；它只适用于可选通道，核心数据库打开失败仍由 `ensureOpen()` 抛出。

### R.3 scope 到 kind：为什么要在 SQL 之前完成语义归一化

```ts
const effectiveKind = options?.kind ?? (
  options?.scope === 'threads' ? 'thread' :
  options?.scope === 'sessions' ? 'session' : undefined
);
const excludeSessionAndThread = options?.scope === 'docs' || options?.scope === 'memory';
```

`scope` 是面向 API 使用者的概念，`kind` 是数据库字段。把映射提前做成 `effectiveKind`，后面 exact、FTS、backfill、entity hydrate 都能复用同一值，避免某一路漏过滤。`options.kind` 优先于 `scope`，意味着内部调用方可以明确指定 `kind='decision'`，覆盖较宽的 scope。TypeScript 的条件表达式返回的是联合类型，源码用类型断言把 `'thread'` 认定为 `EvidenceKind`，因为编译器无法从字符串字面量自动证明它属于项目定义的联合类型。

`docs`/ `memory` 并不是 `kind='doc'`，而是“排除 session 和 thread，保留其他 doc-backed discussion”。这解释了一个常见追问：为什么 docs 查询仍可能出现某些由对话沉淀出的 lesson？因为过滤策略按排除列表定义，而不是硬编码一个不存在的 `doc` kind。`scope='all'` 则不追加任何 kind 资格条件。

随后还有几个资格开关：

```ts
const excludePackKnowledge = effectiveKind !== 'pack-knowledge';
const threadAnchor = options?.threadId ? `thread-${options.threadId}` : undefined;
```

默认排除 `pack-knowledge` 是隔离策略，只有调用方明确查询该 kind 才允许进入。`threadId` 通过约定把业务 ID 转成 anchor，而不是新增一个复杂的 JOIN。这个约定必须和写入端一致：写入端如果使用 `thread:${id}`，这里的过滤就会静默失效。面试时可以指出，这种字符串协议应该由 `makeThreadAnchor()` 之类的单一 helper 封装，并用测试锁定。

### R.4 backstop suppression：动态 import 和 kill switch

源码会在 `includeBackstop` 未打开时尝试读取 F163 的 freeze flags：

```ts
let suppressBackstop = false;
if (!options?.includeBackstop) {
  try {
    const { freezeFlags } = await import('./f163-types.js');
    suppressBackstop = freezeFlags().compression !== 'off';
  } catch {
    // old build: keep backstop visible
  }
}
```

`await import()` 是运行时动态模块加载。它允许 memory 模块在某些裁剪构建、迁移阶段或测试替身中没有 F163 文件时仍能工作。这里 catch 后维持 `false`，即不做 suppression，属于保守的可见性选择。若把 catch 后改成 `true`，旧环境会突然少结果，风险更大。

`freezeFlags().compression !== 'off'` 表示压缩功能进入冻结或启用状态时，原始 backstop 文档不应再次竞争排名。后续每一条召回 SQL 都会重复追加 `activation != 'backstop'`。这是“资格过滤”而不是“排序降权”：被抑制的行根本不会进入候选池，因此不会消耗 RRF 名次，也不会出现在 explain 的负例里。若产品需要显示“此结果被压缩替代”，那应另做审计查询，不能依赖普通 search 返回。

### R.5 exact-anchor bypass：精确优先但不跳过资格条件

精确锚点逻辑的核心 SQL 是：

```sql
SELECT *
FROM evidence_docs
WHERE anchor = ? COLLATE NOCASE
  AND kind = ?
  AND status = ?
  AND updated_at >= ?
  AND activation != 'backstop';
```

源码不是把 query 直接当成“可信 anchor”，而是先建立 `anchorParams`，然后根据选项逐条追加 SQL 和参数。`COLLATE NOCASE` 只影响大小写比较，不会把 `F042` 和 `F-042` 当成相同字符串。它解决的是 tokenizer 语义被拆分的问题：unicode61 可能把 `F042` 拆成 `F` 和 `042`，但主键 anchor 本身应该作为完整字符串比较。

`anchorParams` 是 `unknown[]`，每次 `push` 都和 SQL 中新增的 `?` 一一对应。better-sqlite3 的 `.get(...anchorParams)` 使用展开语法把数组变成位置参数。这里不能把用户输入拼接进 SQL；即便 query 看起来像 `F042' OR 1=1 --`，也只会作为一个绑定值参与比较。关键词过滤也通过 `keywords LIKE ?` 参数化，虽然 JSON 文本 LIKE 不是严格 JSON 查询，但至少避免了注入。

精确命中后，函数做两件事：把 `rowToItem(exactRow)` 放入 `results`，并把 anchor 放入 `seenAnchors`。 `seenAnchors` 是去重集合，后面的 FTS、LIKE、entity 通道都检查它。注意 exact 命中并不立即 return：同一个 query 还可能需要 raw passage、entityMatches、contextWindow 或 hybrid 的语义补充。所谓 bypass 是“绕过 FTS tokenizer”，不是“绕过整个检索管线”。

### R.6 FTS progressive relaxation：三层查询为什么遇到结果就 break

`buildProgressiveFtsQueries(trimmed)` 返回从严格到宽松的一组查询。典型输入 `"cache timeout F042"` 可能得到：

```text
"cache" AND "timeout" AND "F042"
"cache" AND "timeout"
"cache" OR "timeout" OR "F042"
```

实际 builder 会根据 token 数和可用 strong token 组合生成有限序列，但思想是一样的：先保精度，确认没有结果后逐步保召回。每层 SQL 都重新绑定过滤参数，避免第二层忘记 `scope` 或 `dateTo`。

```sql
SELECT d.*, bm25(evidence_fts, 5.0, 1.0) AS rank
FROM evidence_fts f
JOIN evidence_docs d ON d.rowid = f.rowid
WHERE evidence_fts MATCH ?
  AND d.kind = ?
ORDER BY (d.superseded_by IS NOT NULL),
         (d.source_path LIKE 'archive/%'),
         CASE ... END,
         rank
LIMIT ?;
```

这里 `evidence_fts` 是 external-content FTS5 表，真正的业务列仍在 `evidence_docs`。`JOIN ... ON d.rowid = f.rowid` 通过 SQLite 隐藏的 rowid 把 FTS 命中映射回完整事实行。`bm25(evidence_fts, 5.0, 1.0)` 的两个权重对应 FTS 列顺序：title 权重 5，summary 权重 1。标题出现 query 词比摘要出现更重要，但最终 rank 仍只是 lexical 信号，不能和向量余弦相似度直接比较，所以 hybrid 使用名次融合而不是分数相加。

`ORDER BY (d.superseded_by IS NOT NULL)` 利用 SQLite 布尔表达式转成 0/1：未被替代的行先排，已 superseded 的行后排。`(d.source_path LIKE 'archive/%')` 同理，把 archive 记录放到活动记录后面。authority/provenance 的 CASE 使用 NULL-safe 分支，把 authoritative 放在普通 provenance 和 NULL 之前。最后才按 FTS rank 排序，说明“文档生命周期状态”优先于纯文本相关度。

为什么找到 `rows.length > 0` 就 `break`？因为宽松层不是额外候选通道，而是同一路径的兜底。如果 AND-all 已经有 20 条，继续 OR 会把大量低精度结果混进来，破坏精确查询。相反，如果 AND-all 返回 0，第二层仍没有结果，第三层才扩大召回。这个策略可以被描述为“级联召回”，而不是“多路融合”。

### R.7 lexical backfill：FTS 失败时的第二条语义

FTS5 的 tokenizer 对 CJK、带连字符的 anchor、版本号和短字符串不总是友好。例如 query `"F042"` 经过 tokenizer 可能被拆成两个 token，`"v1.2.3"` 的点号也可能改变边界。backfill 直接在 title、summary、keywords 三个文本列上做不区分大小写的 substring：

```sql
WHERE (
  LOWER(title) LIKE ? OR
  LOWER(COALESCE(summary, '')) LIKE ? OR
  LOWER(COALESCE(keywords, '')) LIKE ?
)
```

每个词生成一个 `%word%` pattern，`flatMap` 按 `[titlePattern, summaryPattern, keywordPattern]` 的顺序展平参数数组。`COALESCE(summary, '')` 把 SQL NULL 变成空字符串，防止 LIKE NULL 得到 UNKNOWN 而漏掉 OR 逻辑。多个词之间源码使用 OR，意味着任意一个词命中即可进入 backfill，再由 `rankLexicalBackfillRows` 在内存中计算更细的 signal。

backfill 的代价是可能缺少 SQL LIMIT。它被设计成“低频兜底”，而不是主召回；如果库中几十万行，contains 查询会扫描大量文档。面试官问优化方案，可提出三步：先把候选限制在最近更新时间或 kind 范围；再给 title/summary 建额外 trigram/前缀索引；最后把 signal 计算下推到 SQLite 视图或 FTS tokenizer，而不是直接删除 backfill。

`rankLexicalBackfillRows` 通常会记录每行的 keyword、title、summary 命中数，再由 comparator 按 signal、质量、exact anchor 排序。排序不是简单 `a.signal - b.signal`：JavaScript `sort` comparator 必须返回负数、0、正数，且最好保证在 signal 相同的时候有稳定 tie-breaker，否则同一 query 在不同运行中可能抖动。源码还用 `exactRow?.anchor` 保护精确行，避免 backfill 的内存排序把它挤到后面。

### R.8 Entity Registry：实体召回不是全文检索的替代品

实体通道先把 query 解析成 `QueryEntityMatch[]`，再用 `findMentionAnchors` 查“文档是否提到该实体”。一个实体匹配对象大致包含：规范化实体 ID、匹配 alias、匹配类型和置信度。随后 `hydrateEntityMentionDocs` 把 anchors 转回完整 `evidence_docs` 行：

```sql
SELECT * FROM evidence_docs
WHERE anchor IN (?, ?, ?)
  AND kind = ?
  AND updated_at >= ?;
```

`IN` 占位符数量由命中 anchors 动态生成，但值仍然是参数绑定。实体命中可能没有任何 query 词出现在 title/summary 中，例如用户问“那个负责向量回填的组件”，alias 解析到 `PassageVectorStore`，mention 表则能找出描述它的文档。结果合并时仍使用 `seenAnchors`，所以 entity 通道不会复制 exact/FTS 已有的 item。

在 raw 模式，Entity Registry 还有 passage 级 mention：它把实体命中的 `doc_anchor`、`passage_id` 和位置转成 `PassageResult`，再和 FTS/NN passage 合并。这样查询“F042”既可以找到文档摘要，也能找到实际包含该实体的消息片段。需要区分 `entityMatchesByAnchor` 和 `entityPassageHits`：前者用于 explain 的文档标签，后者用于 raw 的片段召回。

### R.9 为什么 raw 必须在 entity 合并之后分流

源码先完成 exact/FTS/backfill/entity 的文档候选，再判断 `options.depth === 'raw'`。这让 raw 有一个 `baseResults`：即使 passage FTS 或向量不可用，也可以回退到文档级结果。若在 entity 之前就进入 raw，实体 mention 只能查 passage，无法把“实体对应文档但没有 passage 记录”的老数据纳入回退。

```ts
if (options?.depth === 'raw') {
  const raw = await this.rawPassageSearch(...);
  return {
    items: this.enrichWithDrillDown(raw.items, undefined, options, trimmed),
    meta: raw.meta,
  };
}
```

`rawPassageSearch` 返回自己的 `meta`，因为 passage embedding 的健康状态和 doc embedding 不是同一个事实。doc-level semantic 挂掉时当前实现可能仍写 `degraded:false`，但 raw semantic 明确写 `degraded:true` 和 `effectiveMode:'lexical'`。这不是推测，而是两个分支的真实差异；面试时主动指出，能说明你读过实现而不是只背设计文档。

### R.10 mode 分流：lexical、semantic、hybrid 的共同出口

lexical 直接取 `results.slice(0, limit)`；semantic 先检查 `isEmbeddingAvailable()`，再调用 `semanticNNSearch`；hybrid 则把完整 lexical pool 传给 `hybridRRFSearch`。三个分支最后都调用 `attachEntityMatches` 和 `enrichWithDrillDown`，这是一个很有价值的结构：检索通道不同，但业务输出协议一致。

`slice(0, bm25Pool)` 是浅拷贝，不会修改原 `results`；`slice(0, limit)` 也不会改变候选池。hybrid 中 `f200Pool = consumptionRerank !== 'off' ? limit * 3 : limit` 说明消费排序开启时需要多留一些结果给后续 rerank。若把它误改成 `bm25Pool`，可能造成输出超过预期；若只传 `limit`，消费 prior 没有足够候选可排序。

semantic/hybrid 的 embedding 不可用和调用抛异常都回落 lexical。回落时 `items` 是已经经过 exact、FTS、backfill、entity 的 lexicalResults，因此不会因为向量服务故障而变成空数组。当前缺口是 meta 仍是 false；理想改法是把 `degraded:true`、`degradeReason:'embedding_unavailable'` 或 `'vector_search_error'` 写入这两个分支，并补测试确保客户端能显示真实状态。

### R.11 入口级面试追问清单

**问：为什么 `exactRow` 不直接 return？**

答：因为 exact 只保证文档锚点精确命中，raw 还要展开 passage，hybrid 还要合并向量，entity 和 drill-down 也要补充元数据。它是一个高优先级候选，不是整个执行计划。

**问：为什么 FTS 的 `LIMIT` 使用 `bm25Pool` 而不是 `limit`？**

答：hybrid 需要更宽的 lexical 名次列表做 RRF；raw 还要把多个 passage 聚合成 doc。只有 lexical 最终展示时才截到 limit。

**问：`COLLATE NOCASE` 能解决中文吗？**

答：它只解决 SQLite 的 ASCII 大小写比较，不负责中文分词，也不解决全角半角或 Unicode 规范化。中文召回依赖 FTS、LIKE backfill 和向量通道，不能把 NOCASE 说成 Unicode 全面归一化。

**问：为什么 catch 里不记录错误？**

答：当前实现选择 fail-open，但空 catch 会损失诊断能力。生产改造应至少记录结构化计数或 debug 日志，同时避免把 query 原文写入日志造成隐私泄露。

**问：`seenAnchors` 为什么是 Set 而不是数组 includes？**

答：多通道候选合并会频繁查重，Set 平均 O(1)，数组 includes 是 O(n)。候选池上限虽小，但 Set 语义也更清楚地表达“唯一 anchor 集合”。

### R.12 一次具体输入的变量流

假设调用：

```json
{
  "q": " F042 cache timeout ",
  "limit": 5,
  "scope": "threads",
  "mode": "hybrid",
  "depth": "summary",
  "dateTo": "2026-08-19"
}
```

执行顺序可以写成变量表：

|阶段|变量|值|作用|
|---|---|---|---|
|规范化|`trimmed`|`F042 cache timeout`|精确查询和 FTS 输入|
|宽池|`bm25Pool`|20|FTS lexical 候选最多 20|
|范围|`effectiveKind`|`thread`|threads scope 映射成 thread|
|时间|`dateTo`|`2026-08-19T23:59:59`|包含当天整日|
|回填词|`lexicalBackfillWords`|3 个|生成 9 个 LIKE 参数|
|实体|`queryEntityMatches`|依 registry 数据而定|补 mention docs|
|最终|`limit`|5|结果展示和 enrich 截断|

这张表里最容易被追问的是 `dateTo`。源码只在 FTS/backfill SQL 中把十字符日期追加 `T23:59:59`；exact-anchor SQL 片段本身没有 dateFrom/dateTo 条件。如果 exact anchor 同时传日期过滤，当前实现可能先把不在日期范围内的 exact 行加入结果。这是一个值得指出的边界：资格条件在不同通道不完全对称。修复方式是把日期条件抽成 `appendEvidenceFilters` helper，供 exact、FTS、LIKE、entity hydrate 共用，并补“exact + date filter”测试。

### R.13 查询参数的类型边界

`SearchOptions` 同时包含用户输入和内部控制项：`scope`、`kind`、`status`、`dateFrom`、`dateTo`、`keywords`、`threadId`、`provenanceTier`、`worldId`、`sceneId`、`mode`、`depth`、`contextWindow`。TypeScript 只在编译期保护调用方，HTTP JSON 进入系统后仍可能是数字、数组或恶意超长字符串。因此路由层需要运行时 schema：

```ts
const SearchOptionsSchema = z.object({
  limit: z.number().int().min(1).max(100).default(10),
  mode: z.enum(['lexical', 'semantic', 'hybrid']).default('lexical'),
  depth: z.enum(['summary', 'raw']).default('summary'),
  contextWindow: z.number().int().min(0).max(5).optional(),
}).passthrough();
```

这里的 `default` 只影响解析后的对象，不改变 `searchWithMeta` 内部默认值；两层默认值最好保持一致。`passthrough()` 是否使用要谨慎：它允许未知字段继续流入，但当前 store 会忽略未知字段，适合向后兼容；如果安全边界要求严格拒绝，应该改成 `strict()`。

`keywords` 目前作为 JSON 文本 LIKE 条件，传入值会被包成 `%%"kw"%%`。这不是 JSON path 查询，也无法保证 `kw='foo" OR ...'` 的语义正确，只保证 SQL 不注入。更严格的实现可以在写入时把 keywords 拆到 `evidence_doc_keywords(anchor, keyword)` 关联表，查询用参数化 `EXISTS`，但迁移成本和写入事务复杂度都会增加。

### R.14 `rowToItem`：数据库行到领域对象的转换风险

虽然入口代码只显示 `rowToItem` 调用，但这个 helper 是边界层。典型转换包括：`keywords ? JSON.parse(keywords) : []`、`source_ids ? JSON.parse(source_ids) : []`、`contradicts ? JSON.parse(contradicts) : []`，以及把整数标志转成 boolean、把可空列保留为 `undefined`。这里有两个面试要点。

第一，SQLite 没有原生 boolean，`0/1` 需要显式转换；如果直接把 `row.activation` 当 boolean，字符串或 NULL 会产生隐式真值。第二，JSON.parse 可能因为历史损坏数据抛异常。backfill 写入时常常 fail-open，但读取 helper 如果没有 try/catch，会让一条坏记录打断整个 search。稳妥写法是 `parseJsonArray(value, fallback=[])`，失败时记录 anchor 和列名，继续返回可用字段；对于安全敏感字段不能静默吞掉，应把 item 标成 `corrupt_metadata`。

### R.15 入口函数的复杂度和可维护性

`searchWithMeta` 很长并不意味着一定错误，但它承担了四类职责：规范化请求、构造 SQL、召回通道编排、结果 enrich。当前做法的优点是执行顺序一目了然，缺点是过滤条件重复、异常分支重复、meta 容易不一致。一个渐进式重构方案是：

1. 先提取纯函数 `deriveSearchContext(options)`，只返回 effectiveKind、exclude flags、threadAnchor、suppressBackstop。
2. 再提取 `appendDocFilters(sqlParts, params, context)`，统一添加 status/date/provenance/world/scene。
3. 保留 `searchWithMeta` 作为编排器，不把 SQL 逻辑藏进过度抽象的 ORM。
4. 为每个阶段保留独立测试：context、FTS SQL、backfill ranking、mode fallback、raw guard。

这样不会改变外部行为，也能直接修复 exact 和 FTS 过滤不一致的问题。面试官若问“为什么不直接上 ORM”，可以答：这里大量使用 FTS5 MATCH、bm25、external-content trigger 和 vec0，通用 ORM 往往无法表达或会隐藏参数顺序；保留 prepared SQL 更适合审计和性能调优。

### R.16 本卷小结

`searchWithMeta` 的本质不是“查表”，而是先构造一个稳定的 lexical 基线，再把 entity、vector、passage、authority 和 consumption 作为可插拔阶段叠加。你在面试中应能说清楚每个变量的生命周期：`trimmed` 控制输入边界，`bm25Pool` 控制召回宽度，`effectiveKind` 和一组 exclude 标志控制资格，`seenAnchors` 控制跨通道去重，`meta` 记录增强通道是否成功。最值得诚实指出的两个实现细节是：doc-level semantic/hybrid 的降级标志目前可能误报为 false；exact 分支的日期过滤与 FTS 分支不完全对称。能指出这种“代码真实行为和设计意图的差异”，比泛泛说“有降级机制”更能支撑追问。

## S. Raw 检索的真实实现：从消息片段回到可导航的 EvidenceItem

`depth='raw'` 很容易被误解成“同一次文档检索，多带一个 content 字段”。源码并不是这样实现的。summary 检索的粒度是 `evidence_docs` 中的一条 evidence；raw 检索的主召回粒度是 `evidence_passages` 中的一条消息或文本片段。两张表之间通过 `doc_anchor` 关联，最后仍把结果包装回 `EvidenceItem`，这样上层 MCP/API 不必为 raw 单独维护完全不同的返回协议。

### S.1 先读类型：`PassageResult` 是内部态，不是 API 契约

在 `SqliteEvidenceStore.ts` 开头，源码定义了类似下面的内部接口：

```ts
interface PassageResult {
  docAnchor: string;
  passageId: string;
  content: string;
  speaker?: string;
  position?: number;
  rank?: number;
  createdAt?: string;
  context?: PassageResult[];
  _semanticHit?: boolean;
  _alsoLexical?: boolean;
}
```

它和 `EvidenceItem['passages']` 很相似，但有三个关键差异。

1. `rank` 是内部排序数据。passage FTS 的 BM25 与 vec0 的 `distance` 都会暂时放进来，但不承诺直接暴露给客户端。
2. `_semanticHit`、`_alsoLexical` 前缀表示内部控制字段。它们是跨帖子污染守卫的证据，不是用户可见业务字段。
3. `context` 在内部保留 position、docAnchor 等定位信息；`toEvidencePassage` 转为 API 对象时，会额外解析 `threadId`、`messageId`。

这种“两套结构”不是重复造数据模型。内部态可以为了算法存标志，外部契约则只承诺稳定、安全、可解释的字段。面试官若问为什么不直接扩展 `EvidenceItem`，回答是：算法过程和 API 协议的变更频率不同，直接复用会把 `_semanticHit` 这类易变实现细节泄漏出去，也会让调用方错误依赖它。

### S.2 `rawPassageSearch` 的第一道边界：允许哪些 scope

函数一开始是一个很重要的短路：

```ts
if (options.scope && options.scope !== 'all' && options.scope !== 'threads') {
  return {
    items: attachEntityMatches(rankRawResults(baseResults, limit), entityMatchesByAnchor),
    meta: { degraded: false },
  };
}
```

含义是：当前 raw passage 的主路径只为没有 scope、`all`、`threads` 服务。如果请求的是 `docs`、`memory` 或 `sessions`，代码不去检索 `evidence_passages`，而是使用此前文档级 lexical 结果。这是明确的能力边界，不该在讲解时说成“所有 scope 都支持原始消息级语义检索”。

这段还展示了 JavaScript 的短路逻辑。`options.scope && ...` 在 scope 为 `undefined` 时立即停止，表达式为 falsy；scope 存在才继续比较。`rankRawResults(baseResults, limit)` 即便没有 passage，也会维持原始文档排名，然后 `attachEntityMatches` 恢复实体解释。也就是说它不是空结果降级，而是“该 scope 不支持 passage 基础设施时的文档级兼容模式”。`meta.degraded=false` 的含义在这里是“没有请求一个本模块声称可以执行但失败的通道”，而不是“返回了逐消息 raw 内容”。

若业务产品希望 session 也支持 raw，正确改造不是删除这个 if。必须先定义 session passage 如何写入、`doc_anchor` 如何命名、怎样防止 session/thread 数据互串、对应 FTS trigger 和 vec0 warmup 怎么跑，再扩展 scope 白名单。

### S.3 raw 的候选池与 closure：为什么 `lexical` 写成函数

```ts
const mode = options.mode ?? 'lexical';
const pool = Math.min(Math.max(limit * 4, 20), 100);
const timeFilter = { dateFrom: options.dateFrom, dateTo: options.dateTo };
const lexical = (): PassageResult[] => this.searchPassages(query, pool, timeFilter);
```

`pool` 与文档 hybrid 一样采用 `max(limit*4, 20)`、cap 100，但这是独立的 passage 宽池。一个文档可以含很多片段，若 raw 只拿 5 条 passage，再按 doc 聚合，可能最终只覆盖一个 thread；宽池能让更多 parent doc 有机会进入结果。

`lexical` 不是预先执行的数组，而是一个零参数 closure。closure 会捕获当前函数局部变量 `query`、`pool`、`timeFilter`，并在真正需要回退时调用。这有两个实际效果：lexical 模式只调一次；semantic 成功时不做不必要的 FTS；semantic/hybrid 异常时才调 lexical。若写成 `const lexical = this.searchPassages(...)`，embedding 成功时也会产生一次无用 SQL 查询。

`timeFilter` 是对象字面量，并通过属性简写写成 `{ dateFrom: options.dateFrom, dateTo: options.dateTo }`。这不等于深拷贝 options，但这里只包含两个 primitive string/undefined，后续不会修改它。把时间过滤独立传给 `searchPassages`，避免 passage 查询误把文档级的 kind/status 等字段拿去过滤不存在的表列。

### S.4 raw semantic/hybrid 的降级元数据比 doc 级更准确

raw 的分支结构可以压缩为：

```ts
let passages: PassageResult[];
let meta = { degraded: false };

if (mode === 'semantic') {
  if (!await isPassageEmbeddingAvailable()) {
    passages = lexical();
    meta = { degraded: true, degradeReason: 'passage_embedding_unavailable',
             effectiveMode: 'lexical' };
  } else {
    try { passages = await semanticPassageNNSearch(...); }
    catch {
      passages = lexical();
      meta = { degraded: true, degradeReason: 'passage_vector_search_error',
               effectiveMode: 'lexical' };
    }
  }
}
```

这里故意区分“不可用”和“搜索错误”。`isPassageEmbeddingAvailable()` 返回 false 的可能原因是 embed deps 缺失、模式不是 `on`、没有 `passageVectorStore`、reprobe 失败或服务 health 不 ready。进入 NN 后再抛异常，则可能是 HTTP embedding 调用超时、vec0 查询错误或数据维度异常。因此 API 的 `degradeReason` 分成 `passage_embedding_unavailable` 与 `passage_vector_search_error`，前端、监控和测试可以区分配置/服务故障与执行故障。

`effectiveMode:'lexical'` 也很必要。用户请求的是 semantic 或 hybrid，最终却使用 FTS，调用方不能只根据原 request 推测真实行为。相比之下，前面已指出 doc-level semantic/hybrid fallback 仍返回 `degraded:false`，正是本模块在观测协议上的不一致。面试时正确表述是：“passage 路径实现了明确的降级 meta；doc 路径的同类行为尚有缺口”，而不是笼统说“系统总能准确上报降级”。

### S.5 `searchPassages` 与 passage FTS：片段表为什么不直接按 content LIKE

passage 的写入表结构包含：

```sql
CREATE TABLE evidence_passages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  doc_anchor TEXT NOT NULL,
  passage_id TEXT NOT NULL,
  content TEXT NOT NULL,
  speaker TEXT,
  position INTEGER,
  created_at TEXT NOT NULL,
  UNIQUE(doc_anchor, passage_id)
);
```

而检索表是 FTS5 external-content 表：

```sql
CREATE VIRTUAL TABLE passage_fts USING fts5(
  content,
  content=evidence_passages,
  content_rowid=rowid,
  tokenize="unicode61 tokenchars '_-'"
);
```

`content=evidence_passages` 表示 FTS 不把 content 当作另一个业务真相来源；业务事实仍在普通表，FTS 保存倒排索引。`content_rowid=rowid` 指定它们通过哪一列对应。`tokenchars '_-'` 尤其值得解释：默认 unicode61 会把 `_` 和 `-` 作为分界符，而 message ID、feature anchor、代码标识符经常包含这两个字符；把它们纳入 token 字符可以减少 `ADR-005`、`foo_bar` 一类检索被拆坏的概率。这不代表所有分词问题都消失，CJK 和无空格混合文本仍需要 backfill/embedding 兜底。

三个 trigger 负责同步：INSERT 时把新 rowid/content 放入 `passage_fts`；DELETE 时用 FTS5 特殊的 `'delete'` command 删除旧内容；UPDATE 时先 delete old，再 insert new。外部 content FTS 的更新不能只更新普通表然后假定索引自动刷新，否则 FTS 会出现幽灵命中或漏召回。

查询时 `searchPassages` 以 `passage_fts MATCH ?` 作为主谓词，JOIN 回 `evidence_passages` 取 speaker/position/created_at。它根据传入日期追加 `created_at >= ?`、`created_at <= ?`，`dateTo` 为十字符日期时同样转换到当日 `T23:59:59`。这让 raw 的时间边界按消息发生时间工作，而不是按 thread digest 的更新时间工作；两者语义不同，不能混用。

### S.6 Passage vec0 的 key 为什么是 JSON 数组

`PassageVectorStore` 用这个 helper 构建 key：

```ts
function passageVectorKey(docAnchor: string, passageId: string): string {
  return JSON.stringify([docAnchor, passageId]);
}
```

很多人会随手写 `docAnchor + ':' + passageId`。源码选择 JSON 数组有实际好处：两段字符串不需要自定义转义规则。若 anchor 或 message ID 理论上含有冒号，拼接方案要证明分隔符永不冲突；JSON 数组 `["thread-a:b","msg-c:d"]` 可逆，`parsePassageVectorKey` 还能运行时校验“确实是长度为 2 的 string 数组”。这是把持久化 key 当作小型序列化协议，而不是临时字符串。

```ts
const parsed = JSON.parse(key) as unknown;
if (!Array.isArray(parsed) || parsed.length !== 2 ||
    typeof parsed[0] !== 'string' || typeof parsed[1] !== 'string') {
  throw new Error('Invalid passage vector key');
}
```

这里的 `as unknown` 很细：JSON.parse 的返回类型在 TypeScript 中无法自动被信任，先退到 `unknown`，再通过 `Array.isArray`、length、`typeof` 窄化后，才作为 `{ docAnchor, passageId }` 返回。没有这些 guard，坏向量行可能导致 `parsed[0]` 访问错误，甚至把对象/数字转换成错误 SQL 参数。

### S.7 vec0 CRUD：为什么 upsert 是 delete 加 insert

`VectorStore` 和 `PassageVectorStore` 都写成：

```ts
db.prepare('DELETE FROM passage_vectors WHERE passage_key = ?').run(key);
db.prepare('INSERT INTO passage_vectors (passage_key, embedding) VALUES (?, ?)').run(key, vector);
```

原因不是作者不知道 SQLite 的 `INSERT ... ON CONFLICT DO UPDATE`，而是 sqlite-vec 的 `vec0` 虚拟表不支持这类标准 upsert。因此先删后插，模拟替换语义。`embedding` 是 `Float32Array`，better-sqlite3 将其作为 binary buffer 传给扩展；不应把浮点数组 JSON.stringify 后存进去，否则 vec0 无法进行 `MATCH` 最近邻计算。

delete+insert 有一个一致性窗口：若两句 SQL 不在同一事务中，进程在 DELETE 和 INSERT 之间崩溃，会留下没有向量的事实行。设计上这可接受，因为 lexical 是基线，重建/warmup 能补齐向量；但如果需要“文档和向量绝对原子”，就要把写入包入同一 SQLite transaction，或使用待处理状态。还要注意多写者并发时两个请求可以相互交叉，所以外层 store 使用单写者队列。不要把 vec0 的限制误讲成“SQLite 不支持 upsert”。

搜索 SQL 则是：

```sql
SELECT passage_key AS passageKey, distance
FROM passage_vectors
WHERE embedding MATCH ? AND k = ?;
```

`MATCH ?` 的参数是 query 的 Float32Array，`k=?` 限制近邻数量。distance 越小越近，但源码没有把它直接和 BM25 加法融合，而是在语义列表中保留它的排名，再由 RRF 用 rank 合并。这避免不同 embedding 模型、不同向量归一化方式导致 distance 尺度不可比。

### S.8 `semanticPassageNNSearch`：先 embed，后查 vec0，再 hydrate

```ts
const queryVec = await embedding.embed([query]);
const hits = passageVectorStore.search(queryVec[0], limit);
return hydratePassageVectorHits(hits, options);
```

`embed([query])` 即使只嵌入一句话，也遵守 service 的 batch API，因此返回 `Float32Array[]`；源码用第一个元素 `queryVec[0]`。非空 query 已在 `searchWithMeta` 入口保证，服务正常时数组应含一项；更防御性的实现可以在这里验证 `queryVec.length === 1`，并把违反协议归入 vector error。

vec0 命中只带 `passageKey` 和 `distance`，没有 content、speaker、created_at。`hydratePassageVectorHits` 先逐个 parse key，单个坏 key catch 后返回 null，过滤掉坏行而不中断整个列表。之后它动态产生：

```sql
WHERE (doc_anchor = ? AND passage_id = ?)
   OR (doc_anchor = ? AND passage_id = ?)
```

这不是 N+1：所有 hit 一次查询取回。参数由 `flatMap(p => [p.docAnchor, p.passageId])` 构造，顺序严格匹配每个二元 AND 子句。若指定 `threadId`，还会追加 `AND doc_anchor = ?`；dateFrom/dateTo 也是在 hydrate 时过滤。这一点非常重要：向量索引本身只负责相似度，权限、scope、时间等资格条件仍由事实表 SQL 强制执行。

数据库返回的 `rows` 没保证和 NN hit 同序。源码建立 `rowMap`，然后按照 `parsed` 原列表的顺序重新组装 PassageResult。这保持 vec0 近邻顺序，避免 SQL `IN/OR` 返回顺序不确定改变 semantic 排名。`_semanticHit: true` 在此处打上，是后面防 cross-post 的原始证据。

### S.9 passage hybrid RRF：如何标记“既词法又语义”

passage RRF 与 doc RRF 同样用 `rrfK=60`，但它的关键在于合并重复 key：

```ts
for (let i = 0; i < lexical.length; i++) {
  const key = passageVectorKey(p.docAnchor, p.passageId);
  scores.set(key, (scores.get(key) ?? 0) + 1 / (60 + i));
  passageMap.set(key, p);
}
for (let i = 0; i < semantic.length; i++) {
  const existing = passageMap.get(key);
  if (existing && !existing._semanticHit) {
    p._alsoLexical = true;
  }
  scores.set(key, (scores.get(key) ?? 0) + nnWeight / (60 + i));
  passageMap.set(key, p);
}
```

这段看起来有点反直觉：词法先放入 map，语义 hit 后替换 map 中的对象；如果此前存在 lexical 对象，便在新的 semantic 对象上设置 `_alsoLexical=true`。最终 `passageMap` 保存的是语义对象，既有 `_semanticHit=true`，又有 `_alsoLexical=true`。如果只在 lexical 对象上打标，随后 map 被 semantic 对象覆盖，守卫就看不到“同时命中”的事实。

中文/日文/韩文 query 检测由 Unicode property escape 实现：

```ts
/[\p{Script=Han}\p{Script=Hiragana}\p{Script=Katakana}\p{Script=Hangul}]/u
```

`u` flag 必不可少，否则 `\p{Script=...}` 不按 Unicode 属性解析。检测到时 `nnWeight=1.5`，否则为 1。它只放大 NN RRF 贡献，不修改 lexical BM25，也不是“中文模型更准确”的结论；设计假设是 unicode61/BM25 对 CJK 的词法召回较弱，所以融合时避免 lexical 过度压制语义。

### S.10 BUG-UX-14：为什么 raw 需要防 cross-post 污染

设有两个 thread：

```text
thread-A：真正讨论 F042 cache timeout
thread-B：有人转发“看看 thread-A 的 F042 cache timeout”
```

对 `scope='threads'` 的 raw 查询，passage FTS 在全量 `evidence_passages` 中搜索。thread-B 也含 literal 词，因此被找到；如果直接按 parent doc hydrate，用户会看到 B 并点进错误会话，产品上表现为“teleport 到错误 thread”。这不是普通重复，而是范围语义被破坏。

`hydratePassageResults` 先把 passage 按 anchor 分组：

```ts
const passagesByAnchor = new Map<string, PassageResult[]>();
for (const passage of withContext) {
  if (threadAnchor && passage.docAnchor !== threadAnchor) continue;
  const arr = passagesByAnchor.get(passage.docAnchor) ?? [];
  arr.push(passage);
  passagesByAnchor.set(passage.docAnchor, arr);
}
```

显式 threadId 是最强范围限制：不属于目标 anchor 的 passage 在这里直接 `continue`。没有 threadId 时，guard 同时看三个条件：`scopeActive`、`hasExplicitThreadTarget`、`hasDocLevelResults`。只有“scope 活跃、没有显式目标、文档级已经找到主结果”时，才对新增 passage-only parent 施加更严格策略。

```ts
const hasPurelySemanticDiscovery = pList.some(
  p => p._semanticHit && !p._alsoLexical
);
if (!hasPurelySemanticDiscovery) continue;
```

逻辑的推理链是：cross-post 通常共享 literal query，所以会同时出现在 lexical 与 NN 列表，最终带 `_semanticHit && _alsoLexical`；真正语义相关但不含 literal 的新发现，只通过 NN 命中，带 `_semanticHit && !_alsoLexical`。当已经有文档级主结果时，前者更像污染，应阻止新增 parent；后者可能是有价值的扩展，允许进入。

这是启发式，不是数学证明。cross-post 也可能只出现在 lexical 或只出现在 NN；真相关 passage 也可能含 literal 词。代码通过“有 doc 主结果才严守卫”“没有 doc 主结果时让 passage 成为唯一发现通道”折中 recall 与 precision。面试时说清这是 guard heuristic，会比声称它“彻底解决重复内容”更专业。

### S.11 第二道 scope 防线：检查 parent doc kind

当 passage-only anchor 被允许尝试 hydrate 时，源码还会查询 parent：

```sql
SELECT * FROM evidence_docs WHERE anchor = ?;
```

再按 scope 复算：

```ts
if (scope === 'threads' && parentDoc.kind !== 'thread') continue;
if (scope === 'sessions' && parentDoc.kind !== 'session') continue;
if (scope is docs/memory && parent kind is session/thread) continue;
```

这解决另一类泄漏：即使 doc-level 初始结果为空、cross-post guard 被放宽，session 的 passage 也不能混进 threads scope。它属于“先发现、再授权”的 pattern：passage FTS/vec0 召回可广，最终对业务事实行再施加范围约束。

当前实现对每个新的 anchor 都单独 SELECT parentDoc，候选上限为 100，所以最坏会有 N+1。更好的实现是先收集待 hydrate anchors，再用 `IN (?,...)` 一次取回，建立 Map；这和 `hydratePassageVectorHits` 已经采用的模式一致。优化时必须保留原有 anchor-to-passage 分组、scope guard 和结果排序，否则仅减少 SQL 数量就可能改变行为。

首次将 parent doc 加入结果时，源码构造一个后备摘要：

```ts
item.summary = '[passage match] ' + (speaker ? speaker + ': ' : '') + content.slice(0, 200);
```

`slice(0, 200)` 保护列表页不被超长消息撑爆；它按 JavaScript UTF-16 code unit 截断，极少数情况下可能切到 surrogate pair 中间。若要求 Unicode grapheme 安全，应该使用 `Intl.Segmenter` 或按 code point 截取。前缀 `[passage match]` 是解释性标志，让用户知道文档摘要不是原有 summary，而是由命中片段临时生成。

### S.12 `contextWindow`：position 是邻近关系的真正键

`attachPassageContext` 的 SQL 是：

```sql
SELECT doc_anchor, passage_id, content, speaker, position, created_at
FROM evidence_passages
WHERE doc_anchor = ?
  AND position BETWEEN ? AND ?
  AND passage_id != ?
ORDER BY position;
```

给命中 passage 位置 10、`contextWindow=2` 时，参数为 `(anchor, 8, 12, currentPassageId)`。它返回同一 doc 内 position 8 到 12 的其他消息，不会跨 thread，也排除自己。`BETWEEN` 在 SQL 中是闭区间，所以 8 与 12 都包括。`ORDER BY position` 让上下文按会话顺序展示，而不是按 rowid 或 FTS rank。

源码先判断 `!contextWindow || contextWindow <= 0 || !db`。因此 0、undefined、负数都不查上下文；这再次说明 HTTP schema 应限制它为非负整数。若 passage 的 `position == null`，单条也跳过，因为无法定义邻居。上下文查询对每一个命中 passage 单独执行，因此也是一个小 N+1；默认 raw topK 很小，成本受控。若要批量化，SQL 需同时按多个 `(doc_anchor, lower, upper)` 窗口查，并在内存中拆回各 passage，复杂度更高。

IndexBuilder 的实时 Redis 来源用从 0 开始的位置；转录 JSONL backfill 则从 10000 起步，避免两种来源 position 冲突。这一设计让相邻窗口在各自来源内仍成立，但若同一 thread 混合两套来源，10000 的间隔意味着它们不会互相成为邻居。要把它讲准确：它是冲突规避策略，不是完整的跨来源时间排序方案。

### S.13 `toEvidencePassage` 与 raw 最终排序

`toEvidencePassage` 先验证前缀，再用 `slice('thread-'.length)`、`slice('msg-'.length)` 解析 navigation ID。对不符合 naming convention 的历史数据，函数返回 undefined 而不是错误猜测。条件对象展开 `...(passage.context ? { context: ... } : {})` 使没有 context 时字段根本不存在，而不是 `context: undefined`；JSON 响应更简洁，调用方也能区分未附带窗口。

`rankRawResults` 的优先级是：有 `passages` 的 item 在没有 passages 的文档 fallback 前；都有 passages 时按每个 anchor 第一次命中的 passage 顺序排；都没有时保留 baseResults 原始顺序；最后才 `slice(0, limit)`。它用 `Number.MAX_SAFE_INTEGER` 作为没有排序证据时的尾部兜底，不是人为给文档一个“极大相关性分”。

随后 `enrichWithDrillDown` 为带 threadId/messageId 的 primary passage 生成 `cat_cafe_get_thread_context` 坐标，通常请求前后各 3 条消息。thread 只能解析出 threadId 时退到整个 thread；session 用摘要工具；文件 evidence 用 `read_file_slice`。检索结果因此保持短小，原始上下文由下一次工具调用按需取。

生成文件 drill-down 前还会用 `resolve` 和 `relative` 检查路径没有逃出 sourceRoot。索引数据中即使混入 `../../secret.txt`，relative 结果为 `..` 开头会返回 null，不生成危险路径。实际读取工具仍必须再次做相同校验，不能只依赖这层 hint 过滤。

### S.14 raw 路径的测试与面试复述

raw 的测试不应只断言“有一条结果”，至少要覆盖这些输入输出关系：词法 raw 能把命中 message 附到正确 parent；date filter 只保留时间范围内 passage；`contextWindow=1` 在 position 0/1/2 中为命中 1 提供 0 和 2；显式 threadId 排除其他 thread；已有文档主结果时 cross-post 不新增；纯 NN 且非 lexical 的 passage 可以成为语义发现；embedding unavailable 时 FTS 结果仍返回且 meta 是 degraded true；vec0 中坏 JSON key 只丢弃坏行而不打断其他结果。

可用下面一段回答面试官：“Clowder 的 raw 不是把 summary 原文展开，而是切换到 `evidence_passages`。先由 passage FTS 或 passage vec0 召回消息级 candidate，向量 key 用可逆 JSON 二元组保存 docAnchor 和 passageId；再批量 hydrate 出 content、speaker、position，依据 doc_anchor 合并回 EvidenceItem。`contextWindow` 用同 doc 的 position 范围查询附近消息。由于 passage 检索初始范围较宽，系统额外用显式 threadId、parent kind 验证，以及 `_semanticHit/_alsoLexical` 启发式阻止 cross-post 把用户带进错误会话。embedding 服务不可用时 raw 会回退到 passage FTS，并在 SearchExecutionMeta 明确标出 degraded 和 effectiveMode。”

## T. 写入链：IndexBuilder 如何把文件、thread 和 transcript 变成可检索事实

查询代码解决“从索引找什么”，写入代码解决“索引里究竟有什么”。如果只会讲 FTS 和向量，不会讲写入事务、sourceHash、anchor ownership 和 passage upsert，面试官很容易继续追问：“你怎么保证重建不会重复？文件删掉后怎么办？旧向量会不会指向新内容？”

### T.1 `EvidenceItem` 是事实层，不是搜索结果临时对象

`interfaces.ts` 中的 `EvidenceItem` 字段很多，但可按职责分成五组：

|字段组|典型字段|作用|
|---|---|---|
|身份|`anchor`、`kind`|唯一定位和类型分流|
|内容|`title`、`summary`、`keywords`|FTS/backfill 的 lexical 输入|
|来源|`sourcePath`、`sourceHash`、`materializedFrom`|增量扫描、溯源和 drill-down|
|资格|`status`、`provenance`、`authority`、`activation`|查询过滤、权威排序、backstop 控制|
|关系|`sourceIds`、`summaryOfAnchor`、`contradicts`、`supersededBy`|压缩、矛盾、替代和 explain|

内容字段和资格字段分离。title/summary 只回答“文本是否相关”；authority/status/provenance 回答“即使相关，是否有资格展示”。因此 FTS SQL 先取文本命中，再附加 `status`、`kind`、`world_id` 等 WHERE 条件，而不是把 authority 当成全文内容一起嵌入。

### T.2 sourceHash 与 indexing version：增量索引的两个闸门

IndexBuilder 对文件文档通常会计算规范化内容的哈希，并与已有 row 比较：

```ts
const sourceHash = sha256(normalizedContent);
const existing = store.getByAnchor(anchor);
if (!force && existing?.sourceHash === sourceHash &&
    existing?.indexingVersion === INDEXING_VERSION) {
  skipped += 1;
  continue;
}
```

`sourceHash` 解决“文件内容有没有变”；`INDEXING_VERSION` 解决“代码逻辑有没有变”。例如 scanner 以前把 frontmatter keywords 解析错了，即便文件字节完全没变，提升 `INDEXING_VERSION` 后也必须重建。只比较 hash 会把旧错误永久保留；只比较 version 又会让每次启动都重新处理所有文档。

哈希的输入必须先规范化。换行符 CRLF/LF、末尾空格、frontmatter 字段顺序如果不统一，会造成无意义重建；但规范化不能删掉真正影响搜索的内容。若 hash 计算的是原始文件，改行尾也会触发索引；若计算的是 parser 产物，parser bug 需要依赖 version 兜底。

`INDEXING_VERSION` 是代码常量而非数据库自动推导值，提升它必须伴随发布说明和重建策略。生产环境可以在 `index_state` 记录 fingerprint，启动时把旧版本标成 `stale`，让后台任务可恢复执行，而不是在 API 请求线程同步重建。

### T.3 anchor ownership：为什么增量更新不只看 changedPaths

文件路径变化有新增、内容修改、删除或移动。对修改，sourceHash 足够；对删除，磁盘上已经没有文件，scanner 反而无法生成原 anchor。IndexBuilder 因此维护 source path 到 anchor 的 ownership 关系，或者在重建阶段比较“本次扫描得到的所有 anchors”和“库中属于该 sourceRoot 的旧 anchors”：

```text
oldOwned = SELECT anchor FROM evidence_docs WHERE source_path LIKE rootPrefix
newOwned = anchors produced by current scan
stale = oldOwned - newOwned
DELETE stale docs and matching vectors
```

这就是为什么 `removeBySourcePrefix` 要先查 anchors，再分块清理向量。直接只删 `evidence_docs` 会留下 vec0 中无法 hydrate 的孤儿向量；查询 semantic 时这些向量会被 docMap 过滤掉，但它们仍占空间，且会影响 vec0 的 top-k 名次。

### T.4 upsert 的长参数：如何读懂 evidence_docs 写入

`SqliteEvidenceStore.upsert` 使用一条 `INSERT OR REPLACE INTO evidence_docs (...) VALUES (?,...)`。读这类代码最有效的方法是建立列名到表达式的对照：

```ts
stmt.run(
  item.anchor,
  item.kind,
  item.status,
  item.title,
  item.summary ?? null,
  item.keywords ? JSON.stringify(item.keywords) : null,
  item.sourcePath ?? null,
  item.sourceHash ?? null,
  item.supersededBy ?? null,
  item.materializedFrom ?? null,
  item.updatedAt,
  item.packId ?? null,
  item.provenance?.tier ?? null,
  item.provenance?.source ?? null,
  item.generalizable == null ? null : item.generalizable ? 1 : 0,
  item.authority ?? pathToAuthority(item.sourcePath ?? item.anchor),
  item.activation ?? 'query',
  item.verifiedAt ?? null,
  item.sourceIds ? JSON.stringify(item.sourceIds) : null,
  item.summaryOfAnchor ?? null,
  item.compressionRationale ?? null,
  item.contradicts ? JSON.stringify(item.contradicts) : null,
  item.invalidAt ?? null,
  item.reviewCycleDays ?? null,
  item.worldId ?? null,
  item.sceneId ?? null,
  item.firstIndexedAt ?? Date.now(),
);
```

三类转换最值得讲。第一，SQLite NULL 与 TypeScript `undefined` 不一样，源码显式用 `?? null`。第二，数组列用 JSON 文本保存，读取时再 parse；这牺牲了关系查询便利，换来单行 hydrate 简洁。第三，boolean 用 `1/0/null` 三态保存：`generalizable` 的 null 表示未标记，false 是明确不可泛化，true 才是候选。若直接 `item.generalizable ? 1 : 0`，null 会错误变成 0。

`pathToAuthority(item.sourcePath ?? item.anchor)` 是默认 authority 推导。显式 `item.authority` 优先，缺失才从路径/anchor 推导；authority 不是由查询相关度动态猜的，查询阶段的 boost 只能重排，不能把 observed 文档凭空升级成 constitutional。

### T.5 INSERT OR REPLACE 的真实语义与风险

SQLite 的 `INSERT OR REPLACE` 不是标准 SQL 意义上的 update。发生主键冲突时，它可能先 DELETE 旧行，再 INSERT 新行，从而产生新的 rowid。因为 evidence_fts 是 external-content 且由 DELETE/INSERT trigger 同步，通常能保持索引一致，但如果还有外键或依赖 rowid 的外部表，REPLACE 的删除再插入语义要谨慎。

更精确的更新可以使用 `INSERT ... ON CONFLICT(anchor) DO UPDATE SET ...`，保留 rowid；但长列清单更难维护，且 SQLite/扩展兼容性要验证。当前实现选择 REPLACE 的工程理由是写入路径短、幂等直观，FTS trigger 已覆盖 delete+insert。面试时不要说“REPLACE 就是 UPDATE”，应明确它是冲突时的替换语义。

### T.6 单写者队列：为什么 async 代码还需要串行化

`SqliteEvidenceStore` 使用 `writeQueue.enqueue(() => ...)` 包装 upsert、delete、refresh mentions 等写操作。SQLite WAL 允许读写并行，但同一连接上的多个写事务仍可能出现 `SQLITE_BUSY`、触发器交错、向量和事实行短暂不一致。单写者队列把异步调用排成：

```text
write A: docs + FTS trigger + vector delete
write B: docs + FTS trigger + vector delete
write C: entity mention refresh
```

典型 tail 链可以写成：

```ts
const run = tail.then(task, task);
tail = run.then(() => undefined, () => undefined);
return run;
```

第二个 then 的 rejection handler 保证前一个任务失败后队列仍能继续，不会让 tail 永远处于 rejected 状态。若写成 `tail = tail.then(task)`，一次失败可能导致后续所有写入都跳过。这里要把 Promise 的错误传播与队列可继续性分开设计。

### T.7 事务边界：事实、FTS、mention 和向量不是一个原子单位

文档 facts 和 FTS trigger 在同一个 SQLite transaction 内；embedding HTTP 调用则不应被包进长事务。常见顺序是：

1. scanner 解析文件，计算 EvidenceItem；
2. transaction 写入 evidence_docs，触发 evidence_fts；
3. transaction 内删除发生变化文档的旧 vector；
4. 提交本地事务；
5. 后台调用 embedding service，成功后插入新 vector；
6. refresh entity mentions。

这样 embedding 服务挂掉不会阻塞事实索引，lexical 仍可用。代价是短时间内出现“文档已更新、向量尚未更新”的 eventual consistency。若 embedding 成功后进程崩溃，warmup 会依据 hash/version 或 vector 缺失再次补齐。

SQLite transaction 原子只覆盖本地数据库操作，不等于外部 HTTP embedding 也原子。面试时明确区分这两层，避免把它描述成分布式事务。

### T.8 passage upsert：唯一键与向量删除的协同

实时 thread 消息写入大致是：

```sql
INSERT INTO evidence_passages
  (doc_anchor, passage_id, content, speaker, position, created_at)
VALUES (?, ?, ?, ?, ?, ?)
ON CONFLICT(doc_anchor, passage_id) DO UPDATE SET
  content = excluded.content,
  speaker = excluded.speaker,
  position = excluded.position,
  created_at = excluded.created_at;
```

唯一键 `(doc_anchor, passage_id)` 让同一消息重复到达时幂等。若 `changes > 0`，IndexBuilder 删除对应 passage vector：

```ts
if (result.changes > 0) {
  passageVectorStore?.delete(passageVectorKey(docAnchor, passageId));
}
```

内容更新要删 vector，因为旧向量仍代表旧文本。删除后新 passage 可以被 FTS 立即检索；warmup 再按新 content 生成 embedding。若不删，semantic query 会把旧语义当成新消息返回，造成比“暂时没有向量”更隐蔽的错误。

### T.9 transcript JSONL backfill：为什么按 invocationId 聚合

`backfillPassagesFromTranscript(threadId)` 读取 `threads/<threadId>/<catId>/sessions/<sessionId>/events.jsonl`，逐行 `JSON.parse`，只接受 `evt.event?.type === 'text'` 且 content 是 string。多个事件可能是同一 invocation 的分片，因此用 Map：

```ts
const invocationTexts = new Map();
const invId = evt.invocationId ?? (sessionId + '-noninv');
const previous = invocationTexts.get(invId);
if (previous) previous.text += evt.event.content;
else invocationTexts.set(invId, {
  text: evt.event.content,
  catId: evt.catId ?? catId,
  t: evt.t,
});
```

Map 的 key 让聚合复杂度接近 O(event lines)，而不是每次线性寻找同 invocation。没有 invocationId 的事件归到 session 级 fallback key，避免每行变成独立 passage。坏 JSON 行被 catch 后跳过，说明历史 transcript backfill 是 fail-open：一行损坏不应阻塞整个 thread。

插入时 `position` 从 10000 开始递增，和 Redis 实时消息的 0-based position 分区。`passage_id` 由 invocation/session 组合构成，保证重复 backfill `INSERT OR IGNORE` 不会重复。返回的 `added` 只统计 `changes > 0`，调用方可以把它作为进度指标。

### T.10 删除策略与写入链复述

transcript backfill 不会因为本次文件缺少某个旧 invocation 就立刻删除数据库 passage：文件可能只是分段日志、读取过程中被截断，或旧事件仍是用户可追溯历史。这是一种 append-friendly 策略，代价是真正删除的消息不会自动消失；生产上需要 tombstone 或 generation-based GC。

可以这样总结：“scanner 把外部文件或 thread 消息规范化成 EvidenceItem，anchor 是业务唯一键，sourceHash 判断内容变化，INDEXING_VERSION 判断解析代码变化。文档写入 evidence_docs，FTS trigger 维护 evidence_fts；消息按 (doc_anchor, passage_id) 幂等写入 evidence_passages，内容更新先删旧 passage vector。写操作经单写者队列，事务只覆盖本地事实和触发器，不把外部 embedding HTTP 拖进事务。JSONL backfill 按 invocationId 合并分片、坏行跳过、position 从 10000 起步；缺失 passage 默认保留，真正删除需 tombstone/GC。”

## U. EmbeddingService 与向量层：外部 HTTP、健康探测和模型一致性

向量检索不是把模型塞进 API 进程里同步计算。源码中的 `EmbeddingService` 是 HTTP client，真正的模型运行在独立的 embed-api Python 进程。这样做的工程含义是：API 进程不承担 GPU 显存和 ONNX runtime 崩溃风险，embedding 服务可以单独升级、换端口、重启；代价是网络超时、服务发现和数据版本一致性都要显式处理。

### U.1 `resolveBaseUrl`：端点不是一个永远固定的环境变量

服务地址解析会优先读取 service manifest 和持久化 services 配置，环境变量仍可作为 endpointEnvVars/portFallback：

```ts
const service = getServiceManifest('embedding-model');
if (!service) {
  const port = process.env.EMBED_PORT ?? '9880';
  return normalizeLoopbackUrl(process.env.EMBED_URL ?? 'http://127.0.0.1:' + port);
}
const resolved = resolveServiceEndpoint(service, process.env, getServiceConfig('embedding-model'));
return normalizeLoopbackUrl(resolved ?? 'http://127.0.0.1:9880');
```

`?? '9880'` 只在变量不存在时默认，不会把空字符串修成端口；配置层需要另做校验。`normalizeLoopbackUrl` 负责统一 loopback 表达，避免 localhost、127.0.0.1 和尾斜杠组合造成 allowlist 或 URL 拼接差异。URL 解析不应由字符串拼接随意代替，尤其是用户可配置端口时。

动态 resolve 的目的在于 install/reconfigure 改端口后 API 不必重启。面试官若问“这会不会每次请求读磁盘”，应结合实现说明 service config 的缓存/读取机制；核心设计是端点解析在请求时可以看到新配置，而不是把 URL 在构造函数里永久冻结。

### U.2 /health 探测和 `ready` 状态

`load()` 通过固定的 health URL 和 `AbortSignal.timeout(5000)` 判断服务是否可用：

```ts
const res = await fetch(baseUrl + '/health', {
  signal: AbortSignal.timeout(5000),
});
if (!res.ok) throw new Error('Health check failed');
const health = await res.json();
if (health.status === 'ok') {
  this.ready = true;
  this.modelId = health.model || this.config.embedModel;
}
```

HTTP 非 2xx 直接抛错；JSON 中 status 不是 ok 也不会把 ready 设为 true。catch 后 `ready=false`，不把服务失败传播为初始化致命错误。health 使用固定较短探测，真正推理使用配置的 `embedTimeoutMs`；若只设置推理 timeout，启动时服务未运行会长时间卡住。

`reprobeIfNeeded()` 有 30 秒冷却：

```ts
if (ready) return;
if (Date.now() - lastProbeAt < 30_000) return;
lastProbeAt = Date.now();
await load();
```

这避免每一条 semantic query 都向挂掉的服务发 health 请求。它不是断路器：服务在冷却窗口内恢复，第一次查询仍可能等到下一次 reprobe；若需要更快恢复，可缩短 cooldown，但会增加探测压力。

### U.3 batch size 64：客户端为什么还要拆批

服务端 MAX_BATCH_SIZE 是 64，`embed(texts)` 先创建与输入等长的 results，再按 offset 切片：

```ts
for (let offset = 0; offset < texts.length; offset += 64) {
  const batch = texts.slice(offset, offset + 64);
  const vectors = await embedBatch(batch);
  for (let i = 0; i < vectors.length; i++) {
    results[offset + i] = vectors[i]!;
  }
}
```

`results` 预先按输入长度分配，输出顺序与输入索引保持一致。`embedBatch` 收到服务响应后按 `data.index` 排序，这一步不能省：HTTP 服务或 Python batch 实现未必保证 JSON 数组顺序。若 65 条文本不拆批，服务会返回 400；若拆批但没有按 index 放回，文档向量会与错误文本错位，属于静默灾难。

空输入提前返回 `[]`，避免请求服务。`!` 非空断言只告诉 TypeScript“我认为 vectors[i] 存在”，不会在运行时生成值；如果服务少返回一条，数组对应位置仍可能是 undefined，生产代码可增加长度校验并抛出协议错误。

### U.4 维度处理：截断/补零是兼容策略，不是无损

服务 response 的 embedding 可能与配置 `embedDim` 不完全一致。源码分配 `new Float32Array(targetDim)`，循环到 `Math.min(emb.length, targetDim)`，短了自然补 0，长了截断。

这让旧模型或测试 mock 不会因为维度差异立即崩溃，但有明显语义风险：截断改变向量方向，补零也会降低相似度。真正上线时应在 health 和 `embedding_meta` 校验 model/dim，不应长期依赖自动修剪掩盖模型配置错误。面试官追问“为什么不直接 throw”，可以回答：当前代码选择 fail-open 保持 lexical 可用；但向量写入前应监控修剪次数，并在模型切换时清空旧 vector。

### U.5 VectorStore 元数据：向量表和模型版本必须配套

`VectorStore.initMeta(info)` 用 transaction 写入：

```sql
INSERT OR REPLACE INTO embedding_meta(key, value)
VALUES ('embedding_model_id', ?);
```

同时写 model revision 和 dimension。`checkMetaConsistency(current)` 读取 Map 后依次检查 model id、dim；没有历史 meta 时返回 `{ consistent: true, reason: 'no prior meta' }`，这是首次初始化的合法状态，不是错误。

同一个 store 通常由一个 embedding 配置生成，表级元数据足以降低存储和查询成本。代价是模型切换时必须整体重建，不能在同一 vec0 表中混用不同维度。`clearAll()` 同时清 evidence_vectors 和 embedding_meta，避免删了向量却留下看起来一致的旧配置。

### U.6 一次向量查询的完整顺序

一次 doc semantic 查询的真实顺序是：

1. `isEmbeddingAvailable()` 检查 deps mode 是否为 `on`，reprobe，再读 `isReady()`；
2. `embedding.embed([query])` 得到 Float32Array；
3. `vectorStore.search(queryVec[0], pool)` 得到 anchor/distance；
4. 把 anchors 放进一次 `IN (?,...)` hydrate SQL；
5. 在 hydrate SQL 中应用 kind/scope/status/keywords/thread/world/scene/provenance/backstop；
6. 用 `nnResults` 原顺序重排过滤后的 row；
7. 截到最终 limit。

过滤放在 hydrate 后而不是先把向量行删掉，是因为资格字段只在 evidence_docs。它会有一个召回损耗：如果 vec0 top 20 中前 15 条都因 scope 不合格，代码不会自动再查第 21 到 40 条，而是可能返回少于 limit。更高召回的做法是扩大 vector pool 或循环补页，但会增加 vec0 与 hydrate 成本；源码的 cap 100 是对最坏情况的约束。

### U.7 Embedding 故障矩阵

|故障|ready|结果|当前 meta|
|---|---:|---|---|
|服务没启动|false|doc/raw lexical|raw 明确 degraded；doc 分支可能误报 false|
|health 超时|false|lexical|同上|
|embed HTTP 500|调用抛异常|lexical fallback|raw 为 passage_vector_search_error|
|vec0 MATCH 异常|调用抛异常|lexical fallback|raw 同上|
|模型维度错误|可能 ready|可能抛或产生修剪向量|需靠 consistency/监控发现|
|向量表为空|true|semantic 空，hybrid 仍有 lexical|不一定 degraded|

“向量表为空”不应自动等同服务降级。服务健康但 warmup 尚未完成是数据覆盖不足，不是 embedding runtime 错误；可以另加 vector coverage 指标。面试时区分 availability、execution error 和 index coverage，能避免把所有空结果都归咎于模型。

## V. SQLite schema V1-V26：迁移代码如何守住旧数据

memory 目录的 schema 不是一次性建表，而是通过 `schema_version` 逐版本前进。面试官常问“删库重建不就行了，为什么要这么多 migration”，真正答案是：用户已有 evidence、markers、recall_events 和外部 collection 数据不能因为代码升级被抹掉；迁移必须可重复、可中断后恢复、尽量不破坏旧读取逻辑。

### V.1 PRAGMA：WAL、busy timeout 和 foreign keys 各管什么

启动时执行：

```sql
PRAGMA journal_mode = WAL;
PRAGMA journal_size_limit = 67108864;
PRAGMA foreign_keys = ON;
PRAGMA busy_timeout = 5000;
```

WAL 让读者在 writer 提交前仍可读旧快照，减少读写互相阻塞；它不允许两个 writer 真正同时提交。`journal_size_limit` 约束 WAL 文件增长，但不是立即截断保证。`foreign_keys=ON` 必须在每个 SQLite connection 上设置，不能只在某一次迁移连接设置。`busy_timeout=5000` 让遇到短暂写锁时等待 5 秒，而不是立刻抛 `SQLITE_BUSY`；它不是替代 writeQueue 的并发控制。

### V.2 V1 的 external-content FTS

V1 创建 `evidence_docs`、`evidence_fts`、`edges`、`markers` 和 `schema_version`。FTS 声明：

```sql
CREATE VIRTUAL TABLE evidence_fts USING fts5(
  title, summary,
  content=evidence_docs,
  content_rowid=rowid
);
```

这让 FTS 行没有重复保存所有业务列；`rowid` 是桥。随后 `FTS_TRIGGER_STATEMENTS` 是数组，而不是一个包含多个语句的字符串，因为 better-sqlite3 对多语句 trigger 的执行和错误定位更可靠地按条处理。INSERT trigger 写新行，DELETE trigger 使用 FTS5 的 delete command，UPDATE trigger 先删除旧索引再插入新索引。

external-content 的维护要求写入端永远通过 `evidence_docs` 事实表触发同步。若有人直接 INSERT `evidence_fts`，业务行与 FTS 会不一致；一致性检查应比较 evidence_docs 行数、FTS 可回查 anchors 和 trigger 覆盖情况，而不是只看虚拟表 count。

### V.3 `applyMigrations`：为什么先建 schema_version

```ts
db.exec('CREATE TABLE IF NOT EXISTS schema_version (...)');
const currentVersion =
  (db.prepare('SELECT MAX(version) AS v FROM schema_version').get() as
   { v: number | null })?.v ?? 0;
```

空数据库没有 `schema_version`，所以不能先查询再创建。`MAX(version)` 在没有行时返回 NULL，TypeScript 类型把它标成 `number | null`，`?? 0` 把首次启动归到 V0。若历史迁移被部分执行，表中最高版本代表已记录完成的 migration；代码各段用 `if (currentVersion < N)` 继续补齐。

每个版本完成后立即：

```ts
db.prepare(
  'INSERT INTO schema_version(version, applied_at) VALUES (?, ?)'
).run(N, new Date().toISOString());
```

这意味着 migration 不是所有版本包在一个超大 transaction 中。优点是单版本失败后下次可以从未记录的版本继续；缺点是数据库可能暂时处于“V10 已完成、V11 未完成”的中间状态。所有运行时查询应向后兼容已存在的最低版本，或启动阶段在未到 CURRENT_SCHEMA_VERSION 时阻止业务请求。

### V.4 ALTER TABLE + catch：幂等和部分迁移

SQLite 的 `ALTER TABLE ADD COLUMN` 在重复执行时会报 duplicate column name。代码常见模式：

```ts
try {
  db.exec('ALTER TABLE evidence_docs ADD COLUMN authority TEXT');
} catch {
  // column may already exist from a partial migration
}
db.exec('CREATE INDEX IF NOT EXISTS idx_evidence_docs_authority ON evidence_docs(authority)');
```

catch 的真实目的不是吞掉所有 SQL 错误，而是兼容“列已存在但 version row 尚未写入”的部分迁移。当前实现的缺口是 catch 不区分 duplicate column 与磁盘损坏/权限错误；更严谨的实现应检查 error message 或先查询 `PRAGMA table_info(evidence_docs)`。面试时可说“它是幂等容错，但错误分类仍可加强”。

`CREATE INDEX IF NOT EXISTS` 可以安全重跑，且 index 创建与 column 添加分别执行，因为 SQLite 不允许把多个 ALTER TABLE 随意拼成一条。V6 的 pack_id、V10 的 provenance、V13 的 authority/activation/verified_at、V16 的 world/scene、V17 的 collection 字段，都是在不重写旧行的情况下渐进加列。

### V.5 V3 passage 表为什么另有一套 trigger

V3 创建 `evidence_passages` 和 `passage_fts`。它的 tokenizer 写成带引号的字符串：

```sql
tokenize="unicode61 tokenchars '_-'"
```

这是因为 tokenizer 参数本身包含引号和连字符，源码把 FTS 表 SQL 单独放在 `SCHEMA_V3_FTS`，trigger 再放入数组逐条执行。文档 FTS 与 passage FTS 分离，避免消息量巨大时把每条消息和摘要混在同一个 ranking 空间。

V4 的 `summary_segments`/`summary_state` 是 append-only ledger + watermark。segments 保存生成历史，state 保存增量位置；这让摘要重算不会覆盖历史证据，也让任务重启能从 watermark 继续。

### V.6 从 V13 到 V15：多轴元数据和压缩保护

V13 新增 authority、activation、verified_at 以及 f163 cohorts/suggestions/logs。V14 的 `source_ids`、`summary_of_anchor`、`compression_rationale` 让摘要是非替换压缩：源文档仍在，summary 只是额外 evidence。V15 的 `contradicts`、`invalid_at`、`review_cycle_days` 支持矛盾与复核。

写入端的 cascade guard 会查询 source_ids 是否引用了已经是 summary 的文档；若命中则抛 `cascade compression prohibited`。这不是数据库 CHECK constraint，而是业务层在 upsert 前用动态 IN 查询检查，因此所有写入口都必须经过同一个 store，绕过 store 的脚本可能破坏规则。

### V.7 V16 world scope 与 V17 collection scope

V16 给 evidence_docs 增加 world_id、scene_id 和联合索引 `(world_id, scene_id)`。查询端只有 options 传入 worldId/sceneId 才追加条件，未传时保持全局兼容。

V17 增加 collection_id、review_status，并给 markers 加 source/target collection 与 sensitivity 字段。collection 不只是展示标签：KnowledgeResolver 会在 fan-out 中按 manifest 过滤，privacy redactor 也据此决定能否返回 title/summary。

### V.8 迁移测试与复述

至少准备三个 fixture：空 SQLite 执行全部迁移；V1 旧库带一条 evidence_docs 再升级；手工加 authority 列但不写 version 13 的半迁移库。重复调用 applyMigrations 还应不产生重复 index、数据行或 trigger。对 FTS，插入/更新/删除 evidence_docs 后查询 MATCH，证明 trigger 在迁移后仍工作；对 passage，验证带连字符的 anchor 查询行为。

面试复述可以是：“Clowder 把 schema 当作版本化程序：先确保 schema_version，再按 V1 到 CURRENT_SCHEMA_VERSION 条件执行，每完成一版记录 applied_at。V1 建事实表和 external-content FTS，V3 增加 message passage FTS，后续逐步加 provenance、authority、compression、world、collection 和向量元数据。ALTER TABLE 用 try/catch 兼容部分迁移，索引用 IF NOT EXISTS 重入；这保证旧数据不被清空，但空 catch 对磁盘错误的诊断仍可改进。”

## W. 排序后处理：RRF、authority、消费 prior、recency 和 MMR 如何串起来

召回完成不等于排序完成。源码把多个后处理阶段保持为相对独立的 helper：RRF 解决 lexical/semantic 两个列表怎么合并；authority boost 解决可信来源的顺序；consumption prior 解决哪些证据更可能被后续消费；recency decay 抑制过时内容；MMR 抑制前几名彼此重复。理解它们的先后关系，才能回答“一个结果为什么排在另一个前面”。

### W.1 RRF 只融合名次，不假装分数同尺度

doc hybrid 的核心：

```ts
const RRF_K = 60;
for (let i = 0; i < lexical.length; i++) {
  scores.set(anchor, (scores.get(anchor) ?? 0) + 1 / (RRF_K + i));
}
for (let i = 0; i < nn.length; i++) {
  scores.set(anchor, (scores.get(anchor) ?? 0) + nnWeight / (RRF_K + i));
}
```

第一个位置的贡献是 1/60，第二个是 1/61；相邻名次差距很小，两个通道都出现的文档会自然累加。它没有使用 BM25 原始负数 rank，也没有把 cosine distance 直接相加，因为两者的数值范围、方向和模型版本都不同。RRF 的输入是“第几名”，所以更换 embedding 模型只要维持近邻排序，融合公式仍稳定。

源码的 for 从 i=0 开始，数学注释写成 rank_i 时要注意：实现的 rank 是 zero-based，而很多论文用 one-based。这里使用 60+i；若改成 60+i+1，会造成所有分数轻微变化，测试快照和跨版本比较都可能变化。

### W.2 CJK 权重 1.5 的实际影响

hasCJKCharacters 使用 Unicode Script 属性检测 Han、Hiragana、Katakana、Hangul。query “缓存超时”会让 nnWeight=1.5；“cache timeout”为 1.0；混合 query “F042 缓存 timeout”也只要包含一个 CJK 字符就启用 1.5。

举例：某文档 lexical 排第 1、NN 排第 8：

```text
score = 1/60 + 1.5/67 = 0.0392
```

纯 lexical 排第 1 的文档只有 1/60=0.0167，所以一个同时被 NN 找到的 CJK 语义结果可能超过纯词法结果。这不是把中文结果硬置顶，而是提升 semantic 通道的可见性。权重常量应通过 recall fixture 和误命中样本评估。

### W.3 entity result 的半数保护

mergeEntityResults 不是简单 concat。若同时存在普通 semantic/hybrid items，实体结果最多占：

```ts
const entityCap = items.length > 0
  ? Math.max(1, Math.ceil(limit / 2))
  : limit;
```

当 limit=5 且 semantic 有结果时，entityCap=3；先放最多 3 个 entity item，再放普通 item，最终仍 slice(0,5)。这样 alias/mention 召回不会被向量完全淹没，也不会让一个弱 entity alias 占满前十名。若 semantic 为空，entity 可以填满 limit，体现“实体通道是可用的唯一发现路径”。

seen 是 anchor Set。entityItems 与 semanticItems 可能指向同一文档，重复项被跳过；后续普通列表继续填充。这个算法复杂度是 O(entity + semantic)。

### W.4 authority boost：为什么放在 lexical 候选之后

applyAuthorityBoost(results, trimmed) 在 doc-level lexical/FTS/backfill/entity 合并后执行，而不是在 SQL rank 中硬编码所有 authority 规则。这样 authority 规则可以单独 kill-switch，且能在 exact anchor、entity 命中等候选已经确定后调整顺序。

authority boost 不是把所有 constitutional 都放第一：如果完全不相关，仍可能在 matchReason 或 MMR 阶段被限制。代码用 try/catch fail-open，boost 计算失败时保留原 lexical 顺序；这比排序 helper 抛异常导致整个 search 失败更符合增强层定位。

authority 与 relevance 冲突时，资格过滤先于排序：authority 不能让被 status/scope 排除的行重新出现；在合格候选内部，authority 作为稳定 tie-break/boost，具体强度由产品策略和 fixture 校准。

### W.5 consumption prior：Beta(2,8) 的 shrinkage 不是 CTR 原值

消费统计输入一般包含 doc 的 consumed count、shown count、kind 均值和最近消费时间。源码先用 Beta 先验做收缩：

```text
shrunkCtr = (consumed + 2) / (shown + 2 + 8)
rawLift = (shrunkCtr - meanCtrKind) * recencyFactor
```

shown=0 时，Beta(2,8) 给出 0.2 的先验均值；shown 很大时，先验影响变小，逐渐接近真实 CTR。meanCtrKind 用同 kind 的基线，避免 thread 与 decision 的消费率直接横比。

branch 有四种：constitutional 保护 constitutional authority 或 decision/lesson kind；cold-start 没有展示样本，prior 保守为 0；low-sample 只允许非负 lift；full 样本足够，使用完整 rawLift，可正可负。这是排序信号，不是访问控制。

### W.6 14 天 grace period：为什么 firstIndexedAt 要落库

firstIndexedAt 以 epoch milliseconds 保存。若文档首次入索引未满 14 天，进入 grace branch，不因尚未被消费被负向降权。若每次 upsert 都更新 firstIndexedAt，新文档永远处于 grace；所以写入必须在已有行存在时保留旧值，只在首次插入填充。

```ts
const ageDays = (Date.now() - firstIndexedAt) / 86_400_000;
const inGrace = ageDays < 14;
```

生产上要考虑机器时钟回拨，ageDays < 0 可以视为 grace；future timestamp 不应让文档遭受巨大 penalty，最好增加 clamp 和 clock-skew metric。

### W.7 recency decay：按 kind 选择 half-life

computeRecencyDecay(updatedAt, kind) 使用 kind-specific half-life。直观公式：

```text
decay = 0.5 ** (ageDays / halfLifeDays)
```

age=0 时 decay=1；age=halfLife 时 decay=0.5；age=2*halfLife 时 decay=0.25。不同 kind 可以有不同半衰期：lesson/decision 变化慢，thread/session digest 变化快。它通常乘在消费 lift 或作为排序因子，不直接删除旧文档。

### W.8 constitutional immunity 与 pinned position

源码会先找 exact lexical protected anchors，再对非保护结果做消费/MMR rerank：

```ts
const protectedAnchors = findExactLexicalProtectedAnchors(results, query);
const protectedResults = results.filter(r => protectedAnchors.has(r.anchor));
const rerankPool = results.filter(r => !protectedAnchors.has(r.anchor));
```

这解释了“为什么 exact F042 规则不会被 MMR 挤掉”。保护集合通常只包含真正的 anchor/标题精确匹配，而不是任何 substring 命中，避免普通词把所有结果锁死。pinned 结果是否占用 limit、rerankPool 取多少，都是 API 行为，应该由测试锁定。

### W.9 MMR：相关性和多样性的贪心选择

MMR 的常见形式：

```text
MMR(d) = lambda * relevance(d)
       - (1-lambda) * maxSim(d, alreadySelected)
```

当前 lambda=0.7，偏向 relevance 但保留 30% diversity。相似度不一定使用 embedding；源码中有 keyword Jaccard：

```text
jaccard(A,B) = |A ∩ B| / |A ∪ B|
```

候选逐轮选择：第一轮取原始最高 relevance；之后对每个未选文档计算 relevance 与它和已选集合的最大相似度，取 MMR 最大者。它是 O(k*n) 的贪心算法，k=limit 很小，适合内存后处理。

MMR 不是去重唯一键；两个 anchor 不同但内容相同，Set 去重无能为力，MMR 的 Jaccard 才能抑制。反过来，Jaccard 只看关键词/文本 token，可能把同一主题的互补文档误判为重复，所以 lambda 和 token 规范化需要 fixture 校准。

### W.10 explain 字段和排序链复述

annotateMatchReasons 会根据命中位置写 matchReason，例如 anchor/title/summary/keyword/content；explain=true 时还可填 rankingFactors：bm25Score、consumptionPrior、mmrPenalty、confidence。

这些字段是诊断摘要，不是可复现的完整证明。BM25 score 可能只在 lexical 路径存在，hybrid/RRF 后不一定可与最终排序分数直接比较；MMR penalty 是选择该项时相对已选集合的值；confidence 是校准后的区间 [0,1]，不是模型原始概率。

排序复述：“先用 exact、FTS、backfill、entity、NN/RRF 形成候选，资格过滤在各路 hydrate SQL 中完成；RRF 只融合 lexical 与 semantic 名次，k=60，CJK query 对 NN 乘 1.5。候选合并后，authority 提升可信来源，consumption prior 用 Beta(2,8) shrinkage 与 kind baseline 计算 lift，14 天内的新文档有 grace，recency 按 kind half-life 衰减。constitutional/exact anchor 进入 protected 集合，不被消费和 MMR 轻易挤掉；其余结果用 lambda=0.7 的 MMR 和 keyword Jaccard 做多样性选择。”

## X. KnowledgeResolver：多 collection fan-out 怎样做到可控、可脱敏、可回退

单个 SqliteEvidenceStore 只解决一个 collection。Clowder 的 library/project/global 查询由 KnowledgeResolver 把多个 store 编排起来。它不是简单调用 Promise.all，因为每个 collection 有 manifest、sensitivity、review policy、超时和来源标识。

### X.1 collection fan-out 的输入决策

Resolver 先根据 dimension/collections 选择 stores：

```text
dimension=project  -> project store
dimension=global   -> global store
dimension=all      -> built-in project + global
dimension=collection + ids -> catalog 中指定 collection
```

如果用户传 collections，必须先通过 LibraryCatalog 校验 ID；不能把任意字符串直接拼接成数据库文件路径。external collection 的 store path 会把冒号等字符替换成安全形式，并且 factory 对坏 manifest 采用 fail-open skip，避免一个外部库阻塞内置库启动。

### X.2 allSettled 而不是 all

每个 collection 搜索通常带 timeout：

```ts
const settled = await Promise.allSettled(
  selected.map(async ({ id, store }) => {
    const started = Date.now();
    const execution = await withTimeout(searchStore(store, query, options), timeoutMs);
    return { id, execution, durationMs: Date.now() - started };
  }),
);
```

Promise.all 任何一个 store reject 都会让整体 reject；Promise.allSettled 返回 fulfilled/rejected 两类结果，Resolver 可以保留健康 collection 的结果，同时在 collectionGroups 标记 error/timeout。这里的 allSettled 是联邦检索的故障隔离边界。

timeout 需要清理 timer，否则大量查询会积累未触发的 setTimeout。若 withTimeout 用 Promise.race，原始 search Promise 仍可能在后台继续跑；更好的实现传 AbortSignal，或至少在 store 层用查询级 deadline。SQLite 本地查询很快，外部 collection/embedding 更需要 timeout。

### X.3 sensitivity 与 privacy-redactor：先脱敏再融合

collection manifest 携带 public/internal/private sensitivity。public/internal 结果可以保留 title/summary；敏感 collection 返回给上层时，privacy redactor 会把内容替换成类似：

```json
{
  "title": "[redacted — private collection]",
  "summary": null,
  "collectionId": "private-x",
  "anchor": "..."
}
```

redactor 不应只在最终 JSON 输出时做一次。Resolver 在构造 collectionGroups 和 collection-level RRF 前就要保证敏感文本不会被日志、recall_events、shadow ranking 或异常消息泄漏。RRF 可以只使用 rank/anchor，不需要把原文写进融合结构。

### X.4 collection-level RRF：anchor 必须带 collection 前缀

不同 collection 可能有相同 anchor，例如都拥有 F042。Resolver 使用 collectionId + ':' + item.anchor 作为融合 Map key，而不是裸 anchor。否则一个 collection 的 F042 会覆盖另一个 collection 的 F042，结果的 source 也会错。最终 item 要保留 collectionId，drill-down 也要携带 collection sourceRef，不能把外部文件路径当作内置 docs 路径。

collection-level RRF 与单库 RRF 层次不同：单库先合 lexical/NN；Resolver 再按 collection group 内的 rank 合并。不要把两个层次混成一个总分。一个 project 结果和一个 global 结果即使文本相同，也应以不同 collection key 参与融合，后续可按 primary collection penalty 处理。

### X.5 primary collection penalty 与来源平衡

常见排序片段：

```ts
const aPenalty = a.collectionId === primaryCollectionId ? 0 : 2;
const bPenalty = b.collectionId === primaryCollectionId ? 0 : 2;
return (a.score - aPenalty) - (b.score - bPenalty);
```

来源 penalty 的目的是让项目上下文稍微优先，同时保留 global/library 的补充。它不是访问权限，也不是把外部库永远排在后面。若 primary collection 没有命中，global 结果仍可进入；若用户显式指定 collections，primary 的确定逻辑应服从显式选择而不是硬编码 project。

### X.6 degraded meta 的合并

Resolver 会收集各 store 的 SearchExecutionMeta。没有 degraded meta 时返回 false；有多个 degraded 时通常选择最能解释整体结果的 reason，或保留 reasons 数组供诊断。

关键点是不要因为一个 collection timeout 就把所有结果标成“整体空”。collectionGroups 记录每个 group 的 status、durationMs、items；客户端可以显示“3 个库成功，1 个库超时”。这比只给一个全局 degraded boolean 更适合解释联邦故障。

### X.7 GraphResolver 与检索 Resolver 的边界

GraphQueryResolver 处理 exact graph matches、BFS edge traversal 和 collection visibility；KnowledgeResolver 处理文本/向量 evidence fan-out。两者都要调用 catalog 的 canShowCandidate，防止 graph edge 把用户带到没有授权的 collection。

BFS 使用 visited Set 避免环：

```ts
const queue = [{ anchor: start, depth: 0 }];
const visited = new Set([start]);
while (queue.length && visited.size < maxNodes) {
  const current = queue.shift()!;
  for (const edge of edges(current.anchor)) {
    if (visited.has(edge.toAnchor)) continue;
    if (!canShow(edge.toCollectionId)) continue;
    visited.add(edge.toAnchor);
    queue.push({ anchor: edge.toAnchor, depth: current.depth + 1 });
  }
}
```

maxNodes 是截断保护；edge weight 已计算但如果没有优先队列，BFS 顺序仍按层级和插入顺序，不是严格按最高权重优先。面试时应说“权重目前可作为解释/后续排序信号”，不要误称代码已经实现 weighted shortest path。

### X.8 联邦检索测试与复述

需要测试：一个 collection 成功、另一个 reject 时仍返回成功结果；一个超时 group 状态为 timeout；两个库同 anchor 不互相覆盖；private collection 的 title/summary 不出现在返回和 recall log；显式 collections 过滤不会偷偷 fan-out 到其他库；project primary penalty 只影响排序不影响资格；某库 searchWithMeta degraded 时全局 meta 有可解释 reason。

面试复述：“KnowledgeResolver 先根据 dimension/collections 从 catalog 选 store，再用 allSettled 并行搜索并记录每组状态和耗时。单库内部先完成 lexical/semantic/hybrid，联邦层用带 collectionId 前缀的 key 做第二层 RRF，避免同 anchor 冲突。敏感库先脱敏，融合只依赖 rank/anchor，不能把私有文本写入日志。一个库失败不会让所有库失败，最终通过 collectionGroups 和 degraded meta 说明哪些来源成功、哪些降级。”

## Y. 安全、可观测性和测试：把“能搜到”变成“可放心上线”

检索系统的风险不止是漏一条结果。它还可能把 secret 写入索引、让路径逃逸、把 private collection 文本记进 recall log，或者在 vector/FTS 不一致时给出看似可信的错误结果。因此源码中 SecretScanner、privacy redactor、transcript redactor、degraded meta 和 recall_events 都属于检索管线的一部分。

### Y.1 SecretScanner：扫描发生在写入边界

scanner 通常在文件转成 EvidenceItem 之前运行 SecretScanner。它会对 markdown/文本批量扫描常见 token、私钥头、云凭证和高熵字符串，并返回 findings。写入策略是：明确 secret 的文件跳过或标为 rejected；可安全保留的文档做 redaction；记录 fingerprint/原因但不记录完整 secret；单个坏文件 fail-open，不阻塞其他文件。

fingerprint 应用不可逆摘要或稳定 ID，便于重复告警去重，但不能把 SHA-256 当作万能匿名化：低熵 secret 可被字典反查。生产日志还要限制 path、line preview 和 query 原文，避免扫描器自己成为泄漏源。

### Y.2 privacy redaction 的时机

privacy-redactor 接收 collection sensitivity 和 item，返回可展示副本。应该在三个边界都调用：resolver 返回 API 之前；RecallPersistenceRedactor 写 recall_events 之前；shadow ranking/QueryReplayCompare 生成离线 payload 之前。

只在 API 最后脱敏仍有风险，因为中间层可能已经把 summary 写日志或持久化。敏感 collection 的融合结构应只携带 anchor、rank、collectionId，文本字段到最终授权展示时才按 policy hydrate。

### Y.3 transcript redactor 与 raw context

raw context 直接来自消息 content，不能因为它走的是 passage 表就绕过 transcript redactor。speaker、messageId、threadId 也是关联信息：即使 content 已脱敏，组合后的时间和身份仍可能泄漏。工具 drill-down 生成的参数要经过同样的 collection/权限检查。

常见错误是“只 redacted title/summary，passages 原文仍返回”。测试应构造 private thread，要求 summary、passage content、context content 和 drill-down sourceRef 都遵守同一 sensitivity policy。

### Y.4 路径逃逸防护：两次 resolve，两个信任边界

IndexBuilder 扫描路径时要确认文件位于 sourceRoot 下；search 生成 drill-down 时再次确认 sourcePath 没有 parent traversal 逃逸。典型判断：

```ts
const resolved = isAbsolute(sourcePath)
  ? resolve(sourcePath)
  : resolve(sourceRoot, sourcePath);
const rel = relative(sourceRoot, resolved);
if (!rel || rel.startsWith('..') || isAbsolute(rel)) return null;
```

relative 返回空字符串时表示正好指向 root；源码通常拒绝它，因为 drill-down 需要文件而不是目录。Windows 下要同时处理反斜杠和大小写/盘符，不能只用字符串 startsWith(sourceRoot)，例如 C:\docs2 不是 C:\docs 的子路径。

### Y.5 SQL 注入与 FTS 注入是两个问题

值参数化防止 SQL 注入：query、keyword、anchor、日期都绑定到问号。FTS MATCH 的 query 语法仍可能被恶意 token 破坏，因此 fts-query-builder 用 double quote 包 token，并把内部双引号替换成两个双引号。LIKE backfill 还要处理百分号和下划线；JSON keywords LIKE 只是文本匹配，不是 JSON path 查询。

三种 grammar：SQL、FTS5、LIKE，各自需要自己的 escaping 规则。只说“用了 prepared statement 所以安全”是不完整的。

### Y.6 recall_events 与 shadow ranking：指标本身也要脱敏

RecallEventCorrelator 会把 candidates、consumed、shadowRankingJson 等写入 recall_events。它的价值是比较 live 与 shadow ranking 的 MRR、consumption rate，但也可能保存敏感 anchor 或文本。persistence redactor 不仅要处理 items，还要处理 shadow ranking 和 collectionGroups。

shadow ranking 主要是进程内 Map 或 recall event 持久化。进程重启会丢内存 shadow state；长期实验依赖 recall_events，并带 variant/effective flags、query intent、collection sensitivity 的最小必要元数据。完整 query 文本应按 retention policy 截断或哈希。

### Y.7 四层一致性检查

ConsistencyReport 至少检查：

```text
docCount                 = evidence_docs count
ftsReachableCount        = FTS 命中并能 JOIN 回 docs 的数量
passageParentMismatches  = passage 无 parent doc 的数量
orphanVectorCount        = vector key 无事实行的数量
```

FTS 虚拟表不能简单 count 与 docs 比较，因为 external-content 读取和 superseded 行有不同语义。更可靠的是抽样或全量遍历 anchors，验证 FTS delete/update trigger 后仍可查询。vector orphan 不一定立刻影响 lexical，但会浪费 top-k 名额；超过阈值可触发 vector rebuild。

### Y.8 故障矩阵与改造优先级

|故障|用户结果|观测|
|---|---|---|
|FTS syntax error|现有 exact/backfill 仍可返回|FTS error counter|
|LIKE backfill error|保留 FTS results|backfill failure metric|
|embedding health down|lexical fallback|degraded unavailable|
|embedding timeout|lexical fallback|vector_search_error|
|passage parent 缺失|跳过该 passage|orphan parent count|
|private collection timeout|其他库正常返回|collection group timeout|
|坏 JSON keywords|基础字段仍展示|corrupt metadata counter|
|source path escape|不生成 drill-down|security warning|

当前最值得先改的缺口是：doc semantic/hybrid fallback 的 degraded meta；rowToItem 的 JSON 安全解析；passage parent batch hydrate；以及缺失 passage 的 tombstone/GC。graph edge weight 是否改成优先队列，属于后续产品排序选择。

### Y.9 最终复述模板

“这套检索管线的可靠性来自分层和可回退：evidence_docs/FTS 是稳定 lexical 基线，passage 和 vec0 是可选增强，EmbeddingService 通过独立 HTTP health、timeout、batch 接入，模型 id/dim 写入 embedding_meta。所有用户值分别经过 SQL、FTS、LIKE 各自的 escaping；scanner 和 drill-down 都做 sourceRoot 路径边界，SecretScanner 和 privacy redactor 在写入、联邦融合、回放持久化多个边界防泄漏。schema 用 V1-V26 可重入迁移，写入用单写者队列和 SQLite transaction，向量更新是最终一致。排序先 RRF，再 authority/consumption/recency/MMR，并用 protected anchor 保住精确规则。遇到 embedding、FTS、collection 或坏数据故障时，系统尽量保留 lexical 结果，同时通过 degraded meta、collectionGroups、consistency report 和 recall_events 说明真实执行状态。”

## Z. FTS builder 与 lexical backfill：从 token 到 comparator 的逐函数推演

前文解释了它们解决什么问题，这里按源码函数的输入、输出、边界条件再走一遍。面试官如果问“能具体解释 escapeToken 和 isStrongToken 吗”，不能只回答“一个转义，一个判断强词”，要说明它们如何改变最终 MATCH 字符串。

### Z.1 escapeToken：FTS phrase 的双引号规则

实现可缩写为：

    function escapeToken(token: string): string {
      return '"' + token.replace(/"/g, '""') + '"';
    }

FTS5 的 double-quoted phrase 中，内部双引号需要写成两个双引号。输入 foo"bar 会变成 "foo""bar"。replace 中的 g 不能省，否则只替换第一个双引号。

为什么不把 token 用单引号包起来？FTS5 的单引号不是与 SQL 一样的通用 phrase escaping；builder 选择 FTS5 文法中的双引号。query token 通过 MATCH 的参数绑定传入，SQL injection 与 FTS syntax injection 是两层不同问题：参数绑定解决前者，escapeToken 解决后者。

### Z.2 isStrongToken：正则分支的业务含义

强 token 判定有四层：feature/decision/lesson 等实体标识 F042、ADR-005、LL-048、KD-7、AC-B1、HW-6；PhaseA/PhaseB 阶段标识；含 CJK 的 token；长度至少 4 的普通词。

第一层正则是：

    /^(?:F|ADR|LL|KD|TD|AC|HW)-?\d+[a-zA-Z]?$/i

^/$ 锁定整 token，非捕获组只做分组，-? 允许 F042 与 F-042，\d+ 要求至少一个数字，末尾可带一个字母后缀，i 让大小写不敏感。它不会识别 ADR- 没有数字，也不会把普通 F 当实体 ID。

### Z.3 progressive builder 的三个 level

输入先 trim、按连续空白 split、filter 空 token，再对每个 token 做 escape。token 数小于等于 3 时直接返回一个 AND-all。长 query 的 level 1 是所有 token 的 implicit AND；level 2 是 strong AND 与 weak OR；level 3 是 OR-all。

例如：

    "F042" AND "cache" AND ("timeout" OR "when" OR "does")

源码特别强调 strongPart 后面必须写显式 AND，不能只写空格再接 parenthesized OR，否则 FTS5 可能报 syntax error。caller 只在上一 level 完全没有 rows 时进入下一 level，所以它是 progressive fallback，不是把三层结果全部 union。

### Z.4 builder 的输入输出样例

|输入|token 数|生成序列|
|---|---:|---|
|F042 cache|2|一个 AND-all|
|cache timeout F042|3|一个 AND-all|
|F042 cache timeout when does this happen|7|AND-all；strong AND weak OR；OR-all|
|缓存 超时|2|一个 AND-all，CJK token 仍被 quote|
|空白|0|空数组|

短 query 只返回一个 level，即使 FTS 找不到也不会在 builder 内自动生成 OR 版本。若产品希望二词查询也 relaxation，需要修改 builder 和 fixture，不应在 caller 里偷偷追加。

### Z.5 splitLexicalBackfillWords：去重后的 lower-case token

实现等价于：

    return [...new Set(query.trim().toLowerCase().split(/\s+/).filter(Boolean))];

执行顺序是 trim、lowerCase、split、filter、Set、spread。先 lower 再 Set，因此 F042 与 f042 只保留一个。Set 保留首次插入顺序，spread 转回数组供后续 flatMap 和 signal 统计。

这和 FTS builder 的 tokens 不完全同一套：FTS 保留原 token 交给 tokenizer/phrase；backfill 统一 lower-case 做 SQLite LOWER/JS includes。它不是完整自然语言分词器。

### Z.6 computeLexicalBackfillSignal：三个计数器

每行初始化 keywordHits、titleHits、textHits。对每个 query word，keywordHit 使用 keywords.some(keyword => keyword.includes(word))，titleHit 使用 title.includes(word)，summaryHit 使用 summary.includes(word)。keywordHits 和 titleHits 按 query word 计数；textHits 表示该词是否至少在三个字段之一出现。

keywords 先通过 parseKeywordList JSON.parse。raw null、不是数组或 JSON 损坏都返回空数组，signal 仍可由 title/summary 计算。backfill 对坏 keywords fail-open，而 rowToItem 可能仍有 JSON.parse 风险，说明写入/读取边界的错误策略并不完全一致。

### Z.7 comparator：信号优先级和质量 tie-break

compareLexicalBackfillSignals 按 keywordHits 降序、titleHits 降序、textHits 降序；完全相同返回 0。随后 compareLexicalBackfillRowQuality 处理同信号行：superseded_by 非空排后，archive/ 路径排后，authoritative provenance 排前，updated_at 新的排前。

updated_at 使用 ISO 字符串 localeCompare；只有格式统一为 ISO8601 时，字符串比较才等价于时间比较。若历史数据混有本地日期，排序会错，应该在 scanner/schema 边界统一格式。

### Z.8 exact anchor 的保护为何单独在 item comparator

backfill row comparator 只看到 row，不一定知道 exact query anchor。caller 在转换 EvidenceItem 并和已有 results 合并后，再调用 compareEvidenceItemsByLexicalBackfill，并把 exactRow.anchor 传入。若 a.anchor 等于 exactAnchor，直接返回 -1；否则走 signal comparator。

这是一层二次排序保护，避免 backfill 的 keyword/title signal 把用户明确输入的 anchor 挤走。它也说明 exact 保护只作用于 anchor 完整相等，不是任意 title substring。

### Z.9 性能与调试

contains SQL 可能扫描 evidence_docs 全表，复杂度大致是 O(D * W * column scan)，D 是文档数，W 是 query word 数；JavaScript signal 又会对每行 keywords 数组做 some/includes。当前 backfill 没有主 LIMIT，大库最坏成本可观。

遇到“标题明明存在却搜不到”时，按顺序诊断：检查 trimmed 和 token 数；独立调用 buildProgressiveFtsQueries 查看三个 level；直接执行 evidence_fts MATCH 验证 trigger；执行 LOWER(title/summary/keywords) LIKE 验证 backfill；检查 seenAnchors 去重；检查 scope/status/date/provenance 过滤；最后才看排序是否截到 limit。

优化不能简单加 LIMIT 100，因为 SQL 没有 signal 排序，前 100 行可能完全不是 title/keyword 高命中。可先按 sourceRoot/kind/date 缩小范围，再建 keyword 关联表、trigram 索引或做分页扩大，并记录 scanned_rows 和 elapsed_ms。

## AA. 一个完整 query trace：从输入 JSON 到最终 JSON

为了把所有源码分支串起来，假设数据库中有三个文档：

|anchor|kind|title|summary|source|
|---|---|---|---|---|
|F042|decision|Cache timeout decision|use lexical fallback|authoritative|
|thread-A|thread|Debugging cache timeout|讨论 F042 的修复|project|
|thread-B|thread|Forwarded note|转发 F042 cache timeout|project|

thread-A 有 passage msg-10、msg-11；thread-B 有 msg-20。F042 的 embedding 排第 2，thread-A 的 passage 排第 1，thread-B 的 passage 因为 literal 转发也在 lexical 和 NN 列表中。

请求：

    {
      q: " F042 cache timeout ",
      limit: 5,
      scope: "threads",
      mode: "hybrid",
      depth: "raw",
      contextWindow: 1,
      explain: true
    }

### AA.1 入口变量

trimmed 变为 F042 cache timeout；limit 为 5；hybrid 的 bm25Pool 为 max(20, 20) 即 20；scope threads 使 effectiveKind 为 thread；excludePackKnowledge 为 true；threadAnchor 未设置；queryEntityMatches 取决于 EntityRegistry。

exact-anchor SQL 用整个 trimmed 去查 anchor。因为 query 包含三个词，不会命中 F042，但这次查询 harmless，且不会把 F042 当成 anchor。FTS builder 为三个 token 返回一个 AND-all；SQL 追加 d.kind='thread' 和 LIMIT 20，因此先找到 thread-A 和 thread-B。

### AA.2 backfill 与 entity

backfill words 是 f042、cache、timeout，生成三组 title/summary/keywords LIKE 参数。thread-A/B 已在 seenAnchors，backfill 不重复追加；如果某个 thread 的摘要没有 FTS token 但 title substring 命中，它会从 backfill 进入。

EntityRegistry 若把 F042 解析为 feature entity，findMentionAnchors 可能返回 F042 文档；但 effectiveKind=thread 会在 hydrate SQL 中排除 F042，因此 entity doc item 不加入 threads scope。这个例子能说明“entity 命中不等于绕过 scope”。

### AA.3 raw passage 分支

scope 是 threads，进入 rawPassageSearch；pool=max(5*4,20)=20。mode hybrid 且 passage embedding ready，于是先执行 lexical passage FTS，再执行 semanticPassageNNSearch。semantic hits 带 _semanticHit=true；thread-B 若也在 lexical passage map 中，semantic 对象会被标记 _alsoLexical=true。

mergePassageResults 按 passage key 去重；attachPassageContext 对每个命中 position 查同 doc 前后 1 条。thread-A msg-10 的 context 可能是 msg-11；thread-B msg-20 没有相邻消息就为空。

### AA.4 scope guard

baseResults 中已经有 thread-A/thread-B 的 doc-level results，因此 hasDocLevelResults=true，scopeActive=true，hasExplicitThreadTarget=false。对 passagesByAnchor 中已存在的 item，直接附加 passages。若某个 passage parent 是新 anchor：

* 只有纯 NN、即 _semanticHit=true 且 _alsoLexical=false，才可能新增；
* thread-B 的 literal cross-post 同时 lexical+NN，_alsoLexical=true，不作为 passage-only 新发现；
* parent doc kind 还要是 thread，否则 continue。

如果 doc-level FTS 一个结果都没有，hasDocLevelResults=false，guard 会放宽，让 passage 成为唯一发现通道；但 parent kind 检查仍保留。

### AA.5 排序与输出

rankRawResults 把有 passages 的 thread 排在没有 passages 的 fallback 前，按 passage 首次出现顺序排序，截到 limit 5。enrichWithDrillDown 先尝试 consumption rerank，再 annotateMatchReasons；primary passage threadId/messageId 存在，于是返回 cat_cafe_get_thread_context 的 tool、params、hint。

最终每个 passage 转成 content、speaker、createdAt、threadId、messageId；context 中也逐个解析 thread/message ID。内部 _semanticHit/_alsoLexical 不出现在 JSON。

### AA.6 embedding 故障版本

如果 isPassageEmbeddingAvailable 返回 false，passages 直接来自 lexical()；meta 是 degraded true、reason passage_embedding_unavailable、effectiveMode lexical。raw item 仍然可以有 FTS passage 和 context，只是没有语义扩展。

如果 embed 请求成功但 passage vec0 search 抛异常，catch 使用 lexical()，reason 改为 passage_vector_search_error。两种 reason 不能混淆：监控要知道是服务没开还是查询执行失败。

### AA.7 如果 mode 改为 lexical、depth 改为 summary

lexical raw 不调用 embedding；仍然执行 passage FTS 和 entity passage mentions。depth summary 则不进入 rawPassageSearch，authority boost、lexical/semantic/hybrid 分支直接返回 evidence_docs item。这个对照说明 mode 和 depth 是正交轴：mode 决定召回通道，depth 决定主要粒度。

### AA.8 trace 的价值

端到端 trace 不应记录完整敏感 query/content。可以记录 query hash、options 摘要、pool、阶段耗时、candidate counts、degraded reason、collection statuses。这样线上能回答“是 FTS 没找到、scope 过滤掉、vector 没 ready，还是 raw guard 阻止了 cross-post”，又不会把用户原文写进普通日志。

## AB. 测试源码怎么写：从纯函数到 SQLite 集成

这套代码的测试可以分四层：纯函数、SQL builder、store 集成、联邦端到端。分层的目的不是追求测试数量，而是让失败定位到一个明确的责任边界。

### AB.1 纯函数测试：输入输出表驱动

fts-query-builder、lexical-backfill、recency-decay、graph-edge-weight、consumption-prior 都适合 table-driven test：

    const cases = [
      { input: 'F042 cache', expected: ['"F042" "cache"'] },
      { input: '   ', expected: [] },
      { input: '缓存 超时', expected: ['"缓存" "超时"'] },
    ];
    for (const c of cases) expect(buildProgressiveFtsQueries(c.input)).toEqual(c.expected);

测试不应只覆盖 happy path，还要覆盖 quote escaping、大小写、重复 token、null summary、坏 keywords JSON、superseded/archive tie-break、日期边界和 firstIndexedAt grace。

### AB.2 comparator 测试的稳定性

排序测试要验证相对关系，而不仅是第一项：

* keywordHits 2 的行必须在 keywordHits 1 前；
* keywordHits 相同、titleHits 高的行优先；
* 三个 signal 相同，authoritative 比 null provenance 优先；
* exactAnchor 无论 signal 多低都排在其他 backfill 行前；
* comparator 对相同对象返回 0，不能因为调用顺序改变。

JavaScript sort 是否稳定依赖运行时版本，业务不应把稳定性完全交给引擎；如果 tie-break 仍相同，可加入 anchor.localeCompare 作为最终 deterministic key。

### AB.3 SQLite fixture：每个测试独立数据库

better-sqlite3 集成测试应使用内存数据库或临时文件：

    const db = new Database(':memory:');
    applyMigrations(db);
    const store = new SqliteEvidenceStore({ db });
    await store.initialize();

每个 test afterEach 关闭 db，避免 WAL 文件、prepared statement 和全局 writeQueue 泄漏到下一个测试。若使用临时文件，要显式清理 db、db-wal、db-shm 三个文件，不能只删主文件。

### AB.4 FTS trigger 集成测试

流程：

1. upsert title 为 “cache timeout” 的 evidence；
2. search lexical “cache”；
3. update 同 anchor title 为 “database lock”；
4. search “cache” 应不再命中，search “database” 应命中；
5. delete anchor，两个 query 都不应命中。

这个测试证明 update trigger 的 delete old + insert new，而不是只证明表和 FTS 初始存在。再增加 anchor F042、ADR-005，验证 exact bypass 与 tokenizer 差异。

### AB.5 vector mock：不要把真实 GPU 作为单元测试前提

IEmbeddingService 可以用 stub：

    const embedding = {
      embed: vi.fn(async texts => texts.map(t => vectorFor(t))),
      isReady: () => true,
      reprobeIfNeeded: async () => undefined,
    };

VectorStore 可以用真实 SQLite vec0（若测试环境安装扩展），也可以用 fake class 返回预设 NN hits。两类测试目的不同：fake 验证 semantic/hybrid 分支和过滤；真实扩展验证 Float32Array、MATCH、k 参数和 schema。

要测试 fallback，令 isReady 返回 false；要测试执行错误，让 search 抛异常；要测试空覆盖，让 search 返回 []。三个场景最终 items 可能都来自 lexical，但 meta 和日志期望不同。

### AB.6 raw guard 测试的最小数据集

构造：

* doc thread-A，passage 含 literal query；
* doc thread-B，passage 是 literal cross-post；
* doc thread-C，passage 不含 literal 但 fake NN 命中；
* doc session-S，passage 含 literal；
* doc thread-D 没有 parent row，只存在孤儿 passage。

测试 scope=threads、hybrid、doc-level 有结果时：A 保留，B 不作为 passage-only 新项，C 可作为纯 NN 新项，S 被 parent kind guard 排除，D 被 parent lookup 跳过。再把 baseResults 清空，确认 B/S 的 guard 行为符合当前实现。这个矩阵能防止未来重构时误删其中一层 guard。

### AB.7 contextWindow 测试

插入 position 0、1、2、3 四条 passage。命中 position 1：

* window=0：没有 context 字段或空；
* window=1：返回 position 0 和 2，按 position 排序；
* window=2：返回 0、2、3，排除自己；
* position=null：不执行 context 查询；
* 不同 doc_anchor 的 position 相同：不能互相作为 context。

同时测试 date filter：context SQL 当前只按 doc_anchor 和 position，不重新按 dateFrom/dateTo 过滤邻居；如果产品要求上下文也严格落在日期范围，需要补 SQL 条件并更新契约，不能默认为已实现。

### AB.8 schema migration test

空库、V1 库、半迁移库三种 fixture 已在前文说明。额外要验证 schema_version 的 applied_at 是 ISO 字符串，重复运行不会插入第二条相同 version。migration 中 catch 的 duplicate column 不应掩盖真实 SQL syntax error；可以在测试中 monkeypatch db.exec 抛不同 message，要求实现至少记录或重新抛非 duplicate 错误。

### AB.9 KnowledgeResolver 测试

用三个 fake stores：fast success、slow timeout、throw error。断言：

* Promise.allSettled 后 fast item 仍返回；
* timeout/error collectionGroups 状态和 durationMs 存在；
* global degraded meta 不丢失 fast items；
* 两 store 同 anchor 经过 collectionId 前缀后都保留；
* private item 在 redactor 后 title/summary 不再包含原文。

测试不要依赖真实文件路径或用户 home；manifest 和 catalog 使用内存对象，避免外部 collection 测试互相污染。

### AB.10 性能测试关注点

基准至少记录：

* docs 数 1k/10k/100k 时 lexical FTS、LIKE backfill 的 p50/p95；
* hybrid pool 20/50/100 时 vec0+hydrate 总时延；
* raw contextWindow 0/1/3 的 SQL 次数；
* collection 1/5/20 fan-out 的总耗时和 timeout 比例；
* passage parent batch hydrate 前后的 query count。

性能测试结果不能直接成为功能阈值，因为 embedding 服务网络、磁盘缓存和机器 CPU 会漂移；应把 query count、扫描行数和阶段耗时作为更稳定的回归指标。

### AB.11 测试面试复述

“我会先用纯函数 fixture 锁定 FTS progressive、token escaping、backfill comparator、prior/decay/MMR；再用内存 SQLite 验证 migration、FTS trigger、scope/date SQL 和 row hydration；vector 用 fake 验证 fallback，用真实 vec0 验证 Float32Array/MATCH；raw 测试专门覆盖 cross-post、pure NN、parent kind、contextWindow；KnowledgeResolver 用 success/timeout/error 三种 fake store 验证 allSettled 和脱敏。最后用阶段耗时、扫描行数、SQL 次数做性能回归，而不是只看总响应时间。”

## AC. 语法级追问：面试官问到 TypeScript 和 SQLite 细节时怎么答

### AC.1 interface、type 和 Pick 的作用

interfaces.ts 用 interface 描述 EvidenceItem、IEvidenceStore、IEmbeddingService 等对象契约。interface 适合可扩展对象结构；union type 更适合 SearchOptions.mode、depth、scope 这种有限字面量集合。源码常使用 Pick<IEvidenceStore, 'searchWithMeta'> 给 CoverageSearchService 依赖最小接口，意味着该类只承诺需要 searchWithMeta，不需要知道 store 的全部写入能力。

这种最小依赖让测试 stub 很简单：

    const store: Pick<IEvidenceStore, 'searchWithMeta'> = {
      searchWithMeta: async () => ({ items: [], meta: { degraded: false } }),
    };

面试官问为什么不直接把完整 IEvidenceStore 传进去，答：完整接口会迫使测试和调用方实现无关方法，扩大耦合；Pick 是编译期最小权限。

### AC.2 optional、nullable 和 nullish coalescing

SearchOptions.contextWindow? 表示属性可以不存在，数据库 RowShape.summary 可能是 string|null，二者不同：undefined 通常意味着调用方没有传；null 通常意味着数据库明确没有值。源码用 options?.dateTo 安全读取，用 row.summary ?? '' 把 null/undefined 变成空字符串，用 ?? null 把 undefined 显式转成 SQLite NULL。

不要用 || 替代 ??：limit=0、contextWindow=0、空字符串在业务上可能是有效输入或需要专门报错，|| 会把它们都当 falsy。接口层应先验证非法 0，再在实现中使用 ?? 保持语义。

### AC.3 type predicate 和 Map

源码常见：

    .filter((row): row is RowShape => Boolean(row))

这是 type predicate。运行时仍只是 Boolean(row)，但编译器知道过滤后的数组是 RowShape[]，后续可以安全访问 row.anchor。没有 predicate，TypeScript 会保留 (RowShape | undefined)[]，每次访问都需要额外判断。

Map 用于 anchor 到 row、passageKey 到 passage、entityId 到 matches 的索引。new Map(rows.map(r => [r.anchor, r])) 对重复 anchor 采用最后一项覆盖；如果重复本身是数据错误，应该在测试或 consistency report 中发现。

### AC.4 async/await 和错误边界

await 只会把 Promise reject 转成当前 async 函数的 throw；它不会自动重试、取消或并发。源码用 await embedding health、await semantic search，再在小范围 try/catch 中 fallback。try/catch 的边界要包住可选增强调用，不要把 ensureOpen、参数校验和结果转换都包在一个大 catch，否则真正编程错误也会被伪装成 lexical fallback。

Promise.allSettled 的返回项要通过 status 判断后再访问 value/reason；直接读取 rejected 项的 value 是类型和运行时错误。联邦 resolver 之所以使用 allSettled，就是把每个 reject 变成 collection group 状态。

### AC.5 spread、flatMap 和参数顺序

db.prepare(sql).all(...params) 把数组按位置展开成参数。SQL 中问号顺序和 params push 顺序必须完全一致。动态 keywords.map(() => 'keywords LIKE ?').join(' OR ') 生成 N 个问号，随后 params.push(...keywords.map(...)) 生成同样 N 个值。

passage vector hydrate 使用 parsed.flatMap(p => [p.docAnchor, p.passageId])，把二元组列表展平为 SQL 参数序列。若误写成 map，params 会是二维数组，better-sqlite3 不会按预期绑定。

### AC.6 SQL NULL、三值逻辑与 COALESCE

SQL 中 NULL LIKE '%foo%' 结果是 UNKNOWN，不是 false；WHERE 只保留 true，所以源码使用 COALESCE(summary, '')。status = ? 若 status 列为 NULL 也不会命中，符合未设置 status 不等于请求 status。

d.superseded_by IS NOT NULL 返回 0/1，可直接用于 ORDER BY。source_path LIKE 'archive/%' 对 NULL 返回 UNKNOWN，排序时相当于不被识别为 archive；如果要把 NULL 单独排后，应写 CASE。

### AC.7 transaction callback 的 this 和返回值

better-sqlite3 的 db.transaction(fn) 返回一个包装函数，调用 wrapper 才真正执行事务。事务 callback 内抛异常会回滚本地 SQL；异步 callback 不应直接传给 transaction，因为驱动只管理同步事务边界。embedding HTTP 必须在事务外 await。

### AC.8 FTS bm25 的 lower-is-better

SQLite FTS5 bm25() 返回值通常是负数，越小越相关；源码直接让 SQLite rank 参与 ORDER BY，不能在 JS 中误当成越大越好再排序。hybrid 不使用 bm25 原值，而使用 lexical item 的位置 i 做 RRF，避开负数和向量 distance 的尺度问题。

### AC.9 vec0 MATCH 与普通 WHERE 的区别

vec0 的 embedding MATCH queryVec 是虚拟表扩展提供的近邻语义；k=? 是扩展约定，不是普通 SQLite FTS LIMIT。事实表的 scope/date/status 过滤不能直接依赖 vec0，必须 hydrate 后再 WHERE。若先拿 top-k 再过滤，可能得到少于 limit，这是当前实现的 tradeoff。

### AC.10 面试短答模板

问“为什么 sourceHash 和 version 都要有？”答：hash 发现输入变更，version 发现解析逻辑变更，作用不同。问“为什么 FTS 和 vector 不合一个表？”答：事实、倒排索引、向量索引生命周期和失败模式不同，分离才能 lexical 基线可回退。问“为什么 catch 不重试？”答：搜索请求有 bounded latency，增强失败先返回 lexical，后台任务另做重试。问“为什么所有 SQL 都用问号？”答：防 SQL 注入，同时让 query/filter 值和 SQL 结构分离；FTS/LIKE 仍需各自 grammar escaping。问“为什么 raw 有两层 scope guard？”答：passage 召回初始范围宽，先用 heuristic 防 cross-post，再用 parent kind 做最终资格验证。问“为什么不把所有结果总分相加？”答：BM25、vector distance、authority、consumption 的尺度和方向不同，先用 RRF 名次融合，再用可解释的后处理阶段。

## AD. 设计取舍题：如果面试官让你重构或扩展

### AD.1 “把所有召回合成一个 SQL 能不能更快？”

不建议把 FTS、LIKE、entity、vec0、scope guard 和 consumption 全塞进一条 SQL。FTS5 MATCH 和 vec0 MATCH 属于不同虚拟表，向量服务还在进程外；把它们强行 join 会让失败回退、解释字段和索引版本管理都变复杂。当前分阶段编排的好处是：lexical 可以独立成功；semantic 可以独立失败；raw guard 在业务对象 hydrate 后可读懂；每个 helper 有自己的测试。

真正可以优化的是重复过滤构造和 N+1 hydrate。提取 appendDocFilters，把 status/date/provenance/world/scene/backstop 统一追加；把 raw parent docs 批量 IN 查询；把阶段耗时和 candidate count 放进 trace。先减少重复工作，再考虑更大的架构改造。

### AD.2 “为什么不用 Elasticsearch/专用向量数据库？”

当前系统的事实量、部署方式和离线 collection 更适合 SQLite：单文件可备份，FTS5/vec0 同库，写入事务简单，外部 collection 可独立文件。代价是 LIKE backfill、vec0 top-k 过滤、N+1 context 需要应用层补。

若规模增长到千万级 docs、多租户并发写入或需要分布式 ANN，再评估专用引擎。迁移时仍应保留 evidence_docs 的领域 schema、anchor、sourceHash、authority 和 collection policy；外部引擎只能替换派生索引，不能成为唯一事实源。

### AD.3 “为什么 embedding 在独立服务，而不是 Node 进程内？”

独立服务隔离 GPU 内存、模型加载和 Python 依赖；API 只负责 HTTP timeout、batch、health 和向量写入。服务重启不会丢 evidence_docs，失败时 lexical 可用。代价是网络 hop、model endpoint discovery 和最终一致性。

如果改成进程内推理，必须重新设计 API 延迟、worker thread、显存生命周期、模型热更新和 Node 崩溃恢复，不能只把 fetch 换成函数调用。

### AD.4 “为什么 passage vector 不和文档 vector 共用一张表？”

key 结构不同：doc vector 用 anchor，passage vector 用 JSON 二元组；删除触发条件不同：文档摘要变化删除 doc vector，单条消息变化只删 passage vector；查询粒度和 hydration 也不同。共表会增加 discriminator、key 解析和模型覆盖风险，且 raw warmup 可能把大量 message 向量混进 summary top-k。

### AD.5 “如何支持新 scope，比如 sessions raw？”

先定义 contract：session passage 的 source、doc_anchor 命名、position 来源、权限和 drill-down 工具；再扩展 IndexBuilder 写入和 schema/FTS trigger；在 rawPassageSearch 白名单加入 sessions；hydrate parent kind guard；补 context/date/vector warmup 和 cross-post fixture。不能只把 if 条件从 threads 改成 sessions，因为那会让没有相应 passage 语义的数据被误解释。

### AD.6 “如何让 semantic 过滤后仍返回足够 limit？”

当前 vector pool 上限 100，hydrate 后可能因 scope/status 过滤变少。改进方案是分批扩大 k：先查 20，过滤后不足 limit 再查下一页或查 40/80；如果 vec0 不支持 offset，需要维护已见 passage keys，并查询更大的 k 后在应用层跳过。每次扩大都要有 deadline/cap，避免恶意 scope 造成全表扫描。

### AD.7 “如何让 raw context 查询批量化？”

收集所有 primary passages 的 doc_anchor、position、window，构造一条 OR 条件：

    (doc_anchor = ? AND position BETWEEN ? AND ?)
    OR (doc_anchor = ? AND position BETWEEN ? AND ?)

返回后按 passageId 建 context Map。要排除当前 passage，可在内存中过滤，或为每个子句增加 passage_id != ?。批量化会减少 round-trip，但 SQL 参数和重复窗口会膨胀，必须保留按 position 排序和跨 doc 隔离。

### AD.8 “如何修复 degraded meta 不一致？”

抽出统一函数：

    function lexicalFallback(
      items: EvidenceItem[],
      reason: SearchDegradeReason,
    ): EvidenceSearchExecution {
      return {
        items,
        meta: { degraded: true, degradeReason: reason, effectiveMode: 'lexical' },
      };
    }

semantic unavailable、semantic search error、hybrid unavailable、hybrid search error 都调用它。raw 继续使用 passage-specific reason。类型 union 需要扩展 doc-level reason，客户端和测试都按 reason 分支，而不是只看 boolean。

### AD.9 “如何保证 schema migration 失败可恢复？”

每个 migration 版本短小、可重入、完成后写 schema_version。列添加前检查 table_info，索引用 IF NOT EXISTS。对需要多步数据转换的版本，增加 migration_state 或把转换分批记录进 ledger；不要在一个不可恢复的大 transaction 中扫描百万行。

启动流程先 apply migrations，再检查 currentVersion 是否达到 expected；未完成时把 index_state 标为 stale/building，禁止依赖新列的查询或让查询使用兼容分支。迁移日志要包含 version、duration、error type，不要只留下空 catch。

### AD.10 “如何处理真正删除的文档？”

文档层可以用 source ownership 差集删除 evidence_docs，再由 trigger 删除 FTS，向量表按 anchors 分块 delete。passage 层不能因为一次不完整 transcript 缺失就删除；引入 generation/tombstone，只有完整扫描成功提交后才 GC 旧 generation。删除操作也要经 writeQueue，避免和 live upsert 交叉。

### AD.11 “如何衡量检索质量？”

离线：固定 query fixture，记录 Recall@K、MRR、nDCG、exact anchor protection、cross-post false positive。在线：recall_events 关联 candidates 与 consumed，计算 consumed MRR、live vs shadow、degraded rate、collection timeout rate。质量指标要按 query language、mode、depth、scope 和 collection sensitivity 分组，否则 CJK/中文改善会被 ASCII 样本平均掉。

### AD.12 “如何向非算法面试官解释？”

用三层比喻但紧接源码：evidence_docs 是档案柜里的事实卡；FTS 是按词找卡片的目录；vector 是按意思找卡片的索引；passage 是卡片背后的原始对话条目。实际代码中，档案柜由 SQLite 表承载，目录由 FTS trigger 维护，向量由 vec0 维护，搜索先建立 lexical 基线再做 RRF/权限/多样性后处理。比喻只用于建立直觉，随后必须回到变量、SQL 和失败分支。

### AD.13 十分钟复述顺序

第一分钟讲事实与派生索引分离；第二分钟讲 searchWithMeta 入口和 limit/bm25Pool；第三分钟讲 exact、FTS progressive、LIKE backfill、entity；第四分钟讲 mode 分流和 RRF；第五分钟讲 raw passage、contextWindow、cross-post guard；第六分钟讲 EmbeddingService health/batch/vector upsert；第七分钟讲 IndexBuilder sourceHash/version/transaction；第八分钟讲 V1-V26 migration；第九分钟讲 authority/consumption/MMR 和 KnowledgeResolver；第十分钟主动说 meta 误报、JSON parse、N+1、tombstone 等缺口。

这个顺序的优点是先回答“系统做什么”，再回答“代码怎么做”，最后回答“哪里可能出问题以及怎样改”。面试官随机打断时，也可以回到当前层次，而不会把所有名词混成一团。

## AE. 代码审查式深挖：容易被忽略的边界和可验证改进

### AE.1 exact、FTS、backfill 三条 SQL 的过滤不完全相同

读源码时不能看到一条 SQL 的过滤条件，就假设其他通道完全相同。exact anchor 构造器追加 kind、exclude session/thread、exclude pack、status、keywords、threadAnchor、provenance、backstop、world、scene；FTS 还追加 dateFrom/dateTo；backfill 也追加 date/provenance，但历史版本中某些字段可能不同；entity hydrate 又重新构造一遍。

这会产生通道不对称：一个 anchor 通过 exact 命中，另一个相同文档只能通过 FTS 命中，结果可能因为日期或过滤条件不同而不一致。修复策略不是复制更多 if，而是把过滤上下文编译成 SQL fragment + params：

    type FilterSql = { where: string[]; params: unknown[] };
    function buildDocFilters(options, context): FilterSql { ... }

caller 只负责把 where.join(' AND ') 接到不同基表上，字段前缀通过参数传入，例如 d.updated_at 或 updated_at。这样能统一条件，又不会把表名/列名作为用户输入。

### AE.2 日期字符串的边界

dateTo 为十字符 YYYY-MM-DD 时，源码追加 T23:59:59；带时区或毫秒的 ISO 字符串则原样绑定。问题在于：不同 ISO 格式的文本比较依赖存储规范；exact 分支如果没有 date 条件，会与 FTS 分支行为不同；created_at 与 updated_at 是不同业务时间，raw/document 不应混用。

稳妥做法是入口把日期解析成 UTC epoch 或规范化 ISO，再让所有 SQL 用同一种列格式；如果保持文本 ISO，必须在 scanner、message writer、migration backfill 统一写法，并测试跨时区输入。

### AE.3 keywords JSON LIKE 的边界

源码把 keywords 数组 JSON.stringify 存入单列，再以包含双引号的 LIKE pattern 做匹配。它能避免 foo 匹配 foobar 的一部分，但仍有 JSON 转义、大小写、Unicode normalization、坏 JSON 和大字段扫描成本等问题。

如果改成关联表 evidence_doc_keywords(anchor, keyword, primary key)，写入事务同时维护子表，查询用 EXISTS，能获得可索引精确匹配；代价是 migration、delete cascade、upsert 事务和 row hydration 复杂度增加。面试时说明这是可选重构，不要把现有 LIKE 说成严格结构化查询。

### AE.4 superseded 与 archive 的排序顺序

SQL 先按 superseded、archive、provenance，再按 rank。它表达了生命周期优先于文本相关度：一个已被新决策 supersede 的高 BM25 文档可能排在活动文档后面。backfill comparator 复刻了同一思想，保证 FTS 没命中时排序方向接近。

如果产品要求用户明确输入 superseded anchor 时仍展示，需要在 exact protected 集合中允许它；不能简单删除 superseded ORDER BY，因为普通自然语言查询会重新被旧文档污染。

### AE.5 rowToItem 和 source metadata 的可信边界

sourcePath、provenanceSource、materializedFrom 来自 scanner 或外部 collection manifest，不能仅因为存进 SQLite 就视为可信。drill-down 生成时要重新 resolve；collectionId 要重新经过 catalog；provenance tier 只能影响排序，不能代替授权。

rowToItem 还要处理旧 schema 中不存在的列。better-sqlite3 返回对象没有 property 时，读取 helper 应把它转成 undefined/null，而不是假设所有 V26 字段一定存在。离线复制的旧 db、只读测试 fixture 和损坏备份都可能绕过启动流程。

### AE.6 empty catch 的可观测性改法

当前多个 try/catch 只注释 fail-open。改进不一定要把异常抛给用户，可以增加结构化 counter 和 debug logger：

    catch (error) {
      metrics.increment('memory.search.fts_error');
      logger.debug({ stage: 'fts', error: normalizeError(error) });
    }

normalizeError 只保留 name/code/message 摘要，不写 query 原文、summary 或 secret。metrics 标签使用 stage、mode、scope、collectionId 等低基数值，避免把 anchor 当高基数 label 写入监控系统。

### AE.7 N+1 的量化而不是凭感觉

raw hydrate parent 和 contextWindow 都可能产生 N+1。先用 instrumentation 统计 parent hydrate 数量和 context hydrate 数量。在 limit=10、pool=20、contextWindow=1 的常见请求中，这可能是 10 到 20 条小查询；若 latency p95 仍低于目标，批量化优先级不一定高。若磁盘慢导致 p95 上升，再用一次 IN/OR 查询优化。用数据决定，而不是看到 N+1 就盲目重写。

### AE.8 MMR 与 explain 的一致性

如果 MMR 选择第 3 个结果时计算了 mmrPenalty，最终结果被 consumption rerank 再次移动，explain 中 rankingFactors 可能不等于最终排序顺序。解决方法是明确 factors 属于候选阶段还是最终阶段；在最终排序后重新计算展示级解释；或者只暴露 matchReason，不暴露容易被误读的数值。

面试官问 explain 分数能否复现排序，准确回答通常是“不保证，除非定义并持久化完整阶段分数和 tie-break”；当前字段更适合诊断。

### AE.9 recall event 的闭环

RecallEventCorrelator 通过 target_match/consumed detection 把一次 search 与后续真实消费关联。指标计算要注意 denominator：shadowConsumedMRR 和 liveOnShadowSubsetMRR 使用相同 shadow subset，避免一个指标按所有 query、另一个按有消费 query 导致比例失真。

这说明检索质量不能只看离线 Recall@K。一个结果被召回但永远不被工具消费，可能是内容不可信、drill-down 失效、展示位置太低或用户不需要。recall_events 将检索、展示、消费串起来，但隐私 retention 和 redaction 必须先于统计。

### AE.10 交付前检查清单

* 文件只修改了 clowder-book - 副本，不改原 clowder-book；
* UTF-8 读取后字符数与行数稳定；
* 新增章节标题从 R 到 AE 连续，无意外嵌入代码围栏；
* 代码围栏成对，SQL/TypeScript/text 标注没有破坏 Markdown；
* 没有复制整个源文件，只使用缩略代码、SQL 片段、变量表和伪代码；
* 新增内容明确区分当前源码事实、设计意图、建议改进；
* 字符数达到原章节的一倍补充目标；
* 最后复述模板包含入口、raw、写入、迁移、向量、排序、联邦、安全和缺口。

## AF. 终局理解：把源码事实、设计意图和改进建议分开

### AF.1 当前源码事实

当前实现确实做了这些事：searchWithMeta 对 query trim，hybrid 使用 20 到 100 的 lexical pool；scope 映射到 kind 并追加 pack/backstop/world/scene 等条件；exact anchor 用 NOCASE 直接查；FTS builder 有 AND-all、strong/weak、OR-all progressive levels；LIKE backfill 以 lower-case words 计算 keyword/title/text signal；Entity Registry 可补 alias 和 mention；raw 模式召回 evidence_passages，contextWindow 按 position 查询邻近消息；doc/passage hybrid 用 k=60 的 RRF，CJK query 的 NN weight 为 1.5；EmbeddingService 通过 HTTP health 和 batch embedding 接入；VectorStore/PassageVectorStore 因 vec0 不支持标准 upsert 而使用 delete+insert；schema 通过 V1-V26 migration；IndexBuilder 使用 sourceHash、indexing version、single-writer queue 和 passage upsert；KnowledgeResolver 用多 collection fan-out、allSettled、脱敏和 collection-level 融合；SecretScanner、path boundary 和 redactor 参与安全边界。

这些是可以直接从源码、SQL、常量和分支解释的事实。面试中尽量使用“源码当前会……”而不是“系统应该会……”。

### AF.2 设计意图

可以合理归纳出的设计意图包括：把 lexical 作为稳定基线，让 embedding/vec0 成为可选增强；用宽候选池给 RRF 和 raw doc 聚合留下空间；用 exact bypass 弥补 tokenizer 对 anchor 的破坏；用 scope/parent kind/cross-post guard 保护导航正确性；用 Beta shrinkage、grace period 和 constitutional immunity 避免新数据或高权威数据被实验排序误伤；用 collectionGroups 和 degraded meta 让联邦故障可解释。

但设计意图不应覆盖代码差异。例如 doc-level semantic/hybrid fallback 当前 meta 可能仍是 degraded=false；graph edge weight 已计算但 BFS 不一定使用优先队列；rowToItem 的坏 JSON 可能抛异常；passage parent hydrate 有 N+1；缺失 passage 默认不删除。主动区分这些差异，是面试中最有价值的“读过真实实现”的证据。

### AF.3 改进建议的优先级

第一优先级是正确性和可观测性：修 doc fallback meta，统一 exact/FTS/backfill/entity filters，给 rowToItem 做安全 JSON parse，保证 private collection 和 raw context 全链路脱敏。

第二优先级是性能：批量 hydrate parent docs，评估 contextWindow 批量查询，给 backfill 增加受控分页，增加 vector coverage 和 orphan cleanup 指标。

第三优先级是生命周期：为 passage 和 source docs 引入 generation/tombstone GC，建立 embedding model change 的重建流程，完善 schema migration 错误分类和恢复状态。

第四优先级才是复杂排序：weighted graph traversal、学习排序、更多 multilingual tokenizer 或跨 collection 的复杂 calibration。若基础事实、过滤和观测还不稳定，先上复杂模型只会让问题更难定位。

### AF.4 最后一分钟复述

“我把 Clowder memory 看成事实库加多层派生索引。写入侧 scanner/IndexBuilder 生成 EvidenceItem，用 anchor、sourceHash 和 indexing version 保证幂等和可重建；evidence_docs 是事实，FTS5、passage_fts、vec0、entity mentions 是派生索引。查询侧 searchWithMeta 先规范化选项和候选池，exact、progressive FTS、LIKE backfill、entity 组成 lexical 基线；summary 根据 mode 走 lexical、semantic 或 hybrid，hybrid 用 RRF 融合名次；raw 则搜索 passage，再按 doc_anchor、parent kind、thread scope 和 contextWindow hydrate 回统一结果。之后 authority、消费 prior、recency、constitutional protection 和 MMR 做可解释后处理，多 collection 由 KnowledgeResolver 并发 fan-out、脱敏、第二层 RRF。embedding、vec0、外部库、坏 JSON 和历史 transcript 都有 fail-open 或降级路径，但当前代码仍有 meta 误报、N+1、backfill 无 LIMIT、tombstone 缺失等可诚实改进点。这个系统的关键不是某一个算法，而是每个增强阶段都不能破坏 lexical 基线、资格过滤、隐私边界和可观测性。”

## AG. 面试现场的反问与澄清策略

### AG.1 先确认“当前行为”还是“理想设计”

当面试官问“系统是否保证 embedding 失败会标 degraded”，不要直接回答“保证”。可以先说：“raw passage 分支当前明确设置 degraded；doc semantic/hybrid 的 fallback 分支在现有实现里仍可能返回 false，这属于可修缺口。若按设计目标，应统一成 true。”这类回答同时满足事实和设计讨论。

### AG.2 先报边界，再报算法

问“raw 怎么防止错误 thread”时，先说 scope/threadId/parent kind 是资格边界，再说 _semanticHit/_alsoLexical 是启发式。这样不会把 heuristic 当成权限控制。问“private collection 如何融合”时，先说 catalog visibility 和 redaction，再说 collection-level RRF。检索算法不能替代授权。

### AG.3 用一个数字解释常量

问 bm25Pool 为什么最小 20，回答 limit=5 时若只取 5 条，某个向量排第 6 的文档永远没有机会进入 RRF；20 是宽池下限，100 是成本上限。问 RRF k=60，回答 rank0 contribution 是 1/60，rank1 是 1/61，相邻名次差距小，双通道命中自然累加。问 CJK 1.5，回答它只放大 NN 贡献，不修改 BM25。

### AG.4 把 SQL 参数顺序说出来

面试官追问“参数化具体怎样避免注入”，可以说：SQL 字符串只包含固定条件和问号；query、kind、status、date、anchor 依次 push 到 params；最后 better-sqlite3 使用 all(...params) 按位置绑定。FTS token 还要单独 escape 双引号，LIKE pattern 还要 escape 百分号和下划线。prepared statement 不是所有 grammar 的统一 escaping。

### AG.5 把失败结果说清楚

embedding unavailable 和 vector search error 都返回 lexical，但 reason 不同；empty vector table 可能只是 coverage 不足，不一定 degraded；collection timeout 不应清空其他 collection；坏 JSON keywords 应尽量保留基础字段；坏 passage vector key 只丢弃坏 hit。面试官真正想听的是“失败后数据流去哪了”，而不是一句 fail-open。

### AG.6 最终检查

完成本章补充后，应该能用源码坐标回答以下问题：searchWithMeta 的 limit/pool 如何算；scope 如何映射 kind；exact SQL 为什么不用 FTS；progressive builder 的 strong/weak 如何分类；backfill signal 如何排序；raw passage 如何 hydrate；contextWindow 的 BETWEEN 是闭区间还是开区间；vec0 为什么 delete+insert；EmbeddingService 如何 health/reprobe/batch；schema_version 如何恢复；RRF/consumption/MMR 的顺序；KnowledgeResolver 如何 allSettled 和脱敏；当前有哪些真实缺口。

如果这些问题都能用“函数输入、局部变量、SQL 片段、异常分支、最终输出”回答，而不是只说模块名，那么就已经具备支撑面试官继续追问的源码深度。

## AH. Passage embedding warmup：后台补齐为什么是 256/8/3

查询链讲的是“向量已经存在时如何使用”；写入链还要回答“新 passage 的向量何时产生”。IndexBuilder 的 warmup 是后台 best-effort 任务，目标不是阻塞文档索引，而是逐步把 passage 表补到 passage_vectors。

### AH.1 warmup 的候选扫描

warmup 首先查 passage 表中可能需要 embedding 的记录，内部扫描批次为 256。这个数字不是 embedding API batch size，而是 SQLite 读取和内存工作单元。典型过程是：从 evidence_passages 按自增 id/position 取 256 条；对每条生成 passageVectorKey；查询已有 vector keys；只把缺失或失效 passage 放进 embedding 输入；每次向 EmbeddingService 发送最多 8 条文本；成功后写 vector，再移动 cursor。

因此“256 扫描”和“8 embedding batch”是两个不同层次：256 控制数据库游标推进，8 控制外部服务单次延迟与失败影响。单次 256 嵌入会让一次失败的重试粒度过大，也可能超过服务端 batch 或显存约束。

### AH.2 warmup 的单写者边界

passage vector upsert 必须经过 store 的写队列或 exclusive callback。即使 embedding HTTP 在队列外并发执行，最终 delete+insert 仍按单 writer 进入 SQLite。否则 live message update 可能先删 vector，warmup 又用旧 snapshot 插入，出现旧向量复活。

若要严格避免 stale embedding，读取 passage 时可保存 content hash；外部生成后写入前再次确认 passage 仍存在且 hash 未改变，形成 compare-and-swap。当前实现更偏向简单 best-effort，面试时要区分当前行为和可加强方案。

### AH.3 连续 3 次失败后停止

warmup 设置 consecutive failure counter。某个 batch 失败，失败数加一；成功一次清零；连续达到 3 次则停止本轮，避免 embedding 服务已挂时循环扫描和刷错误日志。停止不是永久 disable，下一次定时任务可重新尝试。

要区分 batch failure 和单 passage data error：如果一条 content 太长导致服务拒绝，整个 8 条 batch 失败，简单计数会停止剩余健康 passage。更细实现可把 batch 拆成单条重试一次，把确定性数据错误单独记录，然后继续其他批次；当前连续 3 次策略更像服务级断路保护。

### AH.4 warmup 与模型变更

embedding_meta 记录 model id、revision、dim。模型变化时，旧 vectors 不能继续和新 query embedding 混用；应清空或标记旧向量，再 warmup 全量补齐。仅改变 embedDim 而不清表会在 vec0 insert 或 MATCH 时失败；仅改变 model id 但 dim 不变更隐蔽，因为查询能执行但语义空间不一致。

可恢复流程：

    checkMetaConsistency -> inconsistent
    mark vector index rebuilding
    clear vectors + write new embedding_meta
    warmup in batches
    mark coverage ready

查询在 rebuilding 期间仍走 lexical；不要让半新半旧 vectors 作为正常 semantic 结果返回而不标记状态。

### AH.5 warmup 文本与指标

warmup 的 embedding 文本通常使用 passage content，但 content 可能含 speaker 前缀、控制字符或超长 transcript。规范化应与 query embedding 保持固定模板，保留代码标识符和 CJK，限制最大长度。warmup 不应重新调用 scanner 或 summary model；passage 表是事实输入，warmup 只生成派生 vector。

建议记录 passages_scanned、vectors_missing、vectors_written、batches_ok、batches_failed、consecutive_failures、duration_ms、model_id、coverage_ratio。coverage ratio 是当前 model/version 下 vector_count / passage_count，不应把旧模型 vector 算进去。coverage 低不等于服务不可用，health ready 与 index coverage 应分开。

### AH.6 warmup 面试复述

“IndexBuilder 的 passage warmup 是后台最终一致任务：扫描批次 256，外部 embedding 每批最多 8 条，成功后写 passage vec0；连续 3 批失败停止本轮，避免服务挂掉时打爆日志。向量写入仍走单写者边界，模型 id/dim 从 embedding_meta 校验；模型切换要清旧向量再重建。查询在 warmup 未完成时保留 lexical，不应把 coverage 不足误报成服务 degraded。”

## AI. F200 消费重排：从一行 Exposure 到最终排序的源码执行账本

前文已经介绍了 Beta(2,8)、14 天 grace、MMR 和 shadow 的设计意图。本节把它们放回实际调用链，逐变量解释 'consumption-prior.ts'、'SqliteEvidenceStore.ts'、'RecallEventCorrelator.ts' 与 'RecallMetricsComputer.ts' 如何协作。最容易在面试里说错的一点是：消费重排不是在 SQL 的 'ORDER BY' 里直接算 CTR，也不是每次“展示一条结果”就立刻写一次统计；它是由事件关联任务回填最近窗口的统计，再在下一次检索中作为一个小的排序先验使用。

### AI.1 先把三种数据分清：候选、展示、消费

一次 search 返回的 EvidenceItem 是“候选”或“展示候选”，其中 anchor 是关联的稳定主键。用户或后续 agent 真正打开、读取、引用某个 anchor，才可能被相关性关联逻辑判定为 consumed。两者不能混为一谈：

    搜索阶段:
      query -> candidates[anchor, rank] -> tool 返回

    事件关联阶段:
      一段 invocation 事件 -> 搜索候选 + 后续 read/answer 行为
                         -> recall_events.consumed

    下次搜索阶段:
      recall_events 聚合 -> anchor metrics / kind baseline
                           -> computeConsumptionPrior -> rerank

这样做的原因是“搜索结果第 1 名”不等于“被用户认可”。一个结果可以被展示但未读，或者被读后仍不用于答案。消费信号并非真实相关性的完美标签，却比单独的 BM25 排名多了一层使用反馈。源码把它降格为 prior，而不是把它当监督学习模型的唯一真相。

### AI.2 开关读取：为什么叫 'freezeF200Flags'

'f200-types.ts' 读取环境变量 'F200_CONSUMPTION_RERANK'，类型限定为 'off'、'shadow'、'on'。'applyConsumptionRerank' 的第一句是：

    const f200Flags = freezeF200Flags();
    if (f200Flags.consumptionRerank === 'off' || results.length < 2) return;

这里有两个 TypeScript 细节。

第一，'const' 约束的是变量绑定本身不可重新赋值，不等于返回对象中的所有属性天然深冻结；函数名 freeze 表达的是“为本次调用取得稳定配置”的意图。工程上若要绝对防止对象属性被改，应返回 Readonly 类型并配合 'Object.freeze'；当前重点是不要在同一请求中反复读取可变 process.env。

第二，'return' 的 early return 很重要。只有 0 或 1 个结果时，排序没有可交换的相对顺序，去查 metrics、创建 Map、计算日期只会增加延迟和失败面。面试时可以概括为：增强路径以“候选数量足够”和“功能开关开启”为前提，未满足时保留 lexical 原数组。

### AI.3 exact lexical 保护先于消费重排

函数没有立即给所有 results 打分，而是先调用：

    const exactProtectedAnchors = findExactLexicalProtectedAnchors(results, query);
    const protectedResults = results.filter(item => exactProtectedAnchors.has(item.anchor));
    const rerankPool = results.filter(item => !exactProtectedAnchors.has(item.anchor));

'Set<string>' 的 'has' 是平均 O(1) 成员判断，适合在 filter 回调内反复查 anchor；相对地，若用数组的 'includes'，候选多时会退化为近似 O(n squared) 的扫描。这里定义的“protected”不是 authority 的 constitutional，而是 query 与 title/anchor 等 lexical 证据有足够精确匹配的结果。两类保护目的不同：

- lexical protection 防止“用户明显点名的对象”因低 CTR 被挤到后面；
- constitutional protection 防止高权威规则在重排池内部改变原本占据的位置。

如果存在 exact protected 文档，代码把它们从 rerankPool 拿走，最终再用 '[...protectedResults, ...reranked]' 放回前端。展开运算符会创建一个新数组，保留两个子数组各自的顺序；它没有修改 protectedResults 或 reranked。这个细节意味着 exact result 不仅仅“不降权”，而是被整体放在普通 rerank 的前面。面试官问是否可能改变 protected 文档间的顺序，答案是当前 filter 保持原 results 顺序，所以不改变。

### AI.4 小池子的特殊分支，为什么仍要写 shadow

若移除 protected 后 'rerankPool.length < 2'，普通候选已经没有可重排空间。代码在 on 模式且 protectedResults 非空时，把 'results' 原地改为 protectedResults，然后调用 'storeShadowRanking' 后返回。

这里“原地改数组”的写法是：

    for (let i = 0; i < protectedResults.length; i++) results[i] = protectedResults[i];
    results.length = protectedResults.length;

不能简单写 'results = protectedResults'。参数 'results' 是局部变量，重新赋值只会让局部引用指向新数组，调用方手中的数组不会变；逐下标赋值再改 '.length' 才是改变同一个 Array 实例。'.length = n' 在 n 小于旧长度时会裁掉尾部元素，因此这个语句是有意的可变操作。它适合当前同步函数，但文档调用方必须知道函数有 mutation side effect，避免在外面同时缓存同一数组作为“原始排行”。

即便不能重排，仍存储 shadow ranking 有两个作用：调用链不必为“小池子”设计特殊空值协议；后续 recall event 仍可比较实际返回顺序。这里 store 的是当前数组的 anchor/rank，因此它不是“假设新算法的 shadow”，只是稳定记录可观察顺序。

### AI.5 loadAnchorMetrics：统计查询不应 N 次往返

进入常规分支后，代码把 rerankPool 的 anchor 映射为数组，再一次性传给 'loadAnchorMetrics'：

    const anchorMetrics = loadAnchorMetrics(db, rerankPool.map(r => r.anchor));
    const globalMeanCtr = loadGlobalCtrBaseline(db);

第一个返回 'Map<anchor, metrics>'，第二个返回 'Record<string, number>'。二者的数据结构差别有语义：

- anchor 是运行时任意字符串，使用 Map 可以安全表示任意 key，不受对象原型字段影响，并可用 '.get(anchor)' 返回 'undefined'；
- kind 是有限分类名称，以 Record 表示“字符串索引到 number”的字典，读取时用 'globalMeanCtr[kind] ?? 0.2' 提供未知 kind 的默认值。

这一层的正确实现必须以一次 'IN (?, ?, ...)' 或临时表聚合取得所有 anchor，而不是循环里每个 item 做一条 SQL。前者是 O(1) 次数据库往返，后者是 O(n) 次；候选池通常是 limit 的数倍，N+1 会在高并发时直接转化为 SQLite writer/read contention。源码的 Map 也让缺失统计自然走：

    metrics?.consumed_count_30d ?? 0

'?.' 是 optional chaining。若 metrics 是 undefined，整个表达式结果为 undefined，而 '?? 0' 仅在 null 或 undefined 时替换成 0；它不会把合法的 0 当成缺失值。不能随意用 '|| 0' 作为通用习惯，因为 0、空字符串、false 都是 falsy，在需要保留这些值的场景会产生错误。

### AI.6 Beta shrinkage 的每个分支都可手算

'computeConsumptionPrior' 入口输入有过去 30 天的 consumedCount、exposureCount、距上次 consumed 的天数、kind、authority 和 firstIndexedAt。核心公式为：

    shrunkCtr = (consumedCount30d + 2) / (exposureCount30d + 2 + 8)
    rawLift = (shrunkCtr - meanCtrKind) * recencyFactor

2 和 8 是 Beta 先验 alpha、beta。若新文档 shown=0、consumed=0，shrunkCtr 是 2 / 10 = 0.2，不是未定义的 0 / 0；若 shown=1、consumed=1，shrunkCtr 是 3 / 11，大约 0.273，不会因为一次成功就被当成 100% CTR；若 shown=100、consumed=30，shrunkCtr 变为 32 / 110，大约 0.291，先验影响已经很小。

'meanCtrKind' 不是全库一个均值，而是按 kind 的基线。例如 thread 通常更容易被打开，decision 可能更少但更重要。若不分 kind，一个 thread 的 0.25 CTR 与 decision 的 0.20 CTR 会被错误理解为前者更好；有 kind baseline 后，排名关心的是“相对同类平均值的 lift”。这是一种很轻的分层归一化，不等于完整的因果校正，因为位置偏差、query 难度和用户群差异仍未消除。

### AI.7 14 天 grace 和 low sample 的顺序不能颠倒

源码的第一个 if 是：

    if (input.firstIndexedAt > 0 && Date.now() - input.firstIndexedAt < GRACE_PERIOD_MS) {
      return cold-start prior 0;
    }

只有 firstIndexedAt 大于 0 才触发，0 表示历史数据没有首次索引时间，不能误当“刚刚索引”。'Date.now()' 是毫秒 epoch，常量 '14 * 86_400_000' 也是毫秒；如果把数据库里的 seconds timestamp 直接传进来，会得到几十年前的错误年龄。面试时应主动说明单位是跨模块接口的一部分，最好用变量名 firstIndexedAtMs 或 branded type 来降低混用风险。

grace 后才计算 shrinkage，再判断 authority / kind 和 exposure 数：

    constitutional or decision/lesson -> prior = max(0, rawLift)
    exposure < 5                   -> prior = 0
    exposure < 20                  -> prior = max(0, rawLift)
    otherwise                      -> prior = rawLift

这四个分支并非“constitutional 永远加分”。它只禁止负向 prior，若 rawLift 为负，则 prior 被截为 0。更微妙的是源码把 'decision' 和 'lesson' 也纳入 constitutional branch，即使它们的 authority 字段不是 constitutional；这是业务规则编码在函数里的事实，不能只看类型名猜测。真正 constitutional item 在调用方还会被 pin，二者又是不同层的保护。

low-sample 使用 'max(0, rawLift)'，表示样本少时允许已知的正向信号冒头，但不允许少量不利样本压低文档；full 分支允许负 prior，才真正对稳定的低消费内容施加惩罚。这样避免一个曝光 2 次、从未被读的文档因为偶然性被快速埋没。

### AI.8 dormancy 到 recencyFactor 的数学含义

函数把 'daysSinceLastConsumed == null' 设为 0.5。这不是“从未消费了一定很差”，而是信息未知或从未发生时给予中性偏低折减。对于 half-life 为 null 的 adr、lesson、canon，recencyFactor 始终 1，说明这些 kind 的消费先验不随“上次消费距今多久”衰减；对 thread，half-life 为 14：

    recencyFactor = 14 / (14 + daysSinceLastConsumed)

0 天是 1，14 天是 0.5，42 天是 0.25。它是双曲线而不是指数曲线，尾部下降较缓，不会突然归零。注意这里是“最后被消费”时钟，而 'computeRecencyDecay' 使用的通常是 item.updatedAt 内容时钟，两者不应合并：一篇旧决策可能仍频繁被引用；一篇刚更新的笔记也可能无人消费。

### AI.9 组合分数：不要把 K、BETA、GAMMA 解释成神秘魔法

'applyConsumptionRerank' 对 rerankPool 逐项计算：

    positionalScore = 1 / (i + 60)
    newScore = positionalScore + 0.15 * prior.prior + 0.1 * (decay.factor - 0.5)

i 是 rerankPool 中的位置，0 起始。K=60 让 0 号的 positionalScore 约 0.0167，9 号约 0.0145，原排行仍有影响但差距很小；它更像稳定 tie-breaker，而不是让 BM25 永远压住消费信号。BETA=0.15 将 prior 缩放，GAMMA=0.1 将 recency factor 的 0.5 中性点平移到零。因此 decay.factor 为 1 时贡献 +0.05，为 0 时贡献 -0.05。

这三个量必须通过离线回放和 shadow 实验调参。不能仅凭“0.15 看起来小”断言安全：prior 理论区间由 CTR 差和 factor 决定，仍可能导致相邻名次交换。面试中最严谨的说法是“常量把不同量纲放到可比较但仍需观测的尺度；我会记录交换率、top-k 保留率和 consumed MRR，而不是拍脑袋固定数值”。

### AI.10 constitutional pin：固定的是槽位，不是强行置顶

计算完成后：

    const pinned = new Map<number, EvidenceItem>();
    if (isConstitutional) pinned.set(originalIndex, item);
    else movable.push({ item, newScore });
    movable.sort((a, b) => b.newScore - a.newScore);

'originalIndex' 指的是此 item 在 rerankPool 内原来的位置。重建数组时，循环每个 index：有 pinned 就放 pinned 的 item；没有就从已排序的 movable 依次取一个。这产生“固定槽位”效果：

    原池:        A  B  C  D
    B 是 pin:    _  B  _  _
    movable 排序: D  A  C
    重建:        D  B  A  C

B 没有被提升到第 1 名，也不会被 D/A/C 覆盖它的第 2 名。它保护的是 constitutional item 的原语义排名位置。若说“constitution 一定 top1”，既不符合源码，也容易被面试官用这个反例追问击穿。

### AI.11 MMR 的触发条件和它看到的输入

只有同时满足 targetLimit 存在且 'movable.length >= 3 * targetLimit'，代码才调用：

    applyMMR(movable.map(m => ({ item: m.item, score: m.newScore })), targetLimit, 0.7)

这里的 '3 * targetLimit' 是一个节流条件。候选太少时，本来就没有足够替代项，多样性算法只会增加计算和可解释性负担。满足条件时，MMR 最多从 non-pinned 候选中挑 targetLimit 个；随后代码给每个选中的 item 赋 'targetLimit - i' 的伪 score，仅是为了维持 MMR 选出的次序，原 newScore 不再用于后续排序。

要注意容量语义：pin 不属于 movable，最终数组会把 protectedResults、pinned、MMR selected movable 组合起来。若调用方想保证“最终不超过 limit”，必须在更外层统一 slice 或明确 targetLimit 对哪一段生效。代码审查时需要加一个 pin/protected 数量超过 limit 的 fixture，而不是凭直觉认为 MMR 的 limit 自动覆盖全体。

### AI.12 on 与 shadow：必须存相反序列才有因果比较信号

正常 on 模式下，函数把 'final' 原地写回 results，线上看到的是重排后序列；但 shadow 应保存 'preRerankOrder'，即改动前顺序。shadow 模式相反：线上保留原 results，shadow 保存 would-be final。伪代码如下：

    on:     live = reranked, shadow = before
    shadow: live = before,   shadow = reranked

如果 on 模式错误把 final 同时当 live 与 shadow，两个 MRR 永远相同，指标看起来完美但没有比较价值。源码中的注释专门记录过这种历史错误，因此这是很适合面试追问的“为什么要 snapshot”的例子。

'storeShadowRanking' 使用候选 anchor 的前若干项生成 key，把 ranking 放入进程内 Map，最多保留固定数量的 key；'lookupShadowRanking' 用 'shift()' 取出第一个数组，表现为一次性消费。这种设计只适合把同一进程中紧接的 search 和 trace correlation 接起来：

- 进程重启后 Map 丢失；
- 多实例部署时 search 与 correlation 落在不同实例会 miss；
- 相同候选 key 的并发请求依赖队列顺序，不能当全局精确关联。

真正用于历史分析的持久化字段是 recall_events 的 'shadow_ranking_json'。若系统需要跨进程可靠归因，应在 search 响应中带 resultSetId，并在事件写入时以该 id 直接关联，不应依赖 anchors 拼接的 transient cache。

### AI.13 RecallEventCorrelator：何时刷新统计，为什么先拒绝 private hits

'recall-correlation-hook.ts' 会筛出同一 invocationId 与 catId 的事件，只有工具名属于 search_evidence、graph_resolve、list_recent 才进入 recall 关联。若任一 memory event 的 summary 有 '_f200HasPrivateHits === true'，函数直接 return。原因不是私有结果“没有价值”，而是 recall_events/shadow ranking 可能把可识别 anchor、标题或访问轨迹写入另一张长期表；在没有完成完整持久化脱敏前，保守选择是不记。

关联成功后，hook 为每条 recall event 填 threadId，并从 tool summary 的 '_f200Candidates' 拿 anchor 数组：

    const shadowRanking = lookupShadowRanking(anchors);
    recallEvents[i].shadowRankingJson =
      shadowRanking ? JSON.stringify(shadowRanking) : null;

'JSON.stringify' 把数组变成 TEXT 交给 SQLite 保存。读取端必须把 JSON.parse 放在容错边界内，因为一次历史坏数据不应让全份 metrics 报告失败。随后 'persistBatch' 写事件，再执行 'refreshAnchorMetrics' 与 'refreshGlobalCtrBaseline'。顺序体现了事实与派生统计的关系：先写事件事实，再从事实重新聚合指标。

最后，若 consumedAnchors 非空，代码再从 invocation 的 summary 中取 '_f200Edges'，只记录 edge.to 在 consumed 集合内的边 traversal。'Set' 用于判断可避免重复，且它表示“目标节点被消费过”这一关联，不是“这条边必然导致消费”的因果证明。

### AI.14 shadow MRR 的分母为何要相同

'RecallMetricsComputer.computeShadowComparison' 先筛：

    shadowRows = rows.filter(r => r.shadowRanking !== null && r.consumed.length > 0)

没有可比较行时返回 null 而不是 0。null 表示“样本不存在”，0 表示“有样本但表现为零”，仪表盘和报警策略必须区分。对每行，shadow 找 consumed anchor 在 shadow rankMap 中的最小 rank，命中则加 1/(rank+1)，未命中加 0；live 同样用 consumed 的 live rank，但会先过滤 rank < 0。

rank=-1 的语义是“最后被消费的 anchor 不在该次 live candidate pool”。不能直接 'Math.min(-1, 0)' 后再算 1/(rank+1)，否则会除以 0；源码先 filter(rank >= 0)，若所有 rank 都是 -1 就贡献 0。这个防御很小，却体现指标代码也要对 sentinel value 有明确语义。

shadow 和 live 最后都除以同一个 'shadowRows.length'，而不是各自可命中行数。若 shadow miss 时从自己的分母剔除，就会虚假抬高 shadow MRR；若用所有检索行作 live 分母、只用有 shadow 的行作 shadow 分母，两个指标也不可比较。共享分母后 'shadowConsumedMRR / liveOnShadowSubsetMRR' 才是同一人群上的相对信号。

### AI.15 这段实现的真实边界，以及可答的改进题

消费 prior 的输入是过去 30 天的聚合，存在 feedback loop：排得高的内容曝光更多，曝光更多又更容易有 consumption。Beta shrinkage 只能减小小样本噪声，不能解决位置偏差。下一步可在 shadow/on 分桶中做随机探索、记录展示位置，并按 propensity 做逆倾向加权；但这是产品/统计决策，不应在没有实验设计时直接引入复杂公式。

当前 'Date.now()' 直接在纯函数内调用，使时间测试需要 fake timer。更可测的改造是把 nowMs 作为可选参数或依赖注入，生产默认 Date.now()，测试传固定值。当前 firstIndexedAt 缺失会跳过 grace，历史文档可能立刻接受负 prior；迁移后可按最早已知索引时间回填，但必须注明该值是估计而非真实首入库时间。

面试复述可以是：“F200 把消费当作小幅先验，不替代 lexical/semantic 召回。事件关联先把候选和后续消费写 recall_events，刷新每个 anchor 和每类文档的 30 天指标；下次检索对非 exact 保护候选用 Beta(2,8) 收缩 CTR、kind baseline、消费休眠衰减和内容更新时间衰减计算分数。新文档和低样本只允许正向信号，decision/lesson/constitutional 不接受负 prior，constitutional 还固定原槽位。on 与 shadow 保存相反的排序序列，并以同一消费样本集计算 MRR，避免比较失真。”

## AJ. GraphResolver：图搜索不是向量检索，BFS、别名和脱敏在源码中如何落地

'GraphResolver.ts' 的职责是从一个 anchor 周围构建有限深度子图。它不是 KnowledgeResolver 的替代品：后者按 query 搜索文本和向量，前者假设已经有中心 anchor，再沿 relation 找邻居。把二者都称为“知识图谱搜索”会掩盖访问控制、复杂度和排序语义的关键区别。

### AJ.1 输入接口刻意很窄，方便跨 collection 实现

GraphStore 只要求两个异步方法：

    getByAnchor(anchor) -> Promise<EvidenceItem | null>
    getRelated(anchor) -> Promise<relation[]>

这种 structural typing 是 TypeScript 的特点：一个对象只要形状满足这两个方法，不必显式 'implements GraphStore' 也可传进 resolver。relation 返回 anchor、relation 名、from/to collection、edge sensitivity、provenance、traversalCount、lastTraversedAt，而不把具体 SQLite SQL 暴露给图算法。好处是可以替换为 mock store 写单元测试，也可让不同 collection 用不同后端。

GraphResult 则把内部事实转换为面向调用者的 nodes、edges、center、depth、truncated。'truncated?: boolean' 是 optional property：没有截断时返回 undefined，而不是 false。序列化为 JSON 时 undefined 字段通常被省略，输出更紧凑；调用端若需要布尔判断可以写 'result.truncated === true'，不能把字段缺失误判为数据损坏。

### AJ.2 collection 推断的双路径：前缀快路径与逐 store 慢路径

'inferCollectionIdSync' 遍历 catalog.list()，忽略 archived manifest，然后用：

    anchor.startsWith(collectionId + ':')

匹配常见的 'collectionId:localAnchor' 命名。这是零 I/O 快路径。注意 startsWith 需要带 ':' 分隔符，若只用 'startsWith(collectionId)'，'team' 会误匹配 'team-private'，产生权限边界 bug。

若 anchor 没有可识别前缀，异步 'inferCollectionId' 逐 store 调 'getByAnchor'，首个非 null 的 store 即为归属。它能兼容旧 anchor 或引用别名，但最坏会产生 collection 数量倍增的 lookup。规模扩大后，可以建立 anchor-to-collection 的二级索引或在 catalog 中维护映射；不过要处理 anchor 重名和索引过期。当前实现优先正确兼容，再接受小规模下的线性回退。

### AJ.3 初始化状态：每个 Map/Set 对应一个不变量

'buildSubgraph' 初始化多个容器，不是无意义的样板代码：

- nodesMap：real canonical anchor 到输出 GraphNode，保证节点唯一；
- edgesArr：保留输出顺序的边数组；
- edgeKeySet：为无向视图排重；
- visited：防环和避免重复展开；
- redactedAnchorMap：同一敏感真实 anchor 始终映射到同一个 opaque 名；
- unresolvedRedactionMap：找不到目标 collection 时仍记住“不可泄漏”的敏感性；
- lookupAliasesByCanonicalAnchor：canonical anchor 与数据库中可查询旧别名的关系；
- nodeEdgeCount：每个节点的出边配额；
- frontier / nextFrontier：BFS 的逐层队列。

Map 的 key 使用真实 anchor，而 GraphNode.anchor 可是 '[redacted:1]'。这份“内部真实 id 和对外展示 id 分离”非常关键：若把 opaque anchor 作为内部 key，后续 query store 时已经丢失真实锚点；若把真实 anchor 直接作为输出字段，又会泄漏 restricted collection 的存在和命名。

### AJ.4 opaque anchor 不是哈希，而是请求内稳定代号

内部闭包 'opaqueAnchor(realAnchor)' 先查 redactedAnchorMap，有则复用；没有则递增 counter，创建 '[redacted:1]' 形式字符串。它保证在同一 GraphResult 中，两个指向同一私有节点的边会显示相同的 '[redacted:1]'，用户仍能看出拓扑关系；不同请求 counter 从 0 开始，调用者不能跨请求关联私有对象。

它不是安全哈希，也不是持久 pseudonym。若产品需要“用户 A 多次浏览同一私有节点仍显示相同代号”，要使用带访问域的 HMAC 或服务器端映射，并重新评估是否产生可关联性泄漏。当前临时代号满足最小披露原则，更适合一次响应。

### AJ.5 BFS 的外层循环：深度边界为何是 '<= depth'

核心结构是：

    for (let d = 0; d <= depth && frontier.length > 0; d++) {
      const nextFrontier = [];
      for (const currentAnchor of frontier) { ... }
      frontier = nextFrontier;
    }

d=0 处理中心节点，仍会读取它的边；当 d>=depth 时，节点可以被 materialize 进 nodesMap，但代码 'if (d >= depth) continue'，因此不会继续展开其边。这样 depth=1 的图包含中心、直接邻居和中心发出的边，而不读邻居的二跳边。若写成 'd < depth'，depth=0 会连中心都不处理；若忘记展开前 continue，depth=1 会意外多抓一层关系。

'visited' 在读每个 currentAnchor 前判断并立即 add。立即标记可以避免 A->B、A->C、B->D、C->D 时 D 被 nextFrontier 重复扩展；代价是同一层不同路径到 D 的第二次到达不会再次携带路径信息。此 API 输出的是合并的子图，而不是“所有路径枚举”，所以这个权衡正确。

### AJ.6 canonical anchor 与 lookup alias：修复历史 key 漂移

当前节点查到 doc 后：

    const canonicalAnchor = doc?.anchor ?? currentAnchor;
    rememberLookupAlias(canonicalAnchor, currentAnchor);

若数据库接受旧 anchor 查询但返回的新 canonical anchor，resolver 用 canonicalAnchor 作为 nodesMap 和 visited 的身份，避免同一文档显示成两个节点。同时记住旧 lookup anchor，后续 'lookupAnchorsFor(canonicalAnchor)' 返回 canonical 加所有别名，让 getRelated 能在迁移前后不同 store 的边表中被找到。

'canUseLookupAnchorInStore' 又做一次验证：若 lookupAnchor 与 canonicalAnchor 不同，它先 getByAnchor(lookupAnchor)，仅当返回 doc 的 anchor 等于 canonicalAnchor 才允许用这个 alias 去查 related。否则一个巧合重名的旧 key 可能把另一个文档的关系吸进来。这是一个很好的“别名不是无条件信任”的源码例子。

### AJ.7 centerCollectionId：中心节点选择为何不能只看前缀

对 d=0 的原始 anchor，代码优先尝试 opts.centerCollectionId 的 store：

    const preferredStore = stores.get(centerCollectionId)
    const preferredDoc = preferredStore ? await preferredStore.get(currentAnchor) : null
    const collectionId = preferredDoc ? centerCollectionId : inferCollectionId(...)

当多个 collection 恰好都存在同名、不带前缀的 anchor 时，调用者明确指定的中心 collection 有更高优先级。若 preferred store 没有该 doc，才回到通用推断，不会盲目信任调用参数。该设计把“调用者上下文”作为 disambiguation hint，而不是权限绕过开关。

### AJ.8 sensitivity 的两个问题：节点是否可见，与边是否可说

collection manifest 的 private/restricted 且不在 callerCollections 中时，节点被 redacted。输出节点 collectionId 改为空字符串，kind 改为 'redacted'，title 改为类似 '[redacted - restricted collection]'。清空 collectionId 很重要：即使不返回正文，collection 名也可能泄露客户、项目或领域。

边的规则更严。edgeSensitivity 默认取 edge 自身声明，否则调用 'stricterSensitivity(sensitivity, relSensitivity)'。该函数比较 COLLECTION_SENSITIVITY_ORDER，取限制更严的一方。随后 edgeRedacted 要求调用者同时有两端 collection 的权限；跨越 internal 与 restricted 的关系本身也可能泄露，所以即便 internal 端可见，edge 仍可标为 redacted。

面试官若问“redacted 节点还保留在图中有何意义”，答案是保留结构可解释性：调用者可知道当前可见文档有一个受限依赖或引用，但看不到其身份。若业务连“存在一个受限关系”也不能泄漏，就必须在构建前完全过滤该边，而不是只改 title。

### AJ.9 未解析目标也要继承敏感性

relation 的目标 anchor 有时找不到归属 collection，例如外部 collection 已卸载、索引尚未同步、anchor 已删除。若相关 edge 本身是 private/restricted，代码不会因为“查不到”就把该 anchor 原样吐出去，而是写：

    unresolvedRedactionMap.set(relCanonicalAnchor, {
      sensitivity: edgeSensitivity,
      redacted: true
    })

最后 materialize nodes 和 edges 时再次检查这个 Map，用 opaque anchor 替代真实 anchor。这是典型 fail-closed：解析失败不等于授权成功。很多实现只在“找到 manifest”时脱敏，恰好会在索引不一致的异常路径泄露原始 id；源码的二阶段修正值得重点说明。

### AJ.10 relation filter 必须在 traversal 时执行

opts.relations 非空时变成 'Set<string>'，在读取 related 后、入边和入队前执行：

    if (relationFilter && !relationFilter.has(rel.relation)) continue

把过滤放在 render 最后是不够的。假设调用者只想看 'blocked_by'，但后端先通过 'wikilink' 把节点扩入 frontier，再从该节点走 'blocked_by'，最终可能得到一条“视觉上只有 blocked_by、实际却借 wikilink 扩展到的”路径，同时无意义地消耗 I/O。源码注释明确指出了这个漏洞，并把 relation filter 放在 traversal time。

### AJ.11 两层限流：边配额与总节点配额

常量 'MAX_EDGES_PER_NODE=15'、'MAX_TOTAL_NODES=50' 在内层遍历中检查：

    if (currentNodeEdges >= 15 || nodesMap.size >= 50) {
      truncated = true;
      nodeCapped = true;
      break;
    }

nodeCapped 会退出当前 store/lookup 循环，但不会取消已收集的结果；truncated 让调用者知道“图并非完整”。限制 total nodes 用的是当前 'nodesMap.size'，它在首次 materialize current node 时增长，因此存在“同一轮已经排进 nextFrontier，但尚未 materialize”的暂时超前排队。这是 acceptable 的软上限，不是数据库级硬资源配额。若要更严格，可在 push nextFrontier 前维护 discovered Set 与预算，但会增加实现复杂度。

边数配额用 canonicalAnchor 计数。无论同一节点从多少 store 或 alias 查到关系，超过 15 都停止扩展，防止高连接 hub 一次请求生成大量图。要测试至少四种情形：单节点 16 边、51 个节点、深度恰好边界、环 A-B-A；并断言 truncated 真正返回，而非仅检查 nodes 数量。

### AJ.12 edge 去重的方向假设

edgeKey 是：

    canonicalAnchor + arrow + relCanonicalAnchor + ':' + relation

同时计算 reverseKey，并在任一已存在时跳过。这把同 relation 的 A->B 与 B->A 视为同一条无向关系。对于 'related_to'、wikilink 这种展示型关系合理；但若 relation 是本质有向的 'blocked_by' 或 'evolved_from'，反向关系可能承载不同业务语义，当前策略可能少显示一条边。

因此面试中不能笼统说“代码对所有边去重”。更准确的是“它以 relation 名相同为条件做反向排重，隐含关系近似无向的假设”。若要保留方向，应只用 edgeKey 去重，或为 relation 定义 directed metadata；可视化也要加箭头，而不是只看 GraphEdge 的 from/to 字段。

### AJ.13 edge weight 被计算，但当前 BFS 不按权重扩展

每条保留边都会算：

    daysSinceTraversal = lastTraversedAt ? elapsedDays : null
    ew = computeEdgeWeight(relation, traversalCount, daysSinceTraversal)
    weight = ew.total

'graph-edge-weight.ts' 的 total 是 typeBase 加上：

    0.05 * traversalCount30d * 30 / (30 + daysSinceLastTraversal)

从未 traversal 时 recencyDecay=0，boost 为 0；最近、频繁 traversed 的边会稍高。它可用于 UI explain、未来排序或观测，但 nextFrontier 仍是普通 array append，按 store 和 related 返回顺序做 BFS。代码没有 priority queue，也没有按 weight sort。因此“high weight edge 会优先被深搜”是错误描述。

若产品想让图推荐优先走强关系，需把 frontier 改成最大堆，状态带累计 path score，并重新定义 visited：普通 visited 会让先到的低分路径永久封锁后到高分路径，应改为 bestScoreByAnchor，只有新路径更高才更新。还要设置最大 hops、最大弹出次数，避免正权环不断改善/扩张。这已经不是当前简单 BFS 的微小重构，应单独设计和压测。

### AJ.14 异步循环的性能边界：正确但可能慢

当前实现内含多个 'await'：逐 frontier 节点、逐 store、逐 alias、逐 relation 推断 collection 和读取 doc。好处是流程、权限判断、截断位置明确；代价是一次深度较大的请求会串行积累 I/O 延迟。不能简单把所有 await 改 Promise.all：并发 50 节点乘 15 边乘 N stores，可能瞬间触发数百 SQLite 查询，并且更难在预算达到后停下来。

可控优化方案是按一层 BFS 批处理：先收集本层 anchor，按 collection 分桶；每 store 增加 batchGetByAnchors 和 batchGetRelated；并发限制为小数值，例如 4 到 8；每一层结束后检查 node/edge budget。这样仍保持 depth 的层序语义，也不会把数据库压力变成无界 fan-out。优化前应通过 trace 记录 store getByAnchor/getRelated 次数，以数据确认瓶颈。

### AJ.15 graph traversal 的消费事件如何回流

当 GraphResolver 的结果被工具返回后，调用链会把 edges 放进 summary 的 '_f200Edges'。'triggerRecallCorrelation' 在同 invocation 中找出 consumed anchors，再筛选：

    consumedEdges = edges.filter(edge => consumedAnchors.has(edge.to))
    recordEdgeTraversals(db, consumedEdges)

这里仅对 edge.to 被消费的边增加 traversal 统计。它很自然地建模“沿着该边到达的目标后来被读取”，但仍是 heuristic：用户可能通过其他路径打开这个目标；或者先读了目标，再请求图。统计可以帮助观察而不能证明边的推荐因果。面试要主动说出这层限制，会比把 weight 描述成“学习出的真实图模型”更可信。

### AJ.16 GraphResolver 面试复述

“GraphResolver 是 anchor 起点的受限 BFS，不是 query 检索。它先用 collection 前缀或逐 store lookup 确定归属，canonicalize anchor 并维护旧 alias，d=0 到 depth 层逐层展开；depth 边界只 materialize 最后一层，不展开最后一层的边。节点、边、未解析关系都按 collection 与 edge sensitivity 做 fail-closed 脱敏，同一私有节点在一次响应内使用稳定 opaque id。relation filter 在 traversal 时执行，15 边/节点与 50 节点总量提供可见的 truncated 保护。边会计算 type、历史 traversal、recency weight，但当前仍是按返回顺序的 BFS，weight 尚未驱动优先队列，这是源码事实与下一步优化点。”

## AK. 由代码审查走向可落地改造：八个追问的实现级答案

这一节不提出空泛“可以优化性能”的结论，而是把上一章发现的真实边界转成可实施的改造顺序。每一项都说明改哪里、为什么不能粗暴修改、测试应如何证明行为不回归。面试中，候选人能把“发现问题”推进到“渐进迁移方案”，通常比只背出算法名更有说服力。

### AK.1 统一资格过滤：让 exact、FTS、backfill、semantic 不再各写一套 WHERE

当前多个召回入口需要处理 kind/scope、status、archive、superseded、日期、source 等条件。最大风险不是 SQL 注入，而是条件不对称：FTS 路径排除了 archive，exact 路径漏了日期；semantic hydrate 排除了 superseded，LIKE backfill 没排。这样同一份文档会因为“从哪条召回通道进来”而获得不同资格。

推荐在 'SqliteEvidenceStore.ts' 内部引入只负责生成固定 SQL 片段和参数的 helper，例如：

    buildEligibilityFilter(options) -> {
      whereSql: ['d.status = ?', 'd.kind IN (...)'],
      params: ['active', ...kinds]
    }

调用端再把不同的 MATCH/LIKE/anchor 条件拼到前面。这里不能把用户 query 拼进 helper 返回的 SQL 字符串；helper 只能拼由白名单控制的列名、固定操作符和问号，全部用户值必须进入 params。对于 'IN' 的可变长度，应由 helper 根据数组长度产生相同数量的 '?'，空数组要有明确语义，通常是返回 '1=0' 或避免加这一条件，绝不能生成 'IN ()'。

测试不应只断言 SQL 文本。应建立同一份 SQLite fixture，其中包含 inactive、archived、superseded、日期外、kind 外和合格文档；分别通过 exact、FTS、backfill、entity、semantic hydrate 调用，断言每个通道输出 anchor 集合相同。这样才能捕获“新增一个条件但忘记挂到一个分支”的回归。

### AK.2 把 degraded 变成组合事实，而不是 catch 里的布尔开关

源码的 lexical fallback 是正确方向，但 doc semantic/hybrid 分支存在“最终返回 lexical，却可能 meta.degraded=false”的空隙。根因通常是错误状态留在局部 catch，没有沿 mode 分支写回输出元数据。

可定义内部不可变状态：

    type EnhancementStatus =
      | { channel: 'embedding', state: 'used' }
      | { channel: 'embedding', state: 'unavailable', reason: string }
      | { channel: 'vector', state: 'empty-index' }
      | { channel: 'fts', state: 'failed', reason: string }

每个增强阶段 append status，最后统一计算：

    degraded = statuses.some(s => s.state === 'unavailable' || s.state === 'failed')
    usedSemantic = statuses.some(s => s.channel === 'vector' && s.state === 'used')

“empty-index”是否 degraded 要由产品语义决定：新库 warmup 未完成通常是 coverage 缺失，不是服务故障；但调用者仍应看到 vectorCoverage 或 enhancementSkippedReason，避免把“没有向量”误解为“语义搜索正常但没有命中”。这种结构比在多处写 'meta.degraded = true' 更不容易漏分支。

测试矩阵至少覆盖 embedding health false、HTTP timeout、embedding 返回错误维度、vec0 MATCH 抛错、vec0 空表、FTS 抛错、hybrid 一路成功一路失败。对每行断言 items 是否保留 lexical，degraded 是否正确，reason 是否不泄漏密钥或原始敏感 query。

### AK.3 修复 rowToItem 的坏 JSON 放大：字段级容错，而不是整项消失

SQLite TEXT 中的 keywords、metadata、mentions 等 JSON 来自历史索引、迁移或外部 collection，不能假定永远合法。若 'rowToItem' 对每个字段直接 JSON.parse，单条坏 JSON 会抛异常；若异常被最外层搜索 catch 吞掉，可能把整个 FTS 分支变成空结果，远比“坏字段”影响大。

安全 helper 的接口应显式保留诊断：

    parseJsonField(raw, fallback, field, anchor)
      -> { value: fallback or parsed, warning?: structured warning }

其中先检查 typeof raw === 'string' 和非空；parse 后还要检查 Array.isArray 或 plain object 形状，避免 JSON 合法但类型错误。比如 keywords 期待 string[]，'[1, 2]' 不应原样作为字符串数组使用。warning 不要包含全文敏感内容，包含 anchor、field、error category 即可。

基础 EvidenceItem 应仍可返回 title、summary、content、kind 等原始列，只有坏的派生 metadata 使用空数组/空对象。对不可信 collection，应先走 privacy redactor 再记录 warning，不能把私密 JSON 原文送入日志。测试要有 malformed JSON、合法但错误 shape、null、空串、超大 JSON 四类。

### AK.4 passage hydrate 的 N+1：为何需要 batch，但不能失去原始顺序

raw search 的 passage 命中要 parent document、thread context、邻近 position 等信息。逐 passage 调 getByAnchor 或单条 SQL 容易形成 N+1，尤其 raw pool 达到 100 时。批处理方案是先收集 unique doc_anchor：

    anchors = unique(passages.map(p => p.docAnchor))
    docsByAnchor = batchGetDocs(anchors)
    contextsByAnchor = batchGetPassageWindows(anchor, positions)

再按原 passage hits 数组映射，而不是按 batch SQL 返回顺序输出。SQL 的 'IN' 没有保证结果顺序；若直接把 docs 的返回数组当结果，会打乱 BM25/vector/RRF 的排名。正确数据结构是 Map，再从原 hits 重建。

contextWindow 还包含每个 passage 不同 position，完全通用的单条 SQL 比较复杂，可先按 doc_anchor 分组并每组使用一个窗口查询，再限制并发。若要一次 SQL，可使用 VALUES 表传入 '(doc_anchor, center_position)' 对并 JOIN evidence_passages，但需要注意 SQLite 参数个数和 better-sqlite3 的版本能力。优化前后必须比较相同 fixture 的 passage order、context 边界、scope guard、重复 doc 去重结果。

### AK.5 tombstone 与 generation：派生索引怎样避免旧内容复活

IndexBuilder 对当前扫描到的 EvidenceItem upsert 很好处理，但“本次没有扫到”的旧 passage/vector 不会自动消失。直接在每次扫描前 DELETE 全库然后重建会造成长时间 lexical/vector 空窗，也会让失败扫描删掉好数据。

更稳妥的写法是在一次成功完整 scan 开始生成 generationId：

    documents touched in this scan -> generation_id = current
    successful scan completion -> mark stale rows generation_id != current as deleted
    reader -> status active and generation current or not tombstoned
    background GC -> physically delete old passages/vectors after retention

这叫 tombstone 加延迟 GC。核心不变量是“只有确认扫描完整成功，才让缺席项失效”。如果 scanner 被取消、sourceRoot 不可读或部分 collection 超时，就不得推进 current generation，否则会把未扫描部分误删。向量侧应以 doc anchor/passage key 关联 tombstone，delete+insert 的 vec0 生命周期也要进入同一写队列。

测试要模拟：正常删除一篇文档、半路抛错、同 anchor 内容更新、旧 vector key 仍在、GC 前后查询。断言失败扫描后旧文档仍可查，成功扫描后不再查，GC 后存储空间下降且没有 orphan vector。

### AK.6 embedding 模型迁移：id、revision、dim 与 coverage 要同时验证

'embedding_meta' 存模型信息不只是日志。query embedding 和 doc/passage embedding 必须处于同一向量空间；dim 一样但 model revision 不同也可能语义不可比较。因此启动时至少校验：

    stored model id == configured model id
    stored revision == configured revision
    stored dimension == configured dimension

三者任意不一致时，把 vector state 标为 rebuilding，而不是一边继续旧向量、一边开始写新向量。rebuilding 的查询策略应明确：先 lexical；可选地只使用同一 model generation 的完整覆盖向量；meta 中报告 coverage 及 rebuilding。不能仅以“vec0 查询没有报错”判断模型迁移成功。

warmup 进度也不能只用 vectors 总数，因为旧 generation 向量仍可能存在。coverage 应是：

    active passages with current model vector / active passages

同时记录 doc-level 和 passage-level 两个比率。一个常见错误是 passage coverage 只有 20%，却把 hybrid 的低 recall归咎于 RRF 参数；在指标层先区分服务 health、索引 coverage、查询命中数，定位才不会漂移。

### AK.7 GraphResolver 的并发和方向改造：从 BFS 到加权检索前先定语义

如果只是要降低延迟，采用分层批量读取即可，不要先改为 weighted graph search。若业务确实要求“优先展示常被走且关系更强的路径”，必须先定义：

- relation 是否有方向，'blocked_by' 的 A->B 与 B->A 是否是不同事实；
- weight 是边本身分数还是整条 path 的累积分数；
- traversalCount 是 popularity、可信度还是曝光偏差；
- private/restricted 边在无权限情况下是否参与内部探索；
- 达到 50 节点预算时，按什么规则截断才稳定可解释。

实现上可定义 priority queue entry 为 '{ anchor, depth, pathScore }'；用 max heap pop 最高分；用 'bestScoreByAnchor' 替代纯 visited；在 enqueue 时合成例如 'parentScore * edgeWeight' 或对数相加。必须避免正权回路和历史次数不断抬分，可限制每节点最佳分数只更新一次、限制最大扩展次数，并记录所有被 budget 截断的 frontier。

不过这已经改变“同深度优先”的现有用户预期。迁移路径应先 shadow 生成两份 GraphResult，只记录可见节点/边集合差、延迟和后续 consumed edge，确定收益后再开放。不应仅因为 computeEdgeWeight 已存在就假设 priority BFS 是无风险的一行 sort。

### AK.8 建立端到端可回放测试：从函数真值表到故障注入

最有价值的测试不是把每个私有 helper 都 mock 成绿色，而是建立一组小型真值 SQLite fixture。建议最少包含：

- 两个 collection，含 internal、private、restricted 和 archived；
- active/inactive/superseded 文档，带 exact、FTS、LIKE、entity、vector 各自命中案例；
- 同一个 doc 的多个 passage、跨 post duplicate、不同 contextWindow 边界；
- 新文档、低样本、高样本、constitutional、负 prior 文档；
- 多条图 relation，包括环、别名、未解析 restricted target、超过 15 边 hub；
- 合法 JSON、坏 JSON、旧 schema、模型元数据不匹配；
- embedding timeout、vec0 failure、FTS failure、scan partial failure。

每个场景都要同时断言业务输出和执行元数据。例如 semantic failure 的主断言不是“捕获了异常”，而是“lexical items 仍在、degraded/reason 正确、没有写半条 vector、日志无正文泄漏”。图的主断言不是“有一张图”，而是“深度边界正确、私有真实 anchor 不出现、truncated 有标记、filter 不会作为隐式路径”。

最后将真实匿名 query trace 保存为回放集，冻结 candidate anchors、feature flags、模拟 embedding 输出和 nowMs。每次改 RRF、consumption、MMR、graph weight 或 SQL filter，输出 top-k diff、MRR diff、coverage 和 latency，而不是只看单元测试。排序系统的回归经常不是 crash，而是第 1 到第 5 名悄悄换了对象。

### AK.9 可直接用于面试的改造顺序

若面试官问“给你两周怎么提升这套代码”，可以按风险和收益回答：

1. 先补可观测性和 fixture：统一 meta 状态，记录 lexical/vector/coverage 与排序变更，不先改算法。
2. 修资格过滤不对称、坏 JSON 容错和 raw N+1：它们直接影响正确性、稳定性、延迟，且可用明确回归测试保护。
3. 做 generation tombstone 与模型一致性：解决长期数据正确性，保持 lexical 不中断。
4. 利用 shadow 和回放评估 consumption/MMR 参数：先证实排序改变有收益，再开启 on。
5. 最后才考虑批量 graph traversal 或 weighted frontier：这是行为升级，需要产品定义、权限审查和实验。

这套排序并不是“越复杂越高级”。成熟的检索工程强调：事实表可重建，派生索引可失效，lexical 基线稳定，所有增强可解释、可回退、可测量。能从 'searchWithMeta' 的一个条件分支一路讲到 schema、事件、统计和安全边界，才是真正理解了这条 14 层管线。

### AK.10 本章最终一分钟回答模板

“Clowder 的 memory 不是单一向量库，而是以 evidence_docs 为事实、FTS/passage/vec0/entity 为派生索引的分层系统。查询先保证 exact、progressive FTS 和 lexical backfill 的稳定基线，再按 mode 叠加 embedding 和 RRF；raw 走 passage 命中、parent hydrate、scope guard 与 contextWindow。写入侧用 sourceHash、index version、transaction 和单写者队列维持幂等，schema migration 与 embedding_meta 管理可重建性。排序侧把 authority、消费反馈、内容时效和 MMR 分开：消费 prior 用 Beta 收缩和 kind baseline，精确命中与 constitutional 项有不同的保护方式，shadow 排名以同分母 MRR 验证是否真的改善。多 collection 和 graph traversal 额外处理 catalog、sensitivity、opaque redaction、BFS 上限；embedding 或增强失败时回到 lexical，并且应把 degraded、coverage、失败原因变成可观测事实。当前我会优先修复过滤条件不对称、degraded 漏报、坏 JSON 与 N+1，再用回放和 shadow 驱动后续算法改动。”
